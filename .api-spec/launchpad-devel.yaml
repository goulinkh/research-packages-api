# Source: https://goulinkh.github.io/launchpad-wadl-to-openapi/launchpad-devel.openapi.yaml
# Generator: https://github.com/goulinkh/launchpad-wadl-to-openapi @ d57d33d3519591c0fb633e5fe8bf1d9d05c60ce8 (main)
openapi: 3.0.3
info:
  title: Launchpad Web Service API
  version: devel
  description: |-
    The Launchpad web service allows automated clients to access most of the functionality available on the Launchpad web site. For help getting started, see the help wiki.

    **About version devel**

    This version of the web service reflects the most recent changes made. It may abruptly change without warning. Periodically, these changes are bundled up and given a permanent version number.

    Generated from the Launchpad WADL description. Launchpad exposes a hypermedia web service: resource URLs are discovered at runtime from the service root and from links in other resources. Top-level collections use their real service-root URLs; other resource types are shown at a real example instance URL (e.g. `/ubuntu/+archive/primary`) so requests resolve as sent. Named operations are invoked with a `ws.op` argument on the resource URL, and each is exposed here as its own endpoint keyed by that value (e.g. `/people?ws.op=find`) so it carries its own parameters and response schema. Parameter examples are placeholders.
servers:
  - url: https://api.launchpad.net/devel
tags:
  - name: archive
  - name: archives
  - name: branches
  - name: bug
  - name: bug_trackers
  - name: bugs
  - name: builders
  - name: charm_bases
  - name: charm_recipes
  - name: countries
  - name: craft_recipes
  - name: cve
  - name: cves
  - name: distribution
  - name: distribution_source_package
  - name: distributions
  - name: distro_arch_series
  - name: distro_series
  - name: git_ref
  - name: git_repositories
  - name: git_repository
  - name: languages
  - name: livefses
  - name: packageset
  - name: packagesets
  - name: people
  - name: person
  - name: pillars
  - name: polls
  - name: processor
  - name: processors
  - name: project
  - name: project_group
  - name: project_groups
  - name: project_series
  - name: projects
  - name: questions
  - name: rock_bases
  - name: rock_recipes
  - name: service-root
  - name: service_factory
  - name: snap_bases
  - name: snappy_serieses
  - name: snaps
  - name: source_package
  - name: specifications
  - name: team
  - name: temporary_blobs
  - name: translation_groups
  - name: translation_import_queue_entries
  - name: vulnerabilities
paths:
  /:
    get:
      tags:
        - service-root
      summary: The root of the web service.
      operationId: service-root-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/service-root-json"
  /archives?ws.op=getByReference:
    get:
      tags:
        - archives
      summary: Get by reference
      description: Return the IArchive with the given archive reference.
      operationId: archives-getByReference
      parameters:
        - name: reference
          in: query
          required: true
          description: Archive reference string
          schema:
            type: string
          example: reference
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive-full"
  /archives:
    get:
      tags:
        - archives
      summary: List archives
      description: Interface for ArchiveSet
      operationId: archives-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive-page"
  /branches?ws.op=getByUniqueName:
    get:
      tags:
        - branches
      summary: Get by unique name
      description: Find a branch by its ~owner/product/name unique name. Return None if no match was found.
      operationId: branches-getByUniqueName
      parameters:
        - name: unique_name
          in: query
          required: true
          description: Branch unique name
          schema:
            type: string
          example: ~launchpad/launchpad/master
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-full"
  /branches?ws.op=getByUrl:
    get:
      tags:
        - branches
      summary: Get by url
      description: "Find a branch by URL. Either from the external specified in Branch.url, from the URL on http://bazaar.launchpad.net/ or the lp: URL. This is a frontend shim to IBranchLookup.getByUrl to allow it to be exported over the API. If you want to call this from within the Launchpad app, use the IBranchLookup version instead. Return None if no match was found."
      operationId: branches-getByUrl
      parameters:
        - name: url
          in: query
          required: true
          description: Branch URL
          schema:
            type: string
          example: https://git.launchpad.net/launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-full"
  /branches?ws.op=getByUrls:
    get:
      tags:
        - branches
      summary: Get by urls
      description: "Finds branches by URL. Either from the external specified in Branch.url, from the URL on http://bazaar.launchpad.net/ , or from the lp: URL. This is a frontend shim to IBranchLookup.getByUrls to allow it to be exported over the API. If you want to call this from within the Launchpad app, use the IBranchLookup version instead. param urls: An iterable of URLs expressed as strings. return: A dictionary mapping URLs to branches. If the URL has no associated branch, the URL will map to None."
      operationId: branches-getByUrls
      parameters:
        - name: urls
          in: query
          required: true
          description: "A list of URLs of branches These can be URLs external to Launchpad, lp: URLs, or http://bazaar.launchpad.net/ URLs, or any mix of all these different kinds."
          schema:
            type: string
          example: urls
      responses:
        "200":
          description: Successful response
  /branches?ws.op=getByPath:
    get:
      tags:
        - branches
      summary: Get by path
      description: "Find a branch by its path. The path is the same as its lp: URL, but without the leading lp:, so it may be in any of these forms: Unique names: ~OWNER/PROJECT/NAME ~OWNER/DISTRO/SERIES/SOURCE/NAME ~OWNER/+junk/NAME Aliases linked to other objects: PROJECT PROJECT/SERIES DISTRO/SOURCE DISTRO/SUITE/SOURCE Return None if no match was found."
      operationId: branches-getByPath
      parameters:
        - name: path
          in: query
          required: true
          description: Branch path
          schema:
            type: string
          example: path
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-full"
  /branches?ws.op=getBranches:
    get:
      tags:
        - branches
      summary: Get branches
      description: "Return a collection of branches. param user: An IPerson. Only branches visible by this user will be returned. param order_by: An item from the BranchListingSort enumeration, or None to return an unordered result set. param modified_since_date: If not None, return only branches whose date_last_modified is greater than this date. param eager_load: If True (the default because this is used in the web service and it needs the related objects to create links) eager load related objects (products, code imports etc)."
      operationId: branches-getBranches
      parameters:
        - name: order_by
          in: query
          required: false
          description: Sort order
          schema:
            type: string
            enum:
              - by most interesting
              - by project name
              - by status
              - by branch name
              - by owner name
              - most recently changed first
              - most neglected first
              - newest first
              - oldest first
        - name: modified_since_date
          in: query
          required: false
          description: Modified since date Return only branches whose date_last_modified is greater than or equal to this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /branches?ws.op=getBranchVisibilityInfo:
    get:
      tags:
        - branches
      summary: Get branch visibility info
      description: "Return the named branches visible to both user and person. Anonymous requesters don't get any information. param user: The user requesting the information. If the user is None then we return an empty dict. param person: The person whose branch visibility we wish to check. param branch_names: The unique names of the branches to check. Return a dict with the following values: person_name: the displayname of the person. visible_branches: a list of the unique names of the branches which the requester and specified person can both see. This API call is provided for use by the client Javascript. It is not designed to efficiently scale to handle requests for large numbers of branches."
      operationId: branches-getBranchVisibilityInfo
      parameters:
        - name: person
          in: query
          required: true
          description: The person whose branch visibility is being checked.
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: branch_names
          in: query
          required: true
          description: List of branch unique names
          schema:
            type: string
          example: branch_names
      responses:
        "200":
          description: Successful response
  /branches?ws.op=getMergeProposals:
    get:
      tags:
        - branches
      summary: Get merge proposals
      description: "Return the merge proposals that resulted in this revision. param merged_revision: The revision_id of the revision that resulted from this merge proposal. param visible_by_user: The user to whom the proposals must be visible. If None, only public proposals will be returned."
      operationId: branches-getMergeProposals
      parameters:
        - name: merged_revision
          in: query
          required: true
          schema:
            type: string
          example: merged_revision
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /branches:
    get:
      tags:
        - branches
      summary: List branches
      description: Interface representing the set of branches.
      operationId: branches-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /bugs/bugtrackers?ws.op=getByName:
    get:
      tags:
        - bug_trackers
      summary: Get by name
      description: Get a BugTracker by its name. If no tracker with the given name exists, return default.
      operationId: bug_trackers-getByName
      parameters:
        - name: name
          in: query
          required: true
          description: The bug tracker name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_tracker-full"
  /bugs/bugtrackers?ws.op=queryByBaseURL:
    get:
      tags:
        - bug_trackers
      summary: Query by base url
      description: Return one or None BugTracker's by baseurl
      operationId: bug_trackers-queryByBaseURL
      parameters:
        - name: base_url
          in: query
          required: true
          description: The base URL of the bug tracker
          schema:
            type: string
          example: https://bugzilla.example.com/
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_tracker-full"
  /bugs/bugtrackers?ws.op=ensureBugTracker:
    post:
      tags:
        - bug_trackers
      summary: Ensure bug tracker
      description: Make sure that there is a bugtracker for the given base url. If not, create one using the given attributes.
      operationId: bug_trackers-ensureBugTracker
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                base_url:
                  type: string
                  description: Location The top-level URL for the bug tracker, or an upstream email address. This must be accurate so that Launchpad can link to external bug reports.
                  example: https://bugzilla.example.com/
                bug_tracker_type:
                  type: string
                  enum:
                    - Bugzilla
                    - Debbugs
                    - Roundup
                    - Trac
                    - SourceForge or SourceForge derivative
                    - Mantis
                    - Request Tracker (RT)
                    - Email Address
                    - Savane
                    - PHP Project Bugtracker
                    - Google Code
                    - GitHub Issues
                    - GitLab Issues
                  description: Bug Tracker Type
                title:
                  type: string
                  description: Title A descriptive label for this tracker to show in listings.
                summary:
                  type: string
                  description: Summary A brief introduction or overview of this bug tracker instance.
                contact_details:
                  type: string
                  description: Contact details The contact details for the external bug tracker (so that, for example, its administrators can be contacted about a security breach).
                name:
                  type: string
                  description: Name A URL-friendly name for the bug tracker, such as "mozilla-bugs".
              required:
                - base_url
      responses:
        "200":
          description: Successful response
  /bugs/bugtrackers:
    get:
      tags:
        - bug_trackers
      summary: List bug trackers
      description: A set of IBugTracker's. Each BugTracker is a distinct instance of a bug tracking tool. For example, bugzilla.mozilla.org is distinct from bugzilla.gnome.org.
      operationId: bug_trackers-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_tracker-page"
  /builders?ws.op=getByName:
    get:
      tags:
        - builders
      summary: Get by name
      description: Retrieve a builder by name
      operationId: builders-getByName
      parameters:
        - name: name
          in: query
          required: true
          description: Builder name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/builder-full"
  /builders?ws.op=getBuildQueueSizes:
    get:
      tags:
        - builders
      summary: Get build queue sizes
      description: "Return the number of pending builds for each processor. return: a dict of tuples with the queue size and duration for each processor and virtualisation. For example: { 'virt': { '386': (1, datetime.timedelta(0, 60)), 'amd64': (2, datetime.timedelta(0, 30)), }, 'nonvirt':... } The tuple contains the size of the queue, as an integer, and the sum of the jobs 'estimated_duration' in queue, as a timedelta or None for empty queues."
      operationId: builders-getBuildQueueSizes
      responses:
        "200":
          description: Successful response
  /builders?ws.op=getBuildersForQueue:
    get:
      tags:
        - builders
      summary: Get builders for queue
      description: Return all builders with the given properties.
      operationId: builders-getBuildersForQueue
      parameters:
        - name: processor
          in: query
          required: true
          description: Processor
          schema:
            type: string
            format: uri
            x-lp-resource-type: processor
          example: processor
        - name: virtualized
          in: query
          required: false
          description: Virtualized
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/builder-page"
  /builders?ws.op=new:
    post:
      tags:
        - builders
      summary: New
      description: Create a new builder. The builder will be set to manual. An admin needs to verify its configuration and set it to automatic before jobs will be dispatched.
      operationId: builders-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                processors:
                  type: string
                  description: Processors Processors identifying jobs which can be built by this device.
                  example: processors
                url:
                  type: string
                  description: "URL The URL to the build machine, used as a unique identifier. Includes protocol, host and port only, e.g.: http://farm.com:8221/"
                  example: https://git.launchpad.net/launchpad
                name:
                  type: string
                  description: Name The builder name used for reference purposes
                  example: ubuntu
                title:
                  type: string
                  description: Title The builder title. Should be just a few words.
                  example: title
                active:
                  type: string
                  description: Publicly Visible Whether or not to present the builder publicly.
                virtualized:
                  type: string
                  description: Virtualized Whether or not the builder is a virtual Xen instance.
                  example: virtualized
                vm_host:
                  type: string
                  description: "VM host The machine hostname hosting the virtual buildd-worker, e.g.: foobar-host.ppa"
                open_resources:
                  type: string
                  description: Open resources Resource tags offered by this builder, that can be required by a build and if required must match.
                restricted_resources:
                  type: string
                  description: Restricted resources Resource tags offered by this builder, indicating that the builder may only be used by builds that explicitly require these tags.
              required:
                - processors
                - url
                - name
                - title
                - virtualized
      responses:
        "200":
          description: Successful response
  /builders:
    get:
      tags:
        - builders
      summary: List builders
      description: "Collections of builders. IBuilderSet provides access to all Builders in the system, and also acts as a Factory to allow the creation of new Builders. Methods on this interface should deal with the set of Builders: methods that affect a single Builder should be on IBuilder."
      operationId: builders-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/builder-page"
  /+charm-bases?ws.op=getByDistroSeries:
    get:
      tags:
        - charm_bases
      summary: Get by distro series
      description: "Return the ICharmBase for this distro series. raises NoSuchCharmBase: if no base exists for this distro series."
      operationId: charm_bases-getByDistroSeries
      parameters:
        - name: distro_series
          in: query
          required: true
          description: Distro series
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
          example: noble
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/charm_base-full"
  /+charm-bases?ws.op=new:
    post:
      tags:
        - charm_bases
      summary: New
      description: Create an ICharmBase.
      operationId: charm_bases-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                distro_series:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro series
                  example: noble
                build_snap_channels:
                  type: string
                  description: Source snap channels for builds A dictionary mapping snap names to channels to use when building charm recipes that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
                  example: build_snap_channels
                processors:
                  type: string
              required:
                - distro_series
                - build_snap_channels
      responses:
        "200":
          description: Successful response
  /+charm-bases:
    get:
      tags:
        - charm_bases
      summary: List charm bases
      description: Interface representing the set of bases for charms.
      operationId: charm_bases-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/charm_base-page"
  /+charm-recipes?ws.op=getByName:
    get:
      tags:
        - charm_recipes
      summary: Get by name
      description: Returns the appropriate ICharmRecipe for the given objects.
      operationId: charm_recipes-getByName
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: project
          in: query
          required: true
          description: Project
          schema:
            type: string
            format: uri
            x-lp-resource-type: project
          example: launchpad
        - name: name
          in: query
          required: true
          description: Recipe name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/charm_recipe-full"
  /+charm-recipes?ws.op=findByOwner:
    get:
      tags:
        - charm_recipes
      summary: Find by owner
      description: Return all charm recipes with the given owner.
      operationId: charm_recipes-findByOwner
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/charm_recipe-page"
  /+charm-recipes?ws.op=new:
    post:
      tags:
        - charm_recipes
      summary: New
      description: Create an ICharmRecipe.
      operationId: charm_recipes-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner The owner of this charm recipe.
                  example: ~launchpad
                project:
                  type: string
                  format: uri
                  x-lp-resource-type: project
                  description: The project that this charm recipe is associated with
                  example: launchpad
                name:
                  type: string
                  description: Charm recipe name The name of the charm recipe.
                  example: ubuntu
                description:
                  type: string
                  description: Description A description of the charm recipe.
                git_repository:
                  type: string
                  format: uri
                  x-lp-resource-type: git_repository
                  description: Git repository A Git repository with a branch containing a charm recipe.
                git_repository_url:
                  type: string
                  description: Git repository URL The URL of a Git repository with a branch containing a charmcraft.yaml at the top level.
                git_path:
                  type: string
                  description: Git branch path The path of the Git branch containing a charm recipe.
                git_ref:
                  type: string
                  format: uri
                  x-lp-resource-type: git_ref
                  description: Git branch The Git branch containing a charm recipe.
                build_path:
                  type: string
                  description: Build path Subdirectory within the branch containing metadata.yaml.
                auto_build:
                  type: string
                  description: Automatically build when branch changes Whether this charm recipe is built automatically when its branch changes.
                auto_build_channels:
                  type: string
                  description: "Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this charm recipe. Supported snap names: 'charmcraft', 'core', 'core18', 'core20', 'core22', 'core24', 'core26'"
                store_upload:
                  type: string
                  description: Automatically upload to store Whether builds of this charm recipe are automatically uploaded to the store.
                store_name:
                  type: string
                  description: Registered store name The registered name of this charm in the store.
                store_channels:
                  type: string
                  description: Store channels Channels to release this charm to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
                use_fetch_service:
                  type: string
                  description: Use fetch service If set, Charm builds will use the fetch-service instead of the builder-proxy to access external resources.
                fetch_service_policy:
                  type: string
                  enum:
                    - strict
                    - permissive
                  description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False. The “strict” mode only allows certain resources and formats, and errors out in any case the restrictions are violated. The “permissive” mode works similarly, but only logs a warning when encountering any violations.
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information type The type of information contained in this charm recipe.
              required:
                - owner
                - project
                - name
      responses:
        "200":
          description: Successful response
  /+charm-recipes:
    get:
      tags:
        - charm_recipes
      summary: List charm recipes
      description: A utility to create and access charm recipes.
      operationId: charm_recipes-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/charm_recipe-page"
  /+countries?ws.op=getByName:
    get:
      tags:
        - countries
      summary: Get by name
      description: Return a country by its name.
      operationId: countries-getByName
      parameters:
        - name: name
          in: query
          required: true
          description: Name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/country-full"
  /+countries?ws.op=getByCode:
    get:
      tags:
        - countries
      summary: Get by code
      description: Return a country by its code.
      operationId: countries-getByCode
      parameters:
        - name: code
          in: query
          required: true
          description: Code
          schema:
            type: string
          example: code
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/country-full"
  /+countries:
    get:
      tags:
        - countries
      summary: List countries
      description: A container for countries.
      operationId: countries-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/country-page"
  /+craft-recipes?ws.op=getByName:
    get:
      tags:
        - craft_recipes
      summary: Get by name
      description: Returns the appropriate ICraftRecipe for the given objects.
      operationId: craft_recipes-getByName
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: project
          in: query
          required: true
          description: Project
          schema:
            type: string
            format: uri
            x-lp-resource-type: project
          example: launchpad
        - name: name
          in: query
          required: true
          description: Recipe name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/craft_recipe-full"
  /+craft-recipes?ws.op=findByOwner:
    get:
      tags:
        - craft_recipes
      summary: Find by owner
      description: Return all craft recipes for the given owner.
      operationId: craft_recipes-findByOwner
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/craft_recipe-page"
  /+craft-recipes?ws.op=new:
    post:
      tags:
        - craft_recipes
      summary: New
      description: Create an ICraftRecipe.
      operationId: craft_recipes-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner The owner of this craft recipe.
                  example: ~launchpad
                project:
                  type: string
                  format: uri
                  x-lp-resource-type: project
                  description: The project that this craft recipe is associated with
                  example: launchpad
                name:
                  type: string
                  description: Craft recipe name The name of the craft recipe.
                  example: ubuntu
                description:
                  type: string
                  description: Description A description of the craft recipe.
                git_repository:
                  type: string
                  format: uri
                  x-lp-resource-type: git_repository
                  description: Git repository A Git repository with a branch containing a sourcecraft.yaml recipe.
                git_repository_url:
                  type: string
                  description: Git repository URL The URL of a Git repository with a branch containing a sourcecraft.yaml at the top level.
                git_path:
                  type: string
                  description: Git branch path The path of the Git branch containing a sourcecraft.yaml recipe.
                git_ref:
                  type: string
                  format: uri
                  x-lp-resource-type: git_ref
                  description: Git branch The Git branch containing a craft.yaml recipe.
                build_path:
                  type: string
                  description: Build path Subdirectory within the branch containing craft.yaml.
                auto_build:
                  type: string
                  description: Automatically build when branch changes Whether this craft recipe is built automatically when the branch containing its craft.yaml recipe changes.
                auto_build_channels:
                  type: string
                  description: Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this craft recipe. Currently only 'core', 'core18', 'core20', and 'sourcecraft' keys are supported.
                store_upload:
                  type: string
                  description: Automatically upload to store Whether builds of this craft recipe are automatically uploaded to the store.
                store_name:
                  type: string
                  description: Registered store name The registered name of this craft in the store.
                store_channels:
                  type: string
                  description: Store channels Channels to release this craft to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
                use_fetch_service:
                  type: string
                  description: Use fetch service If set, Craft builds will use the fetch-service instead of the builder-proxy to access external resources.
                fetch_service_policy:
                  type: string
                  enum:
                    - strict
                    - permissive
                  description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False.
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information type The type of information contained in this craft recipe.
              required:
                - owner
                - project
                - name
      responses:
        "200":
          description: Successful response
  /+craft-recipes:
    get:
      tags:
        - craft_recipes
      summary: List craft recipes
      description: A utility to create and access craft recipes.
      operationId: craft_recipes-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/craft_recipe-page"
  /bugs/cve?ws.op=advancedSearch:
    get:
      tags:
        - cves
      summary: Advanced search
      description: "Return cve sequences that matches the given filters. param in_distribution: filter cves that have a vulnerability for all of these distributions. param not_in_distribution: filter cves that have no vulnerability for any of these distributions. param modified_since: only updated cves after modified_since will be returned. param offset: offset of cves to return. It defaults to 0. param limit: return limit cves at max. It defaults to config.launchpad.default_batch_size."
      operationId: cves-advancedSearch
      parameters:
        - name: in_distribution
          in: query
          required: false
          description: Distributions linked to the cve
          schema:
            type: string
        - name: not_in_distribution
          in: query
          required: false
          description: Distributions not linked to the cve
          schema:
            type: string
        - name: modified_since
          in: query
          required: false
          description: Minimum cve.datemodified Ignore cves that are older than this.
          schema:
            type: string
        - name: offset
          in: query
          required: false
          description: Offset of cves to return
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum number of cves to return
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /bugs/cve:
    get:
      tags:
        - cves
      summary: List cves
      description: The set of ICve objects.
      operationId: cves-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cve-page"
  /distros:
    get:
      tags:
        - distributions
      summary: List distributions
      description: Interface for DistrosSet
      operationId: distributions-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution-page"
  /+git?ws.op=getByID:
    get:
      tags:
        - git_repositories
      summary: Get by id
      description: Find a repository by its ID. Return None if no match was found.
      operationId: git_repositories-getByID
      parameters:
        - name: id
          in: query
          required: true
          description: Repository ID
          schema:
            type: string
          example: "1"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-full"
  /+git?ws.op=getByPath:
    get:
      tags:
        - git_repositories
      summary: Get by path
      description: "Find a repository by its path. Any of these forms may be used: Unique names: ~OWNER/PROJECT/+git/NAME ~OWNER/DISTRO/+source/SOURCE/+git/NAME ~OWNER/+git/NAME Owner-target default aliases: ~OWNER/PROJECT ~OWNER/DISTRO/+source/SOURCE Official aliases: PROJECT DISTRO/+source/SOURCE Return None if no match was found."
      operationId: git_repositories-getByPath
      parameters:
        - name: path
          in: query
          required: true
          description: Repository path
          schema:
            type: string
          example: path
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-full"
  /+git?ws.op=getRepositories:
    get:
      tags:
        - git_repositories
      summary: Get repositories
      description: "Get all repositories for a target. param user: An IPerson. Only repositories visible by this user will be returned. param target: An IHasGitRepositories, or None to get repositories for all targets. param order_by: An item from the GitListingSort enumeration, or None to return an unordered result set. param modified_since_date: If not None, return only repositories whose date_last_modified is greater than this date. return: A collection of IGitRepository objects."
      operationId: git_repositories-getRepositories
      parameters:
        - name: target
          in: query
          required: false
          description: Target
          schema:
            type: string
            format: uri
            x-lp-resource-type: git_target
        - name: order_by
          in: query
          required: false
          description: Sort order
          schema:
            type: string
            enum:
              - by most interesting
              - by repository name
              - most recently changed first
              - most neglected first
              - newest first
              - oldest first
        - name: modified_since_date
          in: query
          required: false
          description: Modified since date Return only repositories whose date_last_modified is greater than or equal to this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-page"
  /+git?ws.op=countRepositoriesForRepack:
    get:
      tags:
        - git_repositories
      summary: Count repositories for repack
      description: "Get number of repositories qualifying for a repack. return: The number of IGitRepository objects qualifying for a repack."
      operationId: git_repositories-countRepositoriesForRepack
      responses:
        "200":
          description: Successful response
  /+git?ws.op=getRepositoryVisibilityInfo:
    get:
      tags:
        - git_repositories
      summary: Get repository visibility info
      description: "Return the named repositories visible to both user and person. Anonymous requesters don't get any information. param user: The user requesting the information. If the user is None then we return an empty dict. param person: The person whose repository visibility we wish to check. param repository_names: The unique names of the repositories to check. Return a dict with the following values: person_name: the displayname of the person. visible_repositories: a list of the unique names of the repositories which the requester and specified person can both see. This API call is provided for use by the client Javascript. It is not designed to efficiently scale to handle requests for large numbers of repositories."
      operationId: git_repositories-getRepositoryVisibilityInfo
      parameters:
        - name: person
          in: query
          required: true
          description: The person whose repository visibility is being checked.
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: repository_names
          in: query
          required: true
          description: List of repository unique names
          schema:
            type: string
          example: repository_names
      responses:
        "200":
          description: Successful response
  /+git?ws.op=getDefaultRepository:
    get:
      tags:
        - git_repositories
      summary: Get default repository
      description: "Get the default repository for a target. param target: An IHasGitRepositories. raises GitTargetError: if target is an IPerson. return: An IGitRepository, or None."
      operationId: git_repositories-getDefaultRepository
      parameters:
        - name: target
          in: query
          required: true
          description: Target
          schema:
            type: string
            format: uri
            x-lp-resource-type: git_target
          example: launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-full"
  /+git?ws.op=getDefaultRepositoryForOwner:
    get:
      tags:
        - git_repositories
      summary: Get default repository for owner
      description: "Get a person's default repository for a target. param owner: An IPerson. param target: An IHasGitRepositories. raises GitTargetError: if target is an IPerson. return: An IGitRepository, or None."
      operationId: git_repositories-getDefaultRepositoryForOwner
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: target
          in: query
          required: true
          description: Target
          schema:
            type: string
            format: uri
            x-lp-resource-type: git_target
          example: launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-full"
  /+git?ws.op=getRepositoriesForRepack:
    get:
      tags:
        - git_repositories
      summary: Get repositories for repack
      description: "Get the top badly packed repositories. param limit: The number of badly packed repositories that the endpoint should return - it is 50 by default. return: A list of the worst badly packed repositories."
      operationId: git_repositories-getRepositoriesForRepack
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /+git?ws.op=new:
    post:
      tags:
        - git_repositories
      summary: New
      description: "Create a Git repository and return it. param repository_type: The GitRepositoryType of the new repository. param registrant: The IPerson who registered the new repository. param owner: The IPerson who owns the new repository. param target: The IProduct, IDistributionSourcePackage, IOCIProjectName, or IPerson that the new repository is associated with. param name: The repository name. param information_type: Set the repository's information type to one different from the target's default. The type must conform to the target's code sharing policy. (optional) param with_hosting: Create the repository on the hosting service."
      operationId: git_repositories-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner The owner of this Git repository. This controls who can modify the repository.
                  example: ~launchpad
                target:
                  type: string
                  format: uri
                  x-lp-resource-type: git_target
                  description: Target The target of the repository.
                  example: launchpad
                name:
                  type: string
                  description: Name The repository name. Keep very short, unique, and descriptive, because it will be used in URLs.
                  example: ubuntu
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information type The type of information contained in this repository.
              required:
                - owner
                - target
                - name
      responses:
        "200":
          description: Successful response
  /+git?ws.op=setDefaultRepository:
    post:
      tags:
        - git_repositories
      summary: Set default repository
      description: "Set the default repository for a target. param target: An IHasGitRepositories. param repository: An IGitRepository, or None to unset the default repository. raises GitTargetError: if target is an IPerson."
      operationId: git_repositories-setDefaultRepository
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                target:
                  type: string
                  format: uri
                  x-lp-resource-type: git_target
                  description: Target
                  example: launchpad
                repository:
                  type: string
                  format: uri
                  x-lp-resource-type: git_repository
                  description: Git repository
              required:
                - target
      responses:
        "200":
          description: Successful response
  /+git?ws.op=setDefaultRepositoryForOwner:
    post:
      tags:
        - git_repositories
      summary: Set default repository for owner
      description: "Set a person's default repository for a target. param owner: An IPerson. param target: An IHasGitRepositories. param repository: An IGitRepository, or None to unset the default repository. param user: The IPerson who is making the change. raises GitTargetError: if target is an IPerson."
      operationId: git_repositories-setDefaultRepositoryForOwner
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner
                  example: ~launchpad
                target:
                  type: string
                  format: uri
                  x-lp-resource-type: git_target
                  description: Target
                  example: launchpad
                repository:
                  type: string
                  format: uri
                  x-lp-resource-type: git_repository
                  description: Git repository
              required:
                - owner
                - target
      responses:
        "200":
          description: Successful response
  /+git:
    get:
      tags:
        - git_repositories
      summary: List git repositories
      description: Interface representing the set of Git repositories.
      operationId: git_repositories-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-page"
  /+languages?ws.op=getAllLanguages:
    get:
      tags:
        - languages
      summary: Get all languages
      description: Return a result set of all ILanguages from Launchpad.
      operationId: languages-getAllLanguages
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/language-page"
  /+languages:
    get:
      tags:
        - languages
      summary: List languages
      description: The collection of languages. The standard get method will return only the visible languages. If you want to access all languages known to Launchpad, use the getAllLanguages method.
      operationId: languages-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/language-page"
  /livefses?ws.op=getByName:
    get:
      tags:
        - livefses
      summary: Get by name
      description: Return the appropriate ILiveFS for the given objects.
      operationId: livefses-getByName
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: distro_series
          in: query
          required: true
          description: Distroseries
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
          example: noble
        - name: name
          in: query
          required: true
          description: Live filesystem name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/livefs-full"
  /livefses?ws.op=new:
    post:
      tags:
        - livefses
      summary: New
      description: Create an ILiveFS.
      operationId: livefses-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner The owner of this live filesystem image.
                  example: ~launchpad
                distro_series:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro Series The series for which the image should be built.
                  example: noble
                name:
                  type: string
                  description: Name The name of the live filesystem image.
                  example: ubuntu
                metadata:
                  type: string
                  description: A dict of data about the image. Entries here will be passed to the builder.
                  example: metadata
              required:
                - owner
                - distro_series
                - name
                - metadata
      responses:
        "200":
          description: Successful response
  /livefses:
    get:
      tags:
        - livefses
      summary: List livefses
      description: A utility to create and access live filesystems.
      operationId: livefses-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/livefs-page"
  /bugs?ws.op=searchTasks:
    get:
      tags:
        - bugs
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: bugs-searchTasks
      parameters:
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /bugs?ws.op=getBugData:
    get:
      tags:
        - bugs
      summary: Get bug data
      description: "Search bugtasks matching the specified criteria. The only criteria currently supported is to search for a bugtask with the specified bug id. return: a list of matching bugs represented as json data"
      operationId: bugs-getBugData
      parameters:
        - name: bug_id
          in: query
          required: true
          description: Bug ID
          schema:
            type: string
          example: "1"
        - name: related_bug
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: bug
      responses:
        "200":
          description: Successful response
  /bugs?ws.op=createBug:
    post:
      tags:
        - bugs
      summary: Create bug
      description: "Create a bug (with an appropriate bugtask) and return it. param target: The Project, Distribution or DistributionSourcePackage affected by this bug. param title: The title shown in bug listings. param description: The description of the issue. param information_type: Set the bug's information type to one different from the project's default. The type must conform to the project's bug sharing policy. (optional) param tags: A list of bug tags. (optional) param security_related: Is this bug's information type Private Security? (deprecated) param tags: Is this bug's information type Private user data. (deprecated) Things to note when using this factory: The reporter will be subscribed to the bug. Only people that the project shares with will see the bug when the bug's information type is Proprietary, Private, or Private Security."
      operationId: bugs-createBug
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                title:
                  type: string
                  description: Summary A one-line summary of the problem.
                  example: title
                description:
                  type: string
                  description: Description A detailed description of the problem, including the steps required to reproduce it.
                  example: description
                tags:
                  type: string
                  description: Tags Space-separated keywords for classifying this bug report.
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information Type The type of information contained in this bug report.
                security_related:
                  type: string
                  description: This bug is a security vulnerability.
                private:
                  type: string
                  description: This bug report should be private Private bug reports are visible only to their subscribers.
                target:
                  type: string
                  format: uri
                  x-lp-resource-type: bug_target
                  description: The project, distribution or source package that has this bug.
                  example: launchpad
              required:
                - title
                - description
                - target
      responses:
        "200":
          description: Successful response
  /bugs:
    get:
      tags:
        - bugs
      summary: List bugs
      description: Application root for malone.
      operationId: bugs-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug-page"
  /package-sets?ws.op=getByName:
    get:
      tags:
        - packagesets
      summary: Get by name
      description: "Return the single package set with the given name (if any). param distroseries: the distroseries to which the new packageset is related. param name: the name of the package set sought. return: An IPackageset instance. raise NoSuchPackageSet: if no package set is found."
      operationId: packagesets-getByName
      parameters:
        - name: name
          in: query
          required: true
          description: Valid package set name
          schema:
            type: string
          example: ubuntu
        - name: distroseries
          in: query
          required: true
          description: Distribution series The distroseries to which this package set is related.
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
          example: noble
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-full"
  /package-sets?ws.op=getBySeries:
    get:
      tags:
        - packagesets
      summary: Get by series
      description: "Return the package sets associated with the given distroseries. param distroseries: A DistroSeries. return: An iterable collection of IPackageset instances."
      operationId: packagesets-getBySeries
      parameters:
        - name: distroseries
          in: query
          required: true
          description: Distribution series The distroseries to which this package set is related.
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
          example: noble
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-page"
  /package-sets?ws.op=setsIncludingSource:
    get:
      tags:
        - packagesets
      summary: Sets including source
      description: "Get the package sets that include this source package. Return all package sets that directly or indirectly include the given source package name. param sourcepackagename: the included source package name; can be either a string or a ISourcePackageName. param distroseries: the IDistroSeries in which to look for sets. If omitted, matching package sets from all series will be returned. param direct_inclusion: if this flag is set to True, then only package sets that directly include this source package name will be considered. raises NoSuchSourcePackageName: if a source package with the given name cannot be found. return: A (potentially empty) sequence of IPackageset instances."
      operationId: packagesets-setsIncludingSource
      parameters:
        - name: sourcepackagename
          in: query
          required: true
          description: Source package name
          schema:
            type: string
          example: bash
        - name: distroseries
          in: query
          required: false
          description: Distribution series The distroseries to which this package set is related.
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: direct_inclusion
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-page"
  /package-sets?ws.op=new:
    post:
      tags:
        - packagesets
      summary: New
      description: "Create a new package set. param name: the name of the package set to be created. param description: the description for the package set to be created. param owner: the owner of the package set to be created. param distroseries: the distroseries to which the new packageset is related. param related_set: the newly created package set is to be related to related_set (by being placed in the same package group). raises DuplicatePackagesetName: if a package set with the same name exists in distroseries already. return: a newly created IPackageset."
      operationId: packagesets-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Valid package set name
                  example: ubuntu
                description:
                  type: string
                  description: Package set description
                  example: description
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person The person who owns this package set.
                  example: ~launchpad
                distroseries:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distroseries The distribution series to which the packageset is related.
                  example: noble
                related_set:
                  type: string
                  format: uri
                  x-lp-resource-type: packageset
                  description: Related package set The new package set will share the package set group with this one.
              required:
                - name
                - description
                - owner
                - distroseries
      responses:
        "200":
          description: Successful response
  /package-sets:
    get:
      tags:
        - packagesets
      summary: List packagesets
      description: An interface for multiple package sets.
      operationId: packagesets-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-page"
  /people?ws.op=getByEmail:
    get:
      tags:
        - people
      summary: Get by email
      description: Return the person with the given email address. Return None if there is no person with the given email address.
      operationId: people-getByEmail
      parameters:
        - name: email
          in: query
          required: true
          schema:
            type: string
          example: foo.bar@example.com
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-full"
  /people?ws.op=find:
    get:
      tags:
        - people
      summary: Find
      description: Return all non-merged Persons and Teams whose name, displayname or email address match <text>. The results will be ordered using the default ordering specified in Person._defaultOrder. While we don't have Full Text Indexes in the emailaddress table, we'll be trying to match the text only against the beginning of an email address.
      operationId: people-find
      parameters:
        - name: text
          in: query
          required: true
          description: Search text
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /people?ws.op=findPerson:
    get:
      tags:
        - people
      summary: Find person
      description: Return all non-merged Persons with at least one email address whose name, displayname or email address match <text>. If text is an empty string, all persons with at least one email address will be returned. The results will be ordered using the default ordering specified in Person._defaultOrder. If exclude_inactive_accounts is True, any accounts whose account_status is any of INACTIVE_ACCOUNT_STATUSES will not be in the returned set. If must_have_email is True, only people with one or more email addresses are returned. While we don't have Full Text Indexes in the emailaddress table, we'll be trying to match the text only against the beginning of an email address. If created_before or created_after are not None, they are used to restrict the search to the dates provided.
      operationId: people-findPerson
      parameters:
        - name: text
          in: query
          required: true
          description: Search text
          schema:
            type: string
          example: ubuntu
        - name: created_after
          in: query
          required: false
          description: Created after
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Created before
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /people?ws.op=findTeam:
    get:
      tags:
        - people
      summary: Find team
      description: Return all Teams whose name, displayname or email address match <text>. The results will be ordered using the default ordering specified in Person._defaultOrder. While we don't have Full Text Indexes in the emailaddress table, we'll be trying to match the text only against the beginning of an email address.
      operationId: people-findTeam
      parameters:
        - name: text
          in: query
          required: true
          description: Search text
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /people?ws.op=getUserData:
    get:
      tags:
        - people
      summary: Get user data
      description: Get GDPR-related data for a user from their email address.
      operationId: people-getUserData
      parameters:
        - name: email
          in: query
          required: true
          schema:
            type: string
          example: foo.bar@example.com
      responses:
        "200":
          description: Successful response
  /people?ws.op=getByOpenIDIdentifier:
    get:
      tags:
        - people
      summary: Get by open ididentifier
      description: "Get the person for a given OpenID identifier. param openid_identifier: full OpenID identifier URL for the user. return: the corresponding IPerson or None if the identifier is unknown"
      operationId: people-getByOpenIDIdentifier
      parameters:
        - name: identifier
          in: query
          required: true
          schema:
            type: string
          example: identifier
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-full"
  /people?ws.op=getUsernameForSSO:
    get:
      tags:
        - people
      summary: Get username for sso
      description: "Restricted person creation API for SSO. This method can only be called by the Ubuntu SSO service. It finds the username for an account by OpenID identifier. param user: the IPerson performing the operation. Only the ubuntu-sso celebrity is allowed. param openid_identifier: OpenID identifier suffix for the user. This is not the full URL, just the unique suffix portion."
      operationId: people-getUsernameForSSO
      parameters:
        - name: openid_identifier
          in: query
          required: true
          description: OpenID identifier suffix
          schema:
            type: string
          example: openid_identifier
      responses:
        "200":
          description: Successful response
  /people?ws.op=getSSHKeysForSSO:
    get:
      tags:
        - people
      summary: Get sshkeys for sso
      description: "Restricted SSH key creation API for SSO. This method can only be called by the Ubuntu SSO service. It finds and returns all the SSH keys belonging to the account identified by the openid_identifier parameter. param user: the IPerson performing the operation. Only the ubuntu-sso celebrity is allowed. param openid_identifier: OpenID identifier suffix for the user. This is not the full URL, just the unique suffix portion."
      operationId: people-getSSHKeysForSSO
      parameters:
        - name: openid_identifier
          in: query
          required: true
          description: OpenID identifier suffix
          schema:
            type: string
          example: openid_identifier
      responses:
        "200":
          description: Successful response
  /people?ws.op=newTeam:
    post:
      tags:
        - people
      summary: New team
      description: Create and return a new Team with given arguments.
      operationId: people-newTeam
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
                  example: ubuntu
                display_name:
                  type: string
                  description: Display Name Your name as you would like it displayed throughout Launchpad. Most people use their full name here.
                  example: display_name
                team_description:
                  type: string
                  description: Team Description Obsolete. Use description.
                default_membership_period:
                  type: string
                  description: Subscription period Number of days a new subscription lasts before expiring. You can customize the length of an individual subscription when approving it. Leave this empty or set to 0 for subscriptions to never expire.
                default_renewal_period:
                  type: string
                  description: Self renewal period Number of days members can renew their own membership. The number can be from 1 to 3650 (10 years).
                subscription_policy:
                  type: string
                  enum:
                    - Open Team
                    - Delegated Team
                    - Moderated Team
                    - Restricted Team
                  description: "Membership policy Obsolete: use membership_policy"
                membership_policy:
                  type: string
                  enum:
                    - Open Team
                    - Delegated Team
                    - Moderated Team
                    - Restricted Team
                  description: Membership policy
              required:
                - name
                - display_name
      responses:
        "200":
          description: Successful response
  /people?ws.op=getOrCreateSoftwareCenterCustomer:
    post:
      tags:
        - people
      summary: Get or create software center customer
      description: "Restricted person creation API for Software Center Agent. This method can only be called by Software Center Agent. It gets a person by OpenID identifier or creates a new Launchpad person from the OpenID identifier, email address and display name. param user: the IPerson performing the operation. Only the software-center-agent celebrity is allowed. param openid_identifier: OpenID identifier suffix for the user. This is not the full URL, just the unique suffix portion. param email_address: the email address of the user. param full_name: the full name of the user."
      operationId: people-getOrCreateSoftwareCenterCustomer
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                openid_identifier:
                  type: string
                  description: OpenID identifier suffix
                  example: openid_identifier
                email_address:
                  type: string
                  description: Email address
                  example: email_address
                display_name:
                  type: string
                  description: Display name
                  example: display_name
              required:
                - openid_identifier
                - email_address
                - display_name
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-full"
  /people?ws.op=setUsernameFromSSO:
    post:
      tags:
        - people
      summary: Set username from sso
      description: "Restricted person creation API for SSO. This method can only be called by the Ubuntu SSO service. It reserves a username for an account by OpenID identifier, as long as the user has no Launchpad account. param user: the IPerson performing the operation. Only the ubuntu-sso celebrity is allowed. param openid_identifier: OpenID identifier suffix for the user. This is not the full URL, just the unique suffix portion. param name: the desired username. raises: InvalidName if the username doesn't meet character constraints. raises: NameAlreadyTaken if the username is already in use. raises: NotPlaceholderAccount if the OpenID identifier has a non-placeholder Launchpad account."
      operationId: people-setUsernameFromSSO
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                openid_identifier:
                  type: string
                  description: OpenID identifier suffix
                  example: openid_identifier
                name:
                  type: string
                  description: Name A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
                  example: ubuntu
                dry_run:
                  type: string
                  description: Don't save changes
              required:
                - openid_identifier
                - name
      responses:
        "200":
          description: Successful response
  /people?ws.op=addSSHKeyFromSSO:
    post:
      tags:
        - people
      summary: Add sshkey from sso
      description: "Restricted SSH key creation API for SSO. This method can only be called by the Ubuntu SSO service. It adds a new SSH key to the account identified by 'openid_identifier' based on the 'key_text' parameter. param user: the IPerson performing the operation. Only the ubuntu-sso celebrity is allowed. param openid_identifier: OpenID identifier suffix for the user. This is not the full URL, just the unique suffix portion. param key_text: The full text of the SSH Key. raises NoSuchAccount: If the openid_identifier specified does not match any account. raises SSHKeyAdditionError: If the ssh key_text is invalid."
      operationId: people-addSSHKeyFromSSO
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                openid_identifier:
                  type: string
                  description: OpenID identifier suffix
                  example: openid_identifier
                key_text:
                  type: string
                  description: SSH key text
                  example: key_text
                dry_run:
                  type: string
                  description: Don't save changes
              required:
                - openid_identifier
                - key_text
      responses:
        "200":
          description: Successful response
  /people?ws.op=deleteSSHKeyFromSSO:
    post:
      tags:
        - people
      summary: Delete sshkey from sso
      description: "Restricted SSH key deletion API for SSO. This method can only be called by the Ubuntu SSO service. It deletes an SSH key from the account identified by 'openid_identifier' based on the 'key_text' parameter. param user: the IPerson performing the operation. Only the ubuntu-sso celebrity is allowed. param openid_identifier: OpenID identifier suffix for the user. This is not the full URL, just the unique suffix portion. param key_text: The full text of the SSH Key. raises NoSuchAccount: If the openid_identifier specified does not match any account. raises KeyAdditionError: If the key text is invalid."
      operationId: people-deleteSSHKeyFromSSO
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                openid_identifier:
                  type: string
                  description: OpenID identifier suffix
                  example: openid_identifier
                key_text:
                  type: string
                  description: SSH key text
                  example: key_text
                dry_run:
                  type: string
                  description: Don't save changes
              required:
                - openid_identifier
                - key_text
      responses:
        "200":
          description: Successful response
  /people:
    get:
      tags:
        - people
      summary: List people
      description: Combined schema for operations on a group of Persons.
      operationId: people-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /+polls?ws.op=find:
    get:
      tags:
        - polls
      summary: Find
      description: "Search for polls. param team: An optional ITeam to filter by. param status: A collection containing as many values as you want from PollStatus. Defaults to PollStatus.ALL. param order_by: An optional PollSort item indicating how to sort the results. Defaults to PollSort.NEWEST_FIRST. param when: Used only by tests, to filter for polls open at a specific date."
      operationId: polls-find
      parameters:
        - name: team
          in: query
          required: false
          description: Team
          schema:
            type: string
            format: uri
            x-lp-resource-type: team
        - name: status
          in: query
          required: false
          description: Poll statuses A list of one or more of 'open', 'closed', or 'not-yet-opened'. Defaults to all statuses.
          schema:
            type: string
        - name: order_by
          in: query
          required: false
          description: Sort order
          schema:
            type: string
            enum:
              - oldest first
              - newest first
              - by opening date
              - by closing date
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/poll-page"
  /+polls:
    get:
      tags:
        - polls
      summary: List polls
      description: The set of Poll objects.
      operationId: polls-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/poll-page"
  /+processors?ws.op=getByName:
    get:
      tags:
        - processors
      summary: Get by name
      description: "Return the IProcessor instance with the matching name. param name: The name to look for. raise ProcessorNotFound: if there is no processor with that name. return: A IProcessor instance if found"
      operationId: processors-getByName
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/processor-full"
  /+processors:
    get:
      tags:
        - processors
      summary: List processors
      description: Operations related to Processor instances.
      operationId: processors-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/processor-page"
  /projects?ws.op=licensing_search:
    get:
      tags:
        - projects
      summary: Licensing search
      description: Return an iterator over products that need to be reviewed.
      operationId: projects-licensing_search
      parameters:
        - name: search_text
          in: query
          required: false
          description: Search text
          schema:
            type: string
        - name: active
          in: query
          required: false
          description: Is the project active
          schema:
            type: string
        - name: project_reviewed
          in: query
          required: false
          description: Is the project licence reviewed
          schema:
            type: string
        - name: licenses
          in: query
          required: false
          description: Licenses
          schema:
            type: string
        - name: created_after
          in: query
          required: false
          description: Created after date
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Created before date
          schema:
            type: string
        - name: has_subscription
          in: query
          required: false
          description: Has a commercial subscription
          schema:
            type: string
        - name: subscription_expires_after
          in: query
          required: false
          description: Subscription expires after
          schema:
            type: string
        - name: subscription_expires_before
          in: query
          required: false
          description: Subscription expired before
          schema:
            type: string
        - name: subscription_modified_after
          in: query
          required: false
          description: Subscription modified after
          schema:
            type: string
        - name: subscription_modified_before
          in: query
          required: false
          description: Subscription modified before
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project-page"
  /projects?ws.op=search:
    get:
      tags:
        - projects
      summary: Search
      description: Search through the Registry database for products that match the query terms. text is a piece of text in the title / summary / description fields of product. This call eager loads data appropriate for web API; caution may be needed for other callers.
      operationId: projects-search
      parameters:
        - name: text
          in: query
          required: false
          description: Search text
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project-page"
  /projects?ws.op=latest:
    get:
      tags:
        - projects
      summary: Latest
      description: Return the latest projects registered in Launchpad. The supplied user determines which objects are visible. If the quantity is not specified or is a value that is not 'None' then the set of projects returned is limited to that value (the default quantity is 5). If quantity is 'None' then all projects are returned. For the web service it is not possible to specify the quantity, so all projects are returned, latest first.
      operationId: projects-latest
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project-page"
  /projects?ws.op=new_project:
    post:
      tags:
        - projects
      summary: New project
      description: Create and return a brand new Product. See IProduct for a description of the parameters.
      operationId: projects-new_project
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name At least one lowercase letter or number, followed by letters, numbers, dots, hyphens or pluses. Keep this name short; it is used in URLs as shown above.
                  example: ubuntu
                display_name:
                  type: string
                  description: Display Name The name of the project as it would appear in a paragraph.
                  example: display_name
                title:
                  type: string
                  description: Title The project title. Should be just a few words.
                  example: title
                summary:
                  type: string
                  description: Summary A short paragraph to introduce the project's work.
                  example: summary
                description:
                  type: string
                  description: Description Details about the project's work, highlights, goals, and how to contribute. Use plain text, paragraphs are preserved and URLs are linked in pages. Don't repeat the Summary.
                project_group:
                  type: string
                  format: uri
                  x-lp-resource-type: project_group
                  description: Part of Project group. This is an overarching initiative that includes several related projects. For example, the Mozilla Project produces Firefox, Thunderbird and Gecko. This information is used to group those projects in a coherent way. If you make this project part of a group, the group preferences and decisions around bug tracking, translation and security policy will apply to this project.
                home_page_url:
                  type: string
                  description: Homepage URL The project home page. Please include the http://
                screenshots_url:
                  type: string
                  description: Screenshots URL The full URL for screenshots of this project, if available. Please include the http://
                download_url:
                  type: string
                  description: Download URL The full URL where downloads for this project are located, if available. Please include the http://
                freshmeat_project:
                  type: string
                  description: Freshmeat Project The Freshmeat project name for this project, if it is in freshmeat. [DEPRECATED]
                wiki_url:
                  type: string
                  description: Wiki URL The full URL of this project's wiki, if it has one. Please include the http://
                sourceforge_project:
                  type: string
                  description: Sourceforge Project The SourceForge project name for this project, if it is in sourceforge.
                programming_lang:
                  type: string
                  description: Programming Languages A comma delimited list of programming languages used for this project.
                project_reviewed:
                  type: string
                  description: Project reviewed Whether or not this project has been reviewed. If you looked at the project and how it uses Launchpad, you reviewed it.
                licenses:
                  type: string
                  description: Licences
                license_info:
                  type: string
                  description: Description of additional licences Description of licences that do not appear in the list above.
                registrant:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Registrant This person registered the project in Launchpad.
                bug_supervisor:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Bug Supervisor The Launchpad id of the person or team (preferred) responsible for bug management.
                driver:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Driver This person or team will be able to set feature goals for and approve bug targeting or backporting for ANY major series in this project. You might want to leave this blank and just appoint a team for each specific series, rather than having one project team that does it all.
              required:
                - name
                - display_name
                - title
                - summary
      responses:
        "200":
          description: Successful response
  /projects:
    get:
      tags:
        - projects
      summary: List projects
      operationId: projects-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project-page"
  /projectgroups?ws.op=search:
    get:
      tags:
        - project_groups
      summary: Search
      description: Search through the Registry database for projects that match the query terms. text is a piece of text in the title / summary / description fields of project (and possibly product). soyuz, bazaar, malone etc are hints as to whether the search should be limited to projects that are active in those Launchpad applications.
      operationId: project_groups-search
      parameters:
        - name: text
          in: query
          required: false
          description: Search text
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project_group-page"
  /projectgroups:
    get:
      tags:
        - project_groups
      summary: List project groups
      description: The collection of projects.
      operationId: project_groups-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project_group-page"
  /questions?ws.op=searchQuestions:
    get:
      tags:
        - questions
      summary: Search questions
      description: "Return the questions from the collection matching search criteria. param search_text: A string that is matched against the question title and description. If None, the search_text is not included as a filter criteria. param status: A sequence of QuestionStatus Items. If None or an empty sequence, the status is not included as a filter criteria. param language: An ILanguage or a sequence of ILanguage objects to match against the question's language. If None or an empty sequence, the language is not included as a filter criteria. param sort: An attribute of QuestionSort. If None, a default value is used. When there is a search_text value, the default is to sort by RELEVANCY, otherwise results are sorted NEWEST_FIRST. param created_since: Only return results whose datecreated property is greater than or equal to this date. param created_before: Only return results whose datecreated property is smaller than this date."
      operationId: questions-searchQuestions
      parameters:
        - name: search_text
          in: query
          required: false
          description: Search text
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status
          schema:
            type: string
        - name: language
          in: query
          required: false
          description: Language
          schema:
            type: string
        - name: sort
          in: query
          required: false
          description: Sort
          schema:
            type: string
            enum:
              - by relevancy
              - by status
              - newest first
              - oldest first
              - recently updated first
        - name: created_before
          in: query
          required: false
          description: Search for questions that were created before the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for questions that were created since the given date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /questions?ws.op=getByID:
    get:
      tags:
        - questions
      summary: Get by id
      description: "Return the question with the given id. Return :default: if no such question exists."
      operationId: questions-getByID
      parameters:
        - name: question_id
          in: query
          required: true
          description: The id of the question to get
          schema:
            type: string
          example: "1"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-full"
  /questions:
    get:
      tags:
        - questions
      summary: List questions
      description: A utility that contain all the questions published in Launchpad.
      operationId: questions-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /+rock-bases?ws.op=getByDistroSeries:
    get:
      tags:
        - rock_bases
      summary: Get by distro series
      description: "Return the IRockBase for this distro series. raises NoSuchRockBase: if no base exists for this distro series."
      operationId: rock_bases-getByDistroSeries
      parameters:
        - name: distro_series
          in: query
          required: true
          description: Distro series
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
          example: noble
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/rock_base-full"
  /+rock-bases?ws.op=new:
    post:
      tags:
        - rock_bases
      summary: New
      description: Create an IRockBase.
      operationId: rock_bases-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                distro_series:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro series
                  example: noble
                build_channels:
                  type: string
                  description: Source snap channels for builds A dictionary mapping snap names to channels to use when building rock recipes that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
                  example: build_channels
                processors:
                  type: string
              required:
                - distro_series
                - build_channels
      responses:
        "200":
          description: Successful response
  /+rock-bases:
    get:
      tags:
        - rock_bases
      summary: List rock bases
      description: Interface representing the set of bases for rocks.
      operationId: rock_bases-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/rock_base-page"
  /+rock-recipes?ws.op=getByName:
    get:
      tags:
        - rock_recipes
      summary: Get by name
      description: Returns the appropriate IRockRecipe for the given objects.
      operationId: rock_recipes-getByName
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: project
          in: query
          required: true
          description: Project
          schema:
            type: string
            format: uri
            x-lp-resource-type: project
          example: launchpad
        - name: name
          in: query
          required: true
          description: Recipe name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/rock_recipe-full"
  /+rock-recipes?ws.op=findByOwner:
    get:
      tags:
        - rock_recipes
      summary: Find by owner
      description: Return all rock recipes with the given owner.
      operationId: rock_recipes-findByOwner
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/rock_recipe-page"
  /+rock-recipes?ws.op=new:
    post:
      tags:
        - rock_recipes
      summary: New
      description: Create an IRockRecipe.
      operationId: rock_recipes-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner The owner of this rock recipe.
                  example: ~launchpad
                project:
                  type: string
                  format: uri
                  x-lp-resource-type: project
                  description: The project that this rock recipe is associated with
                  example: launchpad
                name:
                  type: string
                  description: Rock recipe name The name of the rock recipe.
                  example: ubuntu
                description:
                  type: string
                  description: Description A description of the rock recipe.
                git_repository:
                  type: string
                  format: uri
                  x-lp-resource-type: git_repository
                  description: Git repository A Git repository with a branch containing a rockcraft.yaml recipe.
                git_repository_url:
                  type: string
                  description: Git repository URL The URL of a Git repository with a branch containing a rockcraft.yaml at the top level.
                git_path:
                  type: string
                  description: Git branch path The path of the Git branch containing a rockcraft.yaml recipe.
                git_ref:
                  type: string
                  format: uri
                  x-lp-resource-type: git_ref
                  description: Git branch The Git branch containing a rockcraft.yaml recipe.
                build_path:
                  type: string
                  description: Build path Subdirectory within the branch containing rockcraft.yaml.
                auto_build:
                  type: string
                  description: Automatically build when branch changes Whether this rock recipe is built automatically when the branch containing its rockcraft.yaml recipe changes.
                auto_build_channels:
                  type: string
                  description: Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this rock recipe. Currently only 'core', 'core18', 'core20', and 'rockcraft' keys are supported.
                store_upload:
                  type: string
                  description: Automatically upload to store Whether builds of this rock recipe are automatically uploaded to the store.
                store_name:
                  type: string
                  description: Registered store name The registered name of this rock in the store.
                store_channels:
                  type: string
                  description: Store channels Channels to release this rock to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
                use_fetch_service:
                  type: string
                  description: Use fetch service If set, Rock builds will use the fetch-service instead of the builder-proxy to access external resources.
                fetch_service_policy:
                  type: string
                  enum:
                    - strict
                    - permissive
                  description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False.
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information type The type of information contained in this rock recipe.
              required:
                - owner
                - project
                - name
      responses:
        "200":
          description: Successful response
  /+rock-recipes:
    get:
      tags:
        - rock_recipes
      summary: List rock recipes
      description: A utility to create and access rock recipes.
      operationId: rock_recipes-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/rock_recipe-page"
  /+snap-bases?ws.op=getByName:
    get:
      tags:
        - snap_bases
      summary: Get by name
      description: "Return the ISnapBase with this name. raises NoSuchSnapBase: if no base exists with this name."
      operationId: snap_bases-getByName
      parameters:
        - name: name
          in: query
          required: true
          description: Base name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap_base-full"
  /+snap-bases?ws.op=getDefault:
    get:
      tags:
        - snap_bases
      summary: Get default
      description: Get the default base. This will be used to pick the default distro series for snaps that do not specify a base.
      operationId: snap_bases-getDefault
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap_base-full"
  /+snap-bases?ws.op=new:
    post:
      tags:
        - snap_bases
      summary: New
      description: Create an ISnapBase.
      operationId: snap_bases-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name
                  example: ubuntu
                display_name:
                  type: string
                  description: Display name
                  example: display_name
                distro_series:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro series
                  example: noble
                build_channels:
                  type: string
                  description: Source snap channels for builds A dictionary mapping snap names to channels to use when building snaps that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
                  example: build_channels
                features:
                  type: string
                  description: Features supported by this base A dictionary designating the features supported by the base. Key is the name of a feature, value is a boolean indicating whether the feature is supported or not.
                processors:
                  type: string
              required:
                - name
                - display_name
                - distro_series
                - build_channels
      responses:
        "200":
          description: Successful response
  /+snap-bases?ws.op=setDefault:
    post:
      tags:
        - snap_bases
      summary: Set default
      description: "Set the default base. This will be used to pick the default distro series for snaps that do not specify a base. param snap_base: An ISnapBase, or None to unset the default base."
      operationId: snap_bases-setDefault
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                snap_base:
                  type: string
                  format: uri
                  x-lp-resource-type: snap_base
                  description: Base
                  example: snap_base
              required:
                - snap_base
      responses:
        "200":
          description: Successful response
  /+snap-bases:
    get:
      tags:
        - snap_bases
      summary: List snap bases
      description: Interface representing the set of bases for snaps.
      operationId: snap_bases-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap_base-page"
  /+snaps?ws.op=getByName:
    get:
      tags:
        - snaps
      summary: Get by name
      description: Return the appropriate ISnap for the given objects.
      operationId: snaps-getByName
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: name
          in: query
          required: true
          description: Snap name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap-full"
  /+snaps?ws.op=findByOwner:
    get:
      tags:
        - snaps
      summary: Find by owner
      description: Return all snap packages with the given owner.
      operationId: snaps-findByOwner
      parameters:
        - name: owner
          in: query
          required: true
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap-page"
  /+snaps?ws.op=findByURL:
    get:
      tags:
        - snaps
      summary: Find by url
      description: "Return all snap packages that build from the given URL. This currently only works for packages that build directly from a URL, rather than being linked to a Bazaar branch or Git repository hosted in Launchpad. param url: A URL. param owner: Only return packages owned by this user. param visible_by_user: If not None, only return packages visible by this user; otherwise, only return publicly-visible packages."
      operationId: snaps-findByURL
      parameters:
        - name: url
          in: query
          required: true
          description: The URL to search for.
          schema:
            type: string
          example: https://git.launchpad.net/launchpad
        - name: owner
          in: query
          required: false
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap-page"
  /+snaps?ws.op=findByURLPrefix:
    get:
      tags:
        - snaps
      summary: Find by urlprefix
      description: "Return all snap packages that build from a URL with this prefix. This currently only works for packages that build directly from a URL, rather than being linked to a Bazaar branch or Git repository hosted in Launchpad. param url_prefix: A URL prefix. param owner: Only return packages owned by this user. param visible_by_user: If not None, only return packages visible by this user; otherwise, only return publicly-visible packages."
      operationId: snaps-findByURLPrefix
      parameters:
        - name: url_prefix
          in: query
          required: true
          description: The URL prefix to search for.
          schema:
            type: string
          example: url_prefix
        - name: owner
          in: query
          required: false
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap-page"
  /+snaps?ws.op=findByURLPrefixes:
    get:
      tags:
        - snaps
      summary: Find by urlprefixes
      description: "Return all snap packages that build from a URL with any of these prefixes. This currently only works for packages that build directly from a URL, rather than being linked to a Bazaar branch or Git repository hosted in Launchpad. param url_prefixes: A list of URL prefixes. param owner: Only return packages owned by this user. param visible_by_user: If not None, only return packages visible by this user; otherwise, only return publicly-visible packages."
      operationId: snaps-findByURLPrefixes
      parameters:
        - name: url_prefixes
          in: query
          required: true
          description: The URL prefixes to search for.
          schema:
            type: string
          example: url_prefixes
        - name: owner
          in: query
          required: false
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap-page"
  /+snaps?ws.op=findByStoreName:
    get:
      tags:
        - snaps
      summary: Find by store name
      description: "Return all snap packages with the given store package name. param store_name: A registered store package name. param owner: Only return packages owned by this user. param visible_by_user: If not None, only return packages visible by this user; otherwise, only return publicly-visible packages."
      operationId: snaps-findByStoreName
      parameters:
        - name: store_name
          in: query
          required: true
          description: The registered store package name to search for.
          schema:
            type: string
          example: store_name
        - name: owner
          in: query
          required: false
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap-page"
  /+snaps?ws.op=new:
    post:
      tags:
        - snaps
      summary: New
      description: Create an ISnap.
      operationId: snaps-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner The owner of this snap package.
                  example: ~launchpad
                distro_series:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro Series The series for which the snap package should be built. If not set, Launchpad will infer an appropriate series from snapcraft.yaml.
                name:
                  type: string
                  description: Snap recipe name The name of the snap build recipe.
                  example: ubuntu
                description:
                  type: string
                  description: Description A description of the snap package.
                branch:
                  type: string
                  format: uri
                  x-lp-resource-type: branch
                  description: Bazaar branch A Bazaar branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
                git_repository:
                  type: string
                  format: uri
                  x-lp-resource-type: git_repository
                  description: Git repository A Git repository with a branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
                git_repository_url:
                  type: string
                  description: Git repository URL The URL of a Git repository with a branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
                git_path:
                  type: string
                  description: Git branch path The path of the Git branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
                git_ref:
                  type: string
                  format: uri
                  x-lp-resource-type: git_ref
                  description: Git branch The Git branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
                build_path:
                  type: string
                  description: Build path Subdirectory within the branch containing snapcraft.yaml.
                auto_build:
                  type: string
                  description: Automatically build when branch changes Whether this snap package is built automatically when the branch containing its snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe changes.
                auto_build_archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  description: Source archive for automatic builds The archive from which automatic builds of this snap package should be built.
                auto_build_pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket for automatic builds The package stream within the source archive and distribution series to use when building the snap package. If the source archive is a PPA, then the PPA's archive dependencies will be used to select the pocket in the distribution's primary archive.
                store_upload:
                  type: string
                  description: Automatically upload to store Whether builds of this snap package are automatically uploaded to the store.
                store_series:
                  type: string
                  format: uri
                  x-lp-resource-type: snappy_series
                  description: Store series The series in which this snap package should be published in the store.
                store_name:
                  type: string
                  description: Registered store package name The registered name of this snap package in the store.
                store_channels:
                  type: string
                  description: Store channels Channels to release this snap package to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
                project:
                  type: string
                  format: uri
                  x-lp-resource-type: project
                  description: The project that this Snap is associated with
                use_fetch_service:
                  type: string
                  description: Use fetch service If set, Snap builds will use the fetch-service instead of the builder-proxy to access external resources.
                fetch_service_policy:
                  type: string
                  enum:
                    - strict
                    - permissive
                  description: Fetch service policy Which policy to use when using the fetch service. The “strict” mode only allows certain resources and formats, and errors out in any case the restrictions are violated. The “permissive” mode works similarly, but only logs a warning when encountering any violations.
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information type The type of information contained in this Snap recipe.
                processors:
                  type: string
              required:
                - owner
                - name
      responses:
        "200":
          description: Successful response
  /+snaps:
    get:
      tags:
        - snaps
      summary: List snaps
      description: A utility to create and access snap packages.
      operationId: snaps-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snap-page"
  /+snappy-series?ws.op=getByName:
    get:
      tags:
        - snappy_serieses
      summary: Get by name
      description: "Return the ISnappySeries with this name. raises NoSuchSnappySeries: if no snappy series exists with this name."
      operationId: snappy_serieses-getByName
      parameters:
        - name: name
          in: query
          required: true
          description: Snappy series name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snappy_series-full"
  /+snappy-series?ws.op=new:
    post:
      tags:
        - snappy_serieses
      summary: New
      description: Create an ISnappySeries.
      operationId: snappy_serieses-new
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name
                  example: ubuntu
                display_name:
                  type: string
                  description: Display name
                  example: display_name
                status:
                  type: string
                  enum:
                    - Experimental
                    - Active Development
                    - Pre-release Freeze
                    - Current Stable Release
                    - Supported
                    - Obsolete
                    - Future
                  description: Status
                  example: Experimental
              required:
                - name
                - display_name
                - status
      responses:
        "200":
          description: Successful response
  /+snappy-series:
    get:
      tags:
        - snappy_serieses
      summary: List snappy serieses
      description: Interface representing the set of snappy series.
      operationId: snappy_serieses-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/snappy_series-page"
  /specifications?ws.op=createSpecification:
    post:
      tags:
        - specifications
      summary: Create specification
      description: Create a new specification.
      operationId: specifications-createSpecification
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "Name May contain lower-case letters, numbers, and dashes. It will be used in the specification url. Examples: mozilla-type-ahead-find, postgres-smart-serial."
                  example: ubuntu
                title:
                  type: string
                  description: Title Describe the feature as clearly as possible in up to 70 characters. This title is displayed in every feature list or report.
                  example: title
                specurl:
                  type: string
                  description: Specification URL The URL of the specification. This is usually a wiki page.
                summary:
                  type: string
                  description: Summary A single-paragraph description of the feature. This will also be displayed in most feature listings.
                  example: summary
                definition_status:
                  type: string
                  enum:
                    - Approved
                    - Pending Approval
                    - Review
                    - Drafting
                    - Discussion
                    - New
                    - Superseded
                    - Obsolete
                  description: Definition Status The current status of the process to define the feature and get approval for the implementation plan.
                assignee:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Assignee The person responsible for implementing the feature.
                drafter:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Drafter The person responsible for drafting the specification.
                whiteboard:
                  type: string
                  description: Status Whiteboard Any notes on the status of this spec you would like to make. Your changes will override the current text.
                target:
                  type: string
                  format: uri
                  x-lp-resource-type: specification_target
                  description: The product or distribution context of this specification.
                  example: launchpad
              required:
                - name
                - title
                - summary
                - target
      responses:
        "200":
          description: Successful response
  /specifications:
    get:
      tags:
        - specifications
      summary: List specifications
      description: A container for specifications.
      operationId: specifications-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/specification-page"
  /temporary-blobs?ws.op=fetch:
    get:
      tags:
        - temporary_blobs
      summary: Fetch
      description: Retrieve a TemporaryBlobStorage by uuid.
      operationId: temporary_blobs-fetch
      parameters:
        - name: token
          in: query
          required: true
          description: UUID
          schema:
            type: string
          example: token
      responses:
        "200":
          description: Successful response
  /temporary-blobs:
    get:
      tags:
        - temporary_blobs
      summary: List temporary blobs
      description: A tool to create temporary blobs.
      operationId: temporary_blobs-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/temporary_blob-page"
  /+groups?ws.op=getByName:
    get:
      tags:
        - translation_groups
      summary: Get by name
      description: Get a translation group by name.
      operationId: translation_groups-getByName
      parameters:
        - name: name
          in: query
          required: true
          description: Name of the translation group
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_group-full"
  /+groups:
    get:
      tags:
        - translation_groups
      summary: List translation groups
      description: A container for translation groups.
      operationId: translation_groups-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_group-page"
  /+imports?ws.op=getAllEntries:
    get:
      tags:
        - translation_import_queue_entries
      summary: Get all entries
      description: "Return all entries this import queue has. arg target: IPerson, IProduct, IProductSeries, IDistribution, IDistroSeries or ISourcePackage the import entries are attached to or None to get all entries available. arg import_status: RosettaImportStatus entry. arg file_extensions: Sequence of filename suffixes to match, usually 'po' or 'pot'. If any of target, status or file_extension are given, the returned entries are filtered based on those values."
      operationId: translation_import_queue_entries-getAllEntries
      parameters:
        - name: import_status
          in: query
          required: false
          description: The status of the import.
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /+imports?ws.op=getFirstEntryToImport:
    get:
      tags:
        - translation_import_queue_entries
      summary: Get first entry to import
      description: "Return the first entry of the queue ready to be imported. param target: IPerson, IProduct, IProductSeries, IDistribution, IDistroSeries or ISourcePackage the import entries are attached to or None to get all entries available."
      operationId: translation_import_queue_entries-getFirstEntryToImport
      parameters:
        - name: target
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: object_with_translation_imports
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-full"
  /+imports?ws.op=getRequestTargets:
    get:
      tags:
        - translation_import_queue_entries
      summary: Get request targets
      description: "List Product`s and `DistroSeries with pending imports. arg status: Filter by RosettaImportStatus. All returned items will implement IHasTranslationImports."
      operationId: translation_import_queue_entries-getRequestTargets
      parameters:
        - name: status
          in: query
          required: false
          description: The status of the import.
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/object_with_translation_imports-page"
  /+imports:
    get:
      tags:
        - translation_import_queue_entries
      summary: List translation import queue entries
      description: A set of files to be imported into Rosetta.
      operationId: translation_import_queue_entries-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /vulnerabilities?ws.op=getImports:
    get:
      tags:
        - vulnerabilities
      summary: Get imports
      description: "Return the latest 10 import jobs done and their statuses. param handler: Get the import jobs performed by this handler."
      operationId: vulnerabilities-getImports
      parameters:
        - name: handler
          in: query
          required: true
          description: The handler to import or export vulnerability data
          schema:
            type: string
            enum:
              - SOSS Handler
              - UCT Handler
              - CPPA Handler
          example: SOSS Handler
      responses:
        "200":
          description: Successful response
  /vulnerabilities?ws.op=getExports:
    get:
      tags:
        - vulnerabilities
      summary: Get exports
      description: "Return the latest 10 export jobs done and link to their librarian files. param handler: Get the export jobs performed by this handler."
      operationId: vulnerabilities-getExports
      parameters:
        - name: handler
          in: query
          required: true
          description: The handler to import or export vulnerability data
          schema:
            type: string
            enum:
              - SOSS Handler
              - UCT Handler
              - CPPA Handler
          example: SOSS Handler
      responses:
        "200":
          description: Successful response
  /vulnerabilities?ws.op=importData:
    post:
      tags:
        - vulnerabilities
      summary: Import data
      description: 'Trigger a vulnerability data import param handler: The handler that will perform the import. param git_repository: The git repository to import from. param git_ref: The git reference to import from. param git_paths: The paths in the git repo to import from. Set it to [""] if you want to import every file. param information_type: The information type of the vulnerability. If the git repo is private, the information type must be PRIVATE. param import_since_commit_sha1: The commit SHA1 to import from. If null, import all data.'
      operationId: vulnerabilities-importData
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                handler:
                  type: string
                  enum:
                    - SOSS Handler
                    - UCT Handler
                    - CPPA Handler
                  description: The handler to import or export vulnerability data
                  example: SOSS Handler
                git_repository:
                  type: string
                  format: uri
                  x-lp-resource-type: git_repository
                  example: git_repository
                git_ref:
                  type: string
                  description: The git reference to import from.
                  example: git_ref
                git_paths:
                  type: string
                  description: The list of paths to import from.
                  example: git_paths
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information type Indicates the privacy of the vulnerability.
                import_since_commit_sha1:
                  type: string
                  description: Commit sha1 from which to do the import. All files added or modified since this commit will be imported.
              required:
                - handler
                - git_repository
                - git_ref
                - git_paths
      responses:
        "200":
          description: Successful response
  /vulnerabilities?ws.op=exportData:
    post:
      tags:
        - vulnerabilities
      summary: Export data
      description: "Trigger a vulnerability data export param handler: The handler that will perform the export. param sources: List of objects to get data from. If null, import all data."
      operationId: vulnerabilities-exportData
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                handler:
                  type: string
                  enum:
                    - SOSS Handler
                    - UCT Handler
                    - CPPA Handler
                  description: The handler to import or export vulnerability data
                  example: SOSS Handler
                sources:
                  type: string
                  description: List of objects to get data from. If null, import all data.
              required:
                - handler
      responses:
        "200":
          description: Successful response
  /vulnerabilities:
    get:
      tags:
        - vulnerabilities
      summary: List vulnerabilities
      description: The set of all vulnerabilities.
      operationId: vulnerabilities-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/vulnerability-page"
  /{archive_1}/+archive/{archive_2}?ws.op=searchTasks:
    get:
      tags:
        - archive
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: archive-searchTasks
      parameters:
        - &a1
          name: archive_1
          in: path
          required: true
          description: Path segment of the archive resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ubuntu
        - &a2
          name: archive_2
          in: path
          required: true
          description: Path segment of the archive resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: primary
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getBuildRecords:
    get:
      tags:
        - archive
      summary: Get build records
      description: "Return build records in the context it is implemented. It excludes build records generated by Gina (imported from a external repository), where IBuild.datebuilt is null and IBuild.buildstate is BuildStatus.FULLYBUILT. The result is simply not filtered if the optional filters are omitted by call sites. param build_state: optional BuildStatus value for filtering build records; param name: optional string for filtering build source package name. Sub-string matching is allowed via SQL LIKE. param pocket: optional PackagePublishingPocket value for filtering build records; param arch_tag: optional string for filtering build source packages by their architecture tag; param user: optional IPerson corresponding to the user performing the request. It will filter out build records for which the user have no 'view' permission. param binary_only: optional boolean indicating whether only BinaryPackageBuild objects should be returned, or more general PackageBuild objects (which may include, for example, SourcePackageRecipeBuild objects. return: a result set containing IPackageBuild records ordered by descending IPackageBuild.date_finished except when builds are filtered by BuildStatus.NEEDSBUILD, in this case records are ordered by descending BuildQueue.lastscore (dispatching order)."
      operationId: archive-getBuildRecords
      parameters:
        - *a1
        - *a2
        - name: source_name
          in: query
          required: false
          description: Source package name
          schema:
            type: string
        - name: build_state
          in: query
          required: false
          description: Build status The status of this build record
          schema:
            type: string
            enum:
              - Needs building
              - Successfully built
              - Failed to build
              - Dependency wait
              - Chroot problem
              - Build for superseded Source
              - Currently building
              - Failed to upload
              - Uploading build
              - Cancelling build
              - Cancelled build
              - Gathering build output
        - name: pocket
          in: query
          required: false
          description: Pocket The pocket into which this entry is published
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/build-page"
  /{archive_1}/+archive/{archive_2}?ws.op=checkUpload:
    get:
      tags:
        - archive
      summary: Check upload
      description: Wrapper around checkUpload for the web service API.
      operationId: archive-checkUpload
      parameters:
        - *a1
        - *a2
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: distroseries
          in: query
          required: true
          description: The distro series
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
          example: noble
        - name: sourcepackagename
          in: query
          required: true
          description: Source package name
          schema:
            type: string
          example: bash
        - name: component
          in: query
          required: true
          description: Component
          schema:
            type: string
          example: component
        - name: pocket
          in: query
          required: true
          description: Pocket The pocket into which this entry is published
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
          example: Release
        - name: strict_component
          in: query
          required: false
          description: Strict component
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=getUploadersForPackageset:
    get:
      tags:
        - archive
      summary: Get uploaders for packageset
      description: "The ArchivePermission records for uploaders to the package set. param packageset: An IPackageset. param direct_permissions: If True, only consider permissions granted directly for the package set at hand. Otherwise, include any uploaders for package sets that include this one. return: ArchivePermission records for all the uploaders who are authorized to upload to the named source package set."
      operationId: archive-getUploadersForPackageset
      parameters:
        - *a1
        - *a2
        - name: packageset
          in: query
          required: true
          description: Package set
          schema:
            type: string
            format: uri
            x-lp-resource-type: packageset
          example: packageset
        - name: direct_permissions
          in: query
          required: false
          description: Ignore package set hierarchy
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getPackagesetsForUploader:
    get:
      tags:
        - archive
      summary: Get packagesets for uploader
      description: "The ArchivePermission records for the person's package sets. param person: An IPerson for whom you want to find out which package sets they have access to. return: ArchivePermission records for all the package sets that 'person' is allowed to upload to."
      operationId: archive-getPackagesetsForUploader
      parameters:
        - *a1
        - *a2
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getPackagesetsForSourceUploader:
    get:
      tags:
        - archive
      summary: Get packagesets for source uploader
      description: "The package set based permissions for a given source and uploader. Return the IArchivePermission records that apply to this archive relate to package sets that include the given source package name the given person param sourcepackagename: the source package name; can be either a string or a ISourcePackageName. param person: An IPerson for whom you want to find out which package sets they have access to. raises NoSuchSourcePackageName: if a source package with the given name could not be found. return: ArchivePermission records for the package sets that include the given source package name and to which the given person may upload."
      operationId: archive-getPackagesetsForSourceUploader
      parameters:
        - *a1
        - *a2
        - name: sourcepackagename
          in: query
          required: true
          description: Source package name
          schema:
            type: string
          example: bash
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getPackagesetsForSource:
    get:
      tags:
        - archive
      summary: Get packagesets for source
      description: 'All package set based permissions for the given source. This method is meant to aid the process of "debugging" package set based archive permission since It allows the listing of permissions for the given source package in this archive (irrespective of the principal). param sourcepackagename: the source package name; can be either a string or a ISourcePackageName. param direct_permissions: If set only package sets that directly include the given source will be considered. raises NoSuchSourcePackageName: if a source package with the given name could not be found. return: ArchivePermission records for the package sets that include the given source package name and apply to the archive in question.'
      operationId: archive-getPackagesetsForSource
      parameters:
        - *a1
        - *a2
        - name: sourcepackagename
          in: query
          required: true
          description: Source package name
          schema:
            type: string
          example: bash
        - name: direct_permissions
          in: query
          required: false
          description: Ignore package set hierarchy
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=isSourceUploadAllowed:
    get:
      tags:
        - archive
      summary: Is source upload allowed
      description: "True if the person is allowed to upload the given source package. Return True if there exists a permission that combines this archive a package set that includes the given source package name the given person or a team they are a member of If the source package name is included by any package set with an explicit permission then only such explicit permissions will be considered. param sourcepackagename: the source package name; can be either a string or a ISourcePackageName. param person: An IPerson for whom you want to find out which package sets they have access to. param distroseries: The IDistroSeries for which to check permissions. If none is supplied then currentseries in the archive's distribution is assumed. raises NoSuchSourcePackageName: if a source package with the given name could not be found. return: True if the person is allowed to upload the source package."
      operationId: archive-isSourceUploadAllowed
      parameters:
        - *a1
        - *a2
        - name: sourcepackagename
          in: query
          required: true
          description: Source package name
          schema:
            type: string
          example: bash
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: distroseries
          in: query
          required: false
          description: The distro series
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=getBuildCounters:
    get:
      tags:
        - archive
      summary: Get build counters
      description: "Return a dictionary containing the build counters for an archive. This is necessary currently because the IArchive.failed_builds etc. counters are not in use. The returned dictionary contains the following keys and values: 'total': total number of builds (includes SUPERSEDED); 'pending': number of builds in BUILDING or NEEDSBUILD state; 'failed': number of builds in FAILEDTOBUILD, MANUALDEPWAIT, CHROOTWAIT and FAILEDTOUPLOAD state; 'succeeded': number of SUCCESSFULLYBUILT builds. 'superseded': number of SUPERSEDED builds. param include_needsbuild: Indicates whether to include builds with the status NEEDSBUILD in the pending and total counts. This is useful in situations where a build that hasn't started isn't considered a build by the user. type include_needsbuild: bool return: a dictionary with the 4 keys specified above. rtype: dict ."
      operationId: archive-getBuildCounters
      parameters:
        - *a1
        - *a2
        - name: include_needsbuild
          in: query
          required: false
          description: Include builds with state NEEDSBUILD
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=getBuildSummariesForSourceIds:
    get:
      tags:
        - archive
      summary: Get build summaries for source ids
      description: "Return a dictionary containing a summary of the build statuses. Only information for sources belonging to the current archive will be returned. See IPublishingSet.getBuildStatusSummariesForSourceIdsAndArchive() for details. param source_ids: A list of source publishing history record ids. type source_ids: list return: A dict consisting of the overall status summaries for the given ids that belong in the archive."
      operationId: archive-getBuildSummariesForSourceIds
      parameters:
        - *a1
        - *a2
        - name: source_ids
          in: query
          required: true
          description: A list of source publishing history record ids.
          schema:
            type: string
          example: source_ids
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=getArchiveDependency:
    get:
      tags:
        - archive
      summary: Get archive dependency
      description: "Return the IArchiveDependency object for the given dependency. param dependency: is an IArchive object. return: IArchiveDependency or None if a corresponding object could not be found."
      operationId: archive-getArchiveDependency
      parameters:
        - *a1
        - *a2
        - name: dependency
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: archive
          example: dependency
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_dependency-full"
  /{archive_1}/+archive/{archive_2}?ws.op=getPermissionsForPerson:
    get:
      tags:
        - archive
      summary: Get permissions for person
      description: "Return the IArchivePermission records applicable to the person. param person: An IPerson return: A list of IArchivePermission records."
      operationId: archive-getPermissionsForPerson
      parameters:
        - *a1
        - *a2
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getUploadersForPackage:
    get:
      tags:
        - archive
      summary: Get uploaders for package
      description: "Return IArchivePermission records for the package's uploaders. param source_package_name: An ISourcePackageName or textual name for the source package. return: A list of IArchivePermission records."
      operationId: archive-getUploadersForPackage
      parameters:
        - *a1
        - *a2
        - name: source_package_name
          in: query
          required: true
          description: Source Package Name
          schema:
            type: string
          example: source_package_name
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getUploadersForComponent:
    get:
      tags:
        - archive
      summary: Get uploaders for component
      description: "Return IArchivePermission records for the component's uploaders. param component_name: An IComponent or textual name for the component. return: A list of IArchivePermission records."
      operationId: archive-getUploadersForComponent
      parameters:
        - *a1
        - *a2
        - name: component_name
          in: query
          required: false
          description: Component Name
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getQueueAdminsForComponent:
    get:
      tags:
        - archive
      summary: Get queue admins for component
      description: "Return IArchivePermission records for authorized queue admins. param component_name: An IComponent or textual name for the component. return: A list of IArchivePermission records."
      operationId: archive-getQueueAdminsForComponent
      parameters:
        - *a1
        - *a2
        - name: component_name
          in: query
          required: true
          description: Component Name
          schema:
            type: string
          example: component_name
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getComponentsForQueueAdmin:
    get:
      tags:
        - archive
      summary: Get components for queue admin
      description: "Return IArchivePermission for the person's queue admin components. param person: An IPerson. return: A list of IArchivePermission records."
      operationId: archive-getComponentsForQueueAdmin
      parameters:
        - *a1
        - *a2
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getPublishedSources:
    get:
      tags:
        - archive
      summary: Get published sources
      description: All ISourcePackagePublishingHistory target to this archive.
      operationId: archive-getPublishedSources
      parameters:
        - *a1
        - *a2
        - name: source_name
          in: query
          required: false
          description: Source package name
          schema:
            type: string
        - name: version
          in: query
          required: false
          description: Version
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Package Publishing Status The status of this publishing record
          schema:
            type: string
            enum:
              - Pending
              - Published
              - Superseded
              - Deleted
              - Obsolete
        - name: distro_series
          in: query
          required: false
          description: Distroseries name
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: pocket
          in: query
          required: false
          description: Pocket The pocket into which this entry is published
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
        - name: exact_match
          in: query
          required: false
          description: Exact Match Whether or not to filter source names by exact matching.
          schema:
            type: string
        - name: created_since_date
          in: query
          required: false
          description: Created Since Date Return entries whose date_created is greater than or equal to this date.
          schema:
            type: string
        - name: published_since
          in: query
          required: false
          description: Published Since Return entries whose date_published is greater than or equal to this date.
          schema:
            type: string
        - name: published_until
          in: query
          required: false
          description: Published Until Return entries whose date_published is less than or equal to this date.
          schema:
            type: string
        - name: component_name
          in: query
          required: false
          description: Component name
          schema:
            type: string
        - name: order_by_date
          in: query
          required: false
          description: Order by creation date Return newest results first. This is recommended for applications that need to catch up with publications since their last run. If not specified, results are ordered by source package name (lexicographically), then by descending version and then descending ID.
          schema:
            type: string
        - name: order_by_date_ascending
          in: query
          required: false
          description: Order by ascending creation date Return oldest results first.
          schema:
            type: string
        - name: order_by
          in: query
          required: false
          description: Sort order Sort order of the package publication. Can be either ascending or descending of publication date.
          schema:
            type: string
            enum:
              - published_date_asc
              - published_date_desc
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/source_package_publishing_history-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getPublishedBinaries:
    get:
      tags:
        - archive
      summary: Get published binaries
      description: "All IBinaryPackagePublishingHistory target to this archive. param name: binary name filter (exact match or SQL LIKE controlled by 'exact_match' argument). param version: binary version filter (always exact match). param status: PackagePublishingStatus filter, can be a list. param distroarchseries: IDistroArchSeries filter, can be a list. param pocket: PackagePublishingPocket filter. param exact_match: either or not filter source names by exact matching. param created_since_date: Only return publications created on or after this date. param ordered: Normally publications are ordered by binary package name and then ID order (creation order). If this parameter is False then the results will be unordered. This will make the operation much quicker to return results if you don't care about ordering. param order_by_date: Order publications by descending creation date and then by descending ID. This is suitable for applications that need to catch up with publications since their last run. param include_removed: If True, include publications that have been removed from disk as well as those that have not. param only_unpublished: If True, only include publications that have never been published to disk. param component_name: component filter. Only return binaries that are in this component. param distroseries: IDistroSeries filter. return: A collection containing BinaryPackagePublishingHistory."
      operationId: archive-getPublishedBinaries
      parameters:
        - *a1
        - *a2
        - name: binary_name
          in: query
          required: false
          description: Binary Package Name
          schema:
            type: string
        - name: version
          in: query
          required: false
          description: Version
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Package Publishing Status The status of this publishing record
          schema:
            type: string
            enum:
              - Pending
              - Published
              - Superseded
              - Deleted
              - Obsolete
        - name: distro_arch_series
          in: query
          required: false
          description: Distro Arch Series
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_arch_series
        - name: pocket
          in: query
          required: false
          description: Pocket The pocket into which this entry is published
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
        - name: exact_match
          in: query
          required: false
          description: Whether or not to filter binary names by exact matching.
          schema:
            type: string
        - name: created_since_date
          in: query
          required: false
          description: Created Since Date Return entries whose date_created is greater than or equal to this date.
          schema:
            type: string
        - name: published_since
          in: query
          required: false
          description: Published Since Return entries whose date_published is greater than or equal to this date.
          schema:
            type: string
        - name: published_until
          in: query
          required: false
          description: Published Until Return entries whose date_published is less than or equal to this date.
          schema:
            type: string
        - name: ordered
          in: query
          required: false
          description: Ordered Return ordered results by default, but specifying False will return results more quickly.
          schema:
            type: string
        - name: order_by_date
          in: query
          required: false
          description: Order by creation date Return newest results first. This is recommended for applications that need to catch up with publications since their last run.
          schema:
            type: string
        - name: order_by_date_ascending
          in: query
          required: false
          description: Order by ascending creation date Return oldest results first.
          schema:
            type: string
        - name: order_by
          in: query
          required: false
          description: Sort order Sort order of the package publication. Can be either ascending or descending of publication date.
          schema:
            type: string
            enum:
              - published_date_asc
              - published_date_desc
        - name: distro_series
          in: query
          required: false
          description: Distro Series
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: component_name
          in: query
          required: false
          description: Component name
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/binary_package_publishing_history-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getPocketsForUploader:
    get:
      tags:
        - archive
      summary: Get pockets for uploader
      description: "Return the pockets that 'person' can upload to this archive. param person: An IPerson wishing to upload to an archive. return: A set of PackagePublishingPocket items that 'person' can upload to."
      operationId: archive-getPocketsForUploader
      parameters:
        - *a1
        - *a2
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getAllPermissions:
    get:
      tags:
        - archive
      summary: Get all permissions
      description: "Return all IArchivePermission records for this archive. return: A list of IArchivePermission records."
      operationId: archive-getAllPermissions
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getUploadersForPocket:
    get:
      tags:
        - archive
      summary: Get uploaders for pocket
      description: "Return IArchivePermission records for the pocket's uploaders. param pocket: A PackagePublishingPocket. return: A list of IArchivePermission records."
      operationId: archive-getUploadersForPocket
      parameters:
        - *a1
        - *a2
        - name: pocket
          in: query
          required: true
          description: Pocket
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
          example: Release
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getQueueAdminsForPocket:
    get:
      tags:
        - archive
      summary: Get queue admins for pocket
      description: "Return IArchivePermission records for authorized queue admins. param pocket: A PackagePublishingPocket. param distroseries: An optional IDistroSeries. return: A list of IArchivePermission records."
      operationId: archive-getQueueAdminsForPocket
      parameters:
        - *a1
        - *a2
        - name: pocket
          in: query
          required: true
          description: Pocket
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
          example: Release
        - name: distroseries
          in: query
          required: false
          description: Distro series
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getPocketsForQueueAdmin:
    get:
      tags:
        - archive
      summary: Get pockets for queue admin
      description: "Return IArchivePermission for the person's queue admin pockets. param person: An IPerson. return: A list of IArchivePermission records."
      operationId: archive-getPocketsForQueueAdmin
      parameters:
        - *a1
        - *a2
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_permission-page"
  /{archive_1}/+archive/{archive_2}?ws.op=getSigningKeyData:
    get:
      tags:
        - archive
      summary: Get signing key data
      description: Get the public key used to sign this repository. If the repository has a signing key but it cannot be retrieved from the keyserver, then the response will have an appropriate 4xx or 5xx HTTP status code. Otherwise, returns the ASCII-armoured public key material as a text string, or None if the repository has no signing key.
      operationId: archive-getSigningKeyData
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=getNamedAuthToken:
    get:
      tags:
        - archive
      summary: Get named auth token
      description: "Return a named authorization token for the given name in this archive. param name: The identifier string for a token. param as_dict: Optional boolean, controls whether the return value is a dictionary or a full object. return: An ArchiveAuthToken object or a dictionary where the value of token is the secret and the value of archive_url is the externally-usable archive URL including basic auth. raises NotFoundError: if no matching token could be found."
      operationId: archive-getNamedAuthToken
      parameters:
        - *a1
        - *a2
        - name: name
          in: query
          required: true
          description: Authorization token name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=getNamedAuthTokens:
    get:
      tags:
        - archive
      summary: Get named auth tokens
      description: "Return a subset of active named authorization tokens for this archive if names is specified, or all active named authorization tokens for this archive is names is null. param names: An optional list of token names. param as_dict: Optional boolean, controls whether the return value is a list of dictionaries or a list of full objects. return: A list of ArchiveAuthToken objects or a list of dictionaries where token is the secret and archive_url is the externally-usable archive URL including basic auth."
      operationId: archive-getNamedAuthTokens
      parameters:
        - *a1
        - *a2
        - name: names
          in: query
          required: false
          description: Authorization token names
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newWebhook:
    post:
      tags:
        - archive
      summary: New webhook
      description: Create a new webhook.
      operationId: archive-newWebhook
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                delivery_url:
                  type: string
                  description: Delivery URL
                  example: delivery_url
                active:
                  type: string
                  description: Active Deliver details of subscribed events.
                event_types:
                  type: string
                  description: Event types
                  example: event_types
                secret:
                  type: string
                  description: Secret An optional string used to sign delivery bodies with HMAC-SHA1 in the X-Hub-Signature header. This must be less than 200 bytes long.
              required:
                - delivery_url
                - event_types
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newPackageUploader:
    post:
      tags:
        - archive
      summary: New package uploader
      description: "Add permission for a person to upload a package to this archive. param person: An IPerson whom should be given permission. param source_package_name: An ISourcePackageName or textual package name. return: An IArchivePermission which is the newly-created permission."
      operationId: archive-newPackageUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                source_package_name:
                  type: string
                  description: Source Package Name
                  example: source_package_name
              required:
                - person
                - source_package_name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newComponentUploader:
    post:
      tags:
        - archive
      summary: New component uploader
      description: "Add permission for a person to upload to a component. param person: An IPerson whom should be given permission. param component: An IComponent or textual component name. return: An IArchivePermission which is the newly-created permission. raises InvalidComponent: if this archive is a PPA and the component is not 'main'."
      operationId: archive-newComponentUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                component_name:
                  type: string
                  description: Component Name
                  example: component_name
              required:
                - person
                - component_name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newQueueAdmin:
    post:
      tags:
        - archive
      summary: New queue admin
      description: "Add permission for a person to administer a distroseries queue. The supplied person will gain permission to administer the distroseries queue for packages in the supplied component. param person: An IPerson whom should be given permission. param component: An IComponent or textual component name. return: An IArchivePermission which is the newly-created permission."
      operationId: archive-newQueueAdmin
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                component_name:
                  type: string
                  description: Component Name
                  example: component_name
              required:
                - person
                - component_name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newPackagesetUploader:
    post:
      tags:
        - archive
      summary: New packageset uploader
      description: "Add a package set based permission for a person. param person: An IPerson for whom you want to add permission. param packageset: An IPackageset. param explicit: True if the package set in question requires specialist skills for proper handling. return: The new ArchivePermission, or the existing one if it already exists."
      operationId: archive-newPackagesetUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                packageset:
                  type: string
                  format: uri
                  x-lp-resource-type: packageset
                  description: Package set
                  example: packageset
                explicit:
                  type: string
                  description: Explicit
              required:
                - person
                - packageset
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=deletePackageUploader:
    post:
      tags:
        - archive
      summary: Delete package uploader
      description: "Revoke permission for the person to upload the package. param person: An IPerson whose permission should be revoked. param source_package_name: An ISourcePackageName or textual package name."
      operationId: archive-deletePackageUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                source_package_name:
                  type: string
                  description: Source Package Name
                  example: source_package_name
              required:
                - person
                - source_package_name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=deleteComponentUploader:
    post:
      tags:
        - archive
      summary: Delete component uploader
      description: "Revoke permission for the person to upload to the component. param person: An IPerson whose permission should be revoked. param component: An IComponent or textual component name."
      operationId: archive-deleteComponentUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                component_name:
                  type: string
                  description: Component Name
                  example: component_name
              required:
                - person
                - component_name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=deleteQueueAdmin:
    post:
      tags:
        - archive
      summary: Delete queue admin
      description: "Revoke permission for the person to administer distroseries queues. The supplied person will lose permission to administer the distroseries queue for packages in the supplied component. param person: An IPerson whose permission should be revoked. param component: An IComponent or textual component name."
      operationId: archive-deleteQueueAdmin
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                component_name:
                  type: string
                  description: Component Name
                  example: component_name
              required:
                - person
                - component_name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=deletePackagesetUploader:
    post:
      tags:
        - archive
      summary: Delete packageset uploader
      description: "Revoke upload permissions for a person. param person: An IPerson for whom you want to revoke permission. param packageset: An IPackageset. param explicit: The value of the 'explicit' flag for the permission to be revoked."
      operationId: archive-deletePackagesetUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                packageset:
                  type: string
                  format: uri
                  x-lp-resource-type: packageset
                  description: Package set
                  example: packageset
                explicit:
                  type: string
                  description: Explicit
              required:
                - person
                - packageset
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=syncSources:
    post:
      tags:
        - archive
      summary: Sync sources
      description: "Synchronise (copy) named sources into this archive from another. DEPRECATED: syncSource and syncSources are deprecated, and will be removed entirely in the future. Use the asynchronous copyPackage method instead, and poll getPublishedSources if you need to await completion. It will copy the most recent PUBLISHED versions of the named sources to the destination archive if necessary. This operation will only succeeds when all requested packages are synchronised between the archives. If any of the requested copies cannot be performed, the whole operation will fail. There will be no partial changes of the destination archive. If the source or target distribution has a development series alias, then it may be used as the source or target distroseries name respectively; but note that this will always be resolved to the true development series of that distribution, which may not match the alias in the respective published archives. param source_names: a list of string names of packages to copy. param from_archive: the source archive from which to copy. param to_pocket: the target pocket (as a string). param to_series: the target distroseries (as a string). param from_series: the source distroseries (as a string). param include_binaries: optional boolean, controls whether or not the published binaries for each given source should also be copied along with the source. param person: the IPerson who requests the sync. raises NoSuchSourcePackageName: if the source name is invalid raises PocketNotFound: if the pocket name is invalid raises NoSuchDistroSeries: if the distro series name is invalid raises CannotCopy: if there is a problem copying."
      operationId: archive-syncSources
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                source_names:
                  type: string
                  description: Source package names
                  example: source_names
                from_archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: from_archive
                to_pocket:
                  type: string
                  description: Pocket name
                  example: to_pocket
                to_series:
                  type: string
                  description: Distroseries name The distro series to copy packages into.
                from_series:
                  type: string
                  description: Distroseries name The distro series to copy packages from.
                include_binaries:
                  type: string
                  description: Include Binaries Whether or not to copy binaries already built for this source
              required:
                - source_names
                - from_archive
                - to_pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=syncSource:
    post:
      tags:
        - archive
      summary: Sync source
      description: "Synchronise (copy) a single named source into this archive. DEPRECATED: syncSource and syncSources are deprecated, and will be removed entirely in the future. Use the asynchronous copyPackage method instead, and poll getPublishedSources if you need to await completion. Copy a specific version of a named source to the destination archive if necessary. If the source distribution has a development series alias, then it may be used as the source distroseries name; but note that this will always be resolved to the true development series of that distribution, which may not match the alias in the published source archive. param source_name: a string name of the package to copy. param version: the version of the package to copy. param from_archive: the source archive from which to copy. param to_pocket: the target pocket (as a string). param to_series: the target distroseries (as a string). param include_binaries: optional boolean, controls whether or not the published binaries for each given source should also be copied along with the source. param person: the IPerson who requests the sync. raises NoSuchSourcePackageName: if the source name is invalid raises PocketNotFound: if the pocket name is invalid raises NoSuchDistroSeries: if the distro series name is invalid raises CannotCopy: if there is a problem copying."
      operationId: archive-syncSource
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                source_name:
                  type: string
                  description: Source package name
                  example: bash
                version:
                  type: string
                  description: Version
                  example: version
                from_archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: from_archive
                to_pocket:
                  type: string
                  description: Pocket name
                  example: to_pocket
                to_series:
                  type: string
                  description: Distroseries name
                include_binaries:
                  type: string
                  description: Include Binaries Whether or not to copy binaries already built for this source
              required:
                - source_name
                - version
                - from_archive
                - to_pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newSubscription:
    post:
      tags:
        - archive
      summary: New subscription
      description: "Create a new subscription to this archive. Create an ArchiveSubscriber record which allows an IPerson to access a private repository. param subscriber: An IPerson who is allowed to access the repository for this archive. param registrant: An IPerson who created this subscription. param date_expires: When the subscription should expire; None if it should not expire (default). param description: An option textual description of the subscription being created. return: The IArchiveSubscriber that was created."
      operationId: archive-newSubscription
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Subscriber The person who is subscribed.
                  example: subscriber
                date_expires:
                  type: string
                  description: Date of Expiration The timestamp when the subscription will expire.
                description:
                  type: string
                  description: Description Free text describing this subscription.
              required:
                - subscriber
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=enableRestrictedProcessor:
    post:
      tags:
        - archive
      summary: Enable restricted processor
      description: "Add the processor to the set of enabled restricted processors. DEPRECATED. Use setProcessors instead. param processor: is an IProcessor object."
      operationId: archive-enableRestrictedProcessor
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                processor:
                  type: string
                  format: uri
                  x-lp-resource-type: processor
                  example: processor
              required:
                - processor
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=setProcessors:
    post:
      tags:
        - archive
      summary: Set processors
      description: Set the architectures on which the archive can build.
      operationId: archive-setProcessors
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                processors:
                  type: string
                  example: processors
              required:
                - processors
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=copyPackage:
    post:
      tags:
        - archive
      summary: Copy package
      description: "Copy a single named source into this archive. Asynchronously copy a specific version of a named source to the destination archive if necessary. Calls to this method will return immediately if the copy passes basic security checks and the copy will happen sometime later with full checking. If the source or target distribution has a development series alias, then it may be used as the source or target distroseries name respectively; but note that this will always be resolved to the true development series of that distribution, which may not match the alias in the respective published archives. param source_name: a string name of the package to copy. param version: the version of the package to copy. param from_archive: the source archive from which to copy. param to_pocket: the target pocket (as a string). param to_series: the target distroseries (as a string). param include_binaries: optional boolean, controls whether or not the published binaries for each given source should also be copied along with the source. param person: the IPerson who requests the sync. param sponsored: the IPerson who is being sponsored. Specifying this will ensure that the person's email address is used as the \"From:\" on the announcement email and will also be recorded as the creator of the new source publication. param unembargo: if True, allow copying restricted files from a private archive to a public archive, and re-upload them to the public librarian when doing so. param auto_approve: if True and the IPerson requesting the sync has queue admin permissions on the target, accept the copy immediately rather than setting it to unapproved. param silent: Suppress any emails that the copy would generate. Only usable with queue admin permissions on the target. param from_pocket: the source pocket (as a string). If omitted, copy from any pocket with a matching version. param from_series: the source distroseries (as a string). If omitted, copy from any series with a matching version. param phased_update_percentage: the phased update percentage to apply to the copied publication. param move: if True, delete the source publication after copying it to the destination. raises NoSuchSourcePackageName: if the source name is invalid raises PocketNotFound: if the pocket name is invalid raises NoSuchDistroSeries: if the distro series name is invalid raises CannotCopy: if there is a problem copying."
      operationId: archive-copyPackage
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                source_name:
                  type: string
                  description: Source package name
                  example: bash
                version:
                  type: string
                  description: Version
                  example: version
                from_archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: from_archive
                to_pocket:
                  type: string
                  description: Target pocket name
                  example: to_pocket
                to_series:
                  type: string
                  description: Target distroseries name
                include_binaries:
                  type: string
                  description: Include Binaries Whether or not to copy binaries already built for this source
                sponsored:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Sponsored Person The person who is being sponsored for this copy.
                unembargo:
                  type: string
                  description: Unembargo restricted files
                auto_approve:
                  type: string
                  description: Automatic approval Automatically approve this copy (queue admins only).
                silent:
                  type: string
                  description: Silent Don't notify anyone about this copy. For use by queue admins only.
                from_pocket:
                  type: string
                  description: Source pocket name
                from_series:
                  type: string
                  description: Source distroseries name
                phased_update_percentage:
                  type: string
                  description: Phased update percentage The percentage of users for whom this package should be recommended, or None to publish the update for everyone.
                move:
                  type: string
                  description: Move If true, delete the source publication after copying it to the destination.
              required:
                - source_name
                - version
                - from_archive
                - to_pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=copyPackages:
    post:
      tags:
        - archive
      summary: Copy packages
      description: "Copy multiple named sources into this archive from another. Asynchronously copy the most recent PUBLISHED versions of the named sources to the destination archive if necessary. Calls to this method will return immediately if the copy passes basic security checks and the copy will happen sometime later with full checking. Partial changes of the destination archive can happen because each source is copied in its own transaction. If the source or target distribution has a development series alias, then it may be used as the source or target distroseries name respectively; but note that this will always be resolved to the true development series of that distribution, which may not match the alias in the respective published archives. param source_names: a list of string names of packages to copy. param from_archive: the source archive from which to copy. param to_pocket: the target pocket (as a string). param to_series: the target distroseries (as a string). param from_series: the source distroseries (as a string). param include_binaries: optional boolean, controls whether or not the published binaries for each given source should also be copied along with the source. param person: the IPerson who requests the sync. param sponsored: the IPerson who is being sponsored. Specifying this will ensure that the person's email address is used as the \"From:\" on the announcement email and will also be recorded as the creator of the new source publication. param unembargo: if True, allow copying restricted files from a private archive to a public archive, and re-upload them to the public librarian when doing so. param auto_approve: if True and the IPerson requesting the sync has queue admin permissions on the target, accept the copies immediately rather than setting it to unapproved. param silent: Suppress any emails that the copy would generate. Only usable with queue admin permissions on the target. raises NoSuchSourcePackageName: if the source name is invalid raises PocketNotFound: if the pocket name is invalid raises NoSuchDistroSeries: if the distro series name is invalid raises CannotCopy: if there is a problem copying."
      operationId: archive-copyPackages
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                source_names:
                  type: string
                  description: Source package names
                  example: source_names
                from_archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: from_archive
                to_pocket:
                  type: string
                  description: Pocket name
                  example: to_pocket
                to_series:
                  type: string
                  description: Distroseries name The distro series to copy packages into.
                from_series:
                  type: string
                  description: Distroseries name The distro series to copy packages from.
                include_binaries:
                  type: string
                  description: Include Binaries Whether or not to copy binaries already built for this source
                sponsored:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Sponsored Person The person who is being sponsored for this copy.
                unembargo:
                  type: string
                  description: Unembargo restricted files
                auto_approve:
                  type: string
                  description: Automatic approval Automatically approve this copy (queue admins only).
                silent:
                  type: string
                  description: Silent Don't notify anyone about this copy. For use by queue admins only.
              required:
                - source_names
                - from_archive
                - to_pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=uploadCIBuild:
    post:
      tags:
        - archive
      summary: Upload cibuild
      description: Upload the output of a CI build to this archive.
      operationId: archive-uploadCIBuild
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                ci_build:
                  type: string
                  format: uri
                  x-lp-resource-type: ci_build
                  example: ci_build
                to_series:
                  type: string
                  description: Target distroseries name
                  example: to_series
                to_pocket:
                  type: string
                  description: Target pocket name
                  example: to_pocket
                to_channel:
                  type: string
                  description: Target channel
              required:
                - ci_build
                - to_series
                - to_pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newPocketUploader:
    post:
      tags:
        - archive
      summary: New pocket uploader
      description: "Add permission for a person to upload to a pocket. param person: An IPerson whom should be given permission. param pocket: A PackagePublishingPocket. return: An IArchivePermission which is the newly-created permission. raises InvalidPocketForPartnerArchive: if this archive is a partner archive and the pocket is not RELEASE or PROPOSED. raises InvalidPocketForPPA: if this archive is a PPA and the pocket is not RELEASE."
      operationId: archive-newPocketUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket
                  example: Release
              required:
                - person
                - pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newPocketQueueAdmin:
    post:
      tags:
        - archive
      summary: New pocket queue admin
      description: "Add permission for a person to administer a distroseries queue. The supplied person will gain permission to administer the distroseries queue for packages in the supplied series and pocket. param person: An IPerson whom should be given permission. param pocket: A PackagePublishingPocket. param distroseries: An optional IDistroSeries. return: An IArchivePermission which is the newly-created permission."
      operationId: archive-newPocketQueueAdmin
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket
                  example: Release
                distroseries:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro series
              required:
                - person
                - pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=deletePocketUploader:
    post:
      tags:
        - archive
      summary: Delete pocket uploader
      description: "Revoke permission for the person to upload to the pocket. param person: An IPerson whose permission should be revoked. param distroseries: An IDistroSeries. param pocket: A PackagePublishingPocket."
      operationId: archive-deletePocketUploader
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket
                  example: Release
              required:
                - person
                - pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=deletePocketQueueAdmin:
    post:
      tags:
        - archive
      summary: Delete pocket queue admin
      description: "Revoke permission for the person to administer distroseries queues. The supplied person will lose permission to administer the distroseries queue for packages in the supplied series and pocket. param person: An IPerson whose permission should be revoked. param pocket: A PackagePublishingPocket. param distroseries: An optional IDistroSeries."
      operationId: archive-deletePocketQueueAdmin
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  example: ~launchpad
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket
                  example: Release
                distroseries:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro series
              required:
                - person
                - pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=addArchiveDependency:
    post:
      tags:
        - archive
      summary: Add archive dependency
      description: "Record an archive dependency record for the context archive. param dependency: is an IArchive object. param pocket: is an PackagePublishingPocket enum. param component: is the name of a component. If not given, the archive dependency will be tied to the component used for a corresponding source in primary archive. raise: ArchiveDependencyError if given 'dependency' does not fit the context archive. return: a IArchiveDependency object targeted to the context IArchive requiring 'dependency' IArchive."
      operationId: archive-addArchiveDependency
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dependency:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: dependency
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket The pocket into which this entry is published
                  example: Release
                component:
                  type: string
                  description: Component
              required:
                - dependency
                - pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=removeArchiveDependency:
    post:
      tags:
        - archive
      summary: Remove archive dependency
      description: "Remove the IArchiveDependency record for the given dependency. param dependency: is an IArchive object."
      operationId: archive-removeArchiveDependency
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dependency:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: dependency
              required:
                - dependency
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newNamedAuthToken:
    post:
      tags:
        - archive
      summary: New named auth token
      description: "Create a new named authorization token. param name: An identifier string for this token. param token: Optional unicode text to use as the token. One will be generated if not given. param as_dict: Optional boolean, controls whether the return value is a dictionary or a full object. return: An ArchiveAuthToken object or a dictionary where the value of token is the secret and the value of archive_url is the externally-usable archive URL including basic auth."
      operationId: archive-newNamedAuthToken
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Authorization token name
                  example: ubuntu
                token:
                  type: string
                  description: Optional secret for this named token
              required:
                - name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=newNamedAuthTokens:
    post:
      tags:
        - archive
      summary: New named auth tokens
      description: "Create named authorization tokens in bulk. param names: A list of token names. param as_dict: Optional boolean, controls whether the return value is a list of dictionaries or a list of full objects. return: A list of ArchiveAuthToken objects or a dictionary of {name: {token, archive_url} where name is a token name, token is the secret and archive_url is the externally-usable archive URL including basic auth."
      operationId: archive-newNamedAuthTokens
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                names:
                  type: string
                  description: Authorization token names
                  example: names
              required:
                - names
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=revokeNamedAuthToken:
    post:
      tags:
        - archive
      summary: Revoke named auth token
      description: "Deactivate a named authorization token. param name: The identifier string for a token. raises NotFoundError: if no matching token could be found."
      operationId: archive-revokeNamedAuthToken
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Authorization token name
                  example: ubuntu
              required:
                - name
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=revokeNamedAuthTokens:
    post:
      tags:
        - archive
      summary: Revoke named auth tokens
      description: "Deactivate named authorization tokens in bulk. param names: A list of token names."
      operationId: archive-revokeNamedAuthTokens
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                names:
                  type: string
                  description: Authorization token names
                  example: names
              required:
                - names
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=removeCopyNotification:
    post:
      tags:
        - archive
      summary: Remove copy notification
      description: "Remove a copy notification that's displayed on the +packages page. Copy notifications are shown on the +packages page when a PlainPackageCopyJob is in progress or failed. Calling this method will delete failed jobs so they no longer appear on the page. You need to have upload privileges on the PPA to use this. param job_id: The ID of the PlainPackageCopyJob to be removed."
      operationId: archive-removeCopyNotification
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                job_id:
                  type: string
                  example: job_id
              required:
                - job_id
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}?ws.op=markSuiteDirty:
    post:
      tags:
        - archive
      summary: Mark suite dirty
      description: "Mark a suite as dirty in this archive. The next publisher run will publish this suite regardless of whether it has any pending publications. param distroseries: An IDistroSeries. param pocket: A PackagePublishingPocket."
      operationId: archive-markSuiteDirty
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                distroseries:
                  type: string
                  format: uri
                  x-lp-resource-type: distro_series
                  description: Distro series
                  example: noble
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket
                  example: Release
              required:
                - distroseries
                - pocket
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
  /{archive_1}/+archive/{archive_2}:
    get:
      tags:
        - archive
      summary: Get archive
      description: Main Archive interface.
      operationId: archive-get
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive-full"
    put:
      tags:
        - archive
      summary: Replace archive
      description: Main Archive interface.
      operationId: archive-put
      parameters:
        - *a1
        - *a2
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/archive-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - archive
      summary: Update archive
      description: Main Archive interface.
      operationId: archive-patch
      parameters:
        - *a1
        - *a2
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/archive-diff"
      responses:
        "200":
          description: Successful response
    delete:
      tags:
        - archive
      summary: Delete archive
      description: Main Archive interface.
      operationId: archive-delete
      parameters:
        - *a1
        - *a2
      responses:
        "200":
          description: Resource deleted.
  /{bug_1}/{bug_2}?ws.op=canBeNominatedFor:
    get:
      tags:
        - bug
      summary: Can be nominated for
      description: "Can this bug nominated for this target? nomination_target: An IDistroSeries or IProductSeries. Returns True or False."
      operationId: bug-canBeNominatedFor
      parameters:
        - &a3
          name: bug_1
          in: path
          required: true
          description: Path segment of the bug resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: bugs
        - &a4
          name: bug_2
          in: path
          required: true
          description: Path segment of the bug resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: "1"
        - name: target
          in: query
          required: true
          description: Target
          schema:
            type: string
            format: uri
            x-lp-resource-type: bug_target
          example: launchpad
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=getNominationFor:
    get:
      tags:
        - bug
      summary: Get nomination for
      description: "Return the IBugNomination for the target. If no nomination is found, a NotFoundError is raised. param nomination_target: An IDistroSeries or IProductSeries."
      operationId: bug-getNominationFor
      parameters:
        - *a3
        - *a4
        - name: target
          in: query
          required: true
          description: Target
          schema:
            type: string
            format: uri
            x-lp-resource-type: bug_target
          example: launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_nomination-full"
  /{bug_1}/{bug_2}?ws.op=getNominations:
    get:
      tags:
        - bug
      summary: Get nominations
      description: "Return a list of all IBugNominations for this bug. The list is ordered by IBugNominations.target.bugtargetdisplayname. param target: An IProduct or IDistribution. Only nominations for this target are returned. param nominations: The list of nominations to search through. If none is given, the bug's nominations are looked through. This can be useful when having to call this method multiple times, to avoid getting the list of nominations each time."
      operationId: bug-getNominations
      parameters:
        - *a3
        - *a4
        - name: target
          in: query
          required: false
          description: Target
          schema:
            type: string
            format: uri
            x-lp-resource-type: bug_target
        - name: nominations
          in: query
          required: false
          description: Nominations to search through.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_nomination-page"
  /{bug_1}/{bug_2}?ws.op=isExpirable:
    get:
      tags:
        - bug
      summary: Is expirable
      description: Is this bug eligible for expiration and was it last updated more than X days ago? If days_old is None the default number of days without activity is used. Returns True or False.
      operationId: bug-isExpirable
      parameters:
        - *a3
        - *a4
        - name: days_old
          in: query
          required: false
          description: Number of days of inactivity for which to check.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=getVisibleLinkedBranches:
    get:
      tags:
        - bug
      summary: Get visible linked branches
      description: Return all the branches linked to the bug that user can see.
      operationId: bug-getVisibleLinkedBranches
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=getVisibleLinkedMergeProposals:
    get:
      tags:
        - bug
      summary: Get visible linked merge proposals
      description: Return all the MPs linked to the bug that user can see.
      operationId: bug-getVisibleLinkedMergeProposals
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=linkBranch:
    post:
      tags:
        - bug
      summary: Link branch
      description: "Associate a branch with this bug. param branch: The branch being linked to. param registrant: The user linking the branch."
      operationId: bug-linkBranch
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                branch:
                  type: string
                  format: uri
                  x-lp-resource-type: branch
                  example: branch
              required:
                - branch
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=unlinkBranch:
    post:
      tags:
        - bug
      summary: Unlink branch
      description: "Unlink a branch from this bug. param branch: The branch being unlinked from. param user: The user unlinking the branch."
      operationId: bug-unlinkBranch
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                branch:
                  type: string
                  format: uri
                  x-lp-resource-type: branch
                  example: branch
              required:
                - branch
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=addNomination:
    post:
      tags:
        - bug
      summary: Add nomination
      description: "Nominate a bug for an IDistroSeries or IProductSeries. owner: An IPerson. target: An IDistroSeries or IProductSeries. This method creates and returns a BugNomination. (See lp.bugs.model.bugnomination.BugNomination.)"
      operationId: bug-addNomination
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                target:
                  type: string
                  format: uri
                  x-lp-resource-type: bug_target
                  description: Target
                  example: launchpad
              required:
                - target
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=addTask:
    post:
      tags:
        - bug
      summary: Add task
      description: "Create a new bug task on this bug. raises IllegalTarget: if the bug task cannot be added to the bug."
      operationId: bug-addTask
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                target:
                  type: string
                  format: uri
                  x-lp-resource-type: bug_target
                  description: Target The software in which this bug should be fixed.
                  example: launchpad
              required:
                - target
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=linkCVE:
    post:
      tags:
        - bug
      summary: Link cve
      description: Ensure that this CVE is linked to this bug.
      operationId: bug-linkCVE
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cve:
                  type: string
                  format: uri
                  x-lp-resource-type: cve
                  description: CVE
                  example: cve
              required:
                - cve
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=unlinkCVE:
    post:
      tags:
        - bug
      summary: Unlink cve
      description: Ensure that any links between this bug and the given CVE are removed.
      operationId: bug-unlinkCVE
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cve:
                  type: string
                  format: uri
                  x-lp-resource-type: cve
                  description: CVE
                  example: cve
              required:
                - cve
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=addAttachment:
    post:
      tags:
        - bug
      summary: Add attachment
      description: "Attach a file to this bug. owner: An IPerson. data: A file-like object, or a bytes. description: A brief description of the attachment. comment: An IMessage or string. filename: A string. url: External URL of the attachment is_patch: A boolean."
      operationId: bug-addAttachment
      parameters:
        - *a3
        - *a4
        - name: data
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: HostedFile
        - name: comment
          in: query
          required: true
          schema:
            type: string
          example: comment
        - name: filename
          in: query
          required: false
          schema:
            type: string
        - name: url
          in: query
          required: false
          schema:
            type: string
        - name: is_patch
          in: query
          required: false
          schema:
            type: string
        - name: content_type
          in: query
          required: false
          schema:
            type: string
        - name: description
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=addWatch:
    post:
      tags:
        - bug
      summary: Add watch
      description: Create a new watch for this bug on the given remote bug and bug tracker, owned by the person given as the owner.
      operationId: bug-addWatch
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                bug_tracker:
                  type: string
                  format: uri
                  x-lp-resource-type: bug_tracker
                  description: Bug System You can register new bug trackers from the Launchpad Bugs home page.
                  example: bug_tracker
                remote_bug:
                  type: string
                  description: Remote Bug The bug number of this bug in the remote bug tracker.
                  example: remote_bug
              required:
                - bug_tracker
                - remote_bug
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=markUserAffected:
    post:
      tags:
        - bug
      summary: Mark user affected
      description: "Mark :user: as affected by this bug."
      operationId: bug-markUserAffected
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                affected:
                  type: string
                  description: Does this bug affect you?
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=setCommentVisibility:
    post:
      tags:
        - bug
      summary: Set comment visibility
      description: "Set the visible attribute on a bug comment. This is restricted to Launchpad admins, and will return a HTTP Error 401: Unauthorized error for non-admin callers."
      operationId: bug-setCommentVisibility
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                comment_number:
                  type: string
                  description: The number of the comment in the list of messages.
                  example: comment_number
                visible:
                  type: string
                  description: Show this comment?
                  example: visible
              required:
                - comment_number
                - visible
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=newMessage:
    post:
      tags:
        - bug
      summary: New message
      description: Create a new message, and link it to this object.
      operationId: bug-newMessage
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subject:
                  type: string
                  description: Subject
                content:
                  type: string
                  description: Message
                  example: content
                send_notifications:
                  type: string
                  description: Enable email notifications for adding this comment.
              required:
                - content
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=subscribe:
    post:
      tags:
        - bug
      summary: Subscribe
      description: "Subscribe person to the bug. param person: the subscriber. param subscribed_by: the person who created the subscription. param suppress_notify: a flag to suppress notify call. param level: The BugNotificationLevel for the new subscription. return: an IBugSubscription."
      operationId: bug-subscribe
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person
                  example: ~launchpad
                level:
                  type: string
                  enum:
                    - Lifecycle
                    - Details
                    - Discussion
                  description: Level
              required:
                - person
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=unsubscribe:
    post:
      tags:
        - bug
      summary: Unsubscribe
      description: Remove this person's subscription to this bug.
      operationId: bug-unsubscribe
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=unsubscribeFromDupes:
    post:
      tags:
        - bug
      summary: Unsubscribe from dupes
      description: Remove this person's subscription from all dupes of this bug.
      operationId: bug-unsubscribeFromDupes
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=isUserAffected:
    post:
      tags:
        - bug
      summary: Is user affected
      description: "Is :user: marked as affected by this bug?"
      operationId: bug-isUserAffected
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=lock:
    post:
      tags:
        - bug
      summary: Lock
      description: "Lock the bug metadata edits to the relevant roles. param status: The lock status of the bug - one of the values in the BugLockStatus enum. param reason: The reason for locking this bug. param who: The IPerson who is making the change."
      operationId: bug-lock
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - Comment-only
                  description: Lock Status The lock status of this bug.
                reason:
                  type: string
                  description: Lock Reason The reason for locking this bug.
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=unlock:
    post:
      tags:
        - bug
      summary: Unlock
      description: "Unlock the bug metadata edits to the default roles. param who: The IPerson who is making the change."
      operationId: bug-unlock
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=linkMergeProposal:
    post:
      tags:
        - bug
      summary: Link merge proposal
      description: Ensure that this MP is linked to this bug.
      operationId: bug-linkMergeProposal
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                merge_proposal:
                  type: string
                  format: uri
                  x-lp-resource-type: branch_merge_proposal
                  description: Merge proposal
                  example: merge_proposal
              required:
                - merge_proposal
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=unlinkMergeProposal:
    post:
      tags:
        - bug
      summary: Unlink merge proposal
      description: Ensure that any links between this bug and the given MP are removed.
      operationId: bug-unlinkMergeProposal
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                merge_proposal:
                  type: string
                  format: uri
                  x-lp-resource-type: branch_merge_proposal
                  description: Merge proposal
                  example: merge_proposal
              required:
                - merge_proposal
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=transitionToInformationType:
    post:
      tags:
        - bug
      summary: Transition to information type
      description: "Set the information type for this bug. information_type: The InformationType to transition to. who: The IPerson who is making the change."
      operationId: bug-transitionToInformationType
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information Type The type of information contained in this bug report.
                  example: Public
              required:
                - information_type
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=mute:
    post:
      tags:
        - bug
      summary: Mute
      description: Add a muted subscription for person.
      operationId: bug-mute
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}?ws.op=unmute:
    post:
      tags:
        - bug
      summary: Unmute
      description: Remove a muted subscription for person. Returns previously muted direct subscription, if any.
      operationId: bug-unmute
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
  /{bug_1}/{bug_2}:
    get:
      tags:
        - bug
      summary: Get bug
      description: The core bug entry.
      operationId: bug-get
      parameters:
        - *a3
        - *a4
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug-full"
    put:
      tags:
        - bug
      summary: Replace bug
      description: The core bug entry.
      operationId: bug-put
      parameters:
        - *a3
        - *a4
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/bug-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - bug
      summary: Update bug
      description: The core bug entry.
      operationId: bug-patch
      parameters:
        - *a3
        - *a4
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/bug-diff"
      responses:
        "200":
          description: Successful response
  /{cve_1}/{cve_2}/{cve_3}:
    get:
      tags:
        - cve
      summary: Get cve
      description: A single CVE database entry.
      operationId: cve-get
      parameters:
        - &a5
          name: cve_1
          in: path
          required: true
          description: Path segment of the cve resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: bugs
        - &a6
          name: cve_2
          in: path
          required: true
          description: Path segment of the cve resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: cve
        - &a7
          name: cve_3
          in: path
          required: true
          description: Path segment of the cve resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: 2014-0160
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cve-full"
    put:
      tags:
        - cve
      summary: Replace cve
      description: A single CVE database entry.
      operationId: cve-put
      parameters:
        - *a5
        - *a6
        - *a7
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/cve-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - cve
      summary: Update cve
      description: A single CVE database entry.
      operationId: cve-patch
      parameters:
        - *a5
        - *a6
        - *a7
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/cve-diff"
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=getSubscriptions:
    get:
      tags:
        - distribution
      summary: Get subscriptions
      description: "Return all the subscriptions with the specified levels. return: A sequence of IStructuralSubscription."
      operationId: distribution-getSubscriptions
      parameters:
        - &a8
          name: distribution
          in: path
          required: true
          description: Path segment of the distribution resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-page"
  /{distribution}?ws.op=getSubscription:
    get:
      tags:
        - distribution
      summary: Get subscription
      description: Return the subscription for person, if it exists.
      operationId: distribution-getSubscription
      parameters:
        - *a8
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-full"
  /{distribution}?ws.op=userHasBugSubscriptions:
    get:
      tags:
        - distribution
      summary: User has bug subscriptions
      description: Is user subscribed, directly or via a team, to bug mail?
      operationId: distribution-userHasBugSubscriptions
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=searchQuestions:
    get:
      tags:
        - distribution
      summary: Search questions
      description: "Return the questions from the collection matching search criteria. param search_text: A string that is matched against the question title and description. If None, the search_text is not included as a filter criteria. param status: A sequence of QuestionStatus Items. If None or an empty sequence, the status is not included as a filter criteria. The default is to match all status except Expired and Invalid. param language: An ILanguage or a sequence of ILanguage objects to match against the question's language. If None or an empty sequence, the language is not included as a filter criteria. param owner: The IPerson that created the question. param needs_attention_from: Selects questions that need attention from an IPerson. These are the questions in the NEEDSINFO or ANSWERED state owned by the person. The questions not owned by the person but on which the person requested more information or gave an answer and that are back in the OPEN state are also included. param sort: An attribute of QuestionSort. If None, a default value is used. When there is a search_text value, the default is to sort by RELEVANCY, otherwise results are sorted NEWEST_FIRST. param created_since: Only return results whose datecreated property is greater than or equal to this date. param created_before: Only return results whose datecreated property is smaller than this date."
      operationId: distribution-searchQuestions
      parameters:
        - *a8
        - name: search_text
          in: query
          required: false
          description: Search text
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status
          schema:
            type: string
        - name: language
          in: query
          required: false
          description: Language
          schema:
            type: string
        - name: owner
          in: query
          required: false
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: needs_attention_from
          in: query
          required: false
          description: Needs attentions from
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: sort
          in: query
          required: false
          description: Sort
          schema:
            type: string
            enum:
              - by relevancy
              - by status
              - newest first
              - oldest first
              - recently updated first
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /{distribution}?ws.op=getQuestion:
    get:
      tags:
        - distribution
      summary: Get question
      description: "Return the question by its id, if it is applicable to this target. question_id: A question id. If there is no such question number for this target, return None"
      operationId: distribution-getQuestion
      parameters:
        - *a8
        - name: question_id
          in: query
          required: true
          description: Question Number
          schema:
            type: string
          example: "1"
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=findSimilarQuestions:
    get:
      tags:
        - distribution
      summary: Find similar questions
      description: "Return questions similar to phrase. Return a list of question similar to the provided phrase. These questions will be found using a fuzzy search. The list is ordered from the most similar question to the least similar question. param phrase: A phrase such as the summary of a question."
      operationId: distribution-findSimilarQuestions
      parameters:
        - *a8
        - name: phrase
          in: query
          required: true
          description: A phrase
          schema:
            type: string
          example: phrase
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /{distribution}?ws.op=getAnswerContactsForLanguage:
    get:
      tags:
        - distribution
      summary: Get answer contacts for language
      description: Return the list of Persons that provide support for a language. An answer contact supports questions in their preferred languages.
      operationId: distribution-getAnswerContactsForLanguage
      parameters:
        - *a8
        - name: language
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: language
          example: language
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /{distribution}?ws.op=getSupportedLanguages:
    get:
      tags:
        - distribution
      summary: Get supported languages
      description: Return a list of languages spoken by at the answer contacts. An answer contact is considered to speak a given language if that language is listed as one of their preferred languages.
      operationId: distribution-getSupportedLanguages
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/language-page"
  /{distribution}?ws.op=canUserAlterAnswerContact:
    get:
      tags:
        - distribution
      summary: Can user alter answer contact
      description: "Can the user add or remove the answer contact. Users can add or remove themselves or one of the teams they administered. Admins and target owners can add/remove anyone. param person: The IPerson that is or will be an answer contact. param subscribed_by: The IPerson making the change."
      operationId: distribution-canUserAlterAnswerContact
      parameters:
        - *a8
        - name: person
          in: query
          required: true
          description: The user or an administered team
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=getSpecification:
    get:
      tags:
        - distribution
      summary: Get specification
      description: Returns the specification with the given name, for this target, or None.
      operationId: distribution-getSpecification
      parameters:
        - *a8
        - name: name
          in: query
          required: true
          description: The name of the specification
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/specification-full"
  /{distribution}?ws.op=getTranslationImportQueueEntries:
    get:
      tags:
        - distribution
      summary: Get translation import queue entries
      description: "Return entries in the translation import queue for this entity. arg import_status: RosettaImportStatus DB Schema entry. arg file_extension: String with the file type extension, usually 'po' or 'pot'. If one of both of 'import_status' or 'file_extension' are given, the returned entries are filtered based on those values."
      operationId: distribution-getTranslationImportQueueEntries
      parameters:
        - *a8
        - name: import_status
          in: query
          required: false
          description: Status Show only entries with this status
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
        - name: file_extension
          in: query
          required: false
          description: Filename extension Show only entries with this filename suffix
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /{distribution}?ws.op=searchTasks:
    get:
      tags:
        - distribution
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: distribution-searchTasks
      parameters:
        - *a8
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{distribution}?ws.op=getBuildRecords:
    get:
      tags:
        - distribution
      summary: Get build records
      description: "Return build records in the context it is implemented. It excludes build records generated by Gina (imported from a external repository), where IBuild.datebuilt is null and IBuild.buildstate is BuildStatus.FULLYBUILT. The result is simply not filtered if the optional filters are omitted by call sites. param build_state: optional BuildStatus value for filtering build records; param name: optional string for filtering build source package name. Sub-string matching is allowed via SQL LIKE. param pocket: optional PackagePublishingPocket value for filtering build records; param arch_tag: optional string for filtering build source packages by their architecture tag; param user: optional IPerson corresponding to the user performing the request. It will filter out build records for which the user have no 'view' permission. param binary_only: optional boolean indicating whether only BinaryPackageBuild objects should be returned, or more general PackageBuild objects (which may include, for example, SourcePackageRecipeBuild objects. return: a result set containing IPackageBuild records ordered by descending IPackageBuild.date_finished except when builds are filtered by BuildStatus.NEEDSBUILD, in this case records are ordered by descending BuildQueue.lastscore (dispatching order)."
      operationId: distribution-getBuildRecords
      parameters:
        - *a8
        - name: source_name
          in: query
          required: false
          description: Source package name
          schema:
            type: string
        - name: build_state
          in: query
          required: false
          description: Build status The status of this build record
          schema:
            type: string
            enum:
              - Needs building
              - Successfully built
              - Failed to build
              - Dependency wait
              - Chroot problem
              - Build for superseded Source
              - Currently building
              - Failed to upload
              - Uploading build
              - Cancelling build
              - Cancelled build
              - Gathering build output
        - name: pocket
          in: query
          required: false
          description: Pocket The pocket into which this entry is published
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/build-page"
  /{distribution}?ws.op=getMilestone:
    get:
      tags:
        - distribution
      summary: Get milestone
      description: Return a milestone with the given name for this object, or None.
      operationId: distribution-getMilestone
      parameters:
        - *a8
        - name: name
          in: query
          required: true
          description: Name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/milestone-full"
  /{distribution}?ws.op=getArchive:
    get:
      tags:
        - distribution
      summary: Get archive
      description: "Return the distribution archive with the given name. Only distribution archives are considered -- PPAs will not be found. param name: The name of the archive, e.g. 'partner'"
      operationId: distribution-getArchive
      parameters:
        - *a8
        - name: name
          in: query
          required: true
          description: Archive name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive-full"
  /{distribution}?ws.op=getDevelopmentSeries:
    get:
      tags:
        - distribution
      summary: Get development series
      description: Return the DistroSeries which are marked as in development.
      operationId: distribution-getDevelopmentSeries
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_series-page"
  /{distribution}?ws.op=getSeries:
    get:
      tags:
        - distribution
      summary: Get series
      description: "Return the series with the name or version given. param name_or_version: The IDistroSeries.name or IDistroSeries.version."
      operationId: distribution-getSeries
      parameters:
        - *a8
        - name: name_or_version
          in: query
          required: true
          description: Name or version
          schema:
            type: string
          example: noble
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_series-full"
  /{distribution}?ws.op=getMirrorByName:
    get:
      tags:
        - distribution
      summary: Get mirror by name
      description: Return the mirror with the given name for this distribution or None if it's not found.
      operationId: distribution-getMirrorByName
      parameters:
        - *a8
        - name: name
          in: query
          required: true
          description: Name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_mirror-full"
  /{distribution}?ws.op=getCountryMirror:
    get:
      tags:
        - distribution
      summary: Get country mirror
      description: Return the country DNS mirror for a country and content type.
      operationId: distribution-getCountryMirror
      parameters:
        - *a8
        - name: country
          in: query
          required: true
          description: Location The country in which this mirror is based.
          schema:
            type: string
            format: uri
            x-lp-resource-type: country
          example: country
        - name: mirror_type
          in: query
          required: true
          description: Content Choose "CD Image" if this mirror contains CD images of this distribution. Choose "Archive" if this is a mirror of packages for this distribution.
          schema:
            type: string
            enum:
              - Archive
              - CD Image
          example: Archive
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_mirror-full"
  /{distribution}?ws.op=getSourcePackage:
    get:
      tags:
        - distribution
      summary: Get source package
      description: Return a DistributionSourcePackage with the given name for this distribution, or None.
      operationId: distribution-getSourcePackage
      parameters:
        - *a8
        - name: name
          in: query
          required: true
          description: Package name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_source_package-full"
  /{distribution}?ws.op=searchSourcePackages:
    get:
      tags:
        - distribution
      summary: Search source packages
      description: Search for source packages that correspond to the given text. This method just decorates the result of searchSourcePackageCaches() to return DistributionSourcePackages.
      operationId: distribution-searchSourcePackages
      parameters:
        - *a8
        - name: source_match
          in: query
          required: true
          description: Source package name substring match
          schema:
            type: string
          example: source_match
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_source_package-page"
  /{distribution}?ws.op=findReferencedOOPS:
    get:
      tags:
        - distribution
      summary: Find referenced oops
      description: "Find OOPS reports between start_date and end_date. param start_date: Do not look in objects whose last modification time is before this date. param end_date: Do not look in objects whose last modification time is after this date. return: A set of OOPS id's - strings of the form 'OOPS-w+'."
      operationId: distribution-findReferencedOOPS
      parameters:
        - *a8
        - name: start_date
          in: query
          required: true
          description: Modified after date
          schema:
            type: string
          example: start_date
        - name: end_date
          in: query
          required: true
          description: Modified before date
          schema:
            type: string
          example: end_date
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=getBranchTips:
    get:
      tags:
        - distribution
      summary: Get branch tips
      description: "Return a list of branches which have new tips since a date. Each branch information is a tuple of (branch_unique_name, tip_revision, (official_series*)). So for each branch in the distribution, you'll get the branch unique name, the revision id of tip, and if the branch is official for some series, the list of series name. param: user: If specified, shows the branches visible to that user. if not specified, only branches visible to the anonymous user are shown. param since: If specified, limits results to branches modified since that date and time."
      operationId: distribution-getBranchTips
      parameters:
        - *a8
        - name: since
          in: query
          required: false
          description: Time of last change Return branches that have new tips since this timestamp.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=getBestMirrorsForCountry:
    get:
      tags:
        - distribution
      summary: Get best mirrors for country
      description: Return the best mirrors to be used by someone in the given country. The list of mirrors is composed by the official mirrors located in the given country (or in the country's continent if the country doesn't have any) plus the main mirror of that type.
      operationId: distribution-getBestMirrorsForCountry
      parameters:
        - *a8
        - name: country
          in: query
          required: true
          description: Location The country in which this mirror is based.
          schema:
            type: string
            format: uri
            x-lp-resource-type: country
          example: country
        - name: mirror_type
          in: query
          required: true
          description: Content Choose "CD Image" if this mirror contains CD images of this distribution. Choose "Archive" if this is a mirror of packages for this distribution.
          schema:
            type: string
            enum:
              - Archive
              - CD Image
          example: Archive
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_mirror-page"
  /{distribution}?ws.op=searchOCIProjects:
    get:
      tags:
        - distribution
      summary: Search ociprojects
      description: Search for OCI projects that match the title text.
      operationId: distribution-searchOCIProjects
      parameters:
        - *a8
        - name: text
          in: query
          required: false
          description: OCI title substring match
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oci_project-page"
  /{distribution}?ws.op=getOCIProject:
    get:
      tags:
        - distribution
      summary: Get ociproject
      description: Return a OCIProject with the given name for this distribution, or None.
      operationId: distribution-getOCIProject
      parameters:
        - *a8
        - name: name
          in: query
          required: true
          description: OCI project name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/oci_project-full"
  /{distribution}?ws.op=addBugSubscription:
    post:
      tags:
        - distribution
      summary: Add bug subscription
      description: "Add a bug subscription for this structure. This method is used to create a new IStructuralSubscription for the target. This initially has a single filter which will allow all notifications will be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription."
      operationId: distribution-addBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=addBugSubscriptionFilter:
    post:
      tags:
        - distribution
      summary: Add bug subscription filter
      description: "Add a bug subscription filter for this structure. This method is used to create a new IBugSubscriptionFilter for the target. It will initially allow all notifications to be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription filter."
      operationId: distribution-addBugSubscriptionFilter
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=removeBugSubscription:
    post:
      tags:
        - distribution
      summary: Remove bug subscription
      description: "Remove a subscription to bugs from this structure. This will delete all associated filters. subscriber: The IPerson who will be unsubscribed. If omitted, unsubscribed_by will be used. unsubscribed_by: The IPerson removing the subscription."
      operationId: distribution-removeBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to unsubscribe. If omitted, the requesting user will be unsubscribed.
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=addAnswerContact:
    post:
      tags:
        - distribution
      summary: Add answer contact
      description: "Add a new answer contact. param person: An IPerson. param subscribed_by: The user making the change. return: True if the person was added, False if the person already is an answer contact. raises AddAnswerContactError: When the person or team does no have a preferred language."
      operationId: distribution-addAnswerContact
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The user of an administered team
                  example: ~launchpad
              required:
                - person
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=removeAnswerContact:
    post:
      tags:
        - distribution
      summary: Remove answer contact
      description: "Remove an answer contact. param person: An IPerson. param subscribed_by: The user making the change. return: True if the person was removed, False if the person wasn't an answer contact."
      operationId: distribution-removeAnswerContact
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The user of an administered team
                  example: ~launchpad
              required:
                - person
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=newWebhook:
    post:
      tags:
        - distribution
      summary: New webhook
      description: Create a new webhook.
      operationId: distribution-newWebhook
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                delivery_url:
                  type: string
                  description: Delivery URL
                  example: delivery_url
                active:
                  type: string
                  description: Active Deliver details of subscribed events.
                event_types:
                  type: string
                  description: Event types
                  example: event_types
                secret:
                  type: string
                  description: Secret An optional string used to sign delivery bodies with HMAC-SHA1 in the X-Hub-Signature header. This must be less than 200 bytes long.
              required:
                - delivery_url
                - event_types
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=addOfficialBugTag:
    post:
      tags:
        - distribution
      summary: Add official bug tag
      description: Add tag to the official bug tags of this target.
      operationId: distribution-addOfficialBugTag
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                tag:
                  type: string
                  description: The official bug tag
                  example: tag
              required:
                - tag
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=removeOfficialBugTag:
    post:
      tags:
        - distribution
      summary: Remove official bug tag
      description: Remove tag from the official bug tags of this target.
      operationId: distribution-removeOfficialBugTag
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                tag:
                  type: string
                  description: The official bug tag
                  example: tag
              required:
                - tag
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=newOCIProject:
    post:
      tags:
        - distribution
      summary: New ociproject
      description: Create an IOCIProject for this distro.
      operationId: distribution-newOCIProject
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: The OCI project name. The name that groups a set of OCI recipes together.
                  example: ubuntu
                description:
                  type: string
                  description: Description for this OCI project. A short description of this OCI project.
              required:
                - name
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=newVulnerability:
    post:
      tags:
        - distribution
      summary: New vulnerability
      description: Create a new vulnerability in the distribution.
      operationId: distribution-newVulnerability
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - Needs triage
                    - Active
                    - Ignored
                    - Retired
                  description: The status of the vulnerability.
                  example: Needs triage
                creator:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person creating the vulnerability.
                  example: creator
                information_type:
                  type: string
                  enum:
                    - Public
                    - Public Security
                    - Private Security
                    - Private
                    - Proprietary
                    - Embargoed
                  description: Information Type. Defaults to Public.
                  example: Public
                importance:
                  type: string
                  enum:
                    - Unknown
                    - Undecided
                    - Critical
                    - High
                    - Medium
                    - Low
                    - Wishlist
                  description: Indicates the work priority, not the severity. Defaults to Undecided.
                cve:
                  type: string
                  format: uri
                  x-lp-resource-type: cve
                  description: External CVE reference corresponding to this vulnerability, if any.
                description:
                  type: string
                  description: A short description of the vulnerability.
                notes:
                  type: string
                  description: Free-form notes for this vulnerability.
                mitigation:
                  type: string
                  description: Explains why we're ignoring this vulnerability.
                importance_explanation:
                  type: string
                  description: Used to explain why our importance differs from somebody else's CVSS score.
                date_made_public:
                  type: string
                  description: The date this vulnerability was made public.
              required:
                - status
                - creator
                - information_type
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=setOCICredentials:
    post:
      tags:
        - distribution
      summary: Set ocicredentials
      description: Set the credentials for the OCI registry for OCI projects.
      operationId: distribution-setOCICredentials
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                registry_url:
                  type: string
                  description: The registry url. The url of the OCI registry to use.
                  example: registry_url
                region:
                  type: string
                  description: OCI registry region. The region of the OCI registry.
                username:
                  type: string
                  description: Username The username for the OCI registry.
                password:
                  type: string
                  description: Password The password for the OCI registry.
              required:
                - registry_url
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}?ws.op=deleteOCICredentials:
    post:
      tags:
        - distribution
      summary: Delete ocicredentials
      description: Delete any existing OCI credentials for the distribution.
      operationId: distribution-deleteOCICredentials
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
  /{distribution}:
    get:
      tags:
        - distribution
      summary: Get distribution
      description: 'An operating system distribution. Launchpadlib example: retrieving the current version of a package in a particular distroseries. ubuntu = launchpad.distributions["ubuntu"] archive = ubuntu.main_archive series = ubuntu.current_series print(archive.getPublishedSources(exact_match=True, source_name="apport", distro_series=series)[0].source_package_version)'
      operationId: distribution-get
      parameters:
        - *a8
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution-full"
    put:
      tags:
        - distribution
      summary: Replace distribution
      description: 'An operating system distribution. Launchpadlib example: retrieving the current version of a package in a particular distroseries. ubuntu = launchpad.distributions["ubuntu"] archive = ubuntu.main_archive series = ubuntu.current_series print(archive.getPublishedSources(exact_match=True, source_name="apport", distro_series=series)[0].source_package_version)'
      operationId: distribution-put
      parameters:
        - *a8
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distribution-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - distribution
      summary: Update distribution
      description: 'An operating system distribution. Launchpadlib example: retrieving the current version of a package in a particular distroseries. ubuntu = launchpad.distributions["ubuntu"] archive = ubuntu.main_archive series = ubuntu.current_series print(archive.getPublishedSources(exact_match=True, source_name="apport", distro_series=series)[0].source_package_version)'
      operationId: distribution-patch
      parameters:
        - *a8
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distribution-diff"
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=searchQuestions:
    get:
      tags:
        - distribution_source_package
      summary: Search questions
      description: "Return the questions from the collection matching search criteria. param search_text: A string that is matched against the question title and description. If None, the search_text is not included as a filter criteria. param status: A sequence of QuestionStatus Items. If None or an empty sequence, the status is not included as a filter criteria. The default is to match all status except Expired and Invalid. param language: An ILanguage or a sequence of ILanguage objects to match against the question's language. If None or an empty sequence, the language is not included as a filter criteria. param owner: The IPerson that created the question. param needs_attention_from: Selects questions that need attention from an IPerson. These are the questions in the NEEDSINFO or ANSWERED state owned by the person. The questions not owned by the person but on which the person requested more information or gave an answer and that are back in the OPEN state are also included. param sort: An attribute of QuestionSort. If None, a default value is used. When there is a search_text value, the default is to sort by RELEVANCY, otherwise results are sorted NEWEST_FIRST. param created_since: Only return results whose datecreated property is greater than or equal to this date. param created_before: Only return results whose datecreated property is smaller than this date."
      operationId: distribution_source_package-searchQuestions
      parameters:
        - &a9
          name: distribution_source_package_1
          in: path
          required: true
          description: Path segment of the distribution_source_package resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ubuntu
        - &a10
          name: distribution_source_package_2
          in: path
          required: true
          description: Path segment of the distribution_source_package resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: bash
        - name: search_text
          in: query
          required: false
          description: Search text
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status
          schema:
            type: string
        - name: language
          in: query
          required: false
          description: Language
          schema:
            type: string
        - name: owner
          in: query
          required: false
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: needs_attention_from
          in: query
          required: false
          description: Needs attentions from
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: sort
          in: query
          required: false
          description: Sort
          schema:
            type: string
            enum:
              - by relevancy
              - by status
              - newest first
              - oldest first
              - recently updated first
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=getQuestion:
    get:
      tags:
        - distribution_source_package
      summary: Get question
      description: "Return the question by its id, if it is applicable to this target. question_id: A question id. If there is no such question number for this target, return None"
      operationId: distribution_source_package-getQuestion
      parameters:
        - *a9
        - *a10
        - name: question_id
          in: query
          required: true
          description: Question Number
          schema:
            type: string
          example: "1"
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=findSimilarQuestions:
    get:
      tags:
        - distribution_source_package
      summary: Find similar questions
      description: "Return questions similar to phrase. Return a list of question similar to the provided phrase. These questions will be found using a fuzzy search. The list is ordered from the most similar question to the least similar question. param phrase: A phrase such as the summary of a question."
      operationId: distribution_source_package-findSimilarQuestions
      parameters:
        - *a9
        - *a10
        - name: phrase
          in: query
          required: true
          description: A phrase
          schema:
            type: string
          example: phrase
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=getAnswerContactsForLanguage:
    get:
      tags:
        - distribution_source_package
      summary: Get answer contacts for language
      description: Return the list of Persons that provide support for a language. An answer contact supports questions in their preferred languages.
      operationId: distribution_source_package-getAnswerContactsForLanguage
      parameters:
        - *a9
        - *a10
        - name: language
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: language
          example: language
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=getSupportedLanguages:
    get:
      tags:
        - distribution_source_package
      summary: Get supported languages
      description: Return a list of languages spoken by at the answer contacts. An answer contact is considered to speak a given language if that language is listed as one of their preferred languages.
      operationId: distribution_source_package-getSupportedLanguages
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/language-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=canUserAlterAnswerContact:
    get:
      tags:
        - distribution_source_package
      summary: Can user alter answer contact
      description: "Can the user add or remove the answer contact. Users can add or remove themselves or one of the teams they administered. Admins and target owners can add/remove anyone. param person: The IPerson that is or will be an answer contact. param subscribed_by: The IPerson making the change."
      operationId: distribution_source_package-canUserAlterAnswerContact
      parameters:
        - *a9
        - *a10
        - name: person
          in: query
          required: true
          description: The user or an administered team
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=getSubscriptions:
    get:
      tags:
        - distribution_source_package
      summary: Get subscriptions
      description: "Return all the subscriptions with the specified levels. return: A sequence of IStructuralSubscription."
      operationId: distribution_source_package-getSubscriptions
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=getSubscription:
    get:
      tags:
        - distribution_source_package
      summary: Get subscription
      description: Return the subscription for person, if it exists.
      operationId: distribution_source_package-getSubscription
      parameters:
        - *a9
        - *a10
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-full"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=userHasBugSubscriptions:
    get:
      tags:
        - distribution_source_package
      summary: User has bug subscriptions
      description: Is user subscribed, directly or via a team, to bug mail?
      operationId: distribution_source_package-userHasBugSubscriptions
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=searchTasks:
    get:
      tags:
        - distribution_source_package
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: distribution_source_package-searchTasks
      parameters:
        - *a9
        - *a10
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=getMergeProposals:
    get:
      tags:
        - distribution_source_package
      summary: Get merge proposals
      description: "Returns all merge proposals of a given status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param eager_load: If True, load related objects for the whole collection. returns: A list of IBranchMergeProposal."
      operationId: distribution_source_package-getMergeProposals
      parameters:
        - *a9
        - *a10
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=getBranches:
    get:
      tags:
        - distribution_source_package
      summary: Get branches
      description: "Returns all branches with the given lifecycle status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param modified_since: If set, filters the branches being returned to those that have been modified since the specified date/time. param eager_load: If True load related objects for the whole collection. returns: A list of IBranch."
      operationId: distribution_source_package-getBranches
      parameters:
        - *a9
        - *a10
        - name: status
          in: query
          required: false
          description: A list of branch lifecycle statuses to filter by.
          schema:
            type: string
        - name: modified_since
          in: query
          required: false
          description: Limit the branches to those modified since this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=addAnswerContact:
    post:
      tags:
        - distribution_source_package
      summary: Add answer contact
      description: "Add a new answer contact. param person: An IPerson. param subscribed_by: The user making the change. return: True if the person was added, False if the person already is an answer contact. raises AddAnswerContactError: When the person or team does no have a preferred language."
      operationId: distribution_source_package-addAnswerContact
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The user of an administered team
                  example: ~launchpad
              required:
                - person
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=removeAnswerContact:
    post:
      tags:
        - distribution_source_package
      summary: Remove answer contact
      description: "Remove an answer contact. param person: An IPerson. param subscribed_by: The user making the change. return: True if the person was removed, False if the person wasn't an answer contact."
      operationId: distribution_source_package-removeAnswerContact
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The user of an administered team
                  example: ~launchpad
              required:
                - person
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=addBugSubscription:
    post:
      tags:
        - distribution_source_package
      summary: Add bug subscription
      description: "Add a bug subscription for this structure. This method is used to create a new IStructuralSubscription for the target. This initially has a single filter which will allow all notifications will be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription."
      operationId: distribution_source_package-addBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=addBugSubscriptionFilter:
    post:
      tags:
        - distribution_source_package
      summary: Add bug subscription filter
      description: "Add a bug subscription filter for this structure. This method is used to create a new IBugSubscriptionFilter for the target. It will initially allow all notifications to be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription filter."
      operationId: distribution_source_package-addBugSubscriptionFilter
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=removeBugSubscription:
    post:
      tags:
        - distribution_source_package
      summary: Remove bug subscription
      description: "Remove a subscription to bugs from this structure. This will delete all associated filters. subscriber: The IPerson who will be unsubscribed. If omitted, unsubscribed_by will be used. unsubscribed_by: The IPerson removing the subscription."
      operationId: distribution_source_package-removeBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to unsubscribe. If omitted, the requesting user will be unsubscribed.
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=newWebhook:
    post:
      tags:
        - distribution_source_package
      summary: New webhook
      description: Create a new webhook.
      operationId: distribution_source_package-newWebhook
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                delivery_url:
                  type: string
                  description: Delivery URL
                  example: delivery_url
                active:
                  type: string
                  description: Active Deliver details of subscribed events.
                event_types:
                  type: string
                  description: Event types
                  example: event_types
                secret:
                  type: string
                  description: Secret An optional string used to sign delivery bodies with HMAC-SHA1 in the X-Hub-Signature header. This must be less than 200 bytes long.
              required:
                - delivery_url
                - event_types
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}?ws.op=newCodeImport:
    post:
      tags:
        - distribution_source_package
      summary: New code import
      description: "Create a new code import. param registrant: The IPerson to record as the registrant of the import. param branch_name: The name of the branch or repository to create. param rcs_type: The type of the foreign VCS. param target_rcs_type: The type of the branch or repository to create (Bazaar or Git). param url: The URL to import from if the VCS type uses a single URL (i.e. isn't CVS). param cvs_root: The CVSROOT for a CVS import. param cvs_module: The module to import for a CVS import. param owner: Who should own the created branch or repository, or None for it to be the same as the registrant, or the caller over the API. returns: An instance of ICodeImport."
      operationId: distribution_source_package-newCodeImport
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                branch_name:
                  type: string
                  description: Name of branch to create
                rcs_type:
                  type: string
                  enum:
                    - Concurrent Versions System
                    - Subversion via CSCVS
                    - Subversion via bzr-svn
                    - Git
                    - Mercurial
                    - Bazaar
                target_rcs_type:
                  type: string
                  enum:
                    - Bazaar
                    - Git
                url:
                  type: string
                  description: Foreign VCS URL
                cvs_root:
                  type: string
                  description: CVS root URL
                cvs_module:
                  type: string
                  description: CVS module to import
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner of the resulting branch
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
  /{distribution_source_package_1}/+source/{distribution_source_package_2}:
    get:
      tags:
        - distribution_source_package
      summary: Get distribution source package
      description: Represents a source package in a distribution. Create IDistributionSourcePackages by invoking IDistribution.getSourcePackage().
      operationId: distribution_source_package-get
      parameters:
        - *a9
        - *a10
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_source_package-full"
    put:
      tags:
        - distribution_source_package
      summary: Replace distribution source package
      description: Represents a source package in a distribution. Create IDistributionSourcePackages by invoking IDistribution.getSourcePackage().
      operationId: distribution_source_package-put
      parameters:
        - *a9
        - *a10
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distribution_source_package-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - distribution_source_package
      summary: Update distribution source package
      description: Represents a source package in a distribution. Create IDistributionSourcePackages by invoking IDistribution.getSourcePackage().
      operationId: distribution_source_package-patch
      parameters:
        - *a9
        - *a10
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distribution_source_package-diff"
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=getBuildRecords:
    get:
      tags:
        - distro_arch_series
      summary: Get build records
      description: "Return build records in the context it is implemented. It excludes build records generated by Gina (imported from a external repository), where IBuild.datebuilt is null and IBuild.buildstate is BuildStatus.FULLYBUILT. The result is simply not filtered if the optional filters are omitted by call sites. param build_state: optional BuildStatus value for filtering build records; param name: optional string for filtering build source package name. Sub-string matching is allowed via SQL LIKE. param pocket: optional PackagePublishingPocket value for filtering build records; param arch_tag: optional string for filtering build source packages by their architecture tag; param user: optional IPerson corresponding to the user performing the request. It will filter out build records for which the user have no 'view' permission. param binary_only: optional boolean indicating whether only BinaryPackageBuild objects should be returned, or more general PackageBuild objects (which may include, for example, SourcePackageRecipeBuild objects. return: a result set containing IPackageBuild records ordered by descending IPackageBuild.date_finished except when builds are filtered by BuildStatus.NEEDSBUILD, in this case records are ordered by descending BuildQueue.lastscore (dispatching order)."
      operationId: distro_arch_series-getBuildRecords
      parameters:
        - &a11
          name: distro_arch_series_1
          in: path
          required: true
          description: Path segment of the distro_arch_series resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ubuntu
        - &a12
          name: distro_arch_series_2
          in: path
          required: true
          description: Path segment of the distro_arch_series resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: noble
        - &a13
          name: distro_arch_series_3
          in: path
          required: true
          description: Path segment of the distro_arch_series resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: amd64
        - name: source_name
          in: query
          required: false
          description: Source package name
          schema:
            type: string
        - name: build_state
          in: query
          required: false
          description: Build status The status of this build record
          schema:
            type: string
            enum:
              - Needs building
              - Successfully built
              - Failed to build
              - Dependency wait
              - Chroot problem
              - Build for superseded Source
              - Currently building
              - Failed to upload
              - Uploading build
              - Cancelling build
              - Cancelled build
              - Gathering build output
        - name: pocket
          in: query
          required: false
          description: Pocket The pocket into which this entry is published
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/build-page"
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=getChrootURL:
    get:
      tags:
        - distro_arch_series
      summary: Get chroot url
      description: Return the chroot URL for this series, pocket, and image type. The pocket defaults to "Release"; the image type defaults to "Chroot tarball".
      operationId: distro_arch_series-getChrootURL
      parameters:
        - *a11
        - *a12
        - *a13
        - name: pocket
          in: query
          required: false
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
        - name: image_type
          in: query
          required: false
          schema:
            type: string
            enum:
              - Chroot tarball
              - LXD image
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=getChrootHash:
    get:
      tags:
        - distro_arch_series
      summary: Get chroot hash
      description: Return applicable hashes for the current chroot of this pocket.
      operationId: distro_arch_series-getChrootHash
      parameters:
        - *a11
        - *a12
        - *a13
        - name: pocket
          in: query
          required: true
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
          example: Release
        - name: image_type
          in: query
          required: true
          schema:
            type: string
            enum:
              - Chroot tarball
              - LXD image
          example: Chroot tarball
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=getSourceFilter:
    get:
      tags:
        - distro_arch_series
      summary: Get source filter
      description: Get the filter for packages to build for this architecture, if any. Packages are normally built for all available architectures, subject to any constraints in their Architecture field. If a filter is set, then it applies the additional constraint that packages not included by the filter will not be built for this architecture.
      operationId: distro_arch_series-getSourceFilter
      parameters:
        - *a11
        - *a12
        - *a13
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_arch_series_filter-full"
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=setChroot:
    post:
      tags:
        - distro_arch_series
      summary: Set chroot
      description: Set the chroot tarball used for builds in this architecture. The SHA-1 checksum must match the chroot file. The pocket defaults to "Release"; the image type defaults to "Chroot tarball".
      operationId: distro_arch_series-setChroot
      parameters:
        - *a11
        - *a12
        - *a13
        - name: data
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: HostedFile
          example: data
        - name: sha1sum
          in: query
          required: true
          schema:
            type: string
          example: sha1sum
        - name: pocket
          in: query
          required: false
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
        - name: image_type
          in: query
          required: false
          schema:
            type: string
            enum:
              - Chroot tarball
              - LXD image
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=setChrootFromBuild:
    post:
      tags:
        - distro_arch_series
      summary: Set chroot from build
      description: Set the chroot tarball from a live filesystem build. The pocket defaults to "Release"; the image type defaults to "Chroot tarball".
      operationId: distro_arch_series-setChrootFromBuild
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                livefsbuild:
                  type: string
                  format: uri
                  x-lp-resource-type: livefs_build
                  description: Live filesystem build
                  example: livefsbuild
                filename:
                  type: string
                  description: Filename
                  example: filename
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                image_type:
                  type: string
                  enum:
                    - Chroot tarball
                    - LXD image
              required:
                - livefsbuild
                - filename
      parameters:
        - *a11
        - *a12
        - *a13
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=removeChroot:
    post:
      tags:
        - distro_arch_series
      summary: Remove chroot
      description: Remove the chroot tarball used for builds in this architecture. The pocket defaults to "Release"; the image type defaults to "Chroot tarball".
      operationId: distro_arch_series-removeChroot
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                image_type:
                  type: string
                  enum:
                    - Chroot tarball
                    - LXD image
      parameters:
        - *a11
        - *a12
        - *a13
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=setSourceFilter:
    post:
      tags:
        - distro_arch_series
      summary: Set source filter
      description: 'Set a filter for packages to build for this architecture. Packages are normally built for all available architectures, subject to any constraints in their Architecture field. If a filter is set, then it applies the additional constraint that packages not included by the filter will not be built for this architecture. If the sense of the filter is "Include", then the filter only includes packages in the given package set. If the sense of the filter is "Exclude", then the filter only includes packages not in the given package set. Later changes to the given package set will also affect any filters using it. param packageset: An IPackageset to use as a filter. param sense: A DistroArchSeriesFilterSense item indicating whether the filter includes or excludes packages. param creator: The IPerson who is creating this filter.'
      operationId: distro_arch_series-setSourceFilter
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                packageset:
                  type: string
                  format: uri
                  x-lp-resource-type: packageset
                  description: Package set
                  example: packageset
                sense:
                  type: string
                  enum:
                    - Include
                    - Exclude
                  description: Sense
                  example: Include
              required:
                - packageset
                - sense
      parameters:
        - *a11
        - *a12
        - *a13
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}?ws.op=removeSourceFilter:
    post:
      tags:
        - distro_arch_series
      summary: Remove source filter
      description: Remove any filter for packages to build for this architecture. This causes packages to be built for this architecture when they might previously have been filtered, subject to any constraints in their Architecture field.
      operationId: distro_arch_series-removeSourceFilter
      parameters:
        - *a11
        - *a12
        - *a13
      responses:
        "200":
          description: Successful response
  /{distro_arch_series_1}/{distro_arch_series_2}/{distro_arch_series_3}:
    get:
      tags:
        - distro_arch_series
      summary: Get distro arch series
      description: An architecture for a distroseries.
      operationId: distro_arch_series-get
      parameters:
        - *a11
        - *a12
        - *a13
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_arch_series-full"
    put:
      tags:
        - distro_arch_series
      summary: Replace distro arch series
      description: An architecture for a distroseries.
      operationId: distro_arch_series-put
      parameters:
        - *a11
        - *a12
        - *a13
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distro_arch_series-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - distro_arch_series
      summary: Update distro arch series
      description: An architecture for a distroseries.
      operationId: distro_arch_series-patch
      parameters:
        - *a11
        - *a12
        - *a13
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distro_arch_series-diff"
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}?ws.op=getSubscriptions:
    get:
      tags:
        - distro_series
      summary: Get subscriptions
      description: "Return all the subscriptions with the specified levels. return: A sequence of IStructuralSubscription."
      operationId: distro_series-getSubscriptions
      parameters:
        - &a14
          name: distro_series_1
          in: path
          required: true
          description: Path segment of the distro_series resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ubuntu
        - &a15
          name: distro_series_2
          in: path
          required: true
          description: Path segment of the distro_series resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: noble
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getSubscription:
    get:
      tags:
        - distro_series
      summary: Get subscription
      description: Return the subscription for person, if it exists.
      operationId: distro_series-getSubscription
      parameters:
        - *a14
        - *a15
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-full"
  /{distro_series_1}/{distro_series_2}?ws.op=userHasBugSubscriptions:
    get:
      tags:
        - distro_series
      summary: User has bug subscriptions
      description: Is user subscribed, directly or via a team, to bug mail?
      operationId: distro_series-userHasBugSubscriptions
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}?ws.op=getTranslationImportQueueEntries:
    get:
      tags:
        - distro_series
      summary: Get translation import queue entries
      description: "Return entries in the translation import queue for this entity. arg import_status: RosettaImportStatus DB Schema entry. arg file_extension: String with the file type extension, usually 'po' or 'pot'. If one of both of 'import_status' or 'file_extension' are given, the returned entries are filtered based on those values."
      operationId: distro_series-getTranslationImportQueueEntries
      parameters:
        - *a14
        - *a15
        - name: import_status
          in: query
          required: false
          description: Status Show only entries with this status
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
        - name: file_extension
          in: query
          required: false
          description: Filename extension Show only entries with this filename suffix
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getSpecification:
    get:
      tags:
        - distro_series
      summary: Get specification
      description: Returns the specification with the given name, for this target, or None.
      operationId: distro_series-getSpecification
      parameters:
        - *a14
        - *a15
        - name: name
          in: query
          required: true
          description: The name of the specification
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/specification-full"
  /{distro_series_1}/{distro_series_2}?ws.op=searchTasks:
    get:
      tags:
        - distro_series
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: distro_series-searchTasks
      parameters:
        - *a14
        - *a15
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getTranslationTemplates:
    get:
      tags:
        - distro_series
      summary: Get translation templates
      description: "Return an iterator over all its translation templates. The returned templates are either obsolete or current. return: A sequence of IPOTemplate."
      operationId: distro_series-getTranslationTemplates
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_template-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getBuildRecords:
    get:
      tags:
        - distro_series
      summary: Get build records
      description: "Return build records in the context it is implemented. It excludes build records generated by Gina (imported from a external repository), where IBuild.datebuilt is null and IBuild.buildstate is BuildStatus.FULLYBUILT. The result is simply not filtered if the optional filters are omitted by call sites. param build_state: optional BuildStatus value for filtering build records; param name: optional string for filtering build source package name. Sub-string matching is allowed via SQL LIKE. param pocket: optional PackagePublishingPocket value for filtering build records; param arch_tag: optional string for filtering build source packages by their architecture tag; param user: optional IPerson corresponding to the user performing the request. It will filter out build records for which the user have no 'view' permission. param binary_only: optional boolean indicating whether only BinaryPackageBuild objects should be returned, or more general PackageBuild objects (which may include, for example, SourcePackageRecipeBuild objects. return: a result set containing IPackageBuild records ordered by descending IPackageBuild.date_finished except when builds are filtered by BuildStatus.NEEDSBUILD, in this case records are ordered by descending BuildQueue.lastscore (dispatching order)."
      operationId: distro_series-getBuildRecords
      parameters:
        - *a14
        - *a15
        - name: source_name
          in: query
          required: false
          description: Source package name
          schema:
            type: string
        - name: build_state
          in: query
          required: false
          description: Build status The status of this build record
          schema:
            type: string
            enum:
              - Needs building
              - Successfully built
              - Failed to build
              - Dependency wait
              - Chroot problem
              - Build for superseded Source
              - Currently building
              - Failed to upload
              - Uploading build
              - Cancelling build
              - Cancelled build
              - Gathering build output
        - name: pocket
          in: query
          required: false
          description: Pocket The pocket into which this entry is published
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/build-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getDistroArchSeries:
    get:
      tags:
        - distro_series
      summary: Get distro arch series
      description: Return the distroarchseries for this distroseries with the given architecturetag.
      operationId: distro_series-getDistroArchSeries
      parameters:
        - *a14
        - *a15
        - name: archtag
          in: query
          required: true
          description: The architecture tag
          schema:
            type: string
          example: archtag
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_arch_series-full"
  /{distro_series_1}/{distro_series_2}?ws.op=getSourcePackage:
    get:
      tags:
        - distro_series
      summary: Get source package
      description: Return a source package in this distro series by name. The name given may be a string or an ISourcePackageName-providing object. The source package may not be published in the distro series.
      operationId: distro_series-getSourcePackage
      parameters:
        - *a14
        - *a15
        - name: name
          in: query
          required: true
          description: The name of the source package
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/source_package-full"
  /{distro_series_1}/{distro_series_2}?ws.op=getPackageUploads:
    get:
      tags:
        - distro_series
      summary: Get package uploads
      description: 'Get package upload records for this distribution series. param status: Filter results by this PackageUploadStatus, or list of statuses. param created_since_date: If specified, only returns items uploaded since the timestamp supplied. param archive: Filter results for this IArchive. param pocket: Filter results by this PackagePublishingPocket or a list of PackagePublishingPocket. param custom_type: Filter results by this PackageUploadCustomFormat. param name: Filter results by this file name or package name. param version: Filter results by this version number string. param exact_match: If True, look for exact string matches on the name and version filters. If False, look for a substring match so that e.g. a package "kspreadsheetplusplus" would match the search string "spreadsheet". Defaults to False. return: A result set containing IPackageUpload.'
      operationId: distro_series-getPackageUploads
      parameters:
        - *a14
        - *a15
        - name: created_since_date
          in: query
          required: false
          description: Created Since Timestamp Return items that are more recent than this timestamp.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Package Upload Status Return only items that have this status.
          schema:
            type: string
            enum:
              - New
              - Unapproved
              - Accepted
              - Done
              - Rejected
        - name: archive
          in: query
          required: false
          description: Archive Return only items for this archive.
          schema:
            type: string
            format: uri
            x-lp-resource-type: archive
        - name: pocket
          in: query
          required: false
          description: Pocket Return only items targeted to this pocket
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
        - name: custom_type
          in: query
          required: false
          description: Custom Type Return only items with custom files of this type.
          schema:
            type: string
            enum:
              - raw-installer
              - raw-translations
              - raw-dist-upgrader
              - raw-ddtp-tarball
              - raw-translations-static
              - meta-data
              - uefi
              - signing
        - name: name
          in: query
          required: false
          description: Package or file name
          schema:
            type: string
        - name: version
          in: query
          required: false
          description: Package version
          schema:
            type: string
        - name: exact_match
          in: query
          required: false
          description: Exact match Whether to filter name and version by exact matching.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/package_upload-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getDerivedSeries:
    get:
      tags:
        - distro_series
      summary: Get derived series
      description: Get all DistroSeries derived from this one.
      operationId: distro_series-getDerivedSeries
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_series-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getParentSeries:
    get:
      tags:
        - distro_series
      summary: Get parent series
      description: Get all parent DistroSeries.
      operationId: distro_series-getParentSeries
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_series-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getDifferencesTo:
    get:
      tags:
        - distro_series
      summary: Get differences to
      description: "Return the differences between this series and the specified parent_series (or all the parent series if parent_series is None). param parent_series: The parent series for which the differences should be returned. All parents are considered if this is None. param difference_type: The type of the differences to return. param source_package_name_filter: A package name to use as a filter for the differences. param status: The status of the differences to return. param child_version_higher: Only return differences for which the child's version is higher than the parent's version."
      operationId: distro_series-getDifferencesTo
      parameters:
        - *a14
        - *a15
        - name: parent_series
          in: query
          required: false
          description: The parent series to consider.
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: difference_type
          in: query
          required: false
          description: Only return differences of this type.
          schema:
            type: string
            enum:
              - Unique to derived series
              - Missing from derived series
              - Different versions
        - name: source_package_name_filter
          in: query
          required: false
          description: Only return differences for packages matching this name.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Only return differences of this status.
          schema:
            type: string
            enum:
              - Needs attention
              - Blocklisted current version
              - Blocklisted always
              - Resolved
        - name: child_version_higher
          in: query
          required: false
          description: Only return differences for which the child's version is higher than the parent's.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_series_difference-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getDifferenceComments:
    get:
      tags:
        - distro_series
      summary: Get difference comments
      description: "Get IDistroSeriesDifferenceComment items. param since: Ignore comments older than this timestamp. param source_package_name: Return only comments for a source package with this name. return: A Storm result set of IDistroSeriesDifferenceComment objects for this distroseries, ordered from oldest to newest comment."
      operationId: distro_series-getDifferenceComments
      parameters:
        - *a14
        - *a15
        - name: since
          in: query
          required: false
          description: Minimum creation timestamp Ignore comments that are older than this.
          schema:
            type: string
        - name: source_package_name
          in: query
          required: false
          description: Name of source package Only return comments for this source package.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_series_difference_comment-page"
  /{distro_series_1}/{distro_series_2}?ws.op=getTranslationTemplateStatistics:
    get:
      tags:
        - distro_series
      summary: Get translation template statistics
      description: 'Return statistics for translation templates in this series. The return value is a list of dicts for each template in the series, each of which has this form: { "sourcepackage": ..., "translation_domain": ..., "name": ..., "total": ..., "enabled": ..., "languagepack": ..., "priority": ..., "date_last_updated": ..., }'
      operationId: distro_series-getTranslationTemplateStatistics
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}?ws.op=addBugSubscription:
    post:
      tags:
        - distro_series
      summary: Add bug subscription
      description: "Add a bug subscription for this structure. This method is used to create a new IStructuralSubscription for the target. This initially has a single filter which will allow all notifications will be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription."
      operationId: distro_series-addBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}?ws.op=addBugSubscriptionFilter:
    post:
      tags:
        - distro_series
      summary: Add bug subscription filter
      description: "Add a bug subscription filter for this structure. This method is used to create a new IBugSubscriptionFilter for the target. It will initially allow all notifications to be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription filter."
      operationId: distro_series-addBugSubscriptionFilter
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}?ws.op=removeBugSubscription:
    post:
      tags:
        - distro_series
      summary: Remove bug subscription
      description: "Remove a subscription to bugs from this structure. This will delete all associated filters. subscriber: The IPerson who will be unsubscribed. If omitted, unsubscribed_by will be used. unsubscribed_by: The IPerson removing the subscription."
      operationId: distro_series-removeBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to unsubscribe. If omitted, the requesting user will be unsubscribed.
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}?ws.op=newMilestone:
    post:
      tags:
        - distro_series
      summary: New milestone
      description: Create a new milestone for this DistroSeries.
      operationId: distro_series-newMilestone
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name Only letters, numbers, and simple punctuation are allowed.
                  example: ubuntu
                date_targeted:
                  type: string
                  description: "Date Targeted Example: 2005-11-24"
                summary:
                  type: string
                  description: Summary A summary of the features and status of this milestone.
                code_name:
                  type: string
                  description: Code name An alternative name for the milestone.
              required:
                - name
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}?ws.op=initDerivedDistroSeries:
    post:
      tags:
        - distro_series
      summary: Init derived distro series
      description: "Initialize this series from parents. This method performs checks and then creates a job to populate the new distroseries. param parents: The list of parent ids this series will derive from. param architectures: The architectures to copy to the derived series. If not specified, all of the architectures are copied. param archindep_archtag: The architecture tag used to build architecture-independent packages. If not specified, one from the parents' will be used. param packagesets: The packagesets to copy to the derived series. If not specified, all of the packagesets are copied. param rebuild: Whether binaries will be copied to the derived series. If it's true, they will not be, and if it's false, they will be. param overlays: A list of booleans indicating, for each parent, if the parent/child relationship should be an overlay. param overlay_pockets: The list of pockets names to use for overlay relationships. param overlay_components: The list of components names to use for overlay relationships."
      operationId: distro_series-initDerivedDistroSeries
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                parents:
                  type: string
                  description: The list of parents to derive from.
                  example: parents
                architectures:
                  type: string
                  description: The list of architectures to copy to the derived distroseries.
                archindep_archtag:
                  type: string
                  description: Architecture tag to build architecture-independent packages.
                packagesets:
                  type: string
                  description: The list of packagesets to copy to the derived distroseries
                rebuild:
                  type: string
                  description: If binaries will be copied to the derived distroseries.
                overlays:
                  type: string
                  description: The list of booleans indicating, for each parent, if the parent/child relationship should be an overlay.
                overlay_pockets:
                  type: string
                  description: The list of overlay pockets.
                overlay_components:
                  type: string
                  description: The list of overlay components.
              required:
                - parents
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
  /{distro_series_1}/{distro_series_2}:
    get:
      tags:
        - distro_series
      summary: Get distro series
      description: A series of an operating system distribution.
      operationId: distro_series-get
      parameters:
        - *a14
        - *a15
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distro_series-full"
    put:
      tags:
        - distro_series
      summary: Replace distro series
      description: A series of an operating system distribution.
      operationId: distro_series-put
      parameters:
        - *a14
        - *a15
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distro_series-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - distro_series
      summary: Update distro series
      description: A series of an operating system distribution.
      operationId: distro_series-patch
      parameters:
        - *a14
        - *a15
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/distro_series-diff"
      responses:
        "200":
          description: Successful response
  /{git_ref_1}/{git_ref_2}/+git/{git_ref_3}/+ref/{git_ref_4}?ws.op=getGrants:
    get:
      tags:
        - git_ref
      summary: Get grants
      description: Get the access grants specific to this reference. Other grants may apply via wildcard rules.
      operationId: git_ref-getGrants
      parameters:
        - &a16
          name: git_ref_1
          in: path
          required: true
          description: Path segment of the git_ref resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ~launchpad
        - &a17
          name: git_ref_2
          in: path
          required: true
          description: Path segment of the git_ref resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: launchpad
        - &a18
          name: git_ref_3
          in: path
          required: true
          description: Path segment of the git_ref resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: launchpad
        - &a19
          name: git_ref_4
          in: path
          required: true
          description: Path segment of the git_ref resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: master
      responses:
        "200":
          description: Successful response
  /{git_ref_1}/{git_ref_2}/+git/{git_ref_3}/+ref/{git_ref_4}?ws.op=checkPermissions:
    get:
      tags:
        - git_ref
      summary: Check permissions
      description: "Check a person's permissions on this reference. param person: An IPerson to check. return: A list of zero or more of \"create\", \"push\", and \"force-push\", indicating the requested person's effective permissions on this reference."
      operationId: git_ref-checkPermissions
      parameters:
        - *a16
        - *a17
        - *a18
        - *a19
        - name: person
          in: query
          required: true
          description: Person to check
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
  /{git_ref_1}/{git_ref_2}/+git/{git_ref_3}/+ref/{git_ref_4}?ws.op=getMergeProposals:
    get:
      tags:
        - git_ref
      summary: Get merge proposals
      description: Return matching BranchMergeProposals.
      operationId: git_ref-getMergeProposals
      parameters:
        - *a16
        - *a17
        - *a18
        - *a19
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
        - name: merged_revision_ids
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{git_ref_1}/{git_ref_2}/+git/{git_ref_3}/+ref/{git_ref_4}?ws.op=setGrants:
    post:
      tags:
        - git_ref
      summary: Set grants
      description: Set the access grants specific to this reference. Other grants may apply via wildcard rules.
      operationId: git_ref-setGrants
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                grants:
                  type: string
                  description: 'Grants The new list of grants for this reference. For example: [ { "grantee_type": "Repository owner", "can_create": true, "can_push": true, "can_force_push": true }, { "grantee_type": "Person", "grantee_link": "/~example-person", "can_push": true } ]'
                  example: grants
              required:
                - grants
      parameters:
        - *a16
        - *a17
        - *a18
        - *a19
      responses:
        "200":
          description: Successful response
  /{git_ref_1}/{git_ref_2}/+git/{git_ref_3}/+ref/{git_ref_4}?ws.op=createMergeProposal:
    post:
      tags:
        - git_ref
      summary: Create merge proposal
      description: Create a new BranchMergeProposal with this reference as the source. Both the merge_target and the merge_prerequisite, if it is there, must be references whose repositories have the same target as the source. References in personal repositories cannot specify merge proposals.
      operationId: git_ref-createMergeProposal
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                merge_target:
                  type: string
                  format: uri
                  x-lp-resource-type: git_ref
                  example: merge_target
                merge_prerequisite:
                  type: string
                  format: uri
                  x-lp-resource-type: git_ref
                needs_review:
                  type: string
                  description: Needs review If True, the proposal needs review. Otherwise, it will be work in progress.
                initial_comment:
                  type: string
                  description: Initial comment Registrant's initial description of proposal.
                commit_message:
                  type: string
                  description: Commit message Message to use when committing this merge.
                reviewers:
                  type: string
                review_types:
                  type: string
              required:
                - merge_target
      parameters:
        - *a16
        - *a17
        - *a18
        - *a19
      responses:
        "200":
          description: Successful response
  /{git_ref_1}/{git_ref_2}/+git/{git_ref_3}/+ref/{git_ref_4}:
    get:
      tags:
        - git_ref
      summary: Get git ref
      description: A reference in a Git repository.
      operationId: git_ref-get
      parameters:
        - *a16
        - *a17
        - *a18
        - *a19
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_ref-full"
    put:
      tags:
        - git_ref
      summary: Replace git ref
      description: A reference in a Git repository.
      operationId: git_ref-put
      parameters:
        - *a16
        - *a17
        - *a18
        - *a19
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/git_ref-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - git_ref
      summary: Update git ref
      description: A reference in a Git repository.
      operationId: git_ref-patch
      parameters:
        - *a16
        - *a17
        - *a18
        - *a19
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/git_ref-diff"
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=getAccessTokens:
    get:
      tags:
        - git_repository
      summary: Get access tokens
      description: "Return personal access tokens for this target. param visible_by_user: If given, return only access tokens visible by this user. param include_expired: If True, include expired access tokens. This must only be used for non-authentication purposes, such as deleting database rows."
      operationId: git_repository-getAccessTokens
      parameters:
        - &a20
          name: git_repository_1
          in: path
          required: true
          description: Path segment of the git_repository resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ~launchpad
        - &a21
          name: git_repository_2
          in: path
          required: true
          description: Path segment of the git_repository resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: launchpad
        - &a22
          name: git_repository_3
          in: path
          required: true
          description: Path segment of the git_repository resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/access_token-page"
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=getRules:
    get:
      tags:
        - git_repository
      summary: Get rules
      description: Get the access rules for this repository.
      operationId: git_repository-getRules
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=checkRefPermissions:
    get:
      tags:
        - git_repository
      summary: Check ref permissions
      description: "Check a person's permissions on some references in this repository. param person: An IPerson to check. param paths: An iterable of reference paths. return: A dict mapping reference paths to lists of zero or more of \"create\", \"push\", and \"force-push\", indicating the requested person's effective permissions on each of the requested references."
      operationId: git_repository-checkRefPermissions
      parameters:
        - *a20
        - *a21
        - *a22
        - name: person
          in: query
          required: true
          description: Person to check
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
        - name: paths
          in: query
          required: true
          description: Reference paths
          schema:
            type: string
          example: paths
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=canBeDeleted:
    get:
      tags:
        - git_repository
      summary: Can be deleted
      description: Can this repository be deleted in its current state? A repository is considered deletable if it is not linked to any merge proposals.
      operationId: git_repository-canBeDeleted
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=isPersonTrustedReviewer:
    get:
      tags:
        - git_repository
      summary: Is person trusted reviewer
      description: Return true if the reviewer is a trusted reviewer. The reviewer is trusted if they either own the repository, or are in the team that owns the repository, or they are in the review team for the repository.
      operationId: git_repository-isPersonTrustedReviewer
      parameters:
        - *a20
        - *a21
        - *a22
        - name: reviewer
          in: query
          required: true
          description: A person for which the reviewer status is in question.
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: reviewer
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=getRefByPath:
    get:
      tags:
        - git_repository
      summary: Get ref by path
      description: "Look up a single reference in this repository by path. param path: A string to look up as a path. return: An IGitRef, or None."
      operationId: git_repository-getRefByPath
      parameters:
        - *a20
        - *a21
        - *a22
        - name: path
          in: query
          required: true
          description: A string to look up as a path.
          schema:
            type: string
          example: path
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_ref-full"
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=getSubscription:
    get:
      tags:
        - git_repository
      summary: Get subscription
      description: Return the GitSubscription for this person.
      operationId: git_repository-getSubscription
      parameters:
        - *a20
        - *a21
        - *a22
        - name: person
          in: query
          required: true
          description: The person to search for
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_subscription-full"
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=getMergeProposals:
    get:
      tags:
        - git_repository
      summary: Get merge proposals
      description: Return matching BranchMergeProposals.
      operationId: git_repository-getMergeProposals
      parameters:
        - *a20
        - *a21
        - *a22
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
        - name: merged_revision_ids
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=getStatusReports:
    get:
      tags:
        - git_repository
      summary: Get status reports
      description: "Retrieves the list of reports that exist for a commit. param commit_sha1: The commit sha1 for the report. Scopes: repository:build_status"
      operationId: git_repository-getStatusReports
      parameters:
        - *a20
        - *a21
        - *a22
        - name: commit_sha1
          in: query
          required: true
          description: The Git commit for which this report is built.
          schema:
            type: string
          example: commit_sha1
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/revision_status_report-page"
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=newWebhook:
    post:
      tags:
        - git_repository
      summary: New webhook
      description: Create a new webhook.
      operationId: git_repository-newWebhook
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                delivery_url:
                  type: string
                  description: Delivery URL
                  example: delivery_url
                active:
                  type: string
                  description: Active Deliver details of subscribed events.
                event_types:
                  type: string
                  description: Event types
                  example: event_types
                secret:
                  type: string
                  description: Secret An optional string used to sign delivery bodies with HMAC-SHA1 in the X-Hub-Signature header. This must be less than 200 bytes long.
              required:
                - delivery_url
                - event_types
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=issueAccessToken:
    post:
      tags:
        - git_repository
      summary: Issue access token
      description: "Issue an access token for this repository. Access tokens can be used to push to this repository over HTTPS. They are only valid for a single repository, and have a short expiry period (currently fixed at one week), so at the moment they are only suitable in some limited situations. By default they are currently implemented as macaroons. If description and scopes are both given, then issue a personal access token instead, either non-expiring or with an expiry time given by date_expires. These may be used in webservice API requests for certain methods on this repository. This interface is experimental, and may be changed or removed without notice. return: If description and scopes are both given, the secret for a new personal access token (Launchpad only records the hash of this secret and not the secret itself, so the caller must be careful to save this; personal access tokens are in development and may not entirely work yet). Otherwise, a serialised macaroon."
      operationId: git_repository-issueAccessToken
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                description:
                  type: string
                  description: A short description of the token.
                scopes:
                  type: string
                  description: A list of scopes to be granted by this token.
                date_expires:
                  type: string
                  description: When the token should expire.
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=repackRepository:
    post:
      tags:
        - git_repository
      summary: Repack repository
      description: Trigger a repack repository operation. Raises Unauthorized if the repack was attempted by a person that is not an admin or a registry expert.
      operationId: git_repository-repackRepository
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=collectGarbage:
    post:
      tags:
        - git_repository
      summary: Collect garbage
      description: Trigger a gc run for a given git repository. Raises Unauthorized if the repack was attempted by a person that is not an admin or a registry expert.
      operationId: git_repository-collectGarbage
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=setRules:
    post:
      tags:
        - git_repository
      summary: Set rules
      description: Set the access rules for this repository.
      operationId: git_repository-setRules
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                rules:
                  type: string
                  description: 'Rules The new list of rules for this repository. For example: [ { "ref_pattern": "refs/heads/*", "grants": [ { "grantee_type": "Repository owner", "can_create": true, "can_push": true, "can_force_push": true } ] }, { "ref_pattern": "refs/heads/stable/*", "grants": [ { "grantee_type": "Person", "grantee_link": "/~example-stable-team", "can_create": true, "can_push": true } ] } ]'
                  example: rules
              required:
                - rules
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=newStatusReport:
    post:
      tags:
        - git_repository
      summary: New status report
      description: "Create a new status report. param title: The name of the new report. param commit_sha1: The commit sha1 for the report. param url: The external link of the status report. param result_summary: The description of the new report. param result: The result of the new report. Scopes: repository:build_status"
      operationId: git_repository-newStatusReport
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                title:
                  type: string
                  description: A short title for the report.
                  example: title
                commit_sha1:
                  type: string
                  description: The Git commit for which this report is built.
                  example: commit_sha1
                url:
                  type: string
                  description: URL The external url of the report.
                result_summary:
                  type: string
                  description: A short summary of the result.
                result:
                  type: string
                  enum:
                    - Waiting
                    - Running
                    - Succeeded
                    - Failed
                    - Skipped
                    - Cancelled
                  description: Result of the report
              required:
                - title
                - commit_sha1
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=rescan:
    post:
      tags:
        - git_repository
      summary: Rescan
      description: Force a rescan of this repository as a celery task. This may be helpful in cases where a previous scan crashed.
      operationId: git_repository-rescan
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=subscribe:
    post:
      tags:
        - git_repository
      summary: Subscribe
      description: "Subscribe this person to the repository. param person: The Person to subscribe. param notification_level: The kinds of repository changes that cause notification. param max_diff_lines: The maximum number of lines of diff that may appear in a notification. param code_review_level: The kinds of code review activity that cause notification. param subscribed_by: The person who is subscribing the subscriber. Most often the subscriber themselves. return: A new or existing GitSubscription."
      operationId: git_repository-subscribe
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The person to subscribe.
                  example: ~launchpad
                notification_level:
                  type: string
                  enum:
                    - No email
                    - Branch attribute notifications only
                    - Branch revision notifications only
                    - Branch attribute and revision notifications
                  description: The level of notification to subscribe to.
                  example: No email
                max_diff_lines:
                  type: string
                  enum:
                    - Don't send diffs
                    - 500 lines
                    - 1000 lines
                    - 5000 lines
                    - Send entire diff
                  description: The max number of lines for diff email.
                  example: Don't send diffs
                code_review_level:
                  type: string
                  enum:
                    - No email
                    - Status changes only
                    - Email about all changes
                  description: The level of code review notification emails.
                  example: No email
              required:
                - person
                - notification_level
                - max_diff_lines
                - code_review_level
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_subscription-full"
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=unsubscribe:
    post:
      tags:
        - git_repository
      summary: Unsubscribe
      description: "Remove the person's subscription to this repository. param person: The person or team to unsubscribe from the repository. param unsubscribed_by: The person doing the unsubscribing."
      operationId: git_repository-unsubscribe
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The person to unsubscribe
                  example: ~launchpad
              required:
                - person
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}?ws.op=fork:
    post:
      tags:
        - git_repository
      summary: Fork
      description: "Fork this repository to the given user's account. param requester: The IPerson performing this fork. param new_owner: The IPerson that will own the forked repository. return: The newly created GitRepository."
      operationId: git_repository-fork
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                new_owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The person who will own the forked repository.
                  example: new_owner
              required:
                - new_owner
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-full"
  /{git_repository_1}/{git_repository_2}/+git/{git_repository_3}:
    get:
      tags:
        - git_repository
      summary: Get git repository
      description: A Git repository.
      operationId: git_repository-get
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/git_repository-full"
    put:
      tags:
        - git_repository
      summary: Replace git repository
      description: A Git repository.
      operationId: git_repository-put
      parameters:
        - *a20
        - *a21
        - *a22
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/git_repository-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - git_repository
      summary: Update git repository
      description: A Git repository.
      operationId: git_repository-patch
      parameters:
        - *a20
        - *a21
        - *a22
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/git_repository-diff"
      responses:
        "200":
          description: Successful response
    delete:
      tags:
        - git_repository
      summary: Delete git repository
      description: A Git repository.
      operationId: git_repository-delete
      parameters:
        - *a20
        - *a21
        - *a22
      responses:
        "200":
          description: Resource deleted.
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=setsIncludedBy:
    get:
      tags:
        - packageset
      summary: Sets included by
      description: "Get all package sets that include this one. Return all package sets that directly or indirectly include this one. param direct_inclusion: if this flag is set to True only sets directly including this one will be considered. return: A (potentially empty) sequence of IPackageset instances."
      operationId: packageset-setsIncludedBy
      parameters:
        - &a23
          name: packageset_1
          in: path
          required: true
          description: Path segment of the packageset resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: package-sets
        - &a24
          name: packageset_2
          in: path
          required: true
          description: Path segment of the packageset resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ubuntu
        - &a25
          name: packageset_3
          in: path
          required: true
          description: Path segment of the packageset resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: noble
        - &a26
          name: packageset_4
          in: path
          required: true
          description: Path segment of the packageset resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: core
        - name: direct_inclusion
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-page"
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=setsIncluded:
    get:
      tags:
        - packageset
      summary: Sets included
      description: "Get all package sets that are included by this one. Return all package sets that are directly or indirectly included by this one. param direct_inclusion: if this flag is set to True only sets directly included by this one will be considered. return: A (potentially empty) sequence of IPackageset instances."
      operationId: packageset-setsIncluded
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
        - name: direct_inclusion
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-page"
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=getSourcesIncluded:
    get:
      tags:
        - packageset
      summary: Get sources included
      description: "Get all source names associated with this package set. This method returns the source package names that are directly or indirectly associated with the package set at hand. Indirect associations may be defined through package set successors. Please note: this method was mainly introduced in order to facilitate the listing of source package names via the LP web services API. It returns string names as opposed to ISourcePackageName instances. param direct_inclusion: if this flag is set to True only sources directly included by this package set will be considered. return: A (potentially empty) sequence of string source package names."
      operationId: packageset-getSourcesIncluded
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
        - name: direct_inclusion
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=getSourcesSharedBy:
    get:
      tags:
        - packageset
      summary: Get sources shared by
      description: "Get source package names also included by another package set. What source package names does this package set have in common with the other_package_set? Please note: this method was mainly introduced in order to facilitate the listing of source package names via the LP web services API. It returns string names as opposed to ISourcePackageName instances. param other_package_set: the other package set param direct_inclusion: if this flag is set to True only directly included sources will be considered. return: A (potentially empty) sequence of string source package names."
      operationId: packageset-getSourcesSharedBy
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
        - name: other_package_set
          in: query
          required: true
          description: The package set we are comparing to.
          schema:
            type: string
            format: uri
            x-lp-resource-type: packageset
          example: other_package_set
        - name: direct_inclusion
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=getSourcesNotSharedBy:
    get:
      tags:
        - packageset
      summary: Get sources not shared by
      description: "Get source package names not included by another package set. Which source package names included by this package are not included by the other_package_set? Please note: this method was mainly introduced in order to facilitate the listing of source package names via the LP web services API. It returns string names as opposed to ISourcePackageName instances. param other_package_set: the other package set param direct_inclusion: if this flag is set to True only directly included sources will be considered. return: A (potentially empty) sequence of string source package names."
      operationId: packageset-getSourcesNotSharedBy
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
        - name: other_package_set
          in: query
          required: true
          description: The package set we are comparing to.
          schema:
            type: string
            format: uri
            x-lp-resource-type: packageset
          example: other_package_set
        - name: direct_inclusion
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=relatedSets:
    get:
      tags:
        - packageset
      summary: Related sets
      description: "Get all other package sets in this set's PackagesetGroup. Returns all package sets that are related to this one, but not this one itself. return: A (potentially empty) sequence of IPackageset instances."
      operationId: packageset-relatedSets
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-page"
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=addSources:
    post:
      tags:
        - packageset
      summary: Add sources
      description: "Add the named source packages to this package set. Any passed source package names will become directly associated with the package set at hand. This function is idempotent in the sense that source package names that are already directly associated with a package set will be ignored. This method facilitates the addition of source package names to package sets via the LP web services API. It takes string names as opposed to ISourcePackageName instances. Source package names that have not previously been seen by Launchpad will be created. param names: an iterable with string source package names raises InvalidName: if any name is not a valid source package name and no SourcePackageName row already exists for it."
      operationId: packageset-addSources
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                names:
                  type: string
                  description: A list of source package names.
                  example: names
              required:
                - names
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      responses:
        "200":
          description: Successful response
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=removeSources:
    post:
      tags:
        - packageset
      summary: Remove sources
      description: "Remove the named source packages from this package set. Only source package names directly included by this package set can be removed. Any others will be ignored. This method facilitates the removal of source package names from package sets via the LP web services API. It takes string names as opposed to ISourcePackageName instances. param names: an iterable with string source package names"
      operationId: packageset-removeSources
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                names:
                  type: string
                  description: A list of source package names.
                  example: names
              required:
                - names
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      responses:
        "200":
          description: Successful response
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=addSubsets:
    post:
      tags:
        - packageset
      summary: Add subsets
      description: "Add the named package sets as subsets to this package set. Any passed source package names will become directly associated with the package set at hand. This function is idempotent in the sense that package subsets that are already directly associated with a package set will be ignored. This method facilitates the addition of package subsets via the LP web services API. It takes string names as opposed to IPackageset instances. param names: an iterable with string package set names"
      operationId: packageset-addSubsets
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                names:
                  type: string
                  description: A list of package set names.
                  example: names
              required:
                - names
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      responses:
        "200":
          description: Successful response
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}?ws.op=removeSubsets:
    post:
      tags:
        - packageset
      summary: Remove subsets
      description: "Remove the named package subsets from this package set. Only package subsets directly included by this package set can be removed. Any others will be ignored. This method facilitates the removal of package subsets via the LP web services API. It takes string names as opposed to IPackageset instances. param names: an iterable with string package set names"
      operationId: packageset-removeSubsets
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                names:
                  type: string
                  description: A list of package set names.
                  example: names
              required:
                - names
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      responses:
        "200":
          description: Successful response
  /{packageset_1}/{packageset_2}/{packageset_3}/{packageset_4}:
    get:
      tags:
        - packageset
      summary: Get packageset
      description: An interface for package sets.
      operationId: packageset-get
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/packageset-full"
    put:
      tags:
        - packageset
      summary: Replace packageset
      description: An interface for package sets.
      operationId: packageset-put
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/packageset-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - packageset
      summary: Update packageset
      description: An interface for package sets.
      operationId: packageset-patch
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/packageset-diff"
      responses:
        "200":
          description: Successful response
    delete:
      tags:
        - packageset
      summary: Delete packageset
      description: An interface for package sets.
      operationId: packageset-delete
      parameters:
        - *a23
        - *a24
        - *a25
        - *a26
      responses:
        "200":
          description: Resource deleted.
  /people/+me?ws.op=getTranslationImportQueueEntries:
    get:
      tags:
        - person
      summary: Get translation import queue entries
      description: "Return entries in the translation import queue for this entity. arg import_status: RosettaImportStatus DB Schema entry. arg file_extension: String with the file type extension, usually 'po' or 'pot'. If one of both of 'import_status' or 'file_extension' are given, the returned entries are filtered based on those values."
      operationId: person-getTranslationImportQueueEntries
      parameters:
        - name: import_status
          in: query
          required: false
          description: Status Show only entries with this status
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
        - name: file_extension
          in: query
          required: false
          description: Filename extension Show only entries with this filename suffix
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /people/+me?ws.op=searchTasks:
    get:
      tags:
        - person
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: person-searchTasks
      parameters:
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /people/+me?ws.op=getRequestedReviews:
    get:
      tags:
        - person
      summary: Get requested reviews
      description: 'Returns merge proposals where a person was asked to review. This does not include merge proposals that were requested from teams that the person is part of. If status is not passed then it will return proposals that are in the "Needs Review" state. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. returns: A list of IBranchMergeProposal.'
      operationId: person-getRequestedReviews
      parameters:
        - name: status
          in: query
          required: false
          description: A list of merge proposal statuses to filter by.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /people/+me?ws.op=getMergeProposals:
    get:
      tags:
        - person
      summary: Get merge proposals
      description: Return matching BranchMergeProposals.
      operationId: person-getMergeProposals
      parameters:
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for merge proposals that were createdbefore the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for merge proposals that were createdsince the given date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /people/+me?ws.op=getBranches:
    get:
      tags:
        - person
      summary: Get branches
      description: "Returns all branches with the given lifecycle status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param modified_since: If set, filters the branches being returned to those that have been modified since the specified date/time. param eager_load: If True load related objects for the whole collection. returns: A list of IBranch."
      operationId: person-getBranches
      parameters:
        - name: status
          in: query
          required: false
          description: A list of branch lifecycle statuses to filter by.
          schema:
            type: string
        - name: modified_since
          in: query
          required: false
          description: Limit the branches to those modified since this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /people/+me?ws.op=getSocialAccountsByPlatform:
    get:
      tags:
        - person
      summary: Get social accounts by platform
      description: Return Social Accounts associated to the user.
      operationId: person-getSocialAccountsByPlatform
      parameters:
        - name: platform
          in: query
          required: true
          description: Social Platform Type
          schema:
            type: string
            enum:
              - Matrix platform
              - GitHub platform
          example: Matrix platform
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=getRecipe:
    get:
      tags:
        - person
      summary: Get recipe
      description: Return the person's recipe with the given name.
      operationId: person-getRecipe
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/source_package_recipe-full"
  /people/+me?ws.op=getArchiveSubscriptionURLs:
    get:
      tags:
        - person
      summary: Get archive subscription urls
      description: Return private archive URLs that this person can see. For each of the private archives (PPAs) that this person can see, return a URL that includes the HTTP basic auth data. The URL returned is suitable for including in a sources.list file.
      operationId: person-getArchiveSubscriptionURLs
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=getBugSubscriberPackages:
    get:
      tags:
        - person
      summary: Get bug subscriber packages
      description: Return the packages for which this person is a bug subscriber. Returns a list of IDistributionSourcePackage's, ordered alphabetically (A to Z) by name.
      operationId: person-getBugSubscriberPackages
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_source_package-page"
  /people/+me?ws.op=getMembersByStatus:
    get:
      tags:
        - person
      summary: Get members by status
      description: Return the people whose membership on this team match :status:. If no orderby is provided, Person.sortingColumns is used.
      operationId: person-getMembersByStatus
      parameters:
        - name: status
          in: query
          required: true
          description: The state of this membership
          schema:
            type: string
            enum:
              - Proposed
              - Approved
              - Administrator
              - Deactivated
              - Expired
              - Declined
              - Invited
              - Invitation declined
          example: Proposed
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /people/+me?ws.op=getPPAByName:
    get:
      tags:
        - person
      summary: Get ppaby name
      description: "Return a PPA with the given name if it exists. param name: A string with the exact name of the ppa being looked up. raises: NoSuchPPA if a suitable PPA could not be found. return: a PPA IArchive record corresponding to the name."
      operationId: person-getPPAByName
      parameters:
        - name: distribution
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distribution
        - name: name
          in: query
          required: true
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive-full"
  /people/+me?ws.op=searchQuestions:
    get:
      tags:
        - person
      summary: Search questions
      description: "Search the person's questions. param search_text: A string that is matched against the question title and description. If None, the search_text is not included as a filter criteria. param status: A sequence of QuestionStatus Items. If None or an empty sequence, the status is not included as a filter criteria. The default is to match all status except Expired and Invalid. param language: An ILanguage or a sequence of ILanguage objects to match against the question's language. If None or an empty sequence, the language is not included as a filter criteria. param participation: A list of QuestionParticipation that defines the set of relationship to questions that will be searched. If None or an empty sequence, all relationships are considered. param needs_attention: If this flag is true, only questions that need attention the person will be included. These are the questions in the NEEDSINFO or ANSWERED state owned by the person. The questions not owned by the person but on which the person requested more information or gave an answer and that are back in the OPEN state are also included. param sort: An attribute of QuestionSort. If None, a default value is used. When there is a search_text value, the default is to sort by RELEVANCY, otherwise results are sorted NEWEST_FIRST. param created_since: Only return results whose datecreated property is greater than or equal to this date. param created_before: Only return results whose datecreated property is smaller than this date."
      operationId: person-searchQuestions
      parameters:
        - name: search_text
          in: query
          required: false
          description: Search text
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status
          schema:
            type: string
        - name: language
          in: query
          required: false
          description: Language
          schema:
            type: string
        - name: participation
          in: query
          required: false
          description: Participation
          schema:
            type: string
            enum:
              - Owner
              - Subscriber
              - Assignee
              - Commenter
              - Answerer
        - name: needs_attention
          in: query
          required: false
          description: Needs attentions from
          schema:
            type: string
        - name: sort
          in: query
          required: false
          description: Sort
          schema:
            type: string
            enum:
              - by relevancy
              - by status
              - newest first
              - oldest first
              - recently updated first
        - name: created_before
          in: query
          required: false
          description: Search for questions that were created before the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for questions that were created since the given date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /people/+me?ws.op=getDirectAnswerQuestionTargets:
    get:
      tags:
        - person
      summary: Get direct answer question targets
      description: Return a list of IQuestionTargets that a person is subscribed to. This will return IQuestionTargets that the person is registered as an answer contact because they subscribed themselves.
      operationId: person-getDirectAnswerQuestionTargets
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question_target-page"
  /people/+me?ws.op=getTeamAnswerQuestionTargets:
    get:
      tags:
        - person
      summary: Get team answer question targets
      description: Return a list of IQuestionTargets that are indirect subscriptions. This will return IQuestionTargets that the person or team is registered as an answer contact because of their membership in a team.
      operationId: person-getTeamAnswerQuestionTargets
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question_target-page"
  /people/+me?ws.op=getArchiveSubscriptions:
    get:
      tags:
        - person
      summary: Get archive subscriptions
      description: Return (private) archives subscription for this person.
      operationId: person-getArchiveSubscriptions
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_subscriber-page"
  /people/+me?ws.op=getOwnedProjects:
    get:
      tags:
        - person
      summary: Get owned projects
      description: "Projects owned by this person or teams to which they belong. param match_name: string optional project name to screen the results."
      operationId: person-getOwnedProjects
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project-page"
  /people/+me?ws.op=getOwnedTeams:
    get:
      tags:
        - person
      summary: Get owned teams
      description: Return the teams that this person owns. The iterator includes the teams that the user owns, but it not a member of.
      operationId: person-getOwnedTeams
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/team-page"
  /people/+me?ws.op=setLocation:
    post:
      tags:
        - person
      summary: Set location
      description: Specify the location and time zone of a person.
      operationId: person-setLocation
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                latitude:
                  type: string
                  description: The latitude of this object.
                  example: latitude
                longitude:
                  type: string
                  description: The longitude of this object.
                  example: longitude
                time_zone:
                  type: string
                  description: The time zone of this object.
                  example: time_zone
              required:
                - latitude
                - longitude
                - time_zone
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=join:
    post:
      tags:
        - person
      summary: Join
      description: "Join the given team if its membership_policy is not RESTRICTED. Join the given team according to the policies and defaults of that team: If the team membership_policy is OPEN, the user is added as an APPROVED member with a NULL TeamMembership.reviewer. If the team membership_policy is MODERATED, the user is added as a PROPOSED member and one of the team's administrators have to approve the membership. If may_subscribe_to_list is True, then also attempt to subscribe to the team's mailing list, depending on the list status and the person's auto-subscribe settings. param requester: The person who requested the membership on behalf of a team or None when a person requests the membership for themselves. param may_subscribe_to_list: If True, also try subscribing to the team mailing list."
      operationId: person-join
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
              required:
                - team
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=leave:
    post:
      tags:
        - person
      summary: Leave
      description: "Leave the given team. This is a convenience method for retractTeamMembership() that allows a user to leave the given team, or to cancel a PENDING membership request. param team: The team to leave."
      operationId: person-leave
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
              required:
                - team
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=addMember:
    post:
      tags:
        - person
      summary: Add member
      description: "Add the given person as a member of this team. param person: If the given person is already a member of this team we'll simply change its membership status. Otherwise a new TeamMembership is created with the given status. param reviewer: The user who made the given person a member of this team. param comment: String that will be assigned to the proponent_comment, reviewer_comment, or acknowledger comment. param status: TeamMembershipStatus value must be either Approved, Proposed or Admin. If the new member is a team, the status will be changed to Invited unless the user is also an admin of that team. param force_team_add: If the person is actually a team and force_team_add is False, the team will actually be invited to join this one. Otherwise the team is added as if it were a person. param may_subscribe_to_list: If the person is not a team, and may_subscribe_to_list is True, then the person may be subscribed to the team's mailing list, depending on the list status and the person's auto-subscribe settings. return: A tuple containing a boolean indicating when the membership status changed and the current TeamMembershipStatus. This depends on the desired status passed as an argument, the membership policy and the user's privileges."
      operationId: person-addMember
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Member
                  example: ~launchpad
                status:
                  type: string
                  enum:
                    - Proposed
                    - Approved
                    - Administrator
                    - Deactivated
                    - Expired
                    - Declined
                    - Invited
                    - Invitation declined
                  description: The state of this membership
                comment:
                  type: string
              required:
                - person
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=acceptInvitationToBeMemberOf:
    post:
      tags:
        - person
      summary: Accept invitation to be member of
      description: Accept an invitation to become a member of the given team. There must be a TeamMembership for this person and the given team with the INVITED status. The status of this TeamMembership will be changed to APPROVED.
      operationId: person-acceptInvitationToBeMemberOf
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
                comment:
                  type: string
                  example: comment
              required:
                - team
                - comment
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=declineInvitationToBeMemberOf:
    post:
      tags:
        - person
      summary: Decline invitation to be member of
      description: Decline an invitation to become a member of the given team. There must be a TeamMembership for this person and the given team with the INVITED status. The status of this TeamMembership will be changed to INVITATION_DECLINED.
      operationId: person-declineInvitationToBeMemberOf
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
                comment:
                  type: string
                  example: comment
              required:
                - team
                - comment
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=retractTeamMembership:
    post:
      tags:
        - person
      summary: Retract team membership
      description: "Retract this team's membership in the given team. If there's a membership entry for this team on the given team and its status is either APPROVED, ADMIN, PENDING, or INVITED, the status is changed and the relevant entries in TeamParticipation. APPROVED and ADMIN status are changed to DEACTIVATED. PENDING status is changed to DECLINED. INVITED status is changes to INVITATION_DECLINED. param team: The team to leave. param user: The user making the retraction. param comment: An optional explanation about why the change was made."
      operationId: person-retractTeamMembership
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
                comment:
                  type: string
              required:
                - team
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=createPPA:
    post:
      tags:
        - person
      summary: Create ppa
      description: "Create a PPA. param distribution: The distribution that this archive is for. param name: The name of the new PPA to create. param displayname: The displayname for the new PPA. param description: The description for the new PPA. param private: Whether or not to create a private PPA. Defaults to False, which means the PPA will be public. param suppress_subscription_notifications: Whether or not to suppress emails to new subscribers about their subscriptions. Only meaningful for private PPAs. raises: PPACreationError if an error is encountered return: a PPA IArchive record."
      operationId: person-createPPA
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                distribution:
                  type: string
                  format: uri
                  x-lp-resource-type: distribution
                name:
                  type: string
                displayname:
                  type: string
                description:
                  type: string
                private:
                  type: string
                suppress_subscription_notifications:
                  type: string
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=createRecipe:
    post:
      tags:
        - person
      summary: Create recipe
      description: "Create a SourcePackageRecipe owned by this person. param name: the name to use for referring to the recipe. param description: A description of the recipe. param recipe_text: The text of the recipe. param distroseries: The distroseries to use. param registrant: The person who created this recipe. param daily_build_archive: The archive to use for daily builds. param build_daily: If True, build this recipe daily (if changed). return: a SourcePackageRecipe."
      operationId: person-createRecipe
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                description:
                  type: string
                  example: description
                distroseries:
                  type: string
                  example: noble
                name:
                  type: string
                  example: ubuntu
                recipe_text:
                  type: string
                  example: recipe_text
                daily_build_archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                build_daily:
                  type: string
              required:
                - description
                - distroseries
                - name
                - recipe_text
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=getArchiveSubscriptionURL:
    post:
      tags:
        - person
      summary: Get archive subscription url
      description: Get a text line that is suitable to be used for a sources.list entry. It will create a new IArchiveAuthToken if one doesn't already exist. It raises Unauthorized if the context user does not have a valid subscription for the target archive or the caller is not context user itself.
      operationId: person-getArchiveSubscriptionURL
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: archive
              required:
                - archive
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=setAccountStatus:
    post:
      tags:
        - person
      summary: Set account status
      description: Set the status of this person's account.
      operationId: person-setAccountStatus
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - Placeholder
                    - Unactivated
                    - Active
                    - Deactivated
                    - Suspended
                    - Closed
                    - Deceased
                  description: The status of this account
                  example: Placeholder
                comment:
                  type: string
                  description: Status change comment
                  example: comment
              required:
                - status
                - comment
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=addLanguage:
    post:
      tags:
        - person
      summary: Add language
      description: "Add a language to this person's preferences. param language: An object providing ILanguage. If the given language is one of the user's preferred languages already, nothing will happen."
      operationId: person-addLanguage
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                language:
                  type: string
                  format: uri
                  x-lp-resource-type: language
                  example: language
              required:
                - language
      responses:
        "200":
          description: Successful response
  /people/+me?ws.op=removeLanguage:
    post:
      tags:
        - person
      summary: Remove language
      description: "Remove a language from this person's preferences. param language: An object providing ILanguage. If the given language is not present, nothing will happen."
      operationId: person-removeLanguage
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                language:
                  type: string
                  format: uri
                  x-lp-resource-type: language
                  example: language
              required:
                - language
      responses:
        "200":
          description: Successful response
  /people/+me:
    get:
      tags:
        - person
      summary: Get person
      description: A Person.
      operationId: person-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-full"
    put:
      tags:
        - person
      summary: Replace person
      description: A Person.
      operationId: person-put
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/person-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - person
      summary: Update person
      description: A Person.
      operationId: person-patch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/person-diff"
      responses:
        "200":
          description: Successful response
  /pillars?ws.op=search:
    get:
      tags:
        - pillars
      summary: Search
      description: "Return Projects/Project groups/Distros matching :text:. If :limit: is None, the default batch size will be used. The results are ordered descending by rank."
      operationId: pillars-search
      parameters:
        - name: text
          in: query
          required: true
          description: Search text
          schema:
            type: string
          example: ubuntu
        - name: limit
          in: query
          required: false
          description: "Maximum number of items to return. This is a hard limit: any pagination you request will happen within this limit."
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/pillar-page"
  /pillars:
    get:
      tags:
        - pillars
      summary: Get pillars
      description: An object for searching across projects, project groups, and distros. Projects, project groups, and distributions are collectively known as "pillars". This object lets you do a combined search across all types of pillars. It also gives you access to pillars that have been flagged by administrators as "featured" pillars.
      operationId: pillars-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/pillars-full"
    put:
      tags:
        - pillars
      summary: Replace pillars
      description: An object for searching across projects, project groups, and distros. Projects, project groups, and distributions are collectively known as "pillars". This object lets you do a combined search across all types of pillars. It also gives you access to pillars that have been flagged by administrators as "featured" pillars.
      operationId: pillars-put
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/pillars-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - pillars
      summary: Update pillars
      description: An object for searching across projects, project groups, and distros. Projects, project groups, and distributions are collectively known as "pillars". This object lets you do a combined search across all types of pillars. It also gives you access to pillars that have been flagged by administrators as "featured" pillars.
      operationId: pillars-patch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/pillars-diff"
      responses:
        "200":
          description: Successful response
  /+processors/{processor}:
    get:
      tags:
        - processor
      summary: Get processor
      description: The Storm Processor Interface
      operationId: processor-get
      parameters:
        - &a27
          name: processor
          in: path
          required: true
          description: Path segment of the processor resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: amd64
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/processor-full"
    put:
      tags:
        - processor
      summary: Replace processor
      description: The Storm Processor Interface
      operationId: processor-put
      parameters:
        - *a27
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/processor-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - processor
      summary: Update processor
      description: The Storm Processor Interface
      operationId: processor-patch
      parameters:
        - *a27
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/processor-diff"
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=getSubscriptions:
    get:
      tags:
        - project
      summary: Get subscriptions
      description: "Return all the subscriptions with the specified levels. return: A sequence of IStructuralSubscription."
      operationId: project-getSubscriptions
      parameters:
        - &a28
          name: project
          in: path
          required: true
          description: Path segment of the project resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-page"
  /{project}?ws.op=getSubscription:
    get:
      tags:
        - project
      summary: Get subscription
      description: Return the subscription for person, if it exists.
      operationId: project-getSubscription
      parameters:
        - *a28
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-full"
  /{project}?ws.op=userHasBugSubscriptions:
    get:
      tags:
        - project
      summary: User has bug subscriptions
      description: Is user subscribed, directly or via a team, to bug mail?
      operationId: project-userHasBugSubscriptions
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=getSpecification:
    get:
      tags:
        - project
      summary: Get specification
      description: Returns the specification with the given name, for this target, or None.
      operationId: project-getSpecification
      parameters:
        - *a28
        - name: name
          in: query
          required: true
          description: The name of the specification
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/specification-full"
  /{project}?ws.op=searchQuestions:
    get:
      tags:
        - project
      summary: Search questions
      description: "Return the questions from the collection matching search criteria. param search_text: A string that is matched against the question title and description. If None, the search_text is not included as a filter criteria. param status: A sequence of QuestionStatus Items. If None or an empty sequence, the status is not included as a filter criteria. The default is to match all status except Expired and Invalid. param language: An ILanguage or a sequence of ILanguage objects to match against the question's language. If None or an empty sequence, the language is not included as a filter criteria. param owner: The IPerson that created the question. param needs_attention_from: Selects questions that need attention from an IPerson. These are the questions in the NEEDSINFO or ANSWERED state owned by the person. The questions not owned by the person but on which the person requested more information or gave an answer and that are back in the OPEN state are also included. param sort: An attribute of QuestionSort. If None, a default value is used. When there is a search_text value, the default is to sort by RELEVANCY, otherwise results are sorted NEWEST_FIRST. param created_since: Only return results whose datecreated property is greater than or equal to this date. param created_before: Only return results whose datecreated property is smaller than this date."
      operationId: project-searchQuestions
      parameters:
        - *a28
        - name: search_text
          in: query
          required: false
          description: Search text
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status
          schema:
            type: string
        - name: language
          in: query
          required: false
          description: Language
          schema:
            type: string
        - name: owner
          in: query
          required: false
          description: Owner
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: needs_attention_from
          in: query
          required: false
          description: Needs attentions from
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: sort
          in: query
          required: false
          description: Sort
          schema:
            type: string
            enum:
              - by relevancy
              - by status
              - newest first
              - oldest first
              - recently updated first
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /{project}?ws.op=getQuestion:
    get:
      tags:
        - project
      summary: Get question
      description: "Return the question by its id, if it is applicable to this target. question_id: A question id. If there is no such question number for this target, return None"
      operationId: project-getQuestion
      parameters:
        - *a28
        - name: question_id
          in: query
          required: true
          description: Question Number
          schema:
            type: string
          example: "1"
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=findSimilarQuestions:
    get:
      tags:
        - project
      summary: Find similar questions
      description: "Return questions similar to phrase. Return a list of question similar to the provided phrase. These questions will be found using a fuzzy search. The list is ordered from the most similar question to the least similar question. param phrase: A phrase such as the summary of a question."
      operationId: project-findSimilarQuestions
      parameters:
        - *a28
        - name: phrase
          in: query
          required: true
          description: A phrase
          schema:
            type: string
          example: phrase
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /{project}?ws.op=getAnswerContactsForLanguage:
    get:
      tags:
        - project
      summary: Get answer contacts for language
      description: Return the list of Persons that provide support for a language. An answer contact supports questions in their preferred languages.
      operationId: project-getAnswerContactsForLanguage
      parameters:
        - *a28
        - name: language
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: language
          example: language
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /{project}?ws.op=getSupportedLanguages:
    get:
      tags:
        - project
      summary: Get supported languages
      description: Return a list of languages spoken by at the answer contacts. An answer contact is considered to speak a given language if that language is listed as one of their preferred languages.
      operationId: project-getSupportedLanguages
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/language-page"
  /{project}?ws.op=canUserAlterAnswerContact:
    get:
      tags:
        - project
      summary: Can user alter answer contact
      description: "Can the user add or remove the answer contact. Users can add or remove themselves or one of the teams they administered. Admins and target owners can add/remove anyone. param person: The IPerson that is or will be an answer contact. param subscribed_by: The IPerson making the change."
      operationId: project-canUserAlterAnswerContact
      parameters:
        - *a28
        - name: person
          in: query
          required: true
          description: The user or an administered team
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=getTranslationImportQueueEntries:
    get:
      tags:
        - project
      summary: Get translation import queue entries
      description: "Return entries in the translation import queue for this entity. arg import_status: RosettaImportStatus DB Schema entry. arg file_extension: String with the file type extension, usually 'po' or 'pot'. If one of both of 'import_status' or 'file_extension' are given, the returned entries are filtered based on those values."
      operationId: project-getTranslationImportQueueEntries
      parameters:
        - *a28
        - name: import_status
          in: query
          required: false
          description: Status Show only entries with this status
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
        - name: file_extension
          in: query
          required: false
          description: Filename extension Show only entries with this filename suffix
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /{project}?ws.op=searchTasks:
    get:
      tags:
        - project
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: project-searchTasks
      parameters:
        - *a28
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{project}?ws.op=getMergeProposals:
    get:
      tags:
        - project
      summary: Get merge proposals
      description: "Returns all merge proposals of a given status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param eager_load: If True, load related objects for the whole collection. returns: A list of IBranchMergeProposal."
      operationId: project-getMergeProposals
      parameters:
        - *a28
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{project}?ws.op=getBranches:
    get:
      tags:
        - project
      summary: Get branches
      description: "Returns all branches with the given lifecycle status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param modified_since: If set, filters the branches being returned to those that have been modified since the specified date/time. param eager_load: If True load related objects for the whole collection. returns: A list of IBranch."
      operationId: project-getBranches
      parameters:
        - *a28
        - name: status
          in: query
          required: false
          description: A list of branch lifecycle statuses to filter by.
          schema:
            type: string
        - name: modified_since
          in: query
          required: false
          description: Limit the branches to those modified since this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /{project}?ws.op=getMilestone:
    get:
      tags:
        - project
      summary: Get milestone
      description: Return a milestone with the given name for this object, or None.
      operationId: project-getMilestone
      parameters:
        - *a28
        - name: name
          in: query
          required: true
          description: Name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/milestone-full"
  /{project}?ws.op=getSeries:
    get:
      tags:
        - project
      summary: Get series
      description: Return the series for this product for the given name, or None.
      operationId: project-getSeries
      parameters:
        - *a28
        - name: name
          in: query
          required: true
          description: Name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project_series-full"
  /{project}?ws.op=getRelease:
    get:
      tags:
        - project
      summary: Get release
      description: Return the release for this product that has the version given.
      operationId: project-getRelease
      parameters:
        - *a28
        - name: version
          in: query
          required: true
          description: Version
          schema:
            type: string
          example: version
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project_release-full"
  /{project}?ws.op=get_timeline:
    get:
      tags:
        - project
      summary: Get timeline
      description: Return basic timeline data useful for creating a diagram. The number of milestones returned per series is limited.
      operationId: project-get_timeline
      parameters:
        - *a28
        - name: include_inactive
          in: query
          required: false
          description: Include inactive
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=findReferencedOOPS:
    get:
      tags:
        - project
      summary: Find referenced oops
      description: "Find OOPS reports between start_date and end_date. param start_date: Do not look in objects whose last modification time is before this date. param end_date: Do not look in objects whose last modification time is after this date. return: A set of OOPS id's - strings of the form 'OOPS-w+'."
      operationId: project-findReferencedOOPS
      parameters:
        - *a28
        - name: start_date
          in: query
          required: true
          description: Modified after date
          schema:
            type: string
          example: start_date
        - name: end_date
          in: query
          required: true
          description: Modified before date
          schema:
            type: string
          example: end_date
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=getAccessTokens:
    get:
      tags:
        - project
      summary: Get access tokens
      description: "Return personal access tokens for this target. param visible_by_user: If given, return only access tokens visible by this user. param include_expired: If True, include expired access tokens. This must only be used for non-authentication purposes, such as deleting database rows."
      operationId: project-getAccessTokens
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/access_token-page"
  /{project}?ws.op=addBugSubscription:
    post:
      tags:
        - project
      summary: Add bug subscription
      description: "Add a bug subscription for this structure. This method is used to create a new IStructuralSubscription for the target. This initially has a single filter which will allow all notifications will be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription."
      operationId: project-addBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=addBugSubscriptionFilter:
    post:
      tags:
        - project
      summary: Add bug subscription filter
      description: "Add a bug subscription filter for this structure. This method is used to create a new IBugSubscriptionFilter for the target. It will initially allow all notifications to be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription filter."
      operationId: project-addBugSubscriptionFilter
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=removeBugSubscription:
    post:
      tags:
        - project
      summary: Remove bug subscription
      description: "Remove a subscription to bugs from this structure. This will delete all associated filters. subscriber: The IPerson who will be unsubscribed. If omitted, unsubscribed_by will be used. unsubscribed_by: The IPerson removing the subscription."
      operationId: project-removeBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to unsubscribe. If omitted, the requesting user will be unsubscribed.
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=addAnswerContact:
    post:
      tags:
        - project
      summary: Add answer contact
      description: "Add a new answer contact. param person: An IPerson. param subscribed_by: The user making the change. return: True if the person was added, False if the person already is an answer contact. raises AddAnswerContactError: When the person or team does no have a preferred language."
      operationId: project-addAnswerContact
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The user of an administered team
                  example: ~launchpad
              required:
                - person
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=removeAnswerContact:
    post:
      tags:
        - project
      summary: Remove answer contact
      description: "Remove an answer contact. param person: An IPerson. param subscribed_by: The user making the change. return: True if the person was removed, False if the person wasn't an answer contact."
      operationId: project-removeAnswerContact
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: The user of an administered team
                  example: ~launchpad
              required:
                - person
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=issueAccessToken:
    post:
      tags:
        - project
      summary: Issue access token
      description: "Issue a personal access token for this target. Access tokens can be used to push to repositories over HTTPS. These may be used in webservice API requests for certain methods in the target's repositories. They are either non-expiring or with an expiry time given by date_expires. return: The secret for a new personal access token (Launchpad only records the hash of this secret and not the secret itself, so the caller must be careful to save this)."
      operationId: project-issueAccessToken
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                description:
                  type: string
                  description: A short description of the token.
                  example: description
                scopes:
                  type: string
                  description: A list of scopes to be granted by this token.
                  example: scopes
                date_expires:
                  type: string
                  description: When the token should expire.
              required:
                - description
                - scopes
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=newWebhook:
    post:
      tags:
        - project
      summary: New webhook
      description: Create a new webhook.
      operationId: project-newWebhook
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                delivery_url:
                  type: string
                  description: Delivery URL
                  example: delivery_url
                active:
                  type: string
                  description: Active Deliver details of subscribed events.
                event_types:
                  type: string
                  description: Event types
                  example: event_types
                secret:
                  type: string
                  description: Secret An optional string used to sign delivery bodies with HMAC-SHA1 in the X-Hub-Signature header. This must be less than 200 bytes long.
              required:
                - delivery_url
                - event_types
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=newCodeImport:
    post:
      tags:
        - project
      summary: New code import
      description: "Create a new code import. param registrant: The IPerson to record as the registrant of the import. param branch_name: The name of the branch or repository to create. param rcs_type: The type of the foreign VCS. param target_rcs_type: The type of the branch or repository to create (Bazaar or Git). param url: The URL to import from if the VCS type uses a single URL (i.e. isn't CVS). param cvs_root: The CVSROOT for a CVS import. param cvs_module: The module to import for a CVS import. param owner: Who should own the created branch or repository, or None for it to be the same as the registrant, or the caller over the API. returns: An instance of ICodeImport."
      operationId: project-newCodeImport
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                branch_name:
                  type: string
                  description: Name of branch to create
                rcs_type:
                  type: string
                  enum:
                    - Concurrent Versions System
                    - Subversion via CSCVS
                    - Subversion via bzr-svn
                    - Git
                    - Mercurial
                    - Bazaar
                target_rcs_type:
                  type: string
                  enum:
                    - Bazaar
                    - Git
                url:
                  type: string
                  description: Foreign VCS URL
                cvs_root:
                  type: string
                  description: CVS root URL
                cvs_module:
                  type: string
                  description: CVS module to import
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner of the resulting branch
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=newSeries:
    post:
      tags:
        - project
      summary: New series
      description: "Creates a new IProductSeries for this IProduct. param owner: The registrant of this series. param name: The unique name of this series. param summary: The summary of the purpose and focus of development of this series. param branch: The bazaar branch that contains the code for this series. param releasefileglob: The public URL pattern where release files can be automatically downloaded from and linked to this series."
      operationId: project-newSeries
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name The name of the series is a short, unique name that identifies it, being used in URLs. It must be all lowercase, with no special characters. For example, '2.0' or 'trunk'.
                  example: ubuntu
                summary:
                  type: string
                  description: 'Summary A single paragraph that explains the goals of of this series and the intended users. For example: "The 2.0 series of Apache represents the current stable series, and is recommended for all new deployments".'
                  example: summary
                branch:
                  type: string
                  format: uri
                  x-lp-resource-type: branch
                  description: Branch The Bazaar branch for this series. Leave blank if this series is not maintained in Bazaar.
                release_url_pattern:
                  type: string
                  description: "Release URL pattern A URL pattern that matches releases that are part of this series. Launchpad automatically scans this site to import new releases. Example: http://ftp.gnu.org/gnu/emacs/emacs-21.*.tar.gz"
              required:
                - name
                - summary
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=addOfficialBugTag:
    post:
      tags:
        - project
      summary: Add official bug tag
      description: Add tag to the official bug tags of this target.
      operationId: project-addOfficialBugTag
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                tag:
                  type: string
                  description: The official bug tag
                  example: tag
              required:
                - tag
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}?ws.op=removeOfficialBugTag:
    post:
      tags:
        - project
      summary: Remove official bug tag
      description: Remove tag from the official bug tags of this target.
      operationId: project-removeOfficialBugTag
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                tag:
                  type: string
                  description: The official bug tag
                  example: tag
              required:
                - tag
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
  /{project}:
    get:
      tags:
        - project
      summary: Get project
      description: A Product. The Launchpad Registry describes the open source world as ProjectGroups and Products. Each ProjectGroup may be responsible for several Products. For example, the Mozilla Project has Firefox, Thunderbird and The Mozilla App Suite as Products, among others.
      operationId: project-get
      parameters:
        - *a28
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project-full"
    put:
      tags:
        - project
      summary: Replace project
      description: A Product. The Launchpad Registry describes the open source world as ProjectGroups and Products. Each ProjectGroup may be responsible for several Products. For example, the Mozilla Project has Firefox, Thunderbird and The Mozilla App Suite as Products, among others.
      operationId: project-put
      parameters:
        - *a28
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/project-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - project
      summary: Update project
      description: A Product. The Launchpad Registry describes the open source world as ProjectGroups and Products. Each ProjectGroup may be responsible for several Products. For example, the Mozilla Project has Firefox, Thunderbird and The Mozilla App Suite as Products, among others.
      operationId: project-patch
      parameters:
        - *a28
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/project-diff"
      responses:
        "200":
          description: Successful response
  /{project_series_1}/{project_series_2}?ws.op=searchTasks:
    get:
      tags:
        - project_series
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: project_series-searchTasks
      parameters:
        - &a29
          name: project_series_1
          in: path
          required: true
          description: Path segment of the project_series resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: launchpad
        - &a30
          name: project_series_2
          in: path
          required: true
          description: Path segment of the project_series resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: trunk
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{project_series_1}/{project_series_2}?ws.op=getSubscriptions:
    get:
      tags:
        - project_series
      summary: Get subscriptions
      description: "Return all the subscriptions with the specified levels. return: A sequence of IStructuralSubscription."
      operationId: project_series-getSubscriptions
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-page"
  /{project_series_1}/{project_series_2}?ws.op=getSubscription:
    get:
      tags:
        - project_series
      summary: Get subscription
      description: Return the subscription for person, if it exists.
      operationId: project_series-getSubscription
      parameters:
        - *a29
        - *a30
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-full"
  /{project_series_1}/{project_series_2}?ws.op=userHasBugSubscriptions:
    get:
      tags:
        - project_series
      summary: User has bug subscriptions
      description: Is user subscribed, directly or via a team, to bug mail?
      operationId: project_series-userHasBugSubscriptions
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
  /{project_series_1}/{project_series_2}?ws.op=getTranslationImportQueueEntries:
    get:
      tags:
        - project_series
      summary: Get translation import queue entries
      description: "Return entries in the translation import queue for this entity. arg import_status: RosettaImportStatus DB Schema entry. arg file_extension: String with the file type extension, usually 'po' or 'pot'. If one of both of 'import_status' or 'file_extension' are given, the returned entries are filtered based on those values."
      operationId: project_series-getTranslationImportQueueEntries
      parameters:
        - *a29
        - *a30
        - name: import_status
          in: query
          required: false
          description: Status Show only entries with this status
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
        - name: file_extension
          in: query
          required: false
          description: Filename extension Show only entries with this filename suffix
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /{project_series_1}/{project_series_2}?ws.op=getSpecification:
    get:
      tags:
        - project_series
      summary: Get specification
      description: Returns the specification with the given name, for this target, or None.
      operationId: project_series-getSpecification
      parameters:
        - *a29
        - *a30
        - name: name
          in: query
          required: true
          description: The name of the specification
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/specification-full"
  /{project_series_1}/{project_series_2}?ws.op=getTranslationTemplates:
    get:
      tags:
        - project_series
      summary: Get translation templates
      description: "Return an iterator over all its translation templates. The returned templates are either obsolete or current. return: A sequence of IPOTemplate."
      operationId: project_series-getTranslationTemplates
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_template-page"
  /{project_series_1}/{project_series_2}?ws.op=get_timeline:
    get:
      tags:
        - project_series
      summary: Get timeline
      description: Return basic timeline data useful for creating a diagram. The number of milestones returned is limited.
      operationId: project_series-get_timeline
      parameters:
        - *a29
        - *a30
        - name: include_inactive
          in: query
          required: false
          description: Include inactive
          schema:
            type: string
      responses:
        "200":
          description: Successful response
  /{project_series_1}/{project_series_2}?ws.op=addBugSubscription:
    post:
      tags:
        - project_series
      summary: Add bug subscription
      description: "Add a bug subscription for this structure. This method is used to create a new IStructuralSubscription for the target. This initially has a single filter which will allow all notifications will be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription."
      operationId: project_series-addBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
  /{project_series_1}/{project_series_2}?ws.op=addBugSubscriptionFilter:
    post:
      tags:
        - project_series
      summary: Add bug subscription filter
      description: "Add a bug subscription filter for this structure. This method is used to create a new IBugSubscriptionFilter for the target. It will initially allow all notifications to be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription filter."
      operationId: project_series-addBugSubscriptionFilter
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
  /{project_series_1}/{project_series_2}?ws.op=removeBugSubscription:
    post:
      tags:
        - project_series
      summary: Remove bug subscription
      description: "Remove a subscription to bugs from this structure. This will delete all associated filters. subscriber: The IPerson who will be unsubscribed. If omitted, unsubscribed_by will be used. unsubscribed_by: The IPerson removing the subscription."
      operationId: project_series-removeBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to unsubscribe. If omitted, the requesting user will be unsubscribed.
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
  /{project_series_1}/{project_series_2}?ws.op=newMilestone:
    post:
      tags:
        - project_series
      summary: New milestone
      description: Create a new milestone for this ProjectSeries.
      operationId: project_series-newMilestone
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: Name Only letters, numbers, and simple punctuation are allowed.
                  example: ubuntu
                date_targeted:
                  type: string
                  description: "Date Targeted Example: 2005-11-24"
                summary:
                  type: string
                  description: Summary A summary of the features and status of this milestone.
                code_name:
                  type: string
                  description: Code name An alternative name for the milestone.
              required:
                - name
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
  /{project_series_1}/{project_series_2}:
    get:
      tags:
        - project_series
      summary: Get project series
      description: A series of releases. For example '2.0' or '1.3' or 'dev'.
      operationId: project_series-get
      parameters:
        - *a29
        - *a30
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project_series-full"
    put:
      tags:
        - project_series
      summary: Replace project series
      description: A series of releases. For example '2.0' or '1.3' or 'dev'.
      operationId: project_series-put
      parameters:
        - *a29
        - *a30
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/project_series-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - project_series
      summary: Update project series
      description: A series of releases. For example '2.0' or '1.3' or 'dev'.
      operationId: project_series-patch
      parameters:
        - *a29
        - *a30
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/project_series-diff"
      responses:
        "200":
          description: Successful response
  /{project_group}?ws.op=getSubscriptions:
    get:
      tags:
        - project_group
      summary: Get subscriptions
      description: "Return all the subscriptions with the specified levels. return: A sequence of IStructuralSubscription."
      operationId: project_group-getSubscriptions
      parameters:
        - &a31
          name: project_group
          in: path
          required: true
          description: Path segment of the project_group resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: launchpad-project
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-page"
  /{project_group}?ws.op=getSubscription:
    get:
      tags:
        - project_group
      summary: Get subscription
      description: Return the subscription for person, if it exists.
      operationId: project_group-getSubscription
      parameters:
        - *a31
        - name: person
          in: query
          required: true
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
          example: ~launchpad
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/structural_subscription-full"
  /{project_group}?ws.op=userHasBugSubscriptions:
    get:
      tags:
        - project_group
      summary: User has bug subscriptions
      description: Is user subscribed, directly or via a team, to bug mail?
      operationId: project_group-userHasBugSubscriptions
      parameters:
        - *a31
      responses:
        "200":
          description: Successful response
  /{project_group}?ws.op=searchTasks:
    get:
      tags:
        - project_group
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: project_group-searchTasks
      parameters:
        - *a31
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{project_group}?ws.op=getMergeProposals:
    get:
      tags:
        - project_group
      summary: Get merge proposals
      description: "Returns all merge proposals of a given status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param eager_load: If True, load related objects for the whole collection. returns: A list of IBranchMergeProposal."
      operationId: project_group-getMergeProposals
      parameters:
        - *a31
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{project_group}?ws.op=getBranches:
    get:
      tags:
        - project_group
      summary: Get branches
      description: "Returns all branches with the given lifecycle status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param modified_since: If set, filters the branches being returned to those that have been modified since the specified date/time. param eager_load: If True load related objects for the whole collection. returns: A list of IBranch."
      operationId: project_group-getBranches
      parameters:
        - *a31
        - name: status
          in: query
          required: false
          description: A list of branch lifecycle statuses to filter by.
          schema:
            type: string
        - name: modified_since
          in: query
          required: false
          description: Limit the branches to those modified since this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /{project_group}?ws.op=getMilestone:
    get:
      tags:
        - project_group
      summary: Get milestone
      description: Return a milestone with the given name for this object, or None.
      operationId: project_group-getMilestone
      parameters:
        - *a31
        - name: name
          in: query
          required: true
          description: Name
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/milestone-full"
  /{project_group}?ws.op=addBugSubscription:
    post:
      tags:
        - project_group
      summary: Add bug subscription
      description: "Add a bug subscription for this structure. This method is used to create a new IStructuralSubscription for the target. This initially has a single filter which will allow all notifications will be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription."
      operationId: project_group-addBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a31
      responses:
        "200":
          description: Successful response
  /{project_group}?ws.op=addBugSubscriptionFilter:
    post:
      tags:
        - project_group
      summary: Add bug subscription filter
      description: "Add a bug subscription filter for this structure. This method is used to create a new IBugSubscriptionFilter for the target. It will initially allow all notifications to be sent. subscriber: The IPerson who will be subscribed. If omitted, subscribed_by will be used. subscribed_by: The IPerson creating the subscription. return: The new bug subscription filter."
      operationId: project_group-addBugSubscriptionFilter
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to subscribe. If omitted, the requesting user will be subscribed.
      parameters:
        - *a31
      responses:
        "200":
          description: Successful response
  /{project_group}?ws.op=removeBugSubscription:
    post:
      tags:
        - project_group
      summary: Remove bug subscription
      description: "Remove a subscription to bugs from this structure. This will delete all associated filters. subscriber: The IPerson who will be unsubscribed. If omitted, unsubscribed_by will be used. unsubscribed_by: The IPerson removing the subscription."
      operationId: project_group-removeBugSubscription
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                subscriber:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Person to unsubscribe. If omitted, the requesting user will be unsubscribed.
      parameters:
        - *a31
      responses:
        "200":
          description: Successful response
  /{project_group}:
    get:
      tags:
        - project_group
      summary: Get project group
      description: A ProjectGroup.
      operationId: project_group-get
      parameters:
        - *a31
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project_group-full"
    put:
      tags:
        - project_group
      summary: Replace project group
      description: A ProjectGroup.
      operationId: project_group-put
      parameters:
        - *a31
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/project_group-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - project_group
      summary: Update project group
      description: A ProjectGroup.
      operationId: project_group-patch
      parameters:
        - *a31
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/project_group-diff"
      responses:
        "200":
          description: Successful response
  /services:
    get:
      tags:
        - service_factory
      summary: Get service factory
      description: Interface representing a factory used to access named services.
      operationId: service_factory-get
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/service_factory-full"
    put:
      tags:
        - service_factory
      summary: Replace service factory
      description: Interface representing a factory used to access named services.
      operationId: service_factory-put
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/service_factory-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - service_factory
      summary: Update service factory
      description: Interface representing a factory used to access named services.
      operationId: service_factory-patch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/service_factory-diff"
      responses:
        "200":
          description: Successful response
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=getTranslationImportQueueEntries:
    get:
      tags:
        - source_package
      summary: Get translation import queue entries
      description: "Return entries in the translation import queue for this entity. arg import_status: RosettaImportStatus DB Schema entry. arg file_extension: String with the file type extension, usually 'po' or 'pot'. If one of both of 'import_status' or 'file_extension' are given, the returned entries are filtered based on those values."
      operationId: source_package-getTranslationImportQueueEntries
      parameters:
        - &a32
          name: source_package_1
          in: path
          required: true
          description: Path segment of the source_package resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ubuntu
        - &a33
          name: source_package_2
          in: path
          required: true
          description: Path segment of the source_package resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: noble
        - &a34
          name: source_package_3
          in: path
          required: true
          description: Path segment of the source_package resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: bash
        - name: import_status
          in: query
          required: false
          description: Status Show only entries with this status
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
        - name: file_extension
          in: query
          required: false
          description: Filename extension Show only entries with this filename suffix
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=searchTasks:
    get:
      tags:
        - source_package
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: source_package-searchTasks
      parameters:
        - *a32
        - *a33
        - *a34
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=getTranslationTemplates:
    get:
      tags:
        - source_package
      summary: Get translation templates
      description: "Return an iterator over all its translation templates. The returned templates are either obsolete or current. return: A sequence of IPOTemplate."
      operationId: source_package-getTranslationTemplates
      parameters:
        - *a32
        - *a33
        - *a34
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_template-page"
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=getMergeProposals:
    get:
      tags:
        - source_package
      summary: Get merge proposals
      description: "Returns all merge proposals of a given status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param eager_load: If True, load related objects for the whole collection. returns: A list of IBranchMergeProposal."
      operationId: source_package-getMergeProposals
      parameters:
        - *a32
        - *a33
        - *a34
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=getBranches:
    get:
      tags:
        - source_package
      summary: Get branches
      description: "Returns all branches with the given lifecycle status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param modified_since: If set, filters the branches being returned to those that have been modified since the specified date/time. param eager_load: If True load related objects for the whole collection. returns: A list of IBranch."
      operationId: source_package-getBranches
      parameters:
        - *a32
        - *a33
        - *a34
        - name: status
          in: query
          required: false
          description: A list of branch lifecycle statuses to filter by.
          schema:
            type: string
        - name: modified_since
          in: query
          required: false
          description: Limit the branches to those modified since this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=getBranch:
    get:
      tags:
        - source_package
      summary: Get branch
      description: "Get the official branch for this package in the given pocket. param pocket: A PackagePublishingPocket. return: An IBranch."
      operationId: source_package-getBranch
      parameters:
        - *a32
        - *a33
        - *a34
        - name: pocket
          in: query
          required: true
          description: Pocket
          schema:
            type: string
            enum:
              - Release
              - Security
              - Updates
              - Proposed
              - Backports
          example: Release
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-full"
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=linkedBranches:
    get:
      tags:
        - source_package
      summary: Linked branches
      description: "Get the official branches for this package. This operation returns a {Pocket-name : IBranch} dict. return: A {Pocket-name : IBranch} dict."
      operationId: source_package-linkedBranches
      parameters:
        - *a32
        - *a33
        - *a34
      responses:
        "200":
          description: Successful response
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=setBranch:
    post:
      tags:
        - source_package
      summary: Set branch
      description: "Set the official branch for the given pocket of this package. param pocket: A PackagePublishingPocket. param branch: The branch to set as the official branch. param registrant: The individual who created this link. return: None"
      operationId: source_package-setBranch
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                pocket:
                  type: string
                  enum:
                    - Release
                    - Security
                    - Updates
                    - Proposed
                    - Backports
                  description: Pocket
                  example: Release
                branch:
                  type: string
                  format: uri
                  x-lp-resource-type: branch
                  description: Branch
              required:
                - pocket
      parameters:
        - *a32
        - *a33
        - *a34
      responses:
        "200":
          description: Successful response
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=newCodeImport:
    post:
      tags:
        - source_package
      summary: New code import
      description: "Create a new code import. param registrant: The IPerson to record as the registrant of the import. param branch_name: The name of the branch or repository to create. param rcs_type: The type of the foreign VCS. param target_rcs_type: The type of the branch or repository to create (Bazaar or Git). param url: The URL to import from if the VCS type uses a single URL (i.e. isn't CVS). param cvs_root: The CVSROOT for a CVS import. param cvs_module: The module to import for a CVS import. param owner: Who should own the created branch or repository, or None for it to be the same as the registrant, or the caller over the API. returns: An instance of ICodeImport."
      operationId: source_package-newCodeImport
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                branch_name:
                  type: string
                  description: Name of branch to create
                rcs_type:
                  type: string
                  enum:
                    - Concurrent Versions System
                    - Subversion via CSCVS
                    - Subversion via bzr-svn
                    - Git
                    - Mercurial
                    - Bazaar
                target_rcs_type:
                  type: string
                  enum:
                    - Bazaar
                    - Git
                url:
                  type: string
                  description: Foreign VCS URL
                cvs_root:
                  type: string
                  description: CVS root URL
                cvs_module:
                  type: string
                  description: CVS module to import
                owner:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Owner of the resulting branch
      parameters:
        - *a32
        - *a33
        - *a34
      responses:
        "200":
          description: Successful response
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=setPackaging:
    post:
      tags:
        - source_package
      summary: Set packaging
      description: Update the existing packaging record, or create a new packaging record, that links the source package to the given productseries, and record that it was done by the owner.
      operationId: source_package-setPackaging
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                productseries:
                  type: string
                  format: uri
                  x-lp-resource-type: project_series
                  example: productseries
              required:
                - productseries
      parameters:
        - *a32
        - *a33
        - *a34
      responses:
        "200":
          description: Successful response
  /{source_package_1}/{source_package_2}/+source/{source_package_3}?ws.op=deletePackaging:
    post:
      tags:
        - source_package
      summary: Delete packaging
      description: Delete the packaging for this sourcepackage.
      operationId: source_package-deletePackaging
      parameters:
        - *a32
        - *a33
        - *a34
      responses:
        "200":
          description: Successful response
  /{source_package_1}/{source_package_2}/+source/{source_package_3}:
    get:
      tags:
        - source_package
      summary: Get source package
      description: A source package associated to a particular distribution series.
      operationId: source_package-get
      parameters:
        - *a32
        - *a33
        - *a34
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/source_package-full"
    put:
      tags:
        - source_package
      summary: Replace source package
      description: A source package associated to a particular distribution series.
      operationId: source_package-put
      parameters:
        - *a32
        - *a33
        - *a34
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/source_package-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - source_package
      summary: Update source package
      description: A source package associated to a particular distribution series.
      operationId: source_package-patch
      parameters:
        - *a32
        - *a33
        - *a34
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/source_package-diff"
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=getTranslationImportQueueEntries:
    get:
      tags:
        - team
      summary: Get translation import queue entries
      description: "Return entries in the translation import queue for this entity. arg import_status: RosettaImportStatus DB Schema entry. arg file_extension: String with the file type extension, usually 'po' or 'pot'. If one of both of 'import_status' or 'file_extension' are given, the returned entries are filtered based on those values."
      operationId: team-getTranslationImportQueueEntries
      parameters:
        - &a35
          name: team
          in: path
          required: true
          description: Path segment of the team resource URL (editable; the default is a real instance).
          schema:
            type: string
            default: ~launchpad
        - name: import_status
          in: query
          required: false
          description: Status Show only entries with this status
          schema:
            type: string
            enum:
              - Approved
              - Imported
              - Deleted
              - Failed
              - Needs Review
              - Blocked
              - Needs Information
        - name: file_extension
          in: query
          required: false
          description: Filename extension Show only entries with this filename suffix
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/translation_import_queue_entry-page"
  /{team}?ws.op=searchTasks:
    get:
      tags:
        - team
      summary: Search tasks
      description: "Search the IBugTasks reported on this entity. search_params: a BugTaskSearchParams object Return an iterable of matching results. Note: milestone is currently ignored for all IBugTargets except IProduct."
      operationId: team-searchTasks
      parameters:
        - *a35
        - name: order_by
          in: query
          required: false
          description: List of fields by which the results are ordered.
          schema:
            type: string
        - name: search_text
          in: query
          required: false
          description: Bug ID or search text.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status Show only bugs with the given status value or list of values.
          schema:
            type: string
        - name: importance
          in: query
          required: false
          description: Importance Show only bugs with the given importance or list of importances.
          schema:
            type: string
        - name: information_type
          in: query
          required: false
          description: Information Type Show only bugs with the given information type or list of information types.
          schema:
            type: string
        - name: assignee
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_reporter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_supervisor
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_commenter
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: bug_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: structural_subscriber
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: owner
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: affected_user
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: person
        - name: has_patch
          in: query
          required: false
          description: Show only bugs with patches available.
          schema:
            type: string
        - name: has_cve
          in: query
          required: false
          description: Show only bugs associated with a CVE
          schema:
            type: string
        - name: tags
          in: query
          required: false
          description: Tags String or list of strings for tags to search. To exclude, prepend a '-', e.g. '-unwantedtag'
          schema:
            type: string
        - name: tags_combinator
          in: query
          required: false
          description: Tags combination Search for any or all of the tags specified.
          schema:
            type: string
            enum:
              - Any
              - All
        - name: omit_duplicates
          in: query
          required: false
          description: Omit bugs marked as duplicate,
          schema:
            type: string
        - name: status_upstream
          in: query
          required: false
          description: "Status upstream Indicates the status of any remote watches associated with the bug. Possible values include: pending_bugwatch, hide_upstream, resolved_upstream, and open_upstream."
          schema:
            type: string
        - name: milestone
          in: query
          required: false
          description: Milestone Show only bug tasks targeted to this milestone.
          schema:
            type: string
        - name: component
          in: query
          required: false
          description: Component Distribution package archive grouping. E.g. main, universe, multiverse
          schema:
            type: string
        - name: nominated_for
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distro_series
        - name: has_no_package
          in: query
          required: false
          description: Exclude bugs with packages specified
          schema:
            type: string
        - name: linked_branches
          in: query
          required: false
          description: Search for bugs that are linked to branches or for bugs that are not linked to branches.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Branches
              - Show only Bugs without linked Branches
        - name: modified_since
          in: query
          required: false
          description: Search for bugs that have been modified since the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for bugs that have been created since the given date.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for bugs that were created before the given date.
          schema:
            type: string
        - name: omit_targeted
          in: query
          required: false
          description: Omit bugs targeted to a series
          schema:
            type: string
        - name: linked_blueprints
          in: query
          required: false
          description: Search for bugs that are linked to blueprints or for bugs that are not linked to blueprints.
          schema:
            type: string
            enum:
              - Show all bugs
              - Show only Bugs with linked Blueprints
              - Show only Bugs without linked Blueprints
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/bug_task-page"
  /{team}?ws.op=getRequestedReviews:
    get:
      tags:
        - team
      summary: Get requested reviews
      description: 'Returns merge proposals where a person was asked to review. This does not include merge proposals that were requested from teams that the person is part of. If status is not passed then it will return proposals that are in the "Needs Review" state. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. returns: A list of IBranchMergeProposal.'
      operationId: team-getRequestedReviews
      parameters:
        - *a35
        - name: status
          in: query
          required: false
          description: A list of merge proposal statuses to filter by.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{team}?ws.op=getMergeProposals:
    get:
      tags:
        - team
      summary: Get merge proposals
      description: Return matching BranchMergeProposals.
      operationId: team-getMergeProposals
      parameters:
        - *a35
        - name: status
          in: query
          required: false
          description: A list of statuses to filter the merge proposals by.
          schema:
            type: string
        - name: created_before
          in: query
          required: false
          description: Search for merge proposals that were createdbefore the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for merge proposals that were createdsince the given date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch_merge_proposal-page"
  /{team}?ws.op=getBranches:
    get:
      tags:
        - team
      summary: Get branches
      description: "Returns all branches with the given lifecycle status. param status: A list of statuses to filter with. param visible_by_user: Normally the user who is asking. param modified_since: If set, filters the branches being returned to those that have been modified since the specified date/time. param eager_load: If True load related objects for the whole collection. returns: A list of IBranch."
      operationId: team-getBranches
      parameters:
        - *a35
        - name: status
          in: query
          required: false
          description: A list of branch lifecycle statuses to filter by.
          schema:
            type: string
        - name: modified_since
          in: query
          required: false
          description: Limit the branches to those modified since this date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/branch-page"
  /{team}?ws.op=getSocialAccountsByPlatform:
    get:
      tags:
        - team
      summary: Get social accounts by platform
      description: Return Social Accounts associated to the user.
      operationId: team-getSocialAccountsByPlatform
      parameters:
        - *a35
        - name: platform
          in: query
          required: true
          description: Social Platform Type
          schema:
            type: string
            enum:
              - Matrix platform
              - GitHub platform
          example: Matrix platform
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=getRecipe:
    get:
      tags:
        - team
      summary: Get recipe
      description: Return the person's recipe with the given name.
      operationId: team-getRecipe
      parameters:
        - *a35
        - name: name
          in: query
          required: true
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/source_package_recipe-full"
  /{team}?ws.op=getArchiveSubscriptionURLs:
    get:
      tags:
        - team
      summary: Get archive subscription urls
      description: Return private archive URLs that this person can see. For each of the private archives (PPAs) that this person can see, return a URL that includes the HTTP basic auth data. The URL returned is suitable for including in a sources.list file.
      operationId: team-getArchiveSubscriptionURLs
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=getBugSubscriberPackages:
    get:
      tags:
        - team
      summary: Get bug subscriber packages
      description: Return the packages for which this person is a bug subscriber. Returns a list of IDistributionSourcePackage's, ordered alphabetically (A to Z) by name.
      operationId: team-getBugSubscriberPackages
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/distribution_source_package-page"
  /{team}?ws.op=getMembersByStatus:
    get:
      tags:
        - team
      summary: Get members by status
      description: Return the people whose membership on this team match :status:. If no orderby is provided, Person.sortingColumns is used.
      operationId: team-getMembersByStatus
      parameters:
        - *a35
        - name: status
          in: query
          required: true
          description: The state of this membership
          schema:
            type: string
            enum:
              - Proposed
              - Approved
              - Administrator
              - Deactivated
              - Expired
              - Declined
              - Invited
              - Invitation declined
          example: Proposed
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/person-page"
  /{team}?ws.op=getPPAByName:
    get:
      tags:
        - team
      summary: Get ppaby name
      description: "Return a PPA with the given name if it exists. param name: A string with the exact name of the ppa being looked up. raises: NoSuchPPA if a suitable PPA could not be found. return: a PPA IArchive record corresponding to the name."
      operationId: team-getPPAByName
      parameters:
        - *a35
        - name: distribution
          in: query
          required: false
          schema:
            type: string
            format: uri
            x-lp-resource-type: distribution
        - name: name
          in: query
          required: true
          schema:
            type: string
          example: ubuntu
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive-full"
  /{team}?ws.op=searchQuestions:
    get:
      tags:
        - team
      summary: Search questions
      description: "Search the person's questions. param search_text: A string that is matched against the question title and description. If None, the search_text is not included as a filter criteria. param status: A sequence of QuestionStatus Items. If None or an empty sequence, the status is not included as a filter criteria. The default is to match all status except Expired and Invalid. param language: An ILanguage or a sequence of ILanguage objects to match against the question's language. If None or an empty sequence, the language is not included as a filter criteria. param participation: A list of QuestionParticipation that defines the set of relationship to questions that will be searched. If None or an empty sequence, all relationships are considered. param needs_attention: If this flag is true, only questions that need attention the person will be included. These are the questions in the NEEDSINFO or ANSWERED state owned by the person. The questions not owned by the person but on which the person requested more information or gave an answer and that are back in the OPEN state are also included. param sort: An attribute of QuestionSort. If None, a default value is used. When there is a search_text value, the default is to sort by RELEVANCY, otherwise results are sorted NEWEST_FIRST. param created_since: Only return results whose datecreated property is greater than or equal to this date. param created_before: Only return results whose datecreated property is smaller than this date."
      operationId: team-searchQuestions
      parameters:
        - *a35
        - name: search_text
          in: query
          required: false
          description: Search text
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Status
          schema:
            type: string
        - name: language
          in: query
          required: false
          description: Language
          schema:
            type: string
        - name: participation
          in: query
          required: false
          description: Participation
          schema:
            type: string
            enum:
              - Owner
              - Subscriber
              - Assignee
              - Commenter
              - Answerer
        - name: needs_attention
          in: query
          required: false
          description: Needs attentions from
          schema:
            type: string
        - name: sort
          in: query
          required: false
          description: Sort
          schema:
            type: string
            enum:
              - by relevancy
              - by status
              - newest first
              - oldest first
              - recently updated first
        - name: created_before
          in: query
          required: false
          description: Search for questions that were created before the given date.
          schema:
            type: string
        - name: created_since
          in: query
          required: false
          description: Search for questions that were created since the given date.
          schema:
            type: string
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question-page"
  /{team}?ws.op=getDirectAnswerQuestionTargets:
    get:
      tags:
        - team
      summary: Get direct answer question targets
      description: Return a list of IQuestionTargets that a person is subscribed to. This will return IQuestionTargets that the person is registered as an answer contact because they subscribed themselves.
      operationId: team-getDirectAnswerQuestionTargets
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question_target-page"
  /{team}?ws.op=getTeamAnswerQuestionTargets:
    get:
      tags:
        - team
      summary: Get team answer question targets
      description: Return a list of IQuestionTargets that are indirect subscriptions. This will return IQuestionTargets that the person or team is registered as an answer contact because of their membership in a team.
      operationId: team-getTeamAnswerQuestionTargets
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/question_target-page"
  /{team}?ws.op=getArchiveSubscriptions:
    get:
      tags:
        - team
      summary: Get archive subscriptions
      description: Return (private) archives subscription for this person.
      operationId: team-getArchiveSubscriptions
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/archive_subscriber-page"
  /{team}?ws.op=getOwnedProjects:
    get:
      tags:
        - team
      summary: Get owned projects
      description: "Projects owned by this person or teams to which they belong. param match_name: string optional project name to screen the results."
      operationId: team-getOwnedProjects
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/project-page"
  /{team}?ws.op=getOwnedTeams:
    get:
      tags:
        - team
      summary: Get owned teams
      description: Return the teams that this person owns. The iterator includes the teams that the user owns, but it not a member of.
      operationId: team-getOwnedTeams
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/team-page"
  /{team}?ws.op=setLocation:
    post:
      tags:
        - team
      summary: Set location
      description: Specify the location and time zone of a person.
      operationId: team-setLocation
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                latitude:
                  type: string
                  description: The latitude of this object.
                  example: latitude
                longitude:
                  type: string
                  description: The longitude of this object.
                  example: longitude
                time_zone:
                  type: string
                  description: The time zone of this object.
                  example: time_zone
              required:
                - latitude
                - longitude
                - time_zone
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=join:
    post:
      tags:
        - team
      summary: Join
      description: "Join the given team if its membership_policy is not RESTRICTED. Join the given team according to the policies and defaults of that team: If the team membership_policy is OPEN, the user is added as an APPROVED member with a NULL TeamMembership.reviewer. If the team membership_policy is MODERATED, the user is added as a PROPOSED member and one of the team's administrators have to approve the membership. If may_subscribe_to_list is True, then also attempt to subscribe to the team's mailing list, depending on the list status and the person's auto-subscribe settings. param requester: The person who requested the membership on behalf of a team or None when a person requests the membership for themselves. param may_subscribe_to_list: If True, also try subscribing to the team mailing list."
      operationId: team-join
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
              required:
                - team
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=leave:
    post:
      tags:
        - team
      summary: Leave
      description: "Leave the given team. This is a convenience method for retractTeamMembership() that allows a user to leave the given team, or to cancel a PENDING membership request. param team: The team to leave."
      operationId: team-leave
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
              required:
                - team
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=addMember:
    post:
      tags:
        - team
      summary: Add member
      description: "Add the given person as a member of this team. param person: If the given person is already a member of this team we'll simply change its membership status. Otherwise a new TeamMembership is created with the given status. param reviewer: The user who made the given person a member of this team. param comment: String that will be assigned to the proponent_comment, reviewer_comment, or acknowledger comment. param status: TeamMembershipStatus value must be either Approved, Proposed or Admin. If the new member is a team, the status will be changed to Invited unless the user is also an admin of that team. param force_team_add: If the person is actually a team and force_team_add is False, the team will actually be invited to join this one. Otherwise the team is added as if it were a person. param may_subscribe_to_list: If the person is not a team, and may_subscribe_to_list is True, then the person may be subscribed to the team's mailing list, depending on the list status and the person's auto-subscribe settings. return: A tuple containing a boolean indicating when the membership status changed and the current TeamMembershipStatus. This depends on the desired status passed as an argument, the membership policy and the user's privileges."
      operationId: team-addMember
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                person:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Member
                  example: ~launchpad
                status:
                  type: string
                  enum:
                    - Proposed
                    - Approved
                    - Administrator
                    - Deactivated
                    - Expired
                    - Declined
                    - Invited
                    - Invitation declined
                  description: The state of this membership
                comment:
                  type: string
              required:
                - person
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=acceptInvitationToBeMemberOf:
    post:
      tags:
        - team
      summary: Accept invitation to be member of
      description: Accept an invitation to become a member of the given team. There must be a TeamMembership for this person and the given team with the INVITED status. The status of this TeamMembership will be changed to APPROVED.
      operationId: team-acceptInvitationToBeMemberOf
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
                comment:
                  type: string
                  example: comment
              required:
                - team
                - comment
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=declineInvitationToBeMemberOf:
    post:
      tags:
        - team
      summary: Decline invitation to be member of
      description: Decline an invitation to become a member of the given team. There must be a TeamMembership for this person and the given team with the INVITED status. The status of this TeamMembership will be changed to INVITATION_DECLINED.
      operationId: team-declineInvitationToBeMemberOf
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
                comment:
                  type: string
                  example: comment
              required:
                - team
                - comment
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=retractTeamMembership:
    post:
      tags:
        - team
      summary: Retract team membership
      description: "Retract this team's membership in the given team. If there's a membership entry for this team on the given team and its status is either APPROVED, ADMIN, PENDING, or INVITED, the status is changed and the relevant entries in TeamParticipation. APPROVED and ADMIN status are changed to DEACTIVATED. PENDING status is changed to DECLINED. INVITED status is changes to INVITATION_DECLINED. param team: The team to leave. param user: The user making the retraction. param comment: An optional explanation about why the change was made."
      operationId: team-retractTeamMembership
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                team:
                  type: string
                  format: uri
                  x-lp-resource-type: person
                  description: Team
                  example: team
                comment:
                  type: string
              required:
                - team
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=createPPA:
    post:
      tags:
        - team
      summary: Create ppa
      description: "Create a PPA. param distribution: The distribution that this archive is for. param name: The name of the new PPA to create. param displayname: The displayname for the new PPA. param description: The description for the new PPA. param private: Whether or not to create a private PPA. Defaults to False, which means the PPA will be public. param suppress_subscription_notifications: Whether or not to suppress emails to new subscribers about their subscriptions. Only meaningful for private PPAs. raises: PPACreationError if an error is encountered return: a PPA IArchive record."
      operationId: team-createPPA
      requestBody:
        required: false
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                distribution:
                  type: string
                  format: uri
                  x-lp-resource-type: distribution
                name:
                  type: string
                displayname:
                  type: string
                description:
                  type: string
                private:
                  type: string
                suppress_subscription_notifications:
                  type: string
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=createRecipe:
    post:
      tags:
        - team
      summary: Create recipe
      description: "Create a SourcePackageRecipe owned by this person. param name: the name to use for referring to the recipe. param description: A description of the recipe. param recipe_text: The text of the recipe. param distroseries: The distroseries to use. param registrant: The person who created this recipe. param daily_build_archive: The archive to use for daily builds. param build_daily: If True, build this recipe daily (if changed). return: a SourcePackageRecipe."
      operationId: team-createRecipe
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                description:
                  type: string
                  example: description
                distroseries:
                  type: string
                  example: noble
                name:
                  type: string
                  example: ubuntu
                recipe_text:
                  type: string
                  example: recipe_text
                daily_build_archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                build_daily:
                  type: string
              required:
                - description
                - distroseries
                - name
                - recipe_text
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=getArchiveSubscriptionURL:
    post:
      tags:
        - team
      summary: Get archive subscription url
      description: Get a text line that is suitable to be used for a sources.list entry. It will create a new IArchiveAuthToken if one doesn't already exist. It raises Unauthorized if the context user does not have a valid subscription for the target archive or the caller is not context user itself.
      operationId: team-getArchiveSubscriptionURL
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                archive:
                  type: string
                  format: uri
                  x-lp-resource-type: archive
                  example: archive
              required:
                - archive
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=setAccountStatus:
    post:
      tags:
        - team
      summary: Set account status
      description: Set the status of this person's account.
      operationId: team-setAccountStatus
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - Placeholder
                    - Unactivated
                    - Active
                    - Deactivated
                    - Suspended
                    - Closed
                    - Deceased
                  description: The status of this account
                  example: Placeholder
                comment:
                  type: string
                  description: Status change comment
                  example: comment
              required:
                - status
                - comment
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=addLanguage:
    post:
      tags:
        - team
      summary: Add language
      description: "Add a language to this person's preferences. param language: An object providing ILanguage. If the given language is one of the user's preferred languages already, nothing will happen."
      operationId: team-addLanguage
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                language:
                  type: string
                  format: uri
                  x-lp-resource-type: language
                  example: language
              required:
                - language
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}?ws.op=removeLanguage:
    post:
      tags:
        - team
      summary: Remove language
      description: "Remove a language from this person's preferences. param language: An object providing ILanguage. If the given language is not present, nothing will happen."
      operationId: team-removeLanguage
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                language:
                  type: string
                  format: uri
                  x-lp-resource-type: language
                  example: language
              required:
                - language
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
  /{team}:
    get:
      tags:
        - team
      summary: Get team
      description: "A group of people and other teams. Launchpadlib example of getting the date a user joined a team: def get_join_date(team, user): team = launchpad.people[team] members = team.members_details for member in members: if member.member.name == user: return member.date_joined return None Implementation notes: ITeam extends IPerson. The teamowner should never be None."
      operationId: team-get
      parameters:
        - *a35
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/team-full"
    put:
      tags:
        - team
      summary: Replace team
      description: "A group of people and other teams. Launchpadlib example of getting the date a user joined a team: def get_join_date(team, user): team = launchpad.people[team] members = team.members_details for member in members: if member.member.name == user: return member.date_joined return None Implementation notes: ITeam extends IPerson. The teamowner should never be None."
      operationId: team-put
      parameters:
        - *a35
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team-full"
      responses:
        "200":
          description: Successful response
    patch:
      tags:
        - team
      summary: Update team
      description: "A group of people and other teams. Launchpadlib example of getting the date a user joined a team: def get_join_date(team, user): team = launchpad.people[team] members = team.members_details for member in members: if member.member.name == user: return member.date_joined return None Implementation notes: ITeam extends IPerson. The teamowner should never be None."
      operationId: team-patch
      parameters:
        - *a35
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/team-diff"
      responses:
        "200":
          description: Successful response
components:
  schemas:
    service-root-json:
      type: object
      properties:
        archives_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archives
        branches_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branches
        bug_trackers_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_trackers
        bugs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bugs
        builders_collection_link:
          type: string
          format: uri
          x-lp-resource-type: builders
        charm_bases_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_bases
        charm_recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipes
        countries_collection_link:
          type: string
          format: uri
          x-lp-resource-type: countries
        craft_recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipes
        cves_collection_link:
          type: string
          format: uri
          x-lp-resource-type: cves
        distributions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distributions
        git_repositories_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_repositories
        languages_collection_link:
          type: string
          format: uri
          x-lp-resource-type: languages
        livefses_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefses
        me_link:
          type: string
          format: uri
          x-lp-resource-type: person
        packagesets_collection_link:
          type: string
          format: uri
          x-lp-resource-type: packagesets
        people_collection_link:
          type: string
          format: uri
          x-lp-resource-type: people
        pillars_link:
          type: string
          format: uri
          x-lp-resource-type: pillars
        polls_collection_link:
          type: string
          format: uri
          x-lp-resource-type: polls
        processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processors
        project_groups_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_groups
        projects_collection_link:
          type: string
          format: uri
          x-lp-resource-type: projects
        questions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: questions
        rock_bases_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_bases
        rock_recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipes
        services_link:
          type: string
          format: uri
          x-lp-resource-type: service_factory
        snap_bases_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_bases
        snappy_serieses_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snappy_serieses
        snaps_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snaps
        specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specifications
        temporary_blobs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: temporary_blobs
        translation_groups_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_groups
        translation_import_queue_entries_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_import_queue_entries
        vulnerabilities_collection_link:
          type: string
          format: uri
          x-lp-resource-type: vulnerabilities
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
    access_token-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: access_token
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_created:
          type: string
          description: Creation date When the token was created.
        date_expires:
          type: string
          description: Expiry date When the token should expire or was revoked.
        date_last_used:
          type: string
          description: Date last used When the token was last used.
        description:
          type: string
          description: Description A short description of the token.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The person who created the token.
        revoked_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Revoked by The person who revoked the token, if any.
        scopes:
          type: string
          description: Scopes A list of scopes granted by the token.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: access_token_target
          description: Target The target for which the token was issued.
      required:
        - date_created
        - date_expires
        - date_last_used
        - description
        - owner_link
        - revoked_by_link
        - scopes
        - target_link
    access_token-diff:
      type: object
      properties:
        description:
          type: string
          description: Description A short description of the token.
    access_token-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: access_token-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: access_token-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    access_token_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: access_token_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    access_token_target-diff:
      type: object
    access_token_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: access_token_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: access_token_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    archive_dependency-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: archive_dependency
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Target archive The archive that has this dependency.
        component_name:
          type: string
          description: Component name
        date_created:
          type: string
          description: Instant when the dependency was created.
        dependency_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: The archive set as a dependency.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket
        snap_base_link:
          type: string
          format: uri
          x-lp-resource-type: snap_base
          description: Target snap base The snap base that has this dependency.
        title:
          type: string
          description: Archive dependency title.
      required:
        - archive_link
        - component_name
        - date_created
        - dependency_link
        - pocket
        - snap_base_link
        - title
    archive_dependency-diff:
      type: object
    archive_dependency-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_dependency-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_dependency-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    archive-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        authorized_size:
          type: string
          description: Authorized size Maximum size, in MiB, allowed for the archive.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        build_debug_symbols:
          type: string
          description: Build debug symbols Create debug symbol packages for builds in the archive.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        dependencies_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_dependency-page-resource
          description: Archive dependencies recorded for this archive.
        description:
          type: string
          description: Description A short description of the archive. URLs are allowed and will be rendered as links.
        displayname:
          type: string
          description: Display name A short title for the archive.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: The distribution that uses or is used by this archive.
        enabled_restricted_processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
          description: Enabled restricted processors DEPRECATED. Use processors instead.
        external_dependencies:
          type: string
          description: "External dependencies Newline-separated list of repositories to be used to retrieve any external build-dependencies when building packages in the archive, in the format: deb http[s]://[user:pass&#64;]<host>[/path] %(series)s[-pocket] [components] The series variable is replaced with the series name of the context build. NOTE: This is for migration of OEM PPAs only!"
        metadata_overrides:
          type: string
          description: A JSON object containing metadata overrides for this archive. Accepted keys are Origin, Label, Suite and Snapshots.The values for all these keys should be a string and can use a {series} placeholder, which will get substituted by the name of the series that is currently being published.
        name:
          type: string
          description: Name At least one lowercase letter or number, followed by letters, numbers, dots, hyphens or pluses. Keep this name short; it is used in URLs.
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The archive owner.
        permit_obsolete_series_uploads:
          type: string
          description: Permit uploads to obsolete series Allow uploads targeted to obsolete series.
        private:
          type: string
          description: Private Restrict access to the archive to its owner and subscribers. This can only be changed if the archive has never had any sources published.
        processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
          description: Processors The architectures on which the archive can build.
        publish:
          type: string
          description: Publishing enabled Whether or not to update the apt repository. If disabled, nothing will be published. If the archive is private then additionally no builds will be dispatched.
        publish_debug_symbols:
          type: string
          description: Publish debug symbols Publish debug symbol packages in the apt repository.
        publishing_method:
          type: string
          enum:
            - Local
            - Artifactory
          description: Publishing method
        reference:
          type: string
          description: Reference A string to uniquely identify the archive.
        relative_build_score:
          type: string
          description: Relative build score A delta to apply to all build scores for the archive. Builds with a higher score will build sooner.
        repository_format:
          type: string
          enum:
            - Debian
            - Python
            - Conda
            - Go
            - Generic
            - Rust
          description: Repository format
        require_virtualized:
          type: string
          description: Require virtualized builders Only build the archive's packages on virtual builders.
        signing_key_fingerprint:
          type: string
          description: Archive signing key fingerprint A OpenPGP signing key fingerprint (40 chars) for this PPA or None if there is no signing key available.
        status:
          type: string
          description: Status of archive.
        suppress_subscription_notifications:
          type: string
          description: Suppress subscription notifications Whether subscribers to private PPAs get emails about their subscriptions. Has no effect on a public PPA.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - authorized_size
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - build_debug_symbols
        - content_templates
        - dependencies_collection_link
        - description
        - displayname
        - distribution_link
        - enabled_restricted_processors_collection_link
        - external_dependencies
        - metadata_overrides
        - name
        - official_bug_tags
        - owner_link
        - permit_obsolete_series_uploads
        - private
        - processors_collection_link
        - publish
        - publish_debug_symbols
        - publishing_method
        - reference
        - relative_build_score
        - repository_format
        - require_virtualized
        - signing_key_fingerprint
        - status
        - suppress_subscription_notifications
        - translations_usage
        - webhooks_collection_link
    archive-diff:
      type: object
      properties:
        authorized_size:
          type: string
          description: Authorized size Maximum size, in MiB, allowed for the archive.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        build_debug_symbols:
          type: string
          description: Build debug symbols Create debug symbol packages for builds in the archive.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        description:
          type: string
          description: Description A short description of the archive. URLs are allowed and will be rendered as links.
        displayname:
          type: string
          description: Display name A short title for the archive.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: The distribution that uses or is used by this archive.
        external_dependencies:
          type: string
          description: "External dependencies Newline-separated list of repositories to be used to retrieve any external build-dependencies when building packages in the archive, in the format: deb http[s]://[user:pass&#64;]<host>[/path] %(series)s[-pocket] [components] The series variable is replaced with the series name of the context build. NOTE: This is for migration of OEM PPAs only!"
        metadata_overrides:
          type: string
          description: A JSON object containing metadata overrides for this archive. Accepted keys are Origin, Label, Suite and Snapshots.The values for all these keys should be a string and can use a {series} placeholder, which will get substituted by the name of the series that is currently being published.
        name:
          type: string
          description: Name At least one lowercase letter or number, followed by letters, numbers, dots, hyphens or pluses. Keep this name short; it is used in URLs.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The archive owner.
        permit_obsolete_series_uploads:
          type: string
          description: Permit uploads to obsolete series Allow uploads targeted to obsolete series.
        private:
          type: string
          description: Private Restrict access to the archive to its owner and subscribers. This can only be changed if the archive has never had any sources published.
        publish:
          type: string
          description: Publishing enabled Whether or not to update the apt repository. If disabled, nothing will be published. If the archive is private then additionally no builds will be dispatched.
        publish_debug_symbols:
          type: string
          description: Publish debug symbols Publish debug symbol packages in the apt repository.
        publishing_method:
          type: string
          enum:
            - Local
            - Artifactory
          description: Publishing method
        relative_build_score:
          type: string
          description: Relative build score A delta to apply to all build scores for the archive. Builds with a higher score will build sooner.
        repository_format:
          type: string
          enum:
            - Debian
            - Python
            - Conda
            - Go
            - Generic
            - Rust
          description: Repository format
        require_virtualized:
          type: string
          description: Require virtualized builders Only build the archive's packages on virtual builders.
        signing_key_fingerprint:
          type: string
          description: Archive signing key fingerprint A OpenPGP signing key fingerprint (40 chars) for this PPA or None if there is no signing key available.
        suppress_subscription_notifications:
          type: string
          description: Suppress subscription notifications Whether subscribers to private PPAs get emails about their subscriptions. Has no effect on a public PPA.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
    archive-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    archive_permission-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: archive_permission
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The archive that this permission is for.
        component_name:
          type: string
          description: Component Name
        date_created:
          type: string
          description: Date Created The timestamp when the permission was created.
        distro_series_name:
          type: string
          description: The name of the distro series associated with the package set.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro series The distro series that this permission is for (only for pocket permissions).
        explicit:
          type: string
          description: Explicit Set this flag for package sets with high-profile packages requiring specialist skills for proper handling.
        package_set_name:
          type: string
          description: Package set name
        permission:
          type: string
          description: The permission type being granted.
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person or team being granted the permission.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The pocket that this permission is for.
        source_package_name:
          type: string
          description: Source Package Name
      required:
        - archive_link
        - component_name
        - date_created
        - distro_series_name
        - distroseries_link
        - explicit
        - package_set_name
        - permission
        - person_link
        - pocket
        - source_package_name
    archive_permission-diff:
      type: object
      properties:
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The archive that this permission is for.
        component_name:
          type: string
          description: Component Name
        date_created:
          type: string
          description: Date Created The timestamp when the permission was created.
        distro_series_name:
          type: string
          description: The name of the distro series associated with the package set.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro series The distro series that this permission is for (only for pocket permissions).
        explicit:
          type: string
          description: Explicit Set this flag for package sets with high-profile packages requiring specialist skills for proper handling.
        package_set_name:
          type: string
          description: Package set name
        permission:
          type: string
          description: The permission type being granted.
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person or team being granted the permission.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The pocket that this permission is for.
        source_package_name:
          type: string
          description: Source Package Name
    archive_permission-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_permission-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_permission-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    archive_subscriber-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: archive_subscriber
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The archive for this subscription.
        date_created:
          type: string
          description: Date Created The timestamp when the subscription was created.
        date_expires:
          type: string
          description: Date of Expiration The timestamp when the subscription will expire.
        description:
          type: string
          description: Description Free text describing this subscription.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this subscription.
        status:
          type: string
          enum:
            - Active
            - Expired
            - Cancelled
          description: Status The status of this subscription.
        subscriber_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Subscriber The person who is subscribed.
      required:
        - archive_link
        - date_created
        - date_expires
        - description
        - registrant_link
        - status
        - subscriber_link
    archive_subscriber-diff:
      type: object
      properties:
        date_expires:
          type: string
          description: Date of Expiration The timestamp when the subscription will expire.
        description:
          type: string
          description: Description Free text describing this subscription.
        status:
          type: string
          enum:
            - Active
            - Expired
            - Cancelled
          description: Status The status of this subscription.
    archive_subscriber-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_subscriber-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_subscriber-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        arch_tag:
          type: string
          description: Architecture tag
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The Archive context for this build.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildinfo_url:
          type: string
          description: buildinfo file URL The URL for the .buildinfo file for this build.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        changesfile_url:
          type: string
          description: Changes file URL The URL for the changes file for this build. Will be None if the build was imported by Gina.
        current_source_publication_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_publishing_history
          description: Source publication The current source publication for this build.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        external_dependencies:
          type: string
          description: "External dependencies Newline-separated list of repositories to be used to retrieve any external build-dependencies when performing this build, in the format: deb http[s]://[user:pass&#64;]<host>[/path] series[-pocket] [components] This is intended for bootstrapping build-dependency loops."
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        score:
          type: string
          description: Score of the related job (if any)
        source_package_name:
          type: string
          description: Source package name
        source_package_version:
          type: string
          description: Source package version
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - arch_tag
        - archive_link
        - build_log_url
        - builder_link
        - buildinfo_url
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - changesfile_url
        - current_source_publication_link
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_series_link
        - duration
        - external_dependencies
        - pocket
        - score
        - source_package_name
        - source_package_version
        - title
        - upload_log_url
    build-diff:
      type: object
      properties:
        arch_tag:
          type: string
          description: Architecture tag
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        external_dependencies:
          type: string
          description: "External dependencies Newline-separated list of repositories to be used to retrieve any external build-dependencies when performing this build, in the format: deb http[s]://[user:pass&#64;]<host>[/path] series[-pocket] [components] This is intended for bootstrapping build-dependency loops."
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    binary_package_publishing_history-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: binary_package_publishing_history
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        architecture_specific:
          type: string
          description: Architecture Specific
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The context archive for this publication.
        binary_package_name:
          type: string
          description: Binary Package Name
        binary_package_version:
          type: string
          description: Binary Package Version
        build_link:
          type: string
          format: uri
          x-lp-resource-type: build
          description: Build The build that produced this binary package.
        component_name:
          type: string
          description: Component Name
        copied_from_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Original archive ID where this package was copied from.
        creator_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Publication Creator The IPerson who created this publication.
        date_created:
          type: string
          description: Date Created The date on which this record was created
        date_made_pending:
          type: string
          description: Date Made Pending The date on which this record was set as pending removal
        date_published:
          type: string
          description: Date Published The date on which this record was published
        date_removed:
          type: string
          description: Date Removed The date on which this record was removed from the published set
        date_superseded:
          type: string
          description: Date Superseded The date on which this record was marked superseded
        display_name:
          type: string
          description: Display Name Text representation of the current record.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: Distro Arch Series The distroarchseries being published into
        is_debug:
          type: string
          description: Debug Package Is this a debug package publication?
        phased_update_percentage:
          type: string
          description: The percentage of users for whom this package should be recommended, or None to publish the update for everyone
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The pocket into which this entry is published
        priority_name:
          type: string
          description: Priority Name
        removal_comment:
          type: string
          description: Removal Comment Reason why this publication is going to be removed.
        removed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Removed By The Person responsible for the removal
        scheduled_deletion_date:
          type: string
          description: Scheduled Deletion Date The date on which this record is scheduled for deletion
        section_name:
          type: string
          description: Section Name
        source_package_name:
          type: string
          description: Source Package Name The source package name that built this binary.
        source_package_version:
          type: string
          description: Source Package Version The source package version that built this binary.
        status:
          type: string
          enum:
            - Pending
            - Published
            - Superseded
            - Deleted
            - Obsolete
          description: Status The status of this publishing record
      required:
        - architecture_specific
        - archive_link
        - binary_package_name
        - binary_package_version
        - build_link
        - component_name
        - copied_from_archive_link
        - creator_link
        - date_created
        - date_made_pending
        - date_published
        - date_removed
        - date_superseded
        - display_name
        - distro_arch_series_link
        - is_debug
        - phased_update_percentage
        - pocket
        - priority_name
        - removal_comment
        - removed_by_link
        - scheduled_deletion_date
        - section_name
        - source_package_name
        - source_package_version
        - status
    binary_package_publishing_history-diff:
      type: object
      properties:
        date_created:
          type: string
          description: Date Created The date on which this record was created
        date_made_pending:
          type: string
          description: Date Made Pending The date on which this record was set as pending removal
        date_published:
          type: string
          description: Date Published The date on which this record was published
        date_removed:
          type: string
          description: Date Removed The date on which this record was removed from the published set
        date_superseded:
          type: string
          description: Date Superseded The date on which this record was marked superseded
        display_name:
          type: string
          description: Display Name Text representation of the current record.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: Distro Arch Series The distroarchseries being published into
        removal_comment:
          type: string
          description: Removal Comment Reason why this publication is going to be removed.
        removed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Removed By The Person responsible for the removal
        scheduled_deletion_date:
          type: string
          description: Scheduled Deletion Date The date on which this record is scheduled for deletion
        status:
          type: string
          enum:
            - Pending
            - Published
            - Superseded
            - Deleted
            - Obsolete
          description: Status The status of this publishing record
    binary_package_publishing_history-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: binary_package_publishing_history-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: binary_package_publishing_history-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    binary_package_release_download_count-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: binary_package_release_download_count
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive
        binary_package_name:
          type: string
          description: Binary package name
        binary_package_version:
          type: string
          description: Binary package version
        count:
          type: string
          description: Number of downloads
        country_link:
          type: string
          format: uri
          x-lp-resource-type: country
          description: Country
        day:
          type: string
          description: Day of the downloads
      required:
        - archive_link
        - binary_package_name
        - binary_package_version
        - count
        - country_link
        - day
    binary_package_release_download_count-diff:
      type: object
    binary_package_release_download_count-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: binary_package_release_download_count-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: binary_package_release_download_count-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    branch-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        branch_format:
          type: string
          enum:
            - "!Unrecognized!"
            - Fake Bazaar Branch 4 marker
            - Bazaar-NG Branch Reference Format 1
            - Bazaar-NG branch format 5
            - Bazaar Branch Format 6 (bzr 0.15)
            - Bazaar Branch Format 7 (needs bzr 1.6)
            - Bazaar Branch Format 8 (needs bzr 1.15)
            - Bazaar-NG Loom branch format 1
            - Bazaar-NG Loom branch format 6
            - Bazaar-NG Loom branch format 7
          description: Branch Format
        branch_type:
          type: string
          enum:
            - Hosted
            - Mirrored
            - Imported
            - Remote
          description: Branch Type
        bzr_identity:
          type: string
          description: Bazaar Identity The bzr branch path as accessed by Launchpad. If the branch is associated with a product as the primary development focus, then the result should be lp:product. If the branch is related to a series, then lp:product/series. Otherwise the result is lp:~user/product/branch-name.
        code_import_link:
          type: string
          format: uri
          x-lp-resource-type: code_import
          description: The associated CodeImport, if any.
        control_format:
          type: string
          enum:
            - "!Unrecognized!"
            - Bazaar-NG branch, format 0.0.4
            - Bazaar-NG branch, format 5
            - Bazaar-NG branch, format 6
            - Bazaar-NG meta directory, format 1
            - Bazaar meta directory, format 1 (with colocated branches)
          description: Control Directory
        date_created:
          type: string
          description: Date Created
        date_last_modified:
          type: string
          description: Date Last Modified
        dependent_branches_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Dependent Branches A collection of the merge proposals that are dependent on this branch.
        description:
          type: string
          description: Description A short description of the changes in this branch.
        display_name:
          type: string
          description: Display name The branch unique_name.
        explicitly_private:
          type: string
          description: Keep branch confidential Make this branch visible only to its subscribers.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of information contained in this branch.
        landing_candidates_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Landing Candidates A collection of the merge proposals where this branch is the target branch.
        landing_targets_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Landing Targets A collection of the merge proposals where this branch is the source branch.
        last_mirror_attempt:
          type: string
          description: Last time a mirror of this branch was attempted.
        last_mirrored:
          type: string
          description: Last time this branch was successfully mirrored.
        last_scanned:
          type: string
          description: Last time this branch was successfully scanned.
        last_scanned_id:
          type: string
          description: Last scanned revision ID The head revision ID of the branch when last successfully scanned.
        lifecycle_status:
          type: string
          enum:
            - Experimental
            - Development
            - Mature
            - Merged
            - Abandoned
          description: Status
        linked_bugs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
          description: The bugs linked to this branch.
        mirror_status_message:
          type: string
          description: The last message we got when mirroring this branch.
        name:
          type: string
          description: "Name Keep very short, unique, and descriptive, because it will be used in URLs. Examples: main, devel, release-1.0, gnome-vfs."
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner Either yourself or an exclusive team you are a member of. This controls who can modify the branch.
        private:
          type: string
          description: Branch is confidential This branch is visible only to its subscribers.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: Project The project this branch belongs to.
        recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
          description: All recipes associated with the object.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The user that registered the branch.
        repository_format:
          type: string
          enum:
            - "!Unrecognized!"
            - Fake Bazaar repository 4 marker
            - Fake Bazaar repository 5 marker
            - Fake Bazaar repository 6 marker
            - Bazaar-NG Repository format 7
            - Bazaar-NG Knit Repository Format 1
            - Bazaar Knit Repository Format 3 (bzr 0.15)
            - Bazaar Knit Repository Format 4 (bzr 1.0)
            - Bazaar pack repository format 1 (needs bzr 0.92)
            - Bazaar pack repository format 1 with subtree support (needs bzr 0.92)
            - Bazaar pack repository format 1 with rich root (needs bzr 1.0)
            - Bazaar RepositoryFormatKnitPack5 (bzr 1.6)
            - Bazaar RepositoryFormatKnitPack5RichRoot (bzr 1.6)
            - Bazaar RepositoryFormatKnitPack5RichRoot (bzr 1.6.1)
            - Bazaar RepositoryFormatKnitPack6 (bzr 1.9)
            - Bazaar RepositoryFormatKnitPack6RichRoot (bzr 1.9)
            - Bazaar development format 0 (needs bzr.dev from before 1.3)
            - Bazaar development format 0 with subtree support (needs bzr.dev from before 1.3)
            - Bazaar development format 1 (needs bzr.dev from before 1.6)
            - Bazaar development format 1 with subtree support (needs bzr.dev from before 1.6)
            - Bazaar development format 2 (needs bzr.dev from before 1.8)
            - Bazaar development format 2 with subtree support (needs bzr.dev from before 1.8)
            - Bazaar development format 8
            - Bazaar development format - group compression and chk inventory (needs bzr.dev from 1.14)
            - Bazaar development format - chk repository with bencode revision serialization (needs bzr.dev from 1.16)
            - Bazaar repository format 2a (needs bzr 1.16 or later)
          description: Repository Format
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Review Team The reviewer of a branch is the person or exclusive team that is responsible for reviewing proposals and merging into this branch.
        revision_count:
          type: string
          description: Revision count The revision number of the tip of the branch.
        sourcepackage_link:
          type: string
          format: uri
          x-lp-resource-type: source_package
          description: The ISourcePackage that this branch belongs to. None if not a package branch.
        spec_links_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_branch-page-resource
          description: Specification linked to this branch.
        subscribers_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Persons subscribed to this branch.
        subscriptions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_subscription-page-resource
          description: BranchSubscriptions associated to this branch.
        unique_name:
          type: string
          description: Unique name Unique name of the branch, including the owner and project names.
        url:
          type: string
          description: Branch URL The external location where the Bazaar branch is hosted. It is None when the branch is hosted by Launchpad.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
        whiteboard:
          type: string
          description: Whiteboard Notes on the current status of the branch.
      required:
        - branch_format
        - branch_type
        - bzr_identity
        - code_import_link
        - control_format
        - date_created
        - date_last_modified
        - dependent_branches_collection_link
        - description
        - display_name
        - explicitly_private
        - information_type
        - landing_candidates_collection_link
        - landing_targets_collection_link
        - last_mirror_attempt
        - last_mirrored
        - last_scanned
        - last_scanned_id
        - lifecycle_status
        - linked_bugs_collection_link
        - mirror_status_message
        - name
        - owner_link
        - private
        - project_link
        - recipes_collection_link
        - registrant_link
        - repository_format
        - reviewer_link
        - revision_count
        - sourcepackage_link
        - spec_links_collection_link
        - subscribers_collection_link
        - subscriptions_collection_link
        - unique_name
        - url
        - webhooks_collection_link
        - whiteboard
    branch-diff:
      type: object
      properties:
        code_import_link:
          type: string
          format: uri
          x-lp-resource-type: code_import
          description: The associated CodeImport, if any.
        date_last_modified:
          type: string
          description: Date Last Modified
        description:
          type: string
          description: Description A short description of the changes in this branch.
        explicitly_private:
          type: string
          description: Keep branch confidential Make this branch visible only to its subscribers.
        lifecycle_status:
          type: string
          enum:
            - Experimental
            - Development
            - Mature
            - Merged
            - Abandoned
          description: Status
        name:
          type: string
          description: "Name Keep very short, unique, and descriptive, because it will be used in URLs. Examples: main, devel, release-1.0, gnome-vfs."
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Review Team The reviewer of a branch is the person or exclusive team that is responsible for reviewing proposals and merging into this branch.
        url:
          type: string
          description: Branch URL The external location where the Bazaar branch is hosted. It is None when the branch is hosted by Launchpad.
        whiteboard:
          type: string
          description: Whiteboard Notes on the current status of the branch.
    branch-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    branch_merge_proposal-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        address:
          type: string
          description: The email address for this proposal. Any emails sent to this address will resultin comments being added.
        all_comments_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_comment-page-resource
          description: All messages discussing this merge proposal
        bugs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
          description: Bugs related to this object.
        commit_message:
          type: string
          description: Commit message The commit message that should be used when merging the source branch.
        date_created:
          type: string
          description: Date created
        date_merged:
          type: string
          description: Date merged The date that the source branch was merged into the target branch
        date_review_requested:
          type: string
          description: Date review requested
        date_reviewed:
          type: string
          description: Date reviewed
        description:
          type: string
          description: Description A detailed description of the changes that are being addressed by the branch being proposed to be merged.
        merge_reporter_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Merge reporter The user that marked the branch as merged.
        merged_revision_id:
          type: string
          description: Merged revision ID The revision ID on the target branch which contains the merge from the source branch (currently Git only).
        merged_revno:
          type: string
          description: Merged revision number The revision number on the target branch which contains the merge from the source branch (Bazaar only).
        prerequisite_branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Prerequisite branch The branch that the source branch branched from. If this branch is the same as the target branch, then leave this field blank.
        prerequisite_git_path:
          type: string
          description: Prerequisite Git branch path The path of the Git branch that the source branch branched from. If this branch is the same as the target branch, then leave this field blank.
        prerequisite_git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Prerequisite Git repository The Git repository containing the branch that the source branch branched from. If this branch is the same as the target branch, then leave this field blank.
        preview_diff_link:
          type: string
          format: uri
          x-lp-resource-type: preview_diff
          description: The current diff of the source branch against the target branch.
        preview_diffs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: preview_diff-page-resource
          description: All preview diffs for this merge proposal.
        private:
          type: string
          description: Proposal is confidential If True, this proposal is visible only to subscribers.
        queue_status:
          type: string
          enum:
            - Work in progress
            - Needs review
            - Approved
            - Rejected
            - Merged
            - Code failed to merge
            - Queued
            - Superseded
          description: Status The current state of the proposal.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person who registered the merge proposal.
        reviewed_revid:
          type: string
          description: The revision id that has been approved by the reviewer.
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Review person or team The person that accepted (or rejected) the code for merging.
        source_branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Source branch The branch that has code to land.
        source_git_path:
          type: string
          description: Source Git branch path The path of the Git branch that has code to land.
        source_git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Source Git repository The Git repository that has code to land.
        superseded_by_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal
          description: Superseded by The branch merge proposal that supersedes this one.
        supersedes_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal
          description: Supersedes The branch merge proposal that this one supersedes.
        target_branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Target branch The branch that the source branch will be merged into.
        target_git_path:
          type: string
          description: Target Git branch path The path of the Git branch that the source branch will be merged into.
        target_git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Target Git repository The Git repository that the source branch will be merged into.
        votes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_vote_reference-page-resource
          description: The votes cast or expected for this proposal
      required:
        - address
        - all_comments_collection_link
        - bugs_collection_link
        - commit_message
        - date_created
        - date_merged
        - date_review_requested
        - date_reviewed
        - description
        - merge_reporter_link
        - merged_revision_id
        - merged_revno
        - prerequisite_branch_link
        - prerequisite_git_path
        - prerequisite_git_repository_link
        - preview_diff_link
        - preview_diffs_collection_link
        - private
        - queue_status
        - registrant_link
        - reviewed_revid
        - reviewer_link
        - source_branch_link
        - source_git_path
        - source_git_repository_link
        - superseded_by_link
        - supersedes_link
        - target_branch_link
        - target_git_path
        - target_git_repository_link
        - votes_collection_link
    branch_merge_proposal-diff:
      type: object
      properties:
        commit_message:
          type: string
          description: Commit message The commit message that should be used when merging the source branch.
        description:
          type: string
          description: Description A detailed description of the changes that are being addressed by the branch being proposed to be merged.
        reviewed_revid:
          type: string
          description: The revision id that has been approved by the reviewer.
    branch_merge_proposal-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    branch_subscription-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: branch_subscription
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch ID
        max_diff_lines:
          type: string
          enum:
            - Don't send diffs
            - 500 lines
            - 1000 lines
            - 5000 lines
            - Send entire diff
          description: Generated Diff Size Limit Diffs greater than the specified number of lines will not be sent to the subscriber. The subscriber will still receive an email with the new revision details even if the diff is larger than the specified number of lines.
        notification_level:
          type: string
          enum:
            - No email
            - Branch attribute notifications only
            - Branch revision notifications only
            - Branch attribute and revision notifications
          description: Notification Level Attribute notifications are sent when branch details are changed such as lifecycle status and name. Revision notifications are generated when new branch revisions are found due to the branch being updated through either pushes to the hosted branches or the mirrored branches being updated.
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person Enter the launchpad id, or email address of the person you wish to subscribe to this branch. If you are unsure, use the "Choose..." option to find the person in Launchpad. You can only subscribe someone who is a registered user of the system.
        review_level:
          type: string
          enum:
            - No email
            - Status changes only
            - Email about all changes
          description: Code review Level Control the kind of review activity that triggers notifications.
        subscribed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Subscribed by The person who created this subscription.
      required:
        - branch_link
        - max_diff_lines
        - notification_level
        - person_link
        - review_level
        - subscribed_by_link
    branch_subscription-diff:
      type: object
      properties:
        max_diff_lines:
          type: string
          enum:
            - Don't send diffs
            - 500 lines
            - 1000 lines
            - 5000 lines
            - Send entire diff
          description: Generated Diff Size Limit Diffs greater than the specified number of lines will not be sent to the subscriber. The subscriber will still receive an email with the new revision details even if the diff is larger than the specified number of lines.
        notification_level:
          type: string
          enum:
            - No email
            - Branch attribute notifications only
            - Branch revision notifications only
            - Branch attribute and revision notifications
          description: Notification Level Attribute notifications are sent when branch details are changed such as lifecycle status and name. Revision notifications are generated when new branch revisions are found due to the branch being updated through either pushes to the hosted branches or the mirrored branches being updated.
        review_level:
          type: string
          enum:
            - No email
            - Status changes only
            - Email about all changes
          description: Code review Level Control the kind of review activity that triggers notifications.
    branch_subscription-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_subscription-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_subscription-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_activity-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_activity
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: Bug
        datechanged:
          type: string
          description: Date Changed The date on which this activity occurred.
        message:
          type: string
          description: Message Additional information about what changed.
        newvalue:
          type: string
          description: New Value The value after the change.
        oldvalue:
          type: string
          description: Old Value The value before the change.
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person's Launchpad ID or email address.
        whatchanged:
          type: string
          description: What Changed The property of the bug that changed.
      required:
        - bug_link
        - datechanged
        - message
        - newvalue
        - oldvalue
        - person_link
        - whatchanged
    bug_activity-diff:
      type: object
    bug_activity-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_activity-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_activity-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_attachment-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_attachment
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: The bug the attachment belongs to.
        data_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: The attachment content.
        message_link:
          type: string
          format: uri
          x-lp-resource-type: message
          description: The message that was created when we added this attachment.
        title:
          type: string
          description: Title A short and descriptive description of the attachment
        type:
          type: string
          enum:
            - Patch
            - Unspecified
          description: Attachment Type The type of the attachment, for example Patch or Unspecified.
        url:
          type: string
          description: Attachment URL
      required:
        - bug_link
        - data_link
        - message_link
        - title
        - type
        - url
    bug_attachment-diff:
      type: object
      properties:
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: The bug the attachment belongs to.
        message_link:
          type: string
          format: uri
          x-lp-resource-type: message
          description: The message that was created when we added this attachment.
        title:
          type: string
          description: Title A short and descriptive description of the attachment
        type:
          type: string
          enum:
            - Patch
            - Unspecified
          description: Attachment Type The type of the attachment, for example Patch or Unspecified.
    bug_attachment-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_attachment-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_attachment-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_branch-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_branch
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: "Bug #"
      required:
        - branch_link
        - bug_link
    bug_branch-diff:
      type: object
      properties:
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch
    bug_branch-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_branch-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_branch-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        activity_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_activity-page-resource
          description: Log of activity that has occurred on this bug.
        attachments_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_attachment-page-resource
          description: List of bug attachments.
        bug_tasks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_task-page-resource
          description: BugTasks on this bug, sorted upstream, then by ubuntu, then by other distroseries.
        bug_watches_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_watch-page-resource
          description: All bug watches associated with this bug.
        cves_collection_link:
          type: string
          format: uri
          x-lp-resource-type: cve-page-resource
          description: CVE entries related to this bug.
        date_created:
          type: string
          description: Date Created
        date_last_message:
          type: string
          description: Date of last bug message
        date_last_updated:
          type: string
          description: Date Last Updated
        date_made_private:
          type: string
          description: Date Made Private
        description:
          type: string
          description: Description A detailed description of the problem, including the steps required to reproduce it.
        duplicate_of_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: Duplicate Of
        duplicates_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
          description: MultiJoin of bugs which are dupes of this one.
        heat:
          type: string
          description: The 'heat' of the bug
        id:
          type: string
          description: Bug ID
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of information contained in this bug report.
        latest_patch_uploaded:
          type: string
          description: Date when the most recent patch was uploaded.
        linked_branches_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_branch-page-resource
          description: Bazaar branches associated with this bug, usually branches on which this bug is being fixed.
        linked_merge_proposals_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Merge proposals associated with this bug (currently only Git-based merge proposals).
        lock_reason:
          type: string
          description: Lock Reason The reason for locking this bug.
        lock_status:
          type: string
          enum:
            - Unlocked
            - Comment-only
          description: Lock Status The lock status of this bug.
        message_count:
          type: string
          description: The number of comments on this bug
        messages_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message-page-resource
          description: The messages related to this object, in reverse order of creation (so newest first).
        name:
          type: string
          description: Nickname A short and unique name. Add one only if you often need to retype the URL but have trouble remembering the bug number.
        number_of_duplicates:
          type: string
          description: The number of bugs marked as duplicates of this bug
        other_users_affected_count_with_dupes:
          type: string
          description: The number of users affected by this bug (including duplicates), excluding the current user
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The owner's IPerson
        private:
          type: string
          description: This bug report should be private Private bug reports are visible only to their subscribers.
        security_related:
          type: string
          description: This bug is a security vulnerability.
        subscriptions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription-page-resource
          description: Subscriptions
        tags:
          type: string
          description: Tags Space-separated keywords for classifying this bug report.
        title:
          type: string
          description: Summary A one-line summary of the problem.
        users_affected_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: The number of users affected by this bug (not including duplicates)
        users_affected_count:
          type: string
          description: The number of users affected by this bug (not including duplicates)
        users_affected_count_with_dupes:
          type: string
          description: The number of users affected by this bug (including duplicates)
        users_affected_with_dupes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Users affected (including duplicates)
        users_unaffected_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Users explicitly marked as unaffected (not including duplicates)
        users_unaffected_count:
          type: string
          description: The number of users unaffected by this bug
        vulnerabilities_collection_link:
          type: string
          format: uri
          x-lp-resource-type: vulnerability-page-resource
          description: Vulnerability instances related to this bug.
        who_made_private_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Who Made Private The person who set this bug private.
      required:
        - activity_collection_link
        - attachments_collection_link
        - bug_tasks_collection_link
        - bug_watches_collection_link
        - cves_collection_link
        - date_created
        - date_last_message
        - date_last_updated
        - date_made_private
        - description
        - duplicate_of_link
        - duplicates_collection_link
        - heat
        - id
        - information_type
        - latest_patch_uploaded
        - linked_branches_collection_link
        - linked_merge_proposals_collection_link
        - lock_reason
        - lock_status
        - message_count
        - messages_collection_link
        - name
        - number_of_duplicates
        - other_users_affected_count_with_dupes
        - owner_link
        - private
        - security_related
        - subscriptions_collection_link
        - tags
        - title
        - users_affected_collection_link
        - users_affected_count
        - users_affected_count_with_dupes
        - users_affected_with_dupes_collection_link
        - users_unaffected_collection_link
        - users_unaffected_count
        - vulnerabilities_collection_link
        - who_made_private_link
    bug-diff:
      type: object
      properties:
        description:
          type: string
          description: Description A detailed description of the problem, including the steps required to reproduce it.
        duplicate_of_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: Duplicate Of
        lock_reason:
          type: string
          description: Lock Reason The reason for locking this bug.
        name:
          type: string
          description: Nickname A short and unique name. Add one only if you often need to retype the URL but have trouble remembering the bug number.
        private:
          type: string
          description: This bug report should be private Private bug reports are visible only to their subscribers.
        security_related:
          type: string
          description: This bug is a security vulnerability.
        tags:
          type: string
          description: Tags Space-separated keywords for classifying this bug report.
        title:
          type: string
          description: Summary A one-line summary of the problem.
    bug-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_link_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_link_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bugs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
          description: Bugs related to this object.
      required:
        - bugs_collection_link
    bug_link_target-diff:
      type: object
    bug_link_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_link_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_link_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_nomination-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_nomination
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
        date_created:
          type: string
          description: Date Submitted The date on which this nomination was submitted.
        date_decided:
          type: string
          description: Date Decided The date on which this nomination was approved or declined.
        decider_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Decided By
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Series
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Submitter
        productseries_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: Series
        status:
          type: string
          enum:
            - Nominated
            - Approved
            - Declined
          description: Status
        target_link:
          type: string
          format: uri
          x-lp-resource-type: bug_target
          description: The IProductSeries or IDistroSeries of this nomination.
      required:
        - bug_link
        - date_created
        - date_decided
        - decider_link
        - distroseries_link
        - owner_link
        - productseries_link
        - status
        - target_link
    bug_nomination-diff:
      type: object
      properties:
        target_link:
          type: string
          format: uri
          x-lp-resource-type: bug_target
          description: The IProductSeries or IDistroSeries of this nomination.
    bug_nomination-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_nomination-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_nomination-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_subscription-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: Bug
        bug_notification_level:
          type: string
          enum:
            - Lifecycle
            - Details
            - Discussion
          description: Bug notification level The volume and type of bug notifications this subscription will generate.
        date_created:
          type: string
          description: Date subscribed
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person's Launchpad ID or email address.
        subscribed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Subscribed by The person who created this subscription.
      required:
        - bug_link
        - bug_notification_level
        - date_created
        - person_link
        - subscribed_by_link
    bug_subscription-diff:
      type: object
      properties:
        bug_notification_level:
          type: string
          enum:
            - Lifecycle
            - Details
            - Discussion
          description: Bug notification level The volume and type of bug notifications this subscription will generate.
    bug_subscription-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_subscription_filter-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription_filter
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_notification_level:
          type: string
          enum:
            - Lifecycle
            - Details
            - Discussion
          description: Bug notification level The volume and type of bug notifications this subscription will generate.
        description:
          type: string
          description: A short description of this filter
        find_all_tags:
          type: string
          description: Find all tags If enabled, all tags must match, else at least one tag must match.
        importances:
          type: string
          description: The importances interested in (empty for all)
        information_types:
          type: string
          description: The information types interested in (empty for all)
        statuses:
          type: string
          description: The statuses interested in (empty for all)
        structural_subscription_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription
          description: Structural subscription
        tags:
          type: string
          description: The tags interested in
      required:
        - bug_notification_level
        - description
        - find_all_tags
        - importances
        - information_types
        - statuses
        - structural_subscription_link
        - tags
    bug_subscription_filter-diff:
      type: object
      properties:
        bug_notification_level:
          type: string
          enum:
            - Lifecycle
            - Details
            - Discussion
          description: Bug notification level The volume and type of bug notifications this subscription will generate.
        description:
          type: string
          description: A short description of this filter
        find_all_tags:
          type: string
          description: Find all tags If enabled, all tags must match, else at least one tag must match.
        importances:
          type: string
          description: The importances interested in (empty for all)
        information_types:
          type: string
          description: The information types interested in (empty for all)
        statuses:
          type: string
          description: The statuses interested in (empty for all)
        tags:
          type: string
          description: The tags interested in
    bug_subscription_filter-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription_filter-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription_filter-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
      required:
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - content_templates
    bug_target-diff:
      type: object
      properties:
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
    bug_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_task-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_task
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        assignee_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Assigned to
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: Bug
        bug_target_display_name:
          type: string
          description: The short, descriptive name of the target
        bug_target_name:
          type: string
          description: The target as presented in mail notifications
        bug_watch_link:
          type: string
          format: uri
          x-lp-resource-type: bug_watch
          description: Remote Bug Details Select the bug watch that represents this task in the relevant bug tracker. If none of the bug watches represents this particular bug task, leave it as (None). Linking the remote bug watch with the task in this way means that a change in the remote bug status will change the status of this bug task in Launchpad.
        date_assigned:
          type: string
          description: Date Assigned The date on which this task was assigned to someone.
        date_closed:
          type: string
          description: Date Closed The date on which this task was marked Fix Released, Invalid, Won't Fix, Expired or Opinion.
        date_confirmed:
          type: string
          description: Date Confirmed The date on which this task was marked Confirmed.
        date_created:
          type: string
          description: Date Created The date on which this task was created.
        date_deferred:
          type: string
          description: Date deferred The date on which this task was deferred.
        date_fix_committed:
          type: string
          description: Date Fix Committed The date on which this task was marked Fix Committed.
        date_fix_released:
          type: string
          description: Date Fix Released The date on which this task was marked Fix Released.
        date_in_progress:
          type: string
          description: Date In Progress The date on which this task was marked In Progress.
        date_incomplete:
          type: string
          description: Date Incomplete The date on which this task was marked Incomplete.
        date_left_closed:
          type: string
          description: Date left closed The date on which this task was last reopened.
        date_left_new:
          type: string
          description: Date left new The date on which this task was marked with a status higher than New.
        date_triaged:
          type: string
          description: Date Triaged The date on which this task was marked Triaged.
        importance:
          type: string
          enum:
            - Unknown
            - Undecided
            - Critical
            - High
            - Medium
            - Low
            - Wishlist
          description: Importance
        importance_explanation:
          type: string
          description: Importance explanation The explanation for the current importance of this bugtask.
        is_complete:
          type: string
          description: True or False depending on whether or not there is more work required on this bug task.
        milestone_link:
          type: string
          format: uri
          x-lp-resource-type: milestone
          description: Milestone
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The owner
        related_tasks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_task-page-resource
          description: IBugTasks related to this one, namely other IBugTasks on the same IBug.
        status:
          type: string
          enum:
            - New
            - Incomplete
            - Opinion
            - Invalid
            - Won't Fix
            - Expired
            - Confirmed
            - Triaged
            - In Progress
            - Deferred
            - Fix Committed
            - Fix Released
            - Does Not Exist
            - Unknown
          description: Status
        status_explanation:
          type: string
          description: Status explanation The explanation for the current status of this bugtask.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: bug_target
          description: Target The software in which this bug should be fixed.
        title:
          type: string
          description: The title of the bug related to this bugtask
      required:
        - assignee_link
        - bug_link
        - bug_target_display_name
        - bug_target_name
        - bug_watch_link
        - date_assigned
        - date_closed
        - date_confirmed
        - date_created
        - date_deferred
        - date_fix_committed
        - date_fix_released
        - date_in_progress
        - date_incomplete
        - date_left_closed
        - date_left_new
        - date_triaged
        - importance
        - importance_explanation
        - is_complete
        - milestone_link
        - owner_link
        - related_tasks_collection_link
        - status
        - status_explanation
        - target_link
        - title
    bug_task-diff:
      type: object
      properties:
        assignee_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Assigned to
        bug_watch_link:
          type: string
          format: uri
          x-lp-resource-type: bug_watch
          description: Remote Bug Details Select the bug watch that represents this task in the relevant bug tracker. If none of the bug watches represents this particular bug task, leave it as (None). Linking the remote bug watch with the task in this way means that a change in the remote bug status will change the status of this bug task in Launchpad.
        importance:
          type: string
          enum:
            - Unknown
            - Undecided
            - Critical
            - High
            - Medium
            - Low
            - Wishlist
          description: Importance
        importance_explanation:
          type: string
          description: Importance explanation The explanation for the current importance of this bugtask.
        milestone_link:
          type: string
          format: uri
          x-lp-resource-type: milestone
          description: Milestone
        status:
          type: string
          enum:
            - New
            - Incomplete
            - Opinion
            - Invalid
            - Won't Fix
            - Expired
            - Confirmed
            - Triaged
            - In Progress
            - Deferred
            - Fix Committed
            - Fix Released
            - Does Not Exist
            - Unknown
          description: Status
        status_explanation:
          type: string
          description: Status explanation The explanation for the current status of this bugtask.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: bug_target
          description: Target The software in which this bug should be fixed.
    bug_task-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_task-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_task-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_tracker_component-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        component_group_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component_group
          description: Component Group
        distro_source_package_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_source_package
          description: Distribution Source Package The distribution source package object that should be linked to this component.
        is_visible:
          type: string
          description: Is Visible? Should the component be shown in the Launchpad web interface?
        name:
          type: string
          description: Name The name of a software component as shown in Launchpad.
      required:
        - component_group_link
        - distro_source_package_link
        - is_visible
        - name
    bug_tracker_component-diff:
      type: object
      properties:
        component_group_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component_group
          description: Component Group
        distro_source_package_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_source_package
          description: Distribution Source Package The distribution source package object that should be linked to this component.
        is_visible:
          type: string
          description: Is Visible? Should the component be shown in the Launchpad web interface?
        name:
          type: string
          description: Name The name of a software component as shown in Launchpad.
    bug_tracker_component-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_tracker_component_group-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component_group
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: BugTracker
        components_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component-page-resource
          description: Components.
        name:
          type: string
          description: Name The name of the bug tracker product.
      required:
        - bug_tracker_link
        - components_collection_link
        - name
    bug_tracker_component_group-diff:
      type: object
      properties:
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: BugTracker
        name:
          type: string
          description: Name The name of the bug tracker product.
    bug_tracker_component_group-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component_group-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker_component_group-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_tracker-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Updates for this bug tracker are enabled
        base_url:
          type: string
          description: Location The top-level URL for the bug tracker, or an upstream email address. This must be accurate so that Launchpad can link to external bug reports.
        base_url_aliases:
          type: string
          description: Location aliases A list of URLs or email addresses that all lead to the same bug tracker, or commonly seen typos, separated by whitespace.
        bug_tracker_type:
          type: string
          enum:
            - Bugzilla
            - Debbugs
            - Roundup
            - Trac
            - SourceForge or SourceForge derivative
            - Mantis
            - Request Tracker (RT)
            - Email Address
            - Savane
            - PHP Project Bugtracker
            - Google Code
            - GitHub Issues
            - GitLab Issues
          description: Bug Tracker Type
        contact_details:
          type: string
          description: Contact details The contact details for the external bug tracker (so that, for example, its administrators can be contacted about a security breach).
        has_lp_plugin:
          type: string
          description: This bug tracker has a Launchpad plugin installed.
        name:
          type: string
          description: Name A URL-friendly name for the bug tracker, such as "mozilla-bugs".
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
        summary:
          type: string
          description: Summary A brief introduction or overview of this bug tracker instance.
        title:
          type: string
          description: Title A descriptive label for this tracker to show in listings.
        watches_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_watch-page-resource
          description: The remote watches on this bug tracker.
      required:
        - active
        - base_url
        - base_url_aliases
        - bug_tracker_type
        - contact_details
        - has_lp_plugin
        - name
        - registrant_link
        - summary
        - title
        - watches_collection_link
    bug_tracker-diff:
      type: object
      properties:
        active:
          type: string
          description: Updates for this bug tracker are enabled
        base_url:
          type: string
          description: Location The top-level URL for the bug tracker, or an upstream email address. This must be accurate so that Launchpad can link to external bug reports.
        base_url_aliases:
          type: string
          description: Location aliases A list of URLs or email addresses that all lead to the same bug tracker, or commonly seen typos, separated by whitespace.
        bug_tracker_type:
          type: string
          enum:
            - Bugzilla
            - Debbugs
            - Roundup
            - Trac
            - SourceForge or SourceForge derivative
            - Mantis
            - Request Tracker (RT)
            - Email Address
            - Savane
            - PHP Project Bugtracker
            - Google Code
            - GitHub Issues
            - GitLab Issues
          description: Bug Tracker Type
        contact_details:
          type: string
          description: Contact details The contact details for the external bug tracker (so that, for example, its administrators can be contacted about a security breach).
        has_lp_plugin:
          type: string
          description: This bug tracker has a Launchpad plugin installed.
        name:
          type: string
          description: Name A URL-friendly name for the bug tracker, such as "mozilla-bugs".
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
        summary:
          type: string
          description: Summary A brief introduction or overview of this bug tracker instance.
        title:
          type: string
          description: Title A descriptive label for this tracker to show in listings.
    bug_tracker-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    bug_watch-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: bug_watch
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_link:
          type: string
          format: uri
          x-lp-resource-type: bug
          description: Bug
        bug_tasks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_task-page-resource
          description: The tasks which this watch will affect. In Launchpad, a bug watch can be linked to one or more tasks, and if it is linked and we notice a status change in the watched bug then we will try to update the Launchpad bug task accordingly.
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: Bug System You can register new bug trackers from the Launchpad Bugs home page.
        date_created:
          type: string
          description: Date Created
        date_last_changed:
          type: string
          description: Last Changed
        date_last_checked:
          type: string
          description: Last Checked
        date_next_checked:
          type: string
          description: Next Check
        last_error_type:
          type: string
          enum:
            - Unknown
            - Bug Not Found
            - Connection Error
            - Invalid Bug ID
            - Timeout
            - Unparsable Bug
            - Unparsable Bug Tracker Version
            - Unsupported Bugtracker
            - Private Remote Bug
            - Synchronisation succeeded
            - Unable to import comments
            - Unable to push comments
            - Unable to set link remote bug to Launchpad
          description: Last Error Type
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
        remote_bug:
          type: string
          description: Remote Bug The bug number of this bug in the remote bug tracker.
        remote_importance:
          type: string
          description: Remote Importance
        remote_status:
          type: string
          description: Remote Status
        title:
          type: string
          description: Bug watch title
        url:
          type: string
          description: The URL at which to view the remote bug.
      required:
        - bug_link
        - bug_tasks_collection_link
        - bug_tracker_link
        - date_created
        - date_last_changed
        - date_last_checked
        - date_next_checked
        - last_error_type
        - owner_link
        - remote_bug
        - remote_importance
        - remote_status
        - title
        - url
    bug_watch-diff:
      type: object
      properties:
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: Bug System You can register new bug trackers from the Launchpad Bugs home page.
        date_last_changed:
          type: string
          description: Last Changed
        date_last_checked:
          type: string
          description: Last Checked
        date_next_checked:
          type: string
          description: Next Check
        last_error_type:
          type: string
          enum:
            - Unknown
            - Bug Not Found
            - Connection Error
            - Invalid Bug ID
            - Timeout
            - Unparsable Bug
            - Unparsable Bug Tracker Version
            - Unsupported Bugtracker
            - Private Remote Bug
            - Synchronisation succeeded
            - Unable to import comments
            - Unable to push comments
            - Unable to set link remote bug to Launchpad
          description: Last Error Type
        remote_bug:
          type: string
          description: Remote Bug The bug number of this bug in the remote bug tracker.
        remote_importance:
          type: string
          description: Remote Importance
        remote_status:
          type: string
          description: Remote Status
    bug_watch-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_watch-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_watch-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    build_farm_job-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: build_farm_job
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        title:
          type: string
          description: Title
      required:
        - build_log_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - duration
        - title
    build_farm_job-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        title:
          type: string
          description: Title
    build_farm_job-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: build_farm_job-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: build_farm_job-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    builder-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Publicly Visible Whether or not to present the builder publicly.
        builderok:
          type: string
          description: Builder State OK Whether or not the builder is ok
        clean_status:
          type: string
          enum:
            - Clean
            - Dirty
            - Cleaning
          description: Clean status The readiness of the builder to take a job. Only internal build infrastructure bots need to or should write to this.
        current_build_link:
          type: string
          format: uri
          x-lp-resource-type: build_farm_job
          description: Current build The job currently running on this builder.
        date_clean_status_changed:
          type: string
          description: Date clean status changed The date the builder's clean status last changed.
        failnotes:
          type: string
          description: Failure Notes The reason for a builder not being ok
        failure_count:
          type: string
          description: Failure Count Number of consecutive failures for this builder.
        manual:
          type: string
          description: Manual Mode The auto-build system does not dispatch jobs automatically for builders in manual mode.
        name:
          type: string
          description: Name The builder name used for reference purposes
        open_resources:
          type: string
          description: Open resources Resource tags offered by this builder, that can be required by a build and if required must match.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner Builder owner, a Launchpad member which will be responsible for this device.
        processor_link:
          type: string
          format: uri
          x-lp-resource-type: processor
          description: "Processor DEPRECATED: Processor identifying jobs which can be built by this device. Use processors instead to handle multiple supported architectures."
        processors:
          type: string
          description: Processors Processors identifying jobs which can be built by this device.
        restricted_resources:
          type: string
          description: Restricted resources Resource tags offered by this builder, indicating that the builder may only be used by builds that explicitly require these tags.
        title:
          type: string
          description: Title The builder title. Should be just a few words.
        url:
          type: string
          description: "URL The URL to the build machine, used as a unique identifier. Includes protocol, host and port only, e.g.: http://farm.com:8221/"
        version:
          type: string
          description: Version The version of launchpad-buildd on the worker.
        virtualized:
          type: string
          description: Virtualized Whether or not the builder is a virtual Xen instance.
        vm_host:
          type: string
          description: "VM host The machine hostname hosting the virtual buildd-worker, e.g.: foobar-host.ppa"
        vm_reset_protocol:
          type: string
          enum:
            - "1.1"
            - "2.0"
          description: VM reset protocol The protocol version for resetting the VM.
      required:
        - active
        - builderok
        - clean_status
        - current_build_link
        - date_clean_status_changed
        - failnotes
        - failure_count
        - manual
        - name
        - open_resources
        - owner_link
        - processor_link
        - processors
        - restricted_resources
        - title
        - url
        - version
        - virtualized
        - vm_host
        - vm_reset_protocol
    builder-diff:
      type: object
      properties:
        active:
          type: string
          description: Publicly Visible Whether or not to present the builder publicly.
        builderok:
          type: string
          description: Builder State OK Whether or not the builder is ok
        clean_status:
          type: string
          enum:
            - Clean
            - Dirty
            - Cleaning
          description: Clean status The readiness of the builder to take a job. Only internal build infrastructure bots need to or should write to this.
        failnotes:
          type: string
          description: Failure Notes The reason for a builder not being ok
        failure_count:
          type: string
          description: Failure Count Number of consecutive failures for this builder.
        manual:
          type: string
          description: Manual Mode The auto-build system does not dispatch jobs automatically for builders in manual mode.
        name:
          type: string
          description: Name The builder name used for reference purposes
        open_resources:
          type: string
          description: Open resources Resource tags offered by this builder, that can be required by a build and if required must match.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner Builder owner, a Launchpad member which will be responsible for this device.
        processor_link:
          type: string
          format: uri
          x-lp-resource-type: processor
          description: "Processor DEPRECATED: Processor identifying jobs which can be built by this device. Use processors instead to handle multiple supported architectures."
        processors:
          type: string
          description: Processors Processors identifying jobs which can be built by this device.
        restricted_resources:
          type: string
          description: Restricted resources Resource tags offered by this builder, indicating that the builder may only be used by builds that explicitly require these tags.
        title:
          type: string
          description: Title The builder title. Should be just a few words.
        url:
          type: string
          description: "URL The URL to the build machine, used as a unique identifier. Includes protocol, host and port only, e.g.: http://farm.com:8221/"
        version:
          type: string
          description: Version The version of launchpad-buildd on the worker.
        virtualized:
          type: string
          description: Virtualized Whether or not the builder is a virtual Xen instance.
        vm_host:
          type: string
          description: "VM host The machine hostname hosting the virtual buildd-worker, e.g.: foobar-host.ppa"
        vm_reset_protocol:
          type: string
          enum:
            - "1.1"
            - "2.0"
          description: VM reset protocol The protocol version for resetting the VM.
    builder-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: builder-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: builder-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    ci_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: ci_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        arch_tag:
          type: string
          description: Architecture tag
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The Archive context for this build.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        commit_sha1:
          type: string
          description: The Git commit ID for this CI build.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture that this CI build should run on.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        git_refs:
          type: string
          description: The git references that originated this CI Build.
        git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: The Git repository for this CI build.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        results:
          type: string
          description: A mapping from job IDs to result tokens, retrieved from the builder.
        score:
          type: string
          description: Score of the related build farm job (if any).
        stages:
          type: string
          description: A list of stages in this build's configured pipeline.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - arch_tag
        - archive_link
        - build_log_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - commit_sha1
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_arch_series_link
        - distro_series_link
        - duration
        - git_refs
        - git_repository_link
        - pocket
        - results
        - score
        - stages
        - title
        - upload_log_url
    ci_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        results:
          type: string
          description: A mapping from job IDs to result tokens, retrieved from the builder.
        stages:
          type: string
          description: A list of stages in this build's configured pipeline.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    ci_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: ci_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: ci_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    charm_base-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: charm_base
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        build_snap_channels:
          type: string
          description: Source snap channels for builds A dictionary mapping snap names to channels to use when building charm recipes that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
        date_created:
          type: string
          description: Date created
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro series
        processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
          description: Processors The architectures that the charm base supports.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this base.
      required:
        - build_snap_channels
        - date_created
        - distro_series_link
        - processors_collection_link
        - registrant_link
    charm_base-diff:
      type: object
      properties:
        build_snap_channels:
          type: string
          description: Source snap channels for builds A dictionary mapping snap names to channels to use when building charm recipes that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
    charm_base-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_base-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_base-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    charm_recipe_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        arch_tag:
          type: string
          description: Architecture tag
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The Archive context for this build.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        build_metadata_url:
          type: string
          description: URL of the build metadata file URL of the metadata file generated by the fetch service, if it exists.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        channels:
          type: string
          description: "Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Supported snap names: 'charmcraft', 'core', 'core18', 'core20', 'core22', 'core24', 'core26'"
        craft_platform:
          type: string
          description: Craft platform name
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture for which to build.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe
          description: The charm recipe to build.
        requester_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who requested this build.
        revision_id:
          type: string
          description: Revision ID The revision ID of the branch used for this build, if available.
        score:
          type: string
          description: Score of the related build farm job (if any).
        store_upload_error_message:
          type: string
          description: Store upload error message The error message, if any, from the last attempt to upload this charm recipe build to Charmhub.
        store_upload_revision:
          type: string
          description: Store revision The revision assigned to this charm recipe build by Charmhub.
        store_upload_status:
          type: string
          enum:
            - Unscheduled
            - Pending
            - Failed to upload
            - Failed to release to channels
            - Uploaded
          description: Store upload status
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - arch_tag
        - archive_link
        - build_log_url
        - build_metadata_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - channels
        - craft_platform
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_arch_series_link
        - distro_series_link
        - duration
        - pocket
        - recipe_link
        - requester_link
        - revision_id
        - score
        - store_upload_error_message
        - store_upload_revision
        - store_upload_status
        - title
        - upload_log_url
    charm_recipe_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        channels:
          type: string
          description: "Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Supported snap names: 'charmcraft', 'core', 'core18', 'core20', 'core22', 'core24', 'core26'"
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        store_upload_status:
          type: string
          enum:
            - Unscheduled
            - Pending
            - Failed to upload
            - Failed to release to channels
            - Uploaded
          description: Store upload status
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    charm_recipe_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    charm_recipe_build_request-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build_request
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build-page-resource
          description: Builds produced by this request
        date_finished:
          type: string
          description: The time when this request finished
        date_requested:
          type: string
          description: The time when this request was made
        error_message:
          type: string
          description: Error message
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe
          description: Charm recipe
        status:
          type: string
          enum:
            - Pending
            - Failed
            - Completed
          description: Status
      required:
        - builds_collection_link
        - date_finished
        - date_requested
        - error_message
        - recipe_link
        - status
    charm_recipe_build_request-diff:
      type: object
    charm_recipe_build_request-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build_request-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build_request-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    charm_recipe-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this charm recipe is built automatically when its branch changes.
        auto_build_channels:
          type: string
          description: "Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this charm recipe. Supported snap names: 'charmcraft', 'core', 'core18', 'core20', 'core22', 'core24', 'core26'"
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing metadata.yaml.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build-page-resource
          description: All builds of this charm recipe. All builds of this charm recipe, sorted in descending order of finishing (or starting if not completed successfully).
        can_upload_to_store:
          type: string
          description: Can upload to Charmhub Whether everything is set up to allow uploading builds of this charm recipe to Charmhub.
        completed_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build-page-resource
          description: Completed builds of this charm recipe. Completed builds of this charm recipe, sorted in descending order of finishing.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        description:
          type: string
          description: Description A description of the charm recipe.
        failed_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build_request-page-resource
          description: Failed build requests for this charm recipe.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False. The “strict” mode only allows certain resources and formats, and errors out in any case the restrictions are violated. The “permissive” mode works similarly, but only logs a warning when encountering any violations.
        git_path:
          type: string
          description: Git branch path The path of the Git branch containing a charm recipe.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a charm recipe.
        git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Git repository A Git repository with a branch containing a charm recipe.
        git_repository_url:
          type: string
          description: Git repository URL The URL of a Git repository with a branch containing a charmcraft.yaml at the top level.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this charm recipe.
        is_stale:
          type: string
          description: Charm recipe is stale and is due to be rebuilt.
        name:
          type: string
          description: Charm recipe name The name of the charm recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this charm recipe.
        pending_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build_request-page-resource
          description: Pending build requests for this charm recipe.
        pending_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe_build-page-resource
          description: Pending builds of this charm recipe. Pending builds of this charm recipe, sorted in descending order of creation.
        private:
          type: string
          description: Private Whether this charm recipe is private.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this charm recipe is associated with
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this charm recipe.
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this charm recipe on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this charm to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store name The registered name of this charm in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this charm recipe are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Charm builds will use the fetch-service instead of the builder-proxy to access external resources.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - auto_build
        - auto_build_channels
        - build_path
        - builds_collection_link
        - can_upload_to_store
        - completed_builds_collection_link
        - date_created
        - date_last_modified
        - description
        - failed_build_requests_collection_link
        - fetch_service_policy
        - git_path
        - git_ref_link
        - git_repository_link
        - git_repository_url
        - information_type
        - is_stale
        - name
        - owner_link
        - pending_build_requests_collection_link
        - pending_builds_collection_link
        - private
        - project_link
        - registrant_link
        - require_virtualized
        - store_channels
        - store_name
        - store_upload
        - use_fetch_service
        - webhooks_collection_link
    charm_recipe-diff:
      type: object
      properties:
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this charm recipe is built automatically when its branch changes.
        auto_build_channels:
          type: string
          description: "Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this charm recipe. Supported snap names: 'charmcraft', 'core', 'core18', 'core20', 'core22', 'core24', 'core26'"
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing metadata.yaml.
        description:
          type: string
          description: Description A description of the charm recipe.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False. The “strict” mode only allows certain resources and formats, and errors out in any case the restrictions are violated. The “permissive” mode works similarly, but only logs a warning when encountering any violations.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a charm recipe.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this charm recipe.
        name:
          type: string
          description: Charm recipe name The name of the charm recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this charm recipe.
        private:
          type: string
          description: Private Whether this charm recipe is private.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this charm recipe is associated with
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this charm recipe on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this charm to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store name The registered name of this charm in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this charm recipe are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Charm builds will use the fetch-service instead of the builder-proxy to access external resources.
    charm_recipe-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: charm_recipe-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    code_import-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: code_import
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch The Bazaar branch produced by the import system.
        cvs_module:
          type: string
          description: Module The path to import within the repository. Usually, it is the name of the project.
        cvs_root:
          type: string
          description: "Repository The CVSROOT. Example: :pserver:anonymous&#64;anoncvs.gnome.org:/cvs/gnome"
        date_last_successful:
          type: string
          description: Last successful
        git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Git repository The Git repository produced by the import system.
        rcs_type:
          type: string
          enum:
            - Concurrent Versions System
            - Subversion via CSCVS
            - Subversion via bzr-svn
            - Git
            - Mercurial
            - Bazaar
          description: Type of RCS The revision control system to import from.
        review_status:
          type: string
          enum:
            - Pending Review
            - Invalid
            - Reviewed
            - Suspended
            - Failed
          description: Review Status Only reviewed imports are processed.
        target_rcs_type:
          type: string
          enum:
            - Bazaar
            - Git
          description: Type of target RCS The revision control system to import to.
        url:
          type: string
          description: URL The URL of the VCS branch.
      required:
        - branch_link
        - cvs_module
        - cvs_root
        - date_last_successful
        - git_repository_link
        - rcs_type
        - review_status
        - target_rcs_type
        - url
    code_import-diff:
      type: object
      properties:
        url:
          type: string
          description: URL The URL of the VCS branch.
    code_import-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_import-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_import-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    code_review_comment-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_comment
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        as_quoted_email:
          type: string
          description: The message as quoted in email.
        author_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Comment Author
        branch_merge_proposal_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal
          description: The branch merge proposal
        content:
          type: string
          description: All the text/plain chunks joined together as a unicode string.
        date_created:
          type: string
          description: Date Created
        date_deleted:
          type: string
          description: When this message was deleted
        date_last_edited:
          type: string
          description: When this message was last edited
        id:
          type: string
          description: DB ID The tracking number for this comment.
        message_body:
          type: string
          description: Deprecated. Use "content" attribute instead.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person
        revisions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message_revision-page-resource
          description: Message revision history Revision history of this message, sorted in ascending order.
        title:
          type: string
          description: The title of the comment
        vote:
          type: string
          enum:
            - Approve
            - Needs Fixing
            - Needs Information
            - Abstain
            - Disapprove
            - Needs Resubmitting
          description: Review
        vote_tag:
          type: string
          description: Vote tag
      required:
        - as_quoted_email
        - author_link
        - branch_merge_proposal_link
        - content
        - date_created
        - date_deleted
        - date_last_edited
        - id
        - message_body
        - owner_link
        - revisions_collection_link
        - title
        - vote
        - vote_tag
    code_review_comment-diff:
      type: object
      properties:
        title:
          type: string
          description: The title of the comment
        vote:
          type: string
          enum:
            - Approve
            - Needs Fixing
            - Needs Information
            - Abstain
            - Disapprove
            - Needs Resubmitting
          description: Review
        vote_tag:
          type: string
          description: Vote tag
    code_review_comment-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_comment-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_comment-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    code_review_vote_reference-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_vote_reference
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        branch_merge_proposal_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal
          description: The merge proposal that is the subject of this vote
        comment_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_comment
          description: The code review comment that contains the most recent vote.
        date_created:
          type: string
          description: Date Created
        is_pending:
          type: string
          description: Is the pending?
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who originally registered this vote
        review_type:
          type: string
          description: Review type Lowercase keywords describing the type of review.
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Reviewer A person who you want to review this.
      required:
        - branch_merge_proposal_link
        - comment_link
        - date_created
        - is_pending
        - registrant_link
        - review_type
        - reviewer_link
    code_review_vote_reference-diff:
      type: object
      properties:
        branch_merge_proposal_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal
          description: The merge proposal that is the subject of this vote
        comment_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_comment
          description: The code review comment that contains the most recent vote.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who originally registered this vote
        review_type:
          type: string
          description: Review type Lowercase keywords describing the type of review.
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Reviewer A person who you want to review this.
    code_review_vote_reference-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_vote_reference-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: code_review_vote_reference-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    commercial_subscription-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: commercial_subscription
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_created:
          type: string
          description: Date Created The date the first subscription was applied.
        date_expires:
          type: string
          description: Expiration Date The expiration date of the subscription.
        date_last_modified:
          type: string
          description: Date Modified The date the subscription was modified.
        date_starts:
          type: string
          description: Beginning of Subscription The date the subscription starts.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution which has commercial subscription Distribution for which this commercial subscription is applied.
        is_active:
          type: string
          description: Active Whether this subscription is active.
        product_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: Product which has commercial subscription Project for which this commercial subscription is applied.
        purchaser_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Purchaser Person who purchased the voucher.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant Person who redeemed the voucher.
      required:
        - date_created
        - date_expires
        - date_last_modified
        - date_starts
        - distribution_link
        - is_active
        - product_link
        - purchaser_link
        - registrant_link
    commercial_subscription-diff:
      type: object
      properties:
        date_expires:
          type: string
          description: Expiration Date The expiration date of the subscription.
        date_last_modified:
          type: string
          description: Date Modified The date the subscription was modified.
        date_starts:
          type: string
          description: Beginning of Subscription The date the subscription starts.
    commercial_subscription-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: commercial_subscription-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: commercial_subscription-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    country-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: country
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        description:
          type: string
          description: Description
        iso3166code2:
          type: string
          description: iso3166code2
        iso3166code3:
          type: string
          description: iso3166code3
        name:
          type: string
          description: Country name
        title:
          type: string
          description: Country title
      required:
        - description
        - iso3166code2
        - iso3166code3
        - name
        - title
    country-diff:
      type: object
      properties:
        description:
          type: string
          description: Description
        name:
          type: string
          description: Country name
        title:
          type: string
          description: Country title
    country-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: country-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: country-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    craft_recipe_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        arch_tag:
          type: string
          description: Architecture tag
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The Archive context for this build.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        build_metadata_url:
          type: string
          description: URL of the build metadata file URL of the metadata file generated by the fetch service, if it exists.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        channels:
          type: string
          description: Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Currently only 'core', 'core18', 'core20', and 'sourcecraft' keys are supported.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture for which to build.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe
          description: The craft recipe to build.
        requester_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who requested this build.
        revision_id:
          type: string
          description: Revision ID The revision ID of the branch used for this build, if available.
        score:
          type: string
          description: Score of the related build farm job (if any).
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - arch_tag
        - archive_link
        - build_log_url
        - build_metadata_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - channels
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_arch_series_link
        - distro_series_link
        - duration
        - pocket
        - recipe_link
        - requester_link
        - revision_id
        - score
        - title
        - upload_log_url
    craft_recipe_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        channels:
          type: string
          description: Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Currently only 'core', 'core18', 'core20', and 'sourcecraft' keys are supported.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    craft_recipe_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    craft_recipe_build_request-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build_request
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build-page-resource
          description: Builds produced by this request
        date_finished:
          type: string
          description: The time when this request finished
        date_requested:
          type: string
          description: The time when this request was made
        error_message:
          type: string
          description: Error message
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe
          description: Craft recipe
        status:
          type: string
          enum:
            - Pending
            - Failed
            - Completed
          description: Status
      required:
        - builds_collection_link
        - date_finished
        - date_requested
        - error_message
        - recipe_link
        - status
    craft_recipe_build_request-diff:
      type: object
    craft_recipe_build_request-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build_request-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build_request-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    craft_recipe-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this craft recipe is built automatically when the branch containing its craft.yaml recipe changes.
        auto_build_channels:
          type: string
          description: Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this craft recipe. Currently only 'core', 'core18', 'core20', and 'sourcecraft' keys are supported.
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing craft.yaml.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build-page-resource
          description: All builds of this craft recipe. All builds of this craft recipe, sorted in descending order of finishing (or starting if not completed successfully).
        can_upload_to_store:
          type: string
          description: Can upload to the CraftStore Whether everything is set up to allow uploading builds of this craftrecipe to the CraftStore.
        completed_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build-page-resource
          description: Completed builds of this craft recipe. Completed builds of this craft recipe, sorted in descending order of finishing.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        description:
          type: string
          description: Description A description of the craft recipe.
        failed_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build_request-page-resource
          description: Failed build requests for this craft recipe.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False.
        git_path:
          type: string
          description: Git branch path The path of the Git branch containing a sourcecraft.yaml recipe.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a craft.yaml recipe.
        git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Git repository A Git repository with a branch containing a sourcecraft.yaml recipe.
        git_repository_url:
          type: string
          description: Git repository URL The URL of a Git repository with a branch containing a sourcecraft.yaml at the top level.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this craft recipe.
        is_stale:
          type: string
          description: Craft recipe is stale and is due to be rebuilt.
        name:
          type: string
          description: Craft recipe name The name of the craft recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this craft recipe.
        pending_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build_request-page-resource
          description: Pending build requests for this craft recipe.
        pending_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe_build-page-resource
          description: Pending builds of this craft recipe. Pending builds of this craft recipe, sorted in descending order of creation.
        private:
          type: string
          description: Private Whether this craft recipe is private.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this craft recipe is associated with
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this craft recipe.
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this craft recipe on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this craft to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store name The registered name of this craft in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this craft recipe are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Craft builds will use the fetch-service instead of the builder-proxy to access external resources.
      required:
        - auto_build
        - auto_build_channels
        - build_path
        - builds_collection_link
        - can_upload_to_store
        - completed_builds_collection_link
        - date_created
        - date_last_modified
        - description
        - failed_build_requests_collection_link
        - fetch_service_policy
        - git_path
        - git_ref_link
        - git_repository_link
        - git_repository_url
        - information_type
        - is_stale
        - name
        - owner_link
        - pending_build_requests_collection_link
        - pending_builds_collection_link
        - private
        - project_link
        - registrant_link
        - require_virtualized
        - store_channels
        - store_name
        - store_upload
        - use_fetch_service
    craft_recipe-diff:
      type: object
      properties:
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this craft recipe is built automatically when the branch containing its craft.yaml recipe changes.
        auto_build_channels:
          type: string
          description: Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this craft recipe. Currently only 'core', 'core18', 'core20', and 'sourcecraft' keys are supported.
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing craft.yaml.
        description:
          type: string
          description: Description A description of the craft recipe.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a craft.yaml recipe.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this craft recipe.
        name:
          type: string
          description: Craft recipe name The name of the craft recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this craft recipe.
        private:
          type: string
          description: Private Whether this craft recipe is private.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this craft recipe is associated with
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this craft recipe on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this craft to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store name The registered name of this craft in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this craft recipe are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Craft builds will use the fetch-service instead of the builder-proxy to access external resources.
    craft_recipe-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: craft_recipe-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    cve-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: cve
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bugs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
          description: Bugs related to this CVE entry.
        cvss:
          type: string
          description: CVSS The CVSS vector strings from various authorities that publish it.
        date_created:
          type: string
          description: Date Created
        date_made_public:
          type: string
          description: Date Made Public
        date_modified:
          type: string
          description: Date Modified
        description:
          type: string
          description: Title A description of the CVE issue. This will be updated regularly from the CVE database.
        discovered_by:
          type: string
          description: Discovered by The name of person(s) or organization that discovered the CVE
        display_name:
          type: string
          description: Display Name A very brief name describing the ref and state.
        metadata:
          type: string
          description: metadata CVE metadata.
        sequence:
          type: string
          description: CVE Sequence Number Should take the form XXXX-XXXX, all digits.
        status:
          type: string
          enum:
            - Candidate
            - Entry
            - Deprecated
            - Rejected
            - Placeholder
          description: Current CVE State Whether or not the vulnerability has been reviewed and assigned a full CVE number, or is still considered a Candidate, or is deprecated.
        title:
          type: string
          description: Title A title for the CVE
        url:
          type: string
          description: URL Return a URL to the site that has the CVE data for this CVE reference.
      required:
        - bugs_collection_link
        - cvss
        - date_created
        - date_made_public
        - date_modified
        - description
        - discovered_by
        - display_name
        - metadata
        - sequence
        - status
        - title
        - url
    cve-diff:
      type: object
    cve-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: cve-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: cve-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distribution-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Active Whether or not this item is active.
        active_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: The visible and active milestones associated with this object, ordered by date expected.
        all_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: All milestones associated with this object, ordered by date expected.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        archive_mirrors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_mirror-page-resource
          description: All enabled and official ARCHIVE mirrors of this Distribution.
        archives_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive-page-resource
          description: A sequence of the distribution's primary, partner and debug archives.
        branch_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Branch sharing policy Sharing policy for this pillar's branches.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        bug_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Forbidden
            - Embargoed, can be proprietary
          description: Bug sharing policy Sharing policy for this pillar's bugs.
        bug_supervisor_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Bug Supervisor The Launchpad id of the person or team (preferred) responsible for bug management.
        cdimage_mirrors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_mirror-page-resource
          description: All enabled and official RELEASE mirrors of this Distribution.
        code_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Code Administrator The distribution source code administrator.
        commercial_subscription_link:
          type: string
          format: uri
          x-lp-resource-type: commercial_subscription
          description: Commercial subscriptions An object which contains the timeframe and the voucher code of a subscription.
        commercial_subscription_is_due:
          type: string
          description: Commercial subscription is due Whether the distribution's licensing requires a new commercial subscription to use launchpad.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        current_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Current series The current development series of this distribution. Note that all maintainerships refer to the current series. When people ask about the state of packages in the distribution, we should interpret that query in the context of the currentseries.
        date_created:
          type: string
          description: Date created The date this distribution was registered.
        default_traversal_policy:
          type: string
          enum:
            - Series
            - Source package
            - OCI project
          description: Default traversal policy The type of object that /{distro}/{name} URLs for this distribution resolve to.
        derivatives_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series-page-resource
          description: This Distribution's derivatives
        description:
          type: string
          description: Description Details about the distributions's work, highlights, goals, and how to contribute. Use plain text, paragraphs are preserved and URLs are linked in pages. Don't repeat the Summary.
        development_series_alias:
          type: string
          description: Alias for development series If set, an alias for the current development series in this distribution.
        display_name:
          type: string
          description: Display Name The displayable name of the distribution.
        domain_name:
          type: string
          description: Web site URL The distro's web site URL.
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver The person or team responsible for decisions about features and bugs that will be targeted for any series in this distribution. Note that you can also specify a driver on each series whose permissions will be limited to that specific series.
        homepage_content:
          type: string
          description: Homepage Content The content of this distribution's home page. Edit this and it will be displayed for all the world to see. It is NOT a wiki so you cannot undo changes.
        icon_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Icon A small image of exactly 14x14 pixels and at most 5kb in size, that can be used to identify this distribution. The icon will be displayed everywhere we list the distribution and link to it.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of data contained in this item.
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to this distribution. It should be no bigger than 50kb in size.
        main_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Distribution Main Archive.
        members_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Members The distro's members team.
        mirror_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Mirror Administrator The person or team that has the rights to review and mark this distribution's mirrors as official.
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Brand A large image of exactly 192x192 pixels, that will be displayed on this distribution's home page in Launchpad. It should be no bigger than 100kb in size.
        name:
          type: string
          description: Name The distro's name.
        oci_project_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: OCI Project Administrator The person or team that has the rights to manage OCI Projects for this distribution.
        official_answers:
          type: string
          description: People can ask questions in Launchpad Answers
        official_blueprints:
          type: string
          description: This project uses blueprints
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        official_bugs:
          type: string
          description: Bugs in this project are tracked in Launchpad
        official_codehosting:
          type: string
          description: Code for this project is published in Bazaar branches on Launchpad
        official_packages:
          type: string
          description: Packages are tracked in Launchpad
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The restricted team, moderated team, or person who maintains the distribution information in Launchpad.
        private:
          type: string
          description: Distribution is confidential If set, this distribution is visible only to those with access grants.
        redirect_default_traversal:
          type: string
          description: Redirect the default traversal If true, the default traversal is for migration and redirects to a different canonical URL.
        redirect_release_uploads:
          type: string
          description: Redirect release pocket uploads Redirect release pocket uploads to proposed pocket
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The distro's registrant.
        security_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Security Administrator The distribution security administrator.
        series_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series-page-resource
          description: DistroSeries inside this Distribution
        specification_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Blueprint sharing policy Sharing policy for this project's specifications.
        summary:
          type: string
          description: Summary A short paragraph to introduce the goals and highlights of the distribution.
        supports_mirrors:
          type: string
          description: Enable mirror listings and probes
        supports_ppas:
          type: string
          description: Enable PPA creation and publication
        title:
          type: string
          description: Title The distro's title.
        translationgroup_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group
          description: Translation group The translation group that helps review translations for this project or distribution. The group's role depends on the permissions policy selected below.
        translationpermission:
          type: string
          enum:
            - Open
            - Structured
            - Restricted
            - Closed
          description: Translation permissions policy The policy this project or distribution uses to balance openness and control for their translations.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
        vcs:
          type: string
          enum:
            - Bazaar
            - Git
          description: VCS Version control system for this distribution's code.
        vulnerabilities_collection_link:
          type: string
          format: uri
          x-lp-resource-type: vulnerability-page-resource
          description: Vulnerabilities in this distribution.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - active
        - active_milestones_collection_link
        - all_milestones_collection_link
        - all_specifications_collection_link
        - archive_mirrors_collection_link
        - archives_collection_link
        - branch_sharing_policy
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - bug_sharing_policy
        - bug_supervisor_link
        - cdimage_mirrors_collection_link
        - code_admin_link
        - commercial_subscription_link
        - commercial_subscription_is_due
        - content_templates
        - current_series_link
        - date_created
        - default_traversal_policy
        - derivatives_collection_link
        - description
        - development_series_alias
        - display_name
        - domain_name
        - driver_link
        - homepage_content
        - icon_link
        - information_type
        - logo_link
        - main_archive_link
        - members_link
        - mirror_admin_link
        - mugshot_link
        - name
        - oci_project_admin_link
        - official_answers
        - official_blueprints
        - official_bug_tags
        - official_bugs
        - official_codehosting
        - official_packages
        - owner_link
        - private
        - redirect_default_traversal
        - redirect_release_uploads
        - registrant_link
        - security_admin_link
        - series_collection_link
        - specification_sharing_policy
        - summary
        - supports_mirrors
        - supports_ppas
        - title
        - translationgroup_link
        - translationpermission
        - translations_usage
        - valid_specifications_collection_link
        - vcs
        - vulnerabilities_collection_link
        - webhooks_collection_link
    distribution-diff:
      type: object
      properties:
        active:
          type: string
          description: Active Whether or not this item is active.
        branch_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Branch sharing policy Sharing policy for this pillar's branches.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        bug_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Forbidden
            - Embargoed, can be proprietary
          description: Bug sharing policy Sharing policy for this pillar's bugs.
        bug_supervisor_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Bug Supervisor The Launchpad id of the person or team (preferred) responsible for bug management.
        code_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Code Administrator The distribution source code administrator.
        commercial_subscription_link:
          type: string
          format: uri
          x-lp-resource-type: commercial_subscription
          description: Commercial subscriptions An object which contains the timeframe and the voucher code of a subscription.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        current_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Current series The current development series of this distribution. Note that all maintainerships refer to the current series. When people ask about the state of packages in the distribution, we should interpret that query in the context of the currentseries.
        date_created:
          type: string
          description: Date created The date this distribution was registered.
        default_traversal_policy:
          type: string
          enum:
            - Series
            - Source package
            - OCI project
          description: Default traversal policy The type of object that /{distro}/{name} URLs for this distribution resolve to.
        description:
          type: string
          description: Description Details about the distributions's work, highlights, goals, and how to contribute. Use plain text, paragraphs are preserved and URLs are linked in pages. Don't repeat the Summary.
        development_series_alias:
          type: string
          description: Alias for development series If set, an alias for the current development series in this distribution.
        display_name:
          type: string
          description: Display Name The displayable name of the distribution.
        domain_name:
          type: string
          description: Web site URL The distro's web site URL.
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver The person or team responsible for decisions about features and bugs that will be targeted for any series in this distribution. Note that you can also specify a driver on each series whose permissions will be limited to that specific series.
        homepage_content:
          type: string
          description: Homepage Content The content of this distribution's home page. Edit this and it will be displayed for all the world to see. It is NOT a wiki so you cannot undo changes.
        icon_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Icon A small image of exactly 14x14 pixels and at most 5kb in size, that can be used to identify this distribution. The icon will be displayed everywhere we list the distribution and link to it.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of data contained in this item.
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to this distribution. It should be no bigger than 50kb in size.
        members_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Members The distro's members team.
        mirror_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Mirror Administrator The person or team that has the rights to review and mark this distribution's mirrors as official.
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Brand A large image of exactly 192x192 pixels, that will be displayed on this distribution's home page in Launchpad. It should be no bigger than 100kb in size.
        name:
          type: string
          description: Name The distro's name.
        oci_project_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: OCI Project Administrator The person or team that has the rights to manage OCI Projects for this distribution.
        official_answers:
          type: string
          description: People can ask questions in Launchpad Answers
        official_blueprints:
          type: string
          description: This project uses blueprints
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        official_bugs:
          type: string
          description: Bugs in this project are tracked in Launchpad
        official_codehosting:
          type: string
          description: Code for this project is published in Bazaar branches on Launchpad
        official_packages:
          type: string
          description: Packages are tracked in Launchpad
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The restricted team, moderated team, or person who maintains the distribution information in Launchpad.
        redirect_default_traversal:
          type: string
          description: Redirect the default traversal If true, the default traversal is for migration and redirects to a different canonical URL.
        redirect_release_uploads:
          type: string
          description: Redirect release pocket uploads Redirect release pocket uploads to proposed pocket
        security_admin_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Security Administrator The distribution security administrator.
        specification_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Blueprint sharing policy Sharing policy for this project's specifications.
        summary:
          type: string
          description: Summary A short paragraph to introduce the goals and highlights of the distribution.
        supports_mirrors:
          type: string
          description: Enable mirror listings and probes
        supports_ppas:
          type: string
          description: Enable PPA creation and publication
        title:
          type: string
          description: Title The distro's title.
        translationgroup_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group
          description: Translation group The translation group that helps review translations for this project or distribution. The group's role depends on the permissions policy selected below.
        translationpermission:
          type: string
          enum:
            - Open
            - Structured
            - Restricted
            - Closed
          description: Translation permissions policy The policy this project or distribution uses to balance openness and control for their translations.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        vcs:
          type: string
          enum:
            - Bazaar
            - Git
          description: VCS Version control system for this distribution's code.
    distribution-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distribution_mirror-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_mirror
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        base_url:
          type: string
          description: Base URL The HTTP or FTP base URL of this mirror
        content:
          type: string
          enum:
            - Archive
            - CD Image
          description: Content Choose "CD Image" if this mirror contains CD images of this distribution. Choose "Archive" if this is a mirror of packages for this distribution.
        country_link:
          type: string
          format: uri
          x-lp-resource-type: country
          description: Location The country in which this mirror is based.
        country_dns_mirror:
          type: string
          description: Country DNS Mirror Whether this is a country mirror in DNS.
        date_created:
          type: string
          description: Date Created The date on which this mirror was registered.
        date_reviewed:
          type: string
          description: Date reviewed The date on which this mirror was last reviewed by a mirror admin.
        description:
          type: string
          description: Description
        displayname:
          type: string
          description: Organisation The name of the organization hosting this mirror.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution The distribution that is mirrored
        enabled:
          type: string
          description: This mirror was probed successfully.
        ftp_base_url:
          type: string
          description: "FTP URL e.g.: ftp://archive.ubuntu.com/ubuntu/"
        http_base_url:
          type: string
          description: "HTTP URL e.g.: http://archive.ubuntu.com/ubuntu/"
        https_base_url:
          type: string
          description: HTTPS URL
        name:
          type: string
          description: Name A short and unique name for this mirror.
        official_candidate:
          type: string
          description: Apply to be an official mirror Tick this to have your mirror reviewed for display on the official mirrors page (/ubuntu/+cdmirrors or /ubuntu/+archivemirrors).
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The person who is set as the current administrator of thismirror.
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Reviewer The person who last reviewed this mirror.
        rsync_base_url:
          type: string
          description: "Rsync URL e.g.: rsync://archive.ubuntu.com/ubuntu/"
        speed:
          type: string
          enum:
            - 128 Kbps
            - 256 Kbps
            - 512 Kbps
            - 1 Mbps
            - 2 Mbps
            - 10 Mbps
            - 45 Mbps
            - 100 Mbps
            - 1 Gbps
            - 2 Gbps
            - 4 Gbps
            - 10 Gbps
            - 20 Gbps
            - 50 Gbps
            - 100 Gbps
          description: Link Speed
        status:
          type: string
          enum:
            - Pending review
            - Unofficial
            - Official
            - Broken
          description: Status The current status of a mirror's registration.
        whiteboard:
          type: string
          description: Whiteboard Notes on the current status of the mirror (only visible to admins and the mirror's registrant).
      required:
        - base_url
        - content
        - country_link
        - country_dns_mirror
        - date_created
        - date_reviewed
        - description
        - displayname
        - distribution_link
        - enabled
        - ftp_base_url
        - http_base_url
        - https_base_url
        - name
        - official_candidate
        - owner_link
        - reviewer_link
        - rsync_base_url
        - speed
        - status
        - whiteboard
    distribution_mirror-diff:
      type: object
      properties:
        base_url:
          type: string
          description: Base URL The HTTP or FTP base URL of this mirror
        content:
          type: string
          enum:
            - Archive
            - CD Image
          description: Content Choose "CD Image" if this mirror contains CD images of this distribution. Choose "Archive" if this is a mirror of packages for this distribution.
        country_link:
          type: string
          format: uri
          x-lp-resource-type: country
          description: Location The country in which this mirror is based.
        country_dns_mirror:
          type: string
          description: Country DNS Mirror Whether this is a country mirror in DNS.
        description:
          type: string
          description: Description
        displayname:
          type: string
          description: Organisation The name of the organization hosting this mirror.
        ftp_base_url:
          type: string
          description: "FTP URL e.g.: ftp://archive.ubuntu.com/ubuntu/"
        http_base_url:
          type: string
          description: "HTTP URL e.g.: http://archive.ubuntu.com/ubuntu/"
        https_base_url:
          type: string
          description: HTTPS URL
        name:
          type: string
          description: Name A short and unique name for this mirror.
        official_candidate:
          type: string
          description: Apply to be an official mirror Tick this to have your mirror reviewed for display on the official mirrors page (/ubuntu/+cdmirrors or /ubuntu/+archivemirrors).
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The person who is set as the current administrator of thismirror.
        rsync_base_url:
          type: string
          description: "Rsync URL e.g.: rsync://archive.ubuntu.com/ubuntu/"
        speed:
          type: string
          enum:
            - 128 Kbps
            - 256 Kbps
            - 512 Kbps
            - 1 Mbps
            - 2 Mbps
            - 10 Mbps
            - 45 Mbps
            - 100 Mbps
            - 1 Gbps
            - 2 Gbps
            - 4 Gbps
            - 10 Gbps
            - 20 Gbps
            - 50 Gbps
            - 100 Gbps
          description: Link Speed
        status:
          type: string
          enum:
            - Pending review
            - Unofficial
            - Official
            - Broken
          description: Status The current status of a mirror's registration.
        whiteboard:
          type: string
          description: Whiteboard Notes on the current status of the mirror (only visible to admins and the mirror's registrant).
    distribution_mirror-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_mirror-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_mirror-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distribution_source_package-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_source_package
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        display_name:
          type: string
          description: Display name for this package.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: The distribution.
        name:
          type: string
          description: The source package name as text
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        title:
          type: string
          description: Title for this package.
        upstream_product_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The upstream product to which this package is linked.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - content_templates
        - display_name
        - distribution_link
        - name
        - official_bug_tags
        - title
        - upstream_product_link
        - webhooks_collection_link
    distribution_source_package-diff:
      type: object
      properties:
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: The distribution.
    distribution_source_package-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_source_package-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distribution_source_package-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distro_arch_series-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        architecture_tag:
          type: string
          description: Architecture Tag The architecture tag, or short piece of text that identifies this architecture. All binary packages in the archive will use this tag in their filename. Please get it correct. It should really never be changed!
        chroot_url:
          type: string
          description: Build chroot URL The URL to the current build chroot for this distroarchseries.
        display_name:
          type: string
          description: Display name The display name of this distroarchseries.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: The context distroseries
        enabled:
          type: string
          description: Enabled Whether or not this DistroArchSeries is enabled for build creation and publication.
        is_nominated_arch_indep:
          type: string
          description: Is Nominated Arch Independent True if this distroarchseries is the NominatedArchIndep one.
        main_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Main Archive The main archive of the distroarchseries.
        official:
          type: string
          description: Official Support Indicate whether or not this port has official support from the vendor of the distribution.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who registered this port.
        package_count:
          type: string
          description: Package Count A cache of the number of packages published in the RELEASE pocket of this port.
        processor_link:
          type: string
          format: uri
          x-lp-resource-type: processor
          description: Processor
        supports_virtualized:
          type: string
          description: PPA support available Indicate whether or not this port has support for building PPA packages.
        title:
          type: string
          description: Title The title of this distroarchseries.
        underlying_architecture_tag:
          type: string
          description: Underlying Architecture Tag If set, identifies architecture_tag as a 'variant' of the specified architecture.
      required:
        - architecture_tag
        - chroot_url
        - display_name
        - distroseries_link
        - enabled
        - is_nominated_arch_indep
        - main_archive_link
        - official
        - owner_link
        - package_count
        - processor_link
        - supports_virtualized
        - title
        - underlying_architecture_tag
    distro_arch_series-diff:
      type: object
      properties:
        architecture_tag:
          type: string
          description: Architecture Tag The architecture tag, or short piece of text that identifies this architecture. All binary packages in the archive will use this tag in their filename. Please get it correct. It should really never be changed!
        display_name:
          type: string
          description: Display name The display name of this distroarchseries.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: The context distroseries
        enabled:
          type: string
          description: Enabled Whether or not this DistroArchSeries is enabled for build creation and publication.
        is_nominated_arch_indep:
          type: string
          description: Is Nominated Arch Independent True if this distroarchseries is the NominatedArchIndep one.
        main_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Main Archive The main archive of the distroarchseries.
        official:
          type: string
          description: Official Support Indicate whether or not this port has official support from the vendor of the distribution.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who registered this port.
        package_count:
          type: string
          description: Package Count A cache of the number of packages published in the RELEASE pocket of this port.
        title:
          type: string
          description: Title The title of this distroarchseries.
        underlying_architecture_tag:
          type: string
          description: Underlying Architecture Tag If set, identifies architecture_tag as a 'variant' of the specified architecture.
    distro_arch_series-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distro_arch_series_filter-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series_filter
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        creator_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Creator The user who created this filter.
        date_created:
          type: string
          description: Date created The time when this filter was created.
        date_last_modified:
          type: string
          description: Date last modified The time when this filter was last modified.
        distroarchseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: Distro arch series The distro arch series that this filter is for.
        packageset_link:
          type: string
          format: uri
          x-lp-resource-type: packageset
          description: Package set The package set to be included in or excluded from this distro arch series.
        sense:
          type: string
          enum:
            - Include
            - Exclude
          description: Sense Whether the filter represents packages to include or exclude from the distro arch series.
      required:
        - creator_link
        - date_created
        - date_last_modified
        - distroarchseries_link
        - packageset_link
        - sense
    distro_arch_series_filter-diff:
      type: object
    distro_arch_series_filter-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series_filter-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series_filter-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distro_series_difference_comment-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series_difference_comment
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        body_text:
          type: string
          description: Comment text The comment text for the related distro series difference.
        comment_author_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The author of the comment.
        comment_date:
          type: string
          description: Comment date.
        source_package_name:
          type: string
          description: Source package name Name of the source package that this comment is for.
      required:
        - body_text
        - comment_author_link
        - comment_date
        - source_package_name
    distro_series_difference_comment-diff:
      type: object
    distro_series_difference_comment-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series_difference_comment-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series_difference_comment-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distro_series_difference-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series_difference
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        base_version:
          type: string
          description: Base version The common base version of the package for differences with different versions in the parent and derived series.
        derived_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Derived series The distribution series which identifies the derived series with the difference.
        package_diff_status:
          type: string
          enum:
            - Pending
            - Completed
            - Failed
          description: Package diff status The status of the diff between the base version and the derived version.
        package_diff_url:
          type: string
          description: Package diff url The url for the diff between the base version and the derived version.
        parent_package_diff_status:
          type: string
          enum:
            - Pending
            - Completed
            - Failed
          description: Parent package diff status The status of the diff between the base version and the parent version.
        parent_package_diff_url:
          type: string
          description: Parent package diff url The url for the diff between the base version and the parent version.
        parent_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Parent series The distribution series which identifies the parent series with the difference.
        parent_source_version:
          type: string
          description: Parent source version The version of the most recent source publishing in the parent series.
        source_version:
          type: string
          description: Source version The version of the most recent source publishing in the derived series.
        sourcepackagename:
          type: string
          description: Source Package Name
        status:
          type: string
          enum:
            - Needs attention
            - Blocklisted current version
            - Blocklisted always
            - Resolved
          description: Distro series difference status. The current status of this difference.
      required:
        - base_version
        - derived_series_link
        - package_diff_status
        - package_diff_url
        - parent_package_diff_status
        - parent_package_diff_url
        - parent_series_link
        - parent_source_version
        - source_version
        - sourcepackagename
        - status
    distro_series_difference-diff:
      type: object
    distro_series_difference-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series_difference-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series_difference-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    distro_series-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Active Whether or not this series is stable and supported, or under current development. This excludes series which are experimental or obsolete.
        active_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: The visible and active milestones associated with this object, ordered by date expected.
        advertise_by_hash:
          type: string
          description: Advertise by-hash directories Advertise by-hash directories with a flag in the Release file so that apt uses them by default. Only effective if publish_by_hash is also set.
        all_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: All milestones associated with this object, ordered by date expected.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        allowed_hashes:
          type: string
          description: Allowed hash algorithms for publishing A list of hash algorithm names (e.g. ["sha256", "sha512"]) that will be included in published index files (Release, Packages, Sources) and by-hash directories. If not set, all supported hashes are used.
        architectures_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series-page-resource
          description: Enabled architectures All architectures in this series with the 'enabled' flag set.
        backports_not_automatic:
          type: string
          description: "Don't upgrade to backports automatically Set NotAutomatic: yes and ButAutomaticUpgrades: yes in Release files generated for the backports pocket. This tells apt to automatically upgrade within backports, but not into it."
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        changeslist:
          type: string
          description: Email changes to The mailing list or other email address that Launchpad should notify about new uploads.
        component_names:
          type: string
          description: The series component names
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        date_created:
          type: string
          description: The date this series was registered.
        datereleased:
          type: string
          description: Date released
        description:
          type: string
          description: Description A detailed description of this series, with information on the architectures covered, the availability of security updates and any other relevant information.
        displayname:
          type: string
          description: Display name The series displayname.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution The distribution for which this is a series.
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver The person or team responsible for decisions about features and bugs that will be targeted to this series of the distribution.
        drivers_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: A list of the people or teams who are drivers for this series. This list is made up of any drivers or owners from this series and the parent drivers.
        fullseriesname:
          type: string
          description: Series full name The series full name, e.g. Ubuntu Warty
        include_long_descriptions:
          type: string
          description: Include long descriptions in Packages rather than in Translation-en If True, write long descriptions to the per-architecture Packages files; if False, write them to a Translation-en file common across architectures instead. Using a common file reduces the bandwidth footprint of enabling multiarch on clients, which requires downloading Packages files for multiple architectures.
        index_compressors:
          type: string
          description: Compression types to use for published index files A list of compression types to use for published index files (Packages, Sources, etc.).
        language_pack_full_export_requested:
          type: string
          description: Request a full language pack export Whether next language pack generation will be a full export. This information is useful when update packs are too big and want to merge all those changes in the base pack.
        main_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Distribution Main Archive
        name:
          type: string
          description: Name The name of this series.
        nominatedarchindep_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: DistroArchSeries designed to build architecture-independent packages within this distroseries context.
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owning team of the derived series This attribute mirrors the owner of the distribution.
        previous_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Parent series The series from which this one was branched.
        proposed_not_automatic:
          type: string
          description: "Don't upgrade to proposed updates automatically Set NotAutomatic: yes and ButAutomaticUpgrades: yes in Release files generated for the proposed pocket. This tells apt to automatically upgrade within proposed, but not into it."
        publish_by_hash:
          type: string
          description: Publish by-hash directories Publish archive index files in by-hash directories so that apt can retrieve them based on their hash, avoiding race conditions between InRelease and other files during mirror updates.
        publish_i18n_index:
          type: string
          description: Publish I18n index Publish archive i18n/Index file, which is believed to be unused.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        strict_supported_component_dependencies:
          type: string
          description: Strict dependencies of supported components If True, packages in supported components (main and restricted) may not build-depend on packages in unsupported components. Do not rely on the name of this attribute, even for reading; it is currently subject to change.
        suite_names:
          type: string
          description: The series pocket names
        summary:
          type: string
          description: 'Summary A single paragraph that explains the goals of of this series and the intended users. For example: "The 2.0 series of Apache represents the current stable series, and is recommended for all new deployments".'
        supported:
          type: string
          description: Supported Whether or not this series is currently supported.
        title:
          type: string
          description: Title The title of this series. It should be distinctive and designed to look good at the top of a page.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
        valid_until_config:
          type: string
          description: 'Valid-Until configuration Optional dict configuration to control publishing of Valid-Until tags in Release files for main archives. Each key must be one of: Release (can be specified only for unreleased series) Security Updates Proposed Backports Each value must be a dict with two integer fields (both >= 1): refresh_threshold: Refresh when Valid-Until is within this many days (must be <= validity_period) validity_period: How many days the Release file remains valid Example: { "SECURITY": { "refresh_threshold": 7, "validity_period": 14 }, "UPDATES": { "refresh_threshold": 5, "validity_period": 10 } } Note: Setting this attribute will overwrite the existing value entirely. To update the existing configuration, retrieve the current value first, update it, and then set the complete configuration.'
        version:
          type: string
          description: Version The version string for this series.
      required:
        - active
        - active_milestones_collection_link
        - advertise_by_hash
        - all_milestones_collection_link
        - all_specifications_collection_link
        - allowed_hashes
        - architectures_collection_link
        - backports_not_automatic
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - changeslist
        - component_names
        - content_templates
        - date_created
        - datereleased
        - description
        - displayname
        - distribution_link
        - driver_link
        - drivers_collection_link
        - fullseriesname
        - include_long_descriptions
        - index_compressors
        - language_pack_full_export_requested
        - main_archive_link
        - name
        - nominatedarchindep_link
        - official_bug_tags
        - owner_link
        - previous_series_link
        - proposed_not_automatic
        - publish_by_hash
        - publish_i18n_index
        - registrant_link
        - status
        - strict_supported_component_dependencies
        - suite_names
        - summary
        - supported
        - title
        - translations_usage
        - valid_specifications_collection_link
        - valid_until_config
        - version
    distro_series-diff:
      type: object
      properties:
        active:
          type: string
          description: Active Whether or not this series is stable and supported, or under current development. This excludes series which are experimental or obsolete.
        advertise_by_hash:
          type: string
          description: Advertise by-hash directories Advertise by-hash directories with a flag in the Release file so that apt uses them by default. Only effective if publish_by_hash is also set.
        allowed_hashes:
          type: string
          description: Allowed hash algorithms for publishing A list of hash algorithm names (e.g. ["sha256", "sha512"]) that will be included in published index files (Release, Packages, Sources) and by-hash directories. If not set, all supported hashes are used.
        backports_not_automatic:
          type: string
          description: "Don't upgrade to backports automatically Set NotAutomatic: yes and ButAutomaticUpgrades: yes in Release files generated for the backports pocket. This tells apt to automatically upgrade within backports, but not into it."
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        changeslist:
          type: string
          description: Email changes to The mailing list or other email address that Launchpad should notify about new uploads.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        date_created:
          type: string
          description: The date this series was registered.
        datereleased:
          type: string
          description: Date released
        description:
          type: string
          description: Description A detailed description of this series, with information on the architectures covered, the availability of security updates and any other relevant information.
        displayname:
          type: string
          description: Display name The series displayname.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution The distribution for which this is a series.
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver The person or team responsible for decisions about features and bugs that will be targeted to this series of the distribution.
        fullseriesname:
          type: string
          description: Series full name The series full name, e.g. Ubuntu Warty
        include_long_descriptions:
          type: string
          description: Include long descriptions in Packages rather than in Translation-en If True, write long descriptions to the per-architecture Packages files; if False, write them to a Translation-en file common across architectures instead. Using a common file reduces the bandwidth footprint of enabling multiarch on clients, which requires downloading Packages files for multiple architectures.
        index_compressors:
          type: string
          description: Compression types to use for published index files A list of compression types to use for published index files (Packages, Sources, etc.).
        language_pack_full_export_requested:
          type: string
          description: Request a full language pack export Whether next language pack generation will be a full export. This information is useful when update packs are too big and want to merge all those changes in the base pack.
        main_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Distribution Main Archive
        name:
          type: string
          description: Name The name of this series.
        nominatedarchindep_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: DistroArchSeries designed to build architecture-independent packages within this distroseries context.
        proposed_not_automatic:
          type: string
          description: "Don't upgrade to proposed updates automatically Set NotAutomatic: yes and ButAutomaticUpgrades: yes in Release files generated for the proposed pocket. This tells apt to automatically upgrade within proposed, but not into it."
        publish_by_hash:
          type: string
          description: Publish by-hash directories Publish archive index files in by-hash directories so that apt can retrieve them based on their hash, avoiding race conditions between InRelease and other files during mirror updates.
        publish_i18n_index:
          type: string
          description: Publish I18n index Publish archive i18n/Index file, which is believed to be unused.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        strict_supported_component_dependencies:
          type: string
          description: Strict dependencies of supported components If True, packages in supported components (main and restricted) may not build-depend on packages in unsupported components. Do not rely on the name of this attribute, even for reading; it is currently subject to change.
        summary:
          type: string
          description: 'Summary A single paragraph that explains the goals of of this series and the intended users. For example: "The 2.0 series of Apache represents the current stable series, and is recommended for all new deployments".'
        supported:
          type: string
          description: Supported Whether or not this series is currently supported.
        title:
          type: string
          description: Title The title of this series. It should be distinctive and designed to look good at the top of a page.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        valid_until_config:
          type: string
          description: 'Valid-Until configuration Optional dict configuration to control publishing of Valid-Until tags in Release files for main archives. Each key must be one of: Release (can be specified only for unreleased series) Security Updates Proposed Backports Each value must be a dict with two integer fields (both >= 1): refresh_threshold: Refresh when Valid-Until is within this many days (must be <= validity_period) validity_period: How many days the Release file remains valid Example: { "SECURITY": { "refresh_threshold": 7, "validity_period": 14 }, "UPDATES": { "refresh_threshold": 5, "validity_period": 10 } } Note: Setting this attribute will overwrite the existing value entirely. To update the existing configuration, retrieve the current value first, update it, and then set the complete configuration.'
        version:
          type: string
          description: Version The version string for this series.
    distro_series-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    email_address-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: email_address
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        email:
          type: string
          description: Email Address
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person
      required:
        - email
        - person_link
    email_address-diff:
      type: object
      properties:
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person
    email_address-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: email_address-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: email_address-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    faq-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: faq
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        content:
          type: string
          description: Content The answer for this FAQ in plain text. You may choose to include a URL to an external FAQ.
        date_created:
          type: string
          description: Created
        date_last_updated:
          type: string
          description: Last Updated
        id:
          type: string
          description: FAQ Number The unique number identifying the FAQ in Launchpad.
        keywords:
          type: string
          description: Keywords One or more terms that relate to this FAQ.
        last_updated_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Last Updated By The last person who modified the document.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: faq_target
          description: Target Product or distribution containing this FAQ.
        title:
          type: string
          description: Title The title describing this FAQ, often a question.
      required:
        - content
        - date_created
        - date_last_updated
        - id
        - keywords
        - last_updated_by_link
        - target_link
        - title
    faq-diff:
      type: object
      properties:
        content:
          type: string
          description: Content The answer for this FAQ in plain text. You may choose to include a URL to an external FAQ.
        keywords:
          type: string
          description: Keywords One or more terms that relate to this FAQ.
        title:
          type: string
          description: Title The title describing this FAQ, often a question.
    faq-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: faq-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: faq-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    faq_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: faq_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    faq_target-diff:
      type: object
    faq_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: faq_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: faq_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    gpg_key-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: gpg_key
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        fingerprint:
          type: string
          description: User Fingerprint
        keyid:
          type: string
          description: OpenPGP key ID
      required:
        - fingerprint
        - keyid
    gpg_key-diff:
      type: object
    gpg_key-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: gpg_key-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: gpg_key-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    git_ref-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        commit_sha1:
          type: string
          description: Commit SHA-1 The full SHA-1 object name of the commit object referenced by this reference.
        dependent_landings_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Dependent landings A collection of the merge proposals that are dependent on this reference.
        landing_candidates_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Landing candidates A collection of the merge proposals where this reference is the target.
        landing_targets_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Landing targets A collection of the merge proposals where this reference is the source.
        path:
          type: string
          description: Path The full path of this reference, e.g. refs/heads/master.
        recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
          description: All recipes associated with the object.
        repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Repository The Git repository containing this reference.
      required:
        - commit_sha1
        - dependent_landings_collection_link
        - landing_candidates_collection_link
        - landing_targets_collection_link
        - path
        - recipes_collection_link
        - repository_link
    git_ref-diff:
      type: object
    git_ref-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    git_repository-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        branches_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref-page-resource
          description: The branch references present in this repository.
        builder_constraints:
          type: string
          description: Builder constraints Builder resource tags required by builds of this repository.
        code_import_link:
          type: string
          format: uri
          x-lp-resource-type: code_import
          description: The associated CodeImport, if any.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        date_last_repacked:
          type: string
          description: Date last repacked The date that this repository was last repacked.
        date_last_scanned:
          type: string
          description: Date last scanned The date when pack statistics were last updated for this repository.
        default_branch:
          type: string
          description: Default branch The full path to the default branch for this repository, e.g. refs/heads/master.
        dependent_landings_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Dependent landings A collection of the merge proposals that are dependent on this repository.
        description:
          type: string
          description: Description A short description of this repository.
        display_name:
          type: string
          description: Display name Display name of the repository.
        git_https_url:
          type: string
          description: HTTPS URL An HTTPS URL for this repository, or None in the case of private repositories.
        git_identity:
          type: string
          description: Git identity If this is the default repository for some target, then this is 'lp:' plus a shortcut version of the path via that target. Otherwise it is simply 'lp:' plus the unique name.
        git_ssh_url:
          type: string
          description: SSH URL A git+ssh:// URL for this repository.
        id:
          type: string
          description: ID
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this repository.
        landing_candidates_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Landing candidates A collection of the merge proposals where this repository is the target.
        landing_targets_collection_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal-page-resource
          description: Landing targets A collection of the merge proposals where this repository is the source.
        loose_object_count:
          type: string
          description: Loose object count The number of loose objects for this repository.
        name:
          type: string
          description: Name The repository name. Keep very short, unique, and descriptive, because it will be used in URLs.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this Git repository. This controls who can modify the repository.
        owner_default:
          type: string
          description: Owner default Whether this repository is the default for its owner and target.
        pack_count:
          type: string
          description: Pack count The number of packs for this repository.
        private:
          type: string
          description: Private This repository is visible only to its subscribers.
        recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
          description: All recipes associated with the object.
        refs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref-page-resource
          description: The references present in this repository.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this Git repository.
        repository_type:
          type: string
          enum:
            - Hosted
            - Imported
            - Remote
          description: "Repository type The way this repository is hosted: directly on Launchpad, or imported from somewhere else."
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Review Team The reviewer of a repository is the person or exclusive team that is responsible for reviewing proposals and merging into this repository.
        subscribers_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Persons subscribed to this repository.
        subscriptions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_subscription-page-resource
          description: GitSubscriptions associated with this repository.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: git_target
          description: Target The target of the repository.
        target_default:
          type: string
          description: Target default Whether this repository is the default for its target.
        unique_name:
          type: string
          description: Unique name Unique name of the repository, including the owner and project names.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - branches_collection_link
        - builder_constraints
        - code_import_link
        - date_created
        - date_last_modified
        - date_last_repacked
        - date_last_scanned
        - default_branch
        - dependent_landings_collection_link
        - description
        - display_name
        - git_https_url
        - git_identity
        - git_ssh_url
        - id
        - information_type
        - landing_candidates_collection_link
        - landing_targets_collection_link
        - loose_object_count
        - name
        - owner_link
        - owner_default
        - pack_count
        - private
        - recipes_collection_link
        - refs_collection_link
        - registrant_link
        - repository_type
        - reviewer_link
        - subscribers_collection_link
        - subscriptions_collection_link
        - target_link
        - target_default
        - unique_name
        - webhooks_collection_link
    git_repository-diff:
      type: object
      properties:
        builder_constraints:
          type: string
          description: Builder constraints Builder resource tags required by builds of this repository.
        code_import_link:
          type: string
          format: uri
          x-lp-resource-type: code_import
          description: The associated CodeImport, if any.
        default_branch:
          type: string
          description: Default branch The full path to the default branch for this repository, e.g. refs/heads/master.
        description:
          type: string
          description: Description A short description of this repository.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this repository.
        name:
          type: string
          description: Name The repository name. Keep very short, unique, and descriptive, because it will be used in URLs.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this Git repository. This controls who can modify the repository.
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Review Team The reviewer of a repository is the person or exclusive team that is responsible for reviewing proposals and merging into this repository.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: git_target
          description: Target The target of the repository.
    git_repository-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    git_subscription-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: git_subscription
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        max_diff_lines:
          type: string
          enum:
            - Don't send diffs
            - 500 lines
            - 1000 lines
            - 5000 lines
            - Send entire diff
          description: Generated Diff Size Limit Diffs greater than the specified number of lines will not be sent to the subscriber. The subscriber will still receive an email with the new revision details even if the diff is larger than the specified number of lines.
        notification_level:
          type: string
          enum:
            - No email
            - Branch attribute notifications only
            - Branch revision notifications only
            - Branch attribute and revision notifications
          description: Notification Level Attribute notifications are sent when repository details are changed such as lifecycle status and name. Revision notifications are generated when new revisions are found.
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person Enter the launchpad id, or email address of the person you wish to subscribe to this repository. If you are unsure, use the "Choose..." option to find the person in Launchpad. You can only subscribe someone who is a registered user of the system.
        repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Repository ID
        review_level:
          type: string
          enum:
            - No email
            - Status changes only
            - Email about all changes
          description: Code review Level Control the kind of review activity that triggers notifications.
        subscribed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Subscribed by The person who created this subscription.
      required:
        - max_diff_lines
        - notification_level
        - person_link
        - repository_link
        - review_level
        - subscribed_by_link
    git_subscription-diff:
      type: object
      properties:
        max_diff_lines:
          type: string
          enum:
            - Don't send diffs
            - 500 lines
            - 1000 lines
            - 5000 lines
            - Send entire diff
          description: Generated Diff Size Limit Diffs greater than the specified number of lines will not be sent to the subscriber. The subscriber will still receive an email with the new revision details even if the diff is larger than the specified number of lines.
        notification_level:
          type: string
          enum:
            - No email
            - Branch attribute notifications only
            - Branch revision notifications only
            - Branch attribute and revision notifications
          description: Notification Level Attribute notifications are sent when repository details are changed such as lifecycle status and name. Revision notifications are generated when new revisions are found.
        review_level:
          type: string
          enum:
            - No email
            - Status changes only
            - Email about all changes
          description: Code review Level Control the kind of review activity that triggers notifications.
    git_subscription-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_subscription-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_subscription-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    has_bugs-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: has_bugs
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    has_bugs-diff:
      type: object
    has_bugs-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: has_bugs-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: has_bugs-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    git_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: git_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    git_target-diff:
      type: object
    git_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: git_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    has_milestones-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: The visible and active milestones associated with this object, ordered by date expected.
        all_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: All milestones associated with this object, ordered by date expected.
      required:
        - active_milestones_collection_link
        - all_milestones_collection_link
    has_milestones-diff:
      type: object
    has_milestones-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    object_with_translation_imports-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: object_with_translation_imports
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    object_with_translation_imports-diff:
      type: object
    object_with_translation_imports-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: object_with_translation_imports-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: object_with_translation_imports-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    irc_id-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: irc_id
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        network:
          type: string
          description: IRC network
        nickname:
          type: string
          description: Nickname
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
      required:
        - network
        - nickname
        - person_link
    irc_id-diff:
      type: object
      properties:
        network:
          type: string
          description: IRC network
        nickname:
          type: string
          description: Nickname
    irc_id-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: irc_id-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: irc_id-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    jabber_id-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: jabber_id
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        jabberid:
          type: string
          description: New Jabber user ID
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
      required:
        - jabberid
        - person_link
    jabber_id-diff:
      type: object
      properties:
        jabberid:
          type: string
          description: New Jabber user ID
    jabber_id-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: jabber_id-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: jabber_id-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    language-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: language
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        code:
          type: string
          description: The ISO 639 code
        english_name:
          type: string
          description: The English name
        plural_expression:
          type: string
          description: Plural form expression The expression that relates a number of items to the appropriate plural form.
        plural_forms:
          type: string
          description: Number of plural forms The number of plural forms this language has.
        text_direction:
          type: string
          enum:
            - Left to Right
            - Right to Left
          description: Text direction The direction of text in this language.
        translators_count:
          type: string
          description: Total number of translators for this language.
        visible:
          type: string
          description: Visible Whether this language is visible by default.
      required:
        - code
        - english_name
        - plural_expression
        - plural_forms
        - text_direction
        - translators_count
        - visible
    language-diff:
      type: object
      properties:
        code:
          type: string
          description: The ISO 639 code
        english_name:
          type: string
          description: The English name
        plural_expression:
          type: string
          description: Plural form expression The expression that relates a number of items to the appropriate plural form.
        plural_forms:
          type: string
          description: Number of plural forms The number of plural forms this language has.
        text_direction:
          type: string
          enum:
            - Left to Right
            - Right to Left
          description: Text direction The direction of text in this language.
        visible:
          type: string
          description: Visible Whether this language is visible by default.
    language-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: language-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: language-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    livefs_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: livefs_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: The archive from which to build the live filesystem.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture for which to build.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        livefs_link:
          type: string
          format: uri
          x-lp-resource-type: livefs
          description: The live filesystem to build.
        metadata_override:
          type: string
          description: A dict of data about the image; this will be merged into the metadata dict for the live filesystem.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: The pocket for which to build.
        requester_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who requested this build.
        score:
          type: string
          description: Score of the related build farm job (if any).
        title:
          type: string
          description: Title
        unique_key:
          type: string
          description: An optional unique key; if set, this identifies a class of builds for this live filesystem.
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
        version:
          type: string
          description: A version string for this build.
      required:
        - archive_link
        - build_log_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_arch_series_link
        - distro_series_link
        - duration
        - livefs_link
        - metadata_override
        - pocket
        - requester_link
        - score
        - title
        - unique_key
        - upload_log_url
        - version
    livefs_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    livefs_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefs_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefs_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    livefs-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: livefs
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefs_build-page-resource
          description: All builds of this live filesystem. All builds of this live filesystem, sorted in descending order of finishing (or starting if not completed successfully).
        completed_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefs_build-page-resource
          description: Completed builds of this live filesystem. Completed builds of this live filesystem, sorted in descending order of finishing.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro Series The series for which the image should be built.
        id:
          type: string
          description: ID
        keep_binary_files_days:
          type: string
          description: Binary file retention period Keep binary files attached to builds of this live filesystem for at least this many days. If unset, disable pruning.
        metadata:
          type: string
          description: A dict of data about the image. Entries here will be passed to the builder.
        name:
          type: string
          description: Name The name of the live filesystem image.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this live filesystem image.
        pending_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefs_build-page-resource
          description: Pending builds of this live filesystem. Pending builds of this live filesystem, sorted in descending order of creation.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this live filesystem image.
        relative_build_score:
          type: string
          description: Relative build score A delta to apply to all build scores for the live filesystem. Builds with a higher score will build sooner.
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this live filesystem image on virtual builders.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - builds_collection_link
        - completed_builds_collection_link
        - date_created
        - date_last_modified
        - distro_series_link
        - id
        - keep_binary_files_days
        - metadata
        - name
        - owner_link
        - pending_builds_collection_link
        - registrant_link
        - relative_build_score
        - require_virtualized
        - webhooks_collection_link
    livefs-diff:
      type: object
      properties:
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro Series The series for which the image should be built.
        keep_binary_files_days:
          type: string
          description: Binary file retention period Keep binary files attached to builds of this live filesystem for at least this many days. If unset, disable pruning.
        metadata:
          type: string
          description: A dict of data about the image. Entries here will be passed to the builder.
        name:
          type: string
          description: Name The name of the live filesystem image.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this live filesystem image.
        relative_build_score:
          type: string
          description: Relative build score A delta to apply to all build scores for the live filesystem. Builds with a higher score will build sooner.
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this live filesystem image on virtual builders.
    livefs-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefs-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: livefs-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    message-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: message
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_attachments_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_attachment-page-resource
          description: A list of BugAttachments connected to this message.
        content:
          type: string
          description: All the text/plain chunks joined together as a unicode string.
        date_created:
          type: string
          description: Date Created
        date_deleted:
          type: string
          description: When this message was deleted
        date_last_edited:
          type: string
          description: When this message was last edited
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person
        parent_link:
          type: string
          format: uri
          x-lp-resource-type: message
          description: Parent
        revisions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message_revision-page-resource
          description: Message revision history Revision history of this message, sorted in ascending order.
        subject:
          type: string
          description: Subject
        visible:
          type: string
          description: Message visibility. Whether or not the message is visible.
      required:
        - bug_attachments_collection_link
        - content
        - date_created
        - date_deleted
        - date_last_edited
        - owner_link
        - parent_link
        - revisions_collection_link
        - subject
        - visible
    message-diff:
      type: object
    message-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    message_revision-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: message_revision
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        content:
          type: string
          description: The message at the given revision
        date_created:
          type: string
          description: The time when this message revision was created.
        date_created_display:
          type: string
          description: The time when this message revision was created, rendered in the user's timezone for the web UI.
        date_deleted:
          type: string
          description: The time when this message revision was created.
        revision:
          type: string
          description: Revision number
      required:
        - content
        - date_created
        - date_created_display
        - date_deleted
        - revision
    message_revision-diff:
      type: object
    message_revision-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message_revision-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message_revision-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    milestone-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: milestone
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        code_name:
          type: string
          description: Code name An alternative name for the milestone.
        date_targeted:
          type: string
          description: "Date Targeted Example: 2005-11-24"
        is_active:
          type: string
          description: Active Whether or not this object should be shown in web forms for targeting.
        name:
          type: string
          description: Name Only letters, numbers, and simple punctuation are allowed.
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        release_link:
          type: string
          format: uri
          x-lp-resource-type: project_release
          description: The release for this milestone.
        series_target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The productseries or distroseries of this milestone.
        summary:
          type: string
          description: Summary A summary of the features and status of this milestone.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The product, distribution, or project group for this milestone.
        title:
          type: string
          description: A context title for pages.
      required:
        - code_name
        - date_targeted
        - is_active
        - name
        - official_bug_tags
        - release_link
        - series_target_link
        - summary
        - target_link
        - title
    milestone-diff:
      type: object
      properties:
        code_name:
          type: string
          description: Code name An alternative name for the milestone.
        date_targeted:
          type: string
          description: "Date Targeted Example: 2005-11-24"
        is_active:
          type: string
          description: Active Whether or not this object should be shown in web forms for targeting.
        name:
          type: string
          description: Name Only letters, numbers, and simple punctuation are allowed.
        series_target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The productseries or distroseries of this milestone.
        summary:
          type: string
          description: Summary A summary of the features and status of this milestone.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The product, distribution, or project group for this milestone.
    milestone-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    oci_project-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        description:
          type: string
          description: The description for this OCI project.
        display_name:
          type: string
          description: Display name for this OCI project.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: The distribution that this OCI project is associated with.
        name:
          type: string
          description: Name The name of this OCI project.
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this OCI project is associated with.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person that registered this project.
        series_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project_series-page-resource
          description: Series inside this OCI project.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
      required:
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - content_templates
        - date_created
        - date_last_modified
        - description
        - display_name
        - distribution_link
        - name
        - official_bug_tags
        - project_link
        - registrant_link
        - series_collection_link
        - translations_usage
    oci_project-diff:
      type: object
      properties:
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        description:
          type: string
          description: The description for this OCI project.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: The distribution that this OCI project is associated with.
        name:
          type: string
          description: Name The name of this OCI project.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this OCI project is associated with.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
    oci_project-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    oci_project_series-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project_series
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_created:
          type: string
          description: Date created The date on which this series was created in Launchpad.
        name:
          type: string
          description: Name The name of this series.
        oci_project_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project
          description: The OCI project that this series belongs to.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person that registered this series.
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        summary:
          type: string
          description: Summary A brief summary of this series.
      required:
        - date_created
        - name
        - oci_project_link
        - registrant_link
        - status
        - summary
    oci_project_series-diff:
      type: object
      properties:
        name:
          type: string
          description: Name The name of this series.
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        summary:
          type: string
          description: Summary A brief summary of this series.
    oci_project_series-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project_series-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project_series-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    oci_push_rule-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: oci_push_rule
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        image_name:
          type: string
          description: Image name The intended name of the image on the registry.
        registry_url:
          type: string
          description: Registry URL The registry URL for the credentials of this push rule
        username:
          type: string
          description: Username The username for the credentials, if available.
      required:
        - image_name
        - registry_url
        - username
    oci_push_rule-diff:
      type: object
      properties:
        image_name:
          type: string
          description: Image name The intended name of the image on the registry.
    oci_push_rule-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_push_rule-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_push_rule-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    oci_recipe_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        arch_tag:
          type: string
          description: Architecture tag
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The Archive context for this build.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        date:
          type: string
          description: The date when the build completed or is estimated to complete.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture for which to build.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        estimate:
          type: string
          description: If true, the date value is an estimate.
        eta:
          type: string
          description: The datetime when the build job is estimated to complete.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe
          description: The OCI recipe to build.
        registry_upload_error_summary:
          type: string
          description: Registry upload error summary The error summary, if any, from the last attempt to upload this build to a registry.
        registry_upload_errors:
          type: string
          description: Detailed registry upload errors A list of errors, as described in https://docs.docker.com/registry/spec/api/#errors , from the last attempt to upload this build to a registry.
        registry_upload_status:
          type: string
          enum:
            - Unscheduled
            - Pending
            - Failed to upload
            - Uploaded
            - Superseded
          description: Registry upload status
        requester_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Requester The person who requested this OCI recipe build.
        score:
          type: string
          description: Score of the related build farm job (if any).
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - arch_tag
        - archive_link
        - build_log_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - date
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_arch_series_link
        - distro_series_link
        - duration
        - estimate
        - eta
        - pocket
        - recipe_link
        - registry_upload_error_summary
        - registry_upload_errors
        - registry_upload_status
        - requester_link
        - score
        - title
        - upload_log_url
    oci_recipe_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        registry_upload_status:
          type: string
          enum:
            - Unscheduled
            - Pending
            - Failed to upload
            - Uploaded
            - Superseded
          description: Registry upload status
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    oci_recipe_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    oci_recipe_build_request-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe_build_request
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe_build-page-resource
          description: Builds produced by this request
        date_finished:
          type: string
          description: The time when this request finished
        date_requested:
          type: string
          description: The time when this request was made
        error_message:
          type: string
          description: Error message
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe
          description: OCI Recipe
        status:
          type: string
          enum:
            - Pending
            - Failed
            - Completed
          description: Status
      required:
        - builds_collection_link
        - date_finished
        - date_requested
        - error_message
        - recipe_link
        - status
    oci_recipe_build_request-diff:
      type: object
    oci_recipe_build_request-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe_build_request-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe_build_request-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    oci_recipe-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        allow_internet:
          type: string
          description: Allow external network access Allow access to external network resources via a proxy. Resources hosted on Launchpad itself are always allowed.
        build_args:
          type: string
          description: Build ARG variables The dictionary of ARG variables to be used when building this recipe.
        build_daily:
          type: string
          description: Build daily If True, this recipe should be built daily.
        build_file:
          type: string
          description: Build file path The relative path to the file within this recipe's branch that defines how to build the recipe.
        build_path:
          type: string
          description: Build directory context Directory to use for build context and OCIRecipe.build_file location.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        description:
          type: string
          description: Description A short description of this recipe.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a Dockerfile at the location defined by the build_file attribute.
        image_name:
          type: string
          description: Image name Image name to use on upload to registry. Defaults to recipe name if not set. Only used when Distribution credentials are set.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this OCI recipe.
        name:
          type: string
          description: Name The name of this recipe.
        oci_project_link:
          type: string
          format: uri
          x-lp-resource-type: oci_project
          description: OCI project The OCI project that this recipe is for.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this OCI recipe.
        processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
          description: Processors The architectures for which the OCI recipe should be built.
        push_rules_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_push_rule-page-resource
          description: Push rules for this OCI recipe. All of the push rules for registry upload that apply to this recipe.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The user who registered this recipe.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - allow_internet
        - build_args
        - build_daily
        - build_file
        - build_path
        - date_created
        - date_last_modified
        - description
        - git_ref_link
        - image_name
        - information_type
        - name
        - oci_project_link
        - owner_link
        - processors_collection_link
        - push_rules_collection_link
        - registrant_link
        - webhooks_collection_link
    oci_recipe-diff:
      type: object
      properties:
        allow_internet:
          type: string
          description: Allow external network access Allow access to external network resources via a proxy. Resources hosted on Launchpad itself are always allowed.
        build_args:
          type: string
          description: Build ARG variables The dictionary of ARG variables to be used when building this recipe.
        build_daily:
          type: string
          description: Build daily If True, this recipe should be built daily.
        build_file:
          type: string
          description: Build file path The relative path to the file within this recipe's branch that defines how to build the recipe.
        build_path:
          type: string
          description: Build directory context Directory to use for build context and OCIRecipe.build_file location.
        description:
          type: string
          description: Description A short description of this recipe.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a Dockerfile at the location defined by the build_file attribute.
        image_name:
          type: string
          description: Image name Image name to use on upload to registry. Defaults to recipe name if not set. Only used when Distribution credentials are set.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this OCI recipe.
        name:
          type: string
          description: Name The name of this recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this OCI recipe.
    oci_recipe-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: oci_recipe-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    translation_file-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: translation_file
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        id:
          type: string
          description: The translation file id.
      required:
        - id
    translation_file-diff:
      type: object
    translation_file-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_file-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_file-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    translation_template-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: translation_template
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Template is active If unchecked, people can no longer change the template's translations.
        date_last_updated:
          type: string
          description: Date for last update
        description:
          type: string
          description: Description Please provide a brief description of the content of this translation template, for example, telling translators if this template contains strings for end-users or other developers.
        exported_in_languagepacks:
          type: string
          description: Include translations for this template in language packs? Check this box if this template is part of a language pack so its translations should be exported that way.
        format:
          type: string
          enum:
            - PO format
            - MO format
            - Mozilla XPI format
            - KDE PO format
            - XPI PO format
            - Changes from imported translations in partial PO format
          description: File format for the source file
        id:
          type: string
          description: The translation template id.
        language_count:
          type: string
          description: The number of languages for which we have translations.
        message_count:
          type: string
          description: The number of translation messages for this template.
        name:
          type: string
          description: Template name The name of this PO template, for example 'evolution-2.2'. Each translation template has a unique name in its package. It's important to get this correct, because Launchpad will recommend alternative translations based on the name.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of the template in Launchpad can edit the template and change it's status, and can also upload new versions of the template when a new release is made or when the translation strings have been changed during development.
        path:
          type: string
          description: Path of the template in the source tree, including filename.
        priority:
          type: string
          description: Priority A number that describes how important this template is. Often there are multiple templates, and you can use this as a way of indicating which are more important and should be translated first. Pick any number - higher priority templates will generally be listed first.
        translation_domain:
          type: string
          description: Translation domain The translation domain for a translation template. Used with PO file format when generating MO files for inclusion in language pack or MO tarball exports.
        translation_files_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_file-page-resource
          description: All translation files that exist for this template.
      required:
        - active
        - date_last_updated
        - description
        - exported_in_languagepacks
        - format
        - id
        - language_count
        - message_count
        - name
        - owner_link
        - path
        - priority
        - translation_domain
        - translation_files_collection_link
    translation_template-diff:
      type: object
      properties:
        active:
          type: string
          description: Template is active If unchecked, people can no longer change the template's translations.
        date_last_updated:
          type: string
          description: Date for last update
        description:
          type: string
          description: Description Please provide a brief description of the content of this translation template, for example, telling translators if this template contains strings for end-users or other developers.
        exported_in_languagepacks:
          type: string
          description: Include translations for this template in language packs? Check this box if this template is part of a language pack so its translations should be exported that way.
        format:
          type: string
          enum:
            - PO format
            - MO format
            - Mozilla XPI format
            - KDE PO format
            - XPI PO format
            - Changes from imported translations in partial PO format
          description: File format for the source file
        name:
          type: string
          description: Template name The name of this PO template, for example 'evolution-2.2'. Each translation template has a unique name in its package. It's important to get this correct, because Launchpad will recommend alternative translations based on the name.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of the template in Launchpad can edit the template and change it's status, and can also upload new versions of the template when a new release is made or when the translation strings have been changed during development.
        path:
          type: string
          description: Path of the template in the source tree, including filename.
        priority:
          type: string
          description: Priority A number that describes how important this template is. Often there are multiple templates, and you can use this as a way of indicating which are more important and should be translated first. Pick any number - higher priority templates will generally be listed first.
        translation_domain:
          type: string
          description: Translation domain The translation domain for a translation template. Used with PO file format when generating MO files for inclusion in language pack or MO tarball exports.
    translation_template-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_template-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_template-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    package_upload-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The archive for this upload.
        changes_file_url:
          type: string
          description: Changes file URL Librarian URL for the changes file associated with this upload. Will be None if the upload was copied from another series.
        component_name:
          type: string
          description: Source package component name
        contains_build:
          type: string
          description: Whether or not this upload contains binaries
        contains_copy:
          type: string
          description: Whether or not this upload contains a copy from another series.
        contains_source:
          type: string
          description: Whether or not this upload contains sources
        copy_source_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Copy source archive The archive from which this package was copied, if any.
        date_created:
          type: string
          description: Date created The date this package upload was done.
        display_arches:
          type: string
          description: Architectures related to this item
        display_name:
          type: string
          description: Generic displayname for a queue item
        display_version:
          type: string
          description: This item's displayable source package version
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Series The distroseries targeted by this upload.
        id:
          type: string
          description: ID
        logs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload_log-page-resource
          description: The package upload logs
        package_name:
          type: string
          description: Name of the uploaded source package
        package_version:
          type: string
          description: Source package version
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: The pocket The pocket targeted by this upload.
        section_name:
          type: string
          description: Source package section name
        status:
          type: string
          enum:
            - New
            - Unapproved
            - Accepted
            - Done
            - Rejected
          description: Queue status The status of this upload.
      required:
        - archive_link
        - changes_file_url
        - component_name
        - contains_build
        - contains_copy
        - contains_source
        - copy_source_archive_link
        - date_created
        - display_arches
        - display_name
        - display_version
        - distroseries_link
        - id
        - logs_collection_link
        - package_name
        - package_version
        - pocket
        - section_name
        - status
    package_upload-diff:
      type: object
      properties:
        date_created:
          type: string
          description: Date created The date this package upload was done.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Series The distroseries targeted by this upload.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: The pocket The pocket targeted by this upload.
    package_upload-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    package_upload_log-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload_log
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        comment:
          type: string
          description: User's comment about this change.
        date_created:
          type: string
          description: When this action happened.
        new_status:
          type: string
          enum:
            - New
            - Unapproved
            - Accepted
            - Done
            - Rejected
          description: New status.
        old_status:
          type: string
          enum:
            - New
            - Unapproved
            - Accepted
            - Done
            - Rejected
          description: Old status.
        package_upload_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload
          description: The package upload that generated this log
        reviewer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Who did this action.
      required:
        - comment
        - date_created
        - new_status
        - old_status
        - package_upload_link
        - reviewer_link
    package_upload_log-diff:
      type: object
    package_upload_log-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload_log-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload_log-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    packageset-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: packageset
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_created:
          type: string
          description: Date Created The creation date/time for the package set at hand.
        description:
          type: string
          description: Description The description for the package set at hand.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series The distroseries to which this package set is related.
        id:
          type: string
          description: ID
        name:
          type: string
          description: Valid package set name
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person who owns this package set.
        relative_build_score:
          type: string
          description: Build score Build score bonus for packages in this package set.
      required:
        - date_created
        - description
        - distroseries_link
        - id
        - name
        - owner_link
        - relative_build_score
    packageset-diff:
      type: object
      properties:
        description:
          type: string
          description: Description The description for the package set at hand.
        name:
          type: string
          description: Valid package set name
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person who owns this package set.
        relative_build_score:
          type: string
          description: Build score Build score bonus for packages in this package set.
    packageset-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: packageset-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: packageset-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    person-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        account_status:
          type: string
          enum:
            - Placeholder
            - Unactivated
            - Active
            - Deactivated
            - Suspended
            - Closed
            - Deceased
          description: The status of this person's account
        account_status_history:
          type: string
          description: Account status history
        admins_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: List of this team's admins.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Default PPA The PPA named 'ppa' owned by this person.
        confirmed_email_addresses_collection_link:
          type: string
          format: uri
          x-lp-resource-type: email_address-page-resource
          description: Confirmed emails of this person. Confirmed emails are the ones in the VALIDATED state
        date_created:
          type: string
          description: Date Created
        deactivated_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Former members of the team.
        description:
          type: string
          description: Description Details about interests and goals. Use plain text, paragraphs are preserved and URLs are linked.
        display_name:
          type: string
          description: Display Name Your name as you would like it displayed throughout Launchpad. Most people use their full name here.
        expired_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Expired members of the team.
        gpg_keys_collection_link:
          type: string
          format: uri
          x-lp-resource-type: gpg_key-page-resource
          description: List of valid OpenPGP keys ordered by ID
        hide_email_addresses:
          type: string
          description: Hide my email addresses from other Launchpad users
        homepage_content:
          type: string
          description: Homepage Content Obsolete. Use description.
        id:
          type: string
          description: ID Internal immutable identifier for this person. Only visible by privileged users.
        invited_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Other teams which have been invited to become members of this team.
        irc_nicknames_collection_link:
          type: string
          format: uri
          x-lp-resource-type: irc_id-page-resource
          description: List of IRC nicknames of this Person.
        is_probationary:
          type: string
          description: Is this a probationary user?
        is_team:
          type: string
          description: Is this object a team?
        is_ubuntu_coc_signer:
          type: string
          description: Signed Ubuntu Code of Conduct
        is_valid:
          type: string
          description: This is an active user or a team.
        jabber_ids_collection_link:
          type: string
          format: uri
          x-lp-resource-type: jabber_id-page-resource
          description: List of Jabber IDs of this Person.
        karma:
          type: string
          description: Karma The cached total karma for this person.
        languages_collection_link:
          type: string
          format: uri
          x-lp-resource-type: language-page-resource
          description: List of languages known by this person
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to you. Traditionally this is a logo, a small picture or a personal mascot. It should be no bigger than 50kb in size.
        mailing_list_auto_subscribe_policy:
          type: string
          enum:
            - Never subscribe to mailing lists
            - Ask me when I join a team
            - Always subscribe me to mailing lists
          description: Mailing List Auto-subscription Policy This attribute determines whether a person is automatically subscribed to a team's mailing list when the person joins said team.
        members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: List of direct members with ADMIN or APPROVED status
        members_details_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
          description: Active TeamMemberships for this object's members. Active TeamMemberships are the ones with the ADMIN or APPROVED status. The results are ordered using Person.sortingColumns.
        memberships_details_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
          description: All TeamMemberships for Teams this Team or Person is an active member of.
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Mugshot A large image of exactly 192x192 pixels, that will be displayed on your home page in Launchpad. Traditionally this is a great big picture of your grinning face. Make the most of it! It should be no bigger than 100kb in size.
        name:
          type: string
          description: Name A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
        open_membership_invitations_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
          description: Open membership invitations. All TeamMemberships which represent an invitation (to join a team) sent to this person.
        participants_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: All participants of this team. List of all direct and indirect people and teams who, one way or another, are a part of this team. If you want a method to check if a given person is a member of a team, you should probably look at IPerson.inTeam().
        ppas_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive-page-resource
          description: PPAs for this person. PPAs owned by the context person ordered by name.
        preferred_email_address_link:
          type: string
          format: uri
          x-lp-resource-type: email_address
          description: Preferred email address The preferred email address for this person. The one we'll use to communicate with them.
        private:
          type: string
          description: This team is private Private teams are visible only to their members.
        proposed_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: People who have applied to join the team.
        recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
          description: All recipes associated with the object.
        social_accounts_collection_link:
          type: string
          format: uri
          x-lp-resource-type: social_account-page-resource
          description: List of Social Accounts of this Person.
        sshkeys_collection_link:
          type: string
          format: uri
          x-lp-resource-type: ssh_key-page-resource
          description: List of SSH keys
        sub_teams_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team-page-resource
          description: "All subteams of this team. A subteam is any team that is a member (either directly or indirectly) of this team. As an example, let's say we have this hierarchy of teams: Rosetta Translators Rosetta pt Translators Rosetta pt_BR Translators In this case, both 'Rosetta pt Translators' and 'Rosetta pt_BR Translators' are subteams of the 'Rosetta Translators' team, and all members of both subteams are considered members of \"Rosetta Translators\"."
        super_teams_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team-page-resource
          description: "All superteams of this team. A superteam is any team that this team is a member of. For example, let's say we have this hierarchy of teams, and we are the \"Rosetta pt_BR Translators\": Rosetta Translators Rosetta pt Translators Rosetta pt_BR Translators In this case, we will return both 'Rosetta pt Translators' and 'Rosetta Translators', because we are member of both of them."
        team_owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Team Owner
        time_zone:
          type: string
          description: The time zone of this object.
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
        visibility:
          type: string
          enum:
            - Public
            - Private
          description: Visibility Anyone can see a public team's data. Only team members can see private team data.
        wiki_names_collection_link:
          type: string
          format: uri
          x-lp-resource-type: wiki_name-page-resource
          description: All WikiNames of this Person, sorted alphabetically by URL.
      required:
        - account_status
        - account_status_history
        - admins_collection_link
        - all_specifications_collection_link
        - archive_link
        - confirmed_email_addresses_collection_link
        - date_created
        - deactivated_members_collection_link
        - description
        - display_name
        - expired_members_collection_link
        - gpg_keys_collection_link
        - hide_email_addresses
        - homepage_content
        - id
        - invited_members_collection_link
        - irc_nicknames_collection_link
        - is_probationary
        - is_team
        - is_ubuntu_coc_signer
        - is_valid
        - jabber_ids_collection_link
        - karma
        - languages_collection_link
        - logo_link
        - mailing_list_auto_subscribe_policy
        - members_collection_link
        - members_details_collection_link
        - memberships_details_collection_link
        - mugshot_link
        - name
        - open_membership_invitations_collection_link
        - participants_collection_link
        - ppas_collection_link
        - preferred_email_address_link
        - private
        - proposed_members_collection_link
        - recipes_collection_link
        - social_accounts_collection_link
        - sshkeys_collection_link
        - sub_teams_collection_link
        - super_teams_collection_link
        - team_owner_link
        - time_zone
        - valid_specifications_collection_link
        - visibility
        - wiki_names_collection_link
    person-diff:
      type: object
      properties:
        description:
          type: string
          description: Description Details about interests and goals. Use plain text, paragraphs are preserved and URLs are linked.
        display_name:
          type: string
          description: Display Name Your name as you would like it displayed throughout Launchpad. Most people use their full name here.
        hide_email_addresses:
          type: string
          description: Hide my email addresses from other Launchpad users
        homepage_content:
          type: string
          description: Homepage Content Obsolete. Use description.
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to you. Traditionally this is a logo, a small picture or a personal mascot. It should be no bigger than 50kb in size.
        mailing_list_auto_subscribe_policy:
          type: string
          enum:
            - Never subscribe to mailing lists
            - Ask me when I join a team
            - Always subscribe me to mailing lists
          description: Mailing List Auto-subscription Policy This attribute determines whether a person is automatically subscribed to a team's mailing list when the person joins said team.
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Mugshot A large image of exactly 192x192 pixels, that will be displayed on your home page in Launchpad. Traditionally this is a great big picture of your grinning face. Make the most of it! It should be no bigger than 100kb in size.
        name:
          type: string
          description: Name A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
        team_owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Team Owner
        visibility:
          type: string
          enum:
            - Public
            - Private
          description: Visibility Anyone can see a public team's data. Only team members can see private team data.
    person-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    pillar-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: pillar
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Active Whether or not this item is active.
      required:
        - active
    pillar-diff:
      type: object
      properties:
        active:
          type: string
          description: Active Whether or not this item is active.
    pillar-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: pillar-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: pillar-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    pillars-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: pillars
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        featured_pillars_collection_link:
          type: string
          format: uri
          x-lp-resource-type: pillar-page-resource
          description: Projects, project groups, and distributions that are featured on the site.
      required:
        - featured_pillars_collection_link
    pillars-diff:
      type: object
    pillars-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: pillars-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: pillars-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    poll-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: poll
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        allowspoilt:
          type: string
          description: Users can spoil their votes? Allow users to leave the ballot blank (i.e. cast a vote for "None of the above")
        datecloses:
          type: string
          description: The date and time when this poll closes
        dateopens:
          type: string
          description: The date and time when this poll opens
        name:
          type: string
          description: The unique name of this poll A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
        proposition:
          type: string
          description: The proposition that is going to be voted
        secrecy:
          type: string
          enum:
            - Public Votes (Anyone can see a person's vote)
            - Semi-secret Votes (Only team administrators can see a person's vote)
            - Secret Votes (It's impossible to track a person's vote)
          description: The secrecy of the Poll
        team_link:
          type: string
          format: uri
          x-lp-resource-type: team
          description: The team that this poll refers to.
        title:
          type: string
          description: The title of this poll
        type:
          type: string
          enum:
            - Simple Voting
            - Condorcet Voting
          description: The type of this poll
      required:
        - allowspoilt
        - datecloses
        - dateopens
        - name
        - proposition
        - secrecy
        - team_link
        - title
        - type
    poll-diff:
      type: object
      properties:
        allowspoilt:
          type: string
          description: Users can spoil their votes? Allow users to leave the ballot blank (i.e. cast a vote for "None of the above")
        datecloses:
          type: string
          description: The date and time when this poll closes
        dateopens:
          type: string
          description: The date and time when this poll opens
        name:
          type: string
          description: The unique name of this poll A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
        proposition:
          type: string
          description: The proposition that is going to be voted
        secrecy:
          type: string
          enum:
            - Public Votes (Anyone can see a person's vote)
            - Semi-secret Votes (Only team administrators can see a person's vote)
            - Secret Votes (It's impossible to track a person's vote)
          description: The secrecy of the Poll
        title:
          type: string
          description: The title of this poll
        type:
          type: string
          enum:
            - Simple Voting
            - Condorcet Voting
          description: The type of this poll
    poll-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: poll-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: poll-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    preview_diff-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: preview_diff
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        added_lines_count:
          type: string
          description: The number of lines added in this diff.
        branch_merge_proposal_link:
          type: string
          format: uri
          x-lp-resource-type: branch_merge_proposal
          description: The branch merge proposal that diff relates to.
        conflicts:
          type: string
          description: The conflicts text describing any path or text conflicts.
        date_created:
          type: string
          description: Date Created When this diff was created.
        diff_lines_count:
          type: string
          description: The number of lines in this diff.
        diff_text_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Content of this diff
        diffstat:
          type: string
          description: Statistics about this diff
        id:
          type: string
          description: DB ID The tracking number for this diff.
        prerequisite_revision_id:
          type: string
          description: The tip revision id of the prerequisite branch used to generate the diff.
        removed_lines_count:
          type: string
          description: The number of lines removed in this diff.
        source_revision_id:
          type: string
          description: The tip revision id of the source branch used to generate the diff.
        stale:
          type: string
          description: If the preview diff is stale, it is out of date when compared to the tip revisions of the source, target, and possibly prerequisite branches.
        target_revision_id:
          type: string
          description: The tip revision id of the target branch used to generate the diff.
        title:
          type: string
          description: Title PreviewDiff title.
      required:
        - added_lines_count
        - branch_merge_proposal_link
        - conflicts
        - date_created
        - diff_lines_count
        - diff_text_link
        - diffstat
        - id
        - prerequisite_revision_id
        - removed_lines_count
        - source_revision_id
        - stale
        - target_revision_id
        - title
    preview_diff-diff:
      type: object
    preview_diff-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: preview_diff-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: preview_diff-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    processor-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: processor
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        build_by_default:
          type: string
          description: Whether this processor is enabled on archives by default.
        description:
          type: string
          description: Description The Processor Description
        name:
          type: string
          description: Name The Processor Name
        restricted:
          type: string
          description: Whether this processor is restricted.
        supports_nonvirtualized:
          type: string
          description: Supports non-virtualized builds Whether the processor has non-virtualized builders. If not, all builds for this processor will build on virtualized builders, even for non-virtualized archives.
        supports_virtualized:
          type: string
          description: Supports virtualized builds Whether the processor has virtualized builders. If not, archives that require virtualized builds won't build on this processor.
        title:
          type: string
          description: Title The Processor Title
      required:
        - build_by_default
        - description
        - name
        - restricted
        - supports_nonvirtualized
        - supports_virtualized
        - title
    processor-diff:
      type: object
    processor-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    project-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Active Whether or not this item is active.
        active_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: The visible and active milestones associated with this object, ordered by date expected.
        all_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: All milestones associated with this object, ordered by date expected.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        branch_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Branch sharing policy Sharing policy for this project's branches.
        brand_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Brand A large image of exactly 192x192 pixels, that will be displayed on this project's home page in Launchpad. It should be no bigger than 100kb in size.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        bug_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Forbidden
            - Embargoed, can be proprietary
          description: Bug sharing policy Sharing policy for this project's bugs.
        bug_supervisor_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Bug Supervisor The Launchpad id of the person or team (preferred) responsible for bug management.
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: Bugs are tracked
        commercial_subscription_link:
          type: string
          format: uri
          x-lp-resource-type: commercial_subscription
          description: Commercial subscriptions An object which contains the timeframe and the voucher code of a subscription.
        commercial_subscription_is_due:
          type: string
          description: Commercial subscription is due Whether the project's licensing requires a new commercial subscription to use launchpad.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        date_created:
          type: string
          description: Date Created The date this project was created in Launchpad.
        description:
          type: string
          description: Description Details about the project's work, highlights, goals, and how to contribute. Use plain text, paragraphs are preserved and URLs are linked in pages. Don't repeat the Summary.
        development_focus_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: Development focus The series that represents the master or trunk branch. The Bazaar URL lp:<project> points to the development focus series branch.
        display_name:
          type: string
          description: Display Name The name of the project as it would appear in a paragraph.
        download_url:
          type: string
          description: Download URL The full URL where downloads for this project are located, if available. Please include the http://
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver This person or team will be able to set feature goals for and approve bug targeting or backporting for ANY major series in this project. You might want to leave this blank and just appoint a team for each specific series, rather than having one project team that does it all.
        freshmeat_project:
          type: string
          description: Freshmeat Project The Freshmeat project name for this project, if it is in freshmeat. [DEPRECATED]
        homepage_url:
          type: string
          description: Homepage URL The project home page. Please include the http://
        icon_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Icon A small image of exactly 14x14 pixels and at most 5kb in size, that can be used to identify this project. The icon will be displayed next to the project name everywhere in Launchpad that we refer to the project and link to it.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of data contained in this item.
        is_permitted:
          type: string
          description: Is Permitted Whether the project's licensing qualifies for free hosting or the project has an up-to-date subscription.
        license_approved:
          type: string
          description: Licence approved The project is legitimate and its licence appears valid. Not applicable to 'Other/Proprietary'.
        license_info:
          type: string
          description: Description of additional licences Description of licences that do not appear in the list above.
        licenses:
          type: string
          description: Licences
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to this project. It should be no bigger than 50kb in size.
        name:
          type: string
          description: Name At least one lowercase letter or number, followed by letters, numbers, dots, hyphens or pluses. Keep this name short; it is used in URLs as shown above.
        official_answers:
          type: string
          description: People can ask questions in Launchpad Answers
        official_blueprints:
          type: string
          description: This project uses blueprints
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        official_bugs:
          type: string
          description: Bugs in this project are tracked in Launchpad
        official_codehosting:
          type: string
          description: Code for this project is published in Bazaar branches on Launchpad
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Maintainer The restricted team, moderated team, or person who maintains the project information in Launchpad.
        private:
          type: string
          description: Product is confidential This product is visible only to those with access grants.
        programming_language:
          type: string
          description: Programming Languages A comma delimited list of programming languages used for this project.
        project_group_link:
          type: string
          format: uri
          x-lp-resource-type: project_group
          description: Part of Project group. This is an overarching initiative that includes several related projects. For example, the Mozilla Project produces Firefox, Thunderbird and Gecko. This information is used to group those projects in a coherent way. If you make this project part of a group, the group preferences and decisions around bug tracking, translation and security policy will apply to this project.
        project_reviewed:
          type: string
          description: Project reviewed Whether or not this project has been reviewed. If you looked at the project and how it uses Launchpad, you reviewed it.
        qualifies_for_free_hosting:
          type: string
          description: Qualifies for free hosting Whether the project's licensing qualifies it for free use of launchpad.
        recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
          description: All recipes associated with the object.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant This person registered the project in Launchpad.
        releases_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_release-page-resource
          description: An iterator over the ProductReleases for this product.
        remote_product:
          type: string
          description: Remote bug tracker project id Some bug trackers host multiple projects at the same URL and require an identifier for the specific project.
        reviewer_whiteboard:
          type: string
          description: Notes for the project reviewer Notes on the project's licence, editable only by reviewers (Admins and Commercial Admins).
        screenshots_url:
          type: string
          description: Screenshots URL The full URL for screenshots of this project, if available. Please include the http://
        series_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_series-page-resource
        sourceforge_project:
          type: string
          description: Sourceforge Project The SourceForge project name for this project, if it is in sourceforge.
        specification_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Blueprint sharing policy Sharing policy for this project's specifications.
        summary:
          type: string
          description: Summary A short paragraph to introduce the project's work.
        title:
          type: string
          description: Title The project title. Should be just a few words.
        translation_focus_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: Translation focus Project series that translators should focus on.
        translationgroup_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group
          description: Translation group The translation group that helps review translations for this project or distribution. The group's role depends on the permissions policy selected below.
        translationpermission:
          type: string
          enum:
            - Open
            - Structured
            - Restricted
            - Closed
          description: Translation permissions policy The policy this project or distribution uses to balance openness and control for their translations.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
        vcs:
          type: string
          enum:
            - Bazaar
            - Git
          description: VCS Version control system for this project's code.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
        wiki_url:
          type: string
          description: Wiki URL The full URL of this project's wiki, if it has one. Please include the http://
      required:
        - active
        - active_milestones_collection_link
        - all_milestones_collection_link
        - all_specifications_collection_link
        - branch_sharing_policy
        - brand_link
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - bug_sharing_policy
        - bug_supervisor_link
        - bug_tracker_link
        - commercial_subscription_link
        - commercial_subscription_is_due
        - content_templates
        - date_created
        - description
        - development_focus_link
        - display_name
        - download_url
        - driver_link
        - freshmeat_project
        - homepage_url
        - icon_link
        - information_type
        - is_permitted
        - license_approved
        - license_info
        - licenses
        - logo_link
        - name
        - official_answers
        - official_blueprints
        - official_bug_tags
        - official_bugs
        - official_codehosting
        - owner_link
        - private
        - programming_language
        - project_group_link
        - project_reviewed
        - qualifies_for_free_hosting
        - recipes_collection_link
        - registrant_link
        - releases_collection_link
        - remote_product
        - reviewer_whiteboard
        - screenshots_url
        - series_collection_link
        - sourceforge_project
        - specification_sharing_policy
        - summary
        - title
        - translation_focus_link
        - translationgroup_link
        - translationpermission
        - translations_usage
        - valid_specifications_collection_link
        - vcs
        - webhooks_collection_link
        - wiki_url
    project-diff:
      type: object
      properties:
        active:
          type: string
          description: Active Whether or not this item is active.
        branch_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Branch sharing policy Sharing policy for this project's branches.
        brand_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Brand A large image of exactly 192x192 pixels, that will be displayed on this project's home page in Launchpad. It should be no bigger than 100kb in size.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        bug_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Forbidden
            - Embargoed, can be proprietary
          description: Bug sharing policy Sharing policy for this project's bugs.
        bug_supervisor_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Bug Supervisor The Launchpad id of the person or team (preferred) responsible for bug management.
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: Bugs are tracked
        commercial_subscription_link:
          type: string
          format: uri
          x-lp-resource-type: commercial_subscription
          description: Commercial subscriptions An object which contains the timeframe and the voucher code of a subscription.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        description:
          type: string
          description: Description Details about the project's work, highlights, goals, and how to contribute. Use plain text, paragraphs are preserved and URLs are linked in pages. Don't repeat the Summary.
        development_focus_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: Development focus The series that represents the master or trunk branch. The Bazaar URL lp:<project> points to the development focus series branch.
        display_name:
          type: string
          description: Display Name The name of the project as it would appear in a paragraph.
        download_url:
          type: string
          description: Download URL The full URL where downloads for this project are located, if available. Please include the http://
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver This person or team will be able to set feature goals for and approve bug targeting or backporting for ANY major series in this project. You might want to leave this blank and just appoint a team for each specific series, rather than having one project team that does it all.
        freshmeat_project:
          type: string
          description: Freshmeat Project The Freshmeat project name for this project, if it is in freshmeat. [DEPRECATED]
        homepage_url:
          type: string
          description: Homepage URL The project home page. Please include the http://
        icon_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Icon A small image of exactly 14x14 pixels and at most 5kb in size, that can be used to identify this project. The icon will be displayed next to the project name everywhere in Launchpad that we refer to the project and link to it.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of data contained in this item.
        license_approved:
          type: string
          description: Licence approved The project is legitimate and its licence appears valid. Not applicable to 'Other/Proprietary'.
        license_info:
          type: string
          description: Description of additional licences Description of licences that do not appear in the list above.
        licenses:
          type: string
          description: Licences
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to this project. It should be no bigger than 50kb in size.
        name:
          type: string
          description: Name At least one lowercase letter or number, followed by letters, numbers, dots, hyphens or pluses. Keep this name short; it is used in URLs as shown above.
        official_answers:
          type: string
          description: People can ask questions in Launchpad Answers
        official_blueprints:
          type: string
          description: This project uses blueprints
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        official_bugs:
          type: string
          description: Bugs in this project are tracked in Launchpad
        official_codehosting:
          type: string
          description: Code for this project is published in Bazaar branches on Launchpad
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Maintainer The restricted team, moderated team, or person who maintains the project information in Launchpad.
        programming_language:
          type: string
          description: Programming Languages A comma delimited list of programming languages used for this project.
        project_group_link:
          type: string
          format: uri
          x-lp-resource-type: project_group
          description: Part of Project group. This is an overarching initiative that includes several related projects. For example, the Mozilla Project produces Firefox, Thunderbird and Gecko. This information is used to group those projects in a coherent way. If you make this project part of a group, the group preferences and decisions around bug tracking, translation and security policy will apply to this project.
        project_reviewed:
          type: string
          description: Project reviewed Whether or not this project has been reviewed. If you looked at the project and how it uses Launchpad, you reviewed it.
        remote_product:
          type: string
          description: Remote bug tracker project id Some bug trackers host multiple projects at the same URL and require an identifier for the specific project.
        reviewer_whiteboard:
          type: string
          description: Notes for the project reviewer Notes on the project's licence, editable only by reviewers (Admins and Commercial Admins).
        screenshots_url:
          type: string
          description: Screenshots URL The full URL for screenshots of this project, if available. Please include the http://
        sourceforge_project:
          type: string
          description: Sourceforge Project The SourceForge project name for this project, if it is in sourceforge.
        specification_sharing_policy:
          type: string
          enum:
            - Public
            - Public, can be proprietary
            - Proprietary, can be public
            - Proprietary
            - Embargoed, can be proprietary
            - Forbidden
          description: Blueprint sharing policy Sharing policy for this project's specifications.
        summary:
          type: string
          description: Summary A short paragraph to introduce the project's work.
        translation_focus_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: Translation focus Project series that translators should focus on.
        translationgroup_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group
          description: Translation group The translation group that helps review translations for this project or distribution. The group's role depends on the permissions policy selected below.
        translationpermission:
          type: string
          enum:
            - Open
            - Structured
            - Restricted
            - Closed
          description: Translation permissions policy The policy this project or distribution uses to balance openness and control for their translations.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        vcs:
          type: string
          enum:
            - Bazaar
            - Git
          description: VCS Version control system for this project's code.
        wiki_url:
          type: string
          description: Wiki URL The full URL of this project's wiki, if it has one. Please include the http://
    project-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    project_release-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: project_release
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        changelog:
          type: string
          description: Changelog A description of every change in the release.
        date_created:
          type: string
          description: Date Created The date this project release was created in Launchpad.
        date_released:
          type: string
          description: Date released The date this release was published. Before release, this should have an estimated release date.
        display_name:
          type: string
          description: Constructed display name for a project release.
        files_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_release_file-page-resource
          description: Project release files A list of files for this release.
        milestone_link:
          type: string
          format: uri
          x-lp-resource-type: milestone
          description: Milestone for this release A release requires a corresponding milestone that is not attached to another release.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The registrant of this release. The person or who registered this release.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that made this release.
        release_notes:
          type: string
          description: Release notes A description of important new features (though the changelog below might repeat some of this information).
        title:
          type: string
          description: Constructed title for a project release.
        version:
          type: string
          description: Version The specific version number assigned to this release. Letters and numbers are acceptable, for releases like "1.2rc3".
      required:
        - changelog
        - date_created
        - date_released
        - display_name
        - files_collection_link
        - milestone_link
        - owner_link
        - project_link
        - release_notes
        - title
        - version
    project_release-diff:
      type: object
      properties:
        changelog:
          type: string
          description: Changelog A description of every change in the release.
        date_released:
          type: string
          description: Date released The date this release was published. Before release, this should have an estimated release date.
        milestone_link:
          type: string
          format: uri
          x-lp-resource-type: milestone
          description: Milestone for this release A release requires a corresponding milestone that is not attached to another release.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The registrant of this release. The person or who registered this release.
        release_notes:
          type: string
          description: Release notes A description of important new features (though the changelog below might repeat some of this information).
    project_release-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_release-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_release-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    project_release_file-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: project_release_file
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_uploaded:
          type: string
          description: Upload date The date this file was uploaded
        description:
          type: string
          description: Description A detailed description of the file contents
        file_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: File The file contents.
        file_type:
          type: string
          enum:
            - Code Release Tarball
            - README File
            - Release Notes
            - ChangeLog File
            - Installer file
          description: Upstream file type
        project_release_link:
          type: string
          format: uri
          x-lp-resource-type: project_release
          description: Project release The parent product release.
        signature_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: File signature The file signature.
      required:
        - date_uploaded
        - description
        - file_link
        - file_type
        - project_release_link
        - signature_link
    project_release_file-diff:
      type: object
      properties:
        description:
          type: string
          description: Description A detailed description of the file contents
        file_type:
          type: string
          enum:
            - Code Release Tarball
            - README File
            - Release Notes
            - ChangeLog File
            - Installer file
          description: Upstream file type
        project_release_link:
          type: string
          format: uri
          x-lp-resource-type: project_release
          description: Project release The parent product release.
    project_release_file-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_release_file-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_release_file-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    project_series-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Active Whether or not this series is stable and supported, or under current development. This excludes series which are experimental or obsolete.
        active_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: The visible milestones associated with this project series, ordered by date expected.
        all_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: milestone-page-resource
          description: All milestones associated with this project series, ordered by date expected.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch The Bazaar branch for this series. Leave blank if this series is not maintained in Bazaar.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        date_created:
          type: string
          description: Date Registered
        display_name:
          type: string
          description: Display Name Display name. In this case we have removed the underlying database field, and this attribute just returns the name.
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Release manager The person or team responsible for decisions about features and bugs that will be targeted to this series. If you don't nominate someone here, then the owners and drivers of the project and project group will automatically have those permissions.
        drivers_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: A list of the people or teams who are drivers for this series. This list is made up of any drivers or owners from this series and the parent drivers.
        name:
          type: string
          description: Name The name of the series is a short, unique name that identifies it, being used in URLs. It must be all lowercase, with no special characters. For example, '2.0' or 'trunk'.
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner Project owner, either a valid Person or Team
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: Project
        release_finder_url_pattern:
          type: string
          description: "Release URL pattern A URL pattern that matches releases that are part of this series. Launchpad automatically scans this site to import new releases. Example: http://ftp.gnu.org/gnu/emacs/emacs-21.*.tar.gz"
        releases_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_release-page-resource
          description: An iterator over the releases in this Series, sorted with latest release first.
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        summary:
          type: string
          description: 'Summary A single paragraph that explains the goals of of this series and the intended users. For example: "The 2.0 series of Apache represents the current stable series, and is recommended for all new deployments".'
        title:
          type: string
          description: Title The product series title. Should be just a few words.
        translations_autoimport_mode:
          type: string
          enum:
            - None
            - Import template files
            - Import template and translation files
          description: Import settings Specify which files will be imported from the source code branch.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
      required:
        - active
        - active_milestones_collection_link
        - all_milestones_collection_link
        - all_specifications_collection_link
        - branch_link
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - content_templates
        - date_created
        - display_name
        - driver_link
        - drivers_collection_link
        - name
        - official_bug_tags
        - owner_link
        - project_link
        - release_finder_url_pattern
        - releases_collection_link
        - status
        - summary
        - title
        - translations_autoimport_mode
        - translations_usage
        - valid_specifications_collection_link
    project_series-diff:
      type: object
      properties:
        active:
          type: string
          description: Active Whether or not this series is stable and supported, or under current development. This excludes series which are experimental or obsolete.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch The Bazaar branch for this series. Leave blank if this series is not maintained in Bazaar.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        display_name:
          type: string
          description: Display Name Display name. In this case we have removed the underlying database field, and this attribute just returns the name.
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Release manager The person or team responsible for decisions about features and bugs that will be targeted to this series. If you don't nominate someone here, then the owners and drivers of the project and project group will automatically have those permissions.
        name:
          type: string
          description: Name The name of the series is a short, unique name that identifies it, being used in URLs. It must be all lowercase, with no special characters. For example, '2.0' or 'trunk'.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner Project owner, either a valid Person or Team
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: Project
        release_finder_url_pattern:
          type: string
          description: "Release URL pattern A URL pattern that matches releases that are part of this series. Launchpad automatically scans this site to import new releases. Example: http://ftp.gnu.org/gnu/emacs/emacs-21.*.tar.gz"
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        summary:
          type: string
          description: 'Summary A single paragraph that explains the goals of of this series and the intended users. For example: "The 2.0 series of Apache represents the current stable series, and is recommended for all new deployments".'
        title:
          type: string
          description: Title The product series title. Should be just a few words.
        translations_autoimport_mode:
          type: string
          enum:
            - None
            - Import template files
            - Import template and translation files
          description: Import settings Specify which files will be imported from the source code branch.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
    project_series-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_series-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_series-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    project_group-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: project_group
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Active Whether or not this item is active.
        active_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_group_milestone-page-resource
          description: The visible and active milestones associated with this object, ordered by date expected.
        all_milestones_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_group_milestone-page-resource
          description: All milestones associated with this object, ordered by date expected.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: If I’m reporting a bug, I should include, if possible These guidelines will be shown to anyone reporting a bug.
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: Bug Tracker The bug tracker the projects in this project group use.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        date_created:
          type: string
          description: Date Created The date this project group was created in Launchpad.
        description:
          type: string
          description: Description Details about the project group's work, goals, and how to contribute. Use plain text, paragraphs are preserved and URLs are linked in pages. Don't repeat the Summary.
        display_name:
          type: string
          description: 'Display Name Appropriately capitalised, and typically ending in "Project". Examples: the Apache Project, the Mozilla Project, the Gimp Project.'
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver This is a project group-wide appointment. Think carefully here! This person or team will be able to set feature goals and approve bug targeting and backporting for ANY series in ANY project in this group. You can also appoint drivers at the level of a specific project or series. So you may just want to leave this space blank, and instead let the individual projects and series have drivers.
        freshmeat_project:
          type: string
          description: Freshmeat Project Name The Freshmeat project name for this project group, if it is in Freshmeat. [DEPRECATED]
        homepage_content:
          type: string
          description: Homepage Content The content of this project group's home page. Edit this and it will be displayed for all the world to see. It is NOT a wiki so you cannot undo changes.
        homepage_url:
          type: string
          description: Homepage URL The project group home page. Please include the http://
        icon_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Icon A small image of exactly 14x14 pixels and at most 5kb in size, that can be used to identify this project group. The icon will be displayed in Launchpad everywhere that we link to this project group. For example in listings or tables of active project groups.
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to this project group. It should be no bigger than 50kb in size.
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Brand A large image of exactly 192x192 pixels, that will be displayed on this project group's home page in Launchpad. It should be no bigger than 100kb in size.
        name:
          type: string
          description: "Name A unique name, used in URLs, identifying the project group. All lowercase, no special characters. Examples: apache, mozilla, gimp."
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Maintainer The restricted team, moderated team, or person who maintains the project group information in Launchpad.
        projects_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project-page-resource
          description: List of active projects for this project group.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant Project group registrant. Must be a valid Launchpad Person.
        reviewed:
          type: string
          description: Reviewed Whether or not this project group has been reviewed.
        sourceforge_project:
          type: string
          description: SourceForge Project Name The SourceForge project name for this project group, if it is in SourceForge.
        summary:
          type: string
          description: Project Group Summary A short paragraph to introduce the project group's work.
        title:
          type: string
          description: Title The full name of the project group, which can contain spaces, special characters, etc.
        translationgroup_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group
          description: Translation group The translation group that helps review translations for this project or distribution. The group's role depends on the permissions policy selected below.
        translationpermission:
          type: string
          enum:
            - Open
            - Structured
            - Restricted
            - Closed
          description: Translation permissions policy The policy this project or distribution uses to balance openness and control for their translations.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
        wiki_url:
          type: string
          description: Wiki URL The URL of this project group's wiki, if it has one. Please include the http://
      required:
        - active
        - active_milestones_collection_link
        - all_milestones_collection_link
        - all_specifications_collection_link
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - bug_tracker_link
        - content_templates
        - date_created
        - description
        - display_name
        - driver_link
        - freshmeat_project
        - homepage_content
        - homepage_url
        - icon_link
        - logo_link
        - mugshot_link
        - name
        - official_bug_tags
        - owner_link
        - projects_collection_link
        - registrant_link
        - reviewed
        - sourceforge_project
        - summary
        - title
        - translationgroup_link
        - translationpermission
        - translations_usage
        - valid_specifications_collection_link
        - wiki_url
    project_group-diff:
      type: object
      properties:
        active:
          type: string
          description: Active Whether or not this item is active.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: If I’m reporting a bug, I should include, if possible These guidelines will be shown to anyone reporting a bug.
        bug_tracker_link:
          type: string
          format: uri
          x-lp-resource-type: bug_tracker
          description: Bug Tracker The bug tracker the projects in this project group use.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        description:
          type: string
          description: Description Details about the project group's work, goals, and how to contribute. Use plain text, paragraphs are preserved and URLs are linked in pages. Don't repeat the Summary.
        display_name:
          type: string
          description: 'Display Name Appropriately capitalised, and typically ending in "Project". Examples: the Apache Project, the Mozilla Project, the Gimp Project.'
        driver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Driver This is a project group-wide appointment. Think carefully here! This person or team will be able to set feature goals and approve bug targeting and backporting for ANY series in ANY project in this group. You can also appoint drivers at the level of a specific project or series. So you may just want to leave this space blank, and instead let the individual projects and series have drivers.
        freshmeat_project:
          type: string
          description: Freshmeat Project Name The Freshmeat project name for this project group, if it is in Freshmeat. [DEPRECATED]
        homepage_content:
          type: string
          description: Homepage Content The content of this project group's home page. Edit this and it will be displayed for all the world to see. It is NOT a wiki so you cannot undo changes.
        homepage_url:
          type: string
          description: Homepage URL The project group home page. Please include the http://
        icon_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Icon A small image of exactly 14x14 pixels and at most 5kb in size, that can be used to identify this project group. The icon will be displayed in Launchpad everywhere that we link to this project group. For example in listings or tables of active project groups.
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to this project group. It should be no bigger than 50kb in size.
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Brand A large image of exactly 192x192 pixels, that will be displayed on this project group's home page in Launchpad. It should be no bigger than 100kb in size.
        name:
          type: string
          description: "Name A unique name, used in URLs, identifying the project group. All lowercase, no special characters. Examples: apache, mozilla, gimp."
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Maintainer The restricted team, moderated team, or person who maintains the project group information in Launchpad.
        reviewed:
          type: string
          description: Reviewed Whether or not this project group has been reviewed.
        sourceforge_project:
          type: string
          description: SourceForge Project Name The SourceForge project name for this project group, if it is in SourceForge.
        summary:
          type: string
          description: Project Group Summary A short paragraph to introduce the project group's work.
        title:
          type: string
          description: Title The full name of the project group, which can contain spaces, special characters, etc.
        translationgroup_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group
          description: Translation group The translation group that helps review translations for this project or distribution. The group's role depends on the permissions policy selected below.
        translationpermission:
          type: string
          enum:
            - Open
            - Structured
            - Restricted
            - Closed
          description: Translation permissions policy The policy this project or distribution uses to balance openness and control for their translations.
        translations_usage:
          type: string
          enum:
            - Unknown
            - Launchpad
            - External
            - Not Applicable
          description: Type of service for translations application Where does this pillar do translations?
        wiki_url:
          type: string
          description: Wiki URL The URL of this project group's wiki, if it has one. Please include the http://
    project_group-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_group-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_group-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    project_group_milestone-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: project_group_milestone
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        code_name:
          type: string
          description: Code name An alternative name for the milestone.
        date_targeted:
          type: string
          description: "Date Targeted Example: 2005-11-24"
        is_active:
          type: string
          description: Active Whether or not this object should be shown in web forms for targeting.
        name:
          type: string
          description: Name Only letters, numbers, and simple punctuation are allowed.
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        release_link:
          type: string
          format: uri
          x-lp-resource-type: project_release
          description: The release for this milestone.
        series_target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The productseries or distroseries of this milestone.
        summary:
          type: string
          description: Summary A summary of the features and status of this milestone.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The product, distribution, or project group for this milestone.
        title:
          type: string
          description: A context title for pages.
      required:
        - code_name
        - date_targeted
        - is_active
        - name
        - official_bug_tags
        - release_link
        - series_target_link
        - summary
        - target_link
        - title
    project_group_milestone-diff:
      type: object
      properties:
        code_name:
          type: string
          description: Code name An alternative name for the milestone.
        date_targeted:
          type: string
          description: "Date Targeted Example: 2005-11-24"
        is_active:
          type: string
          description: Active Whether or not this object should be shown in web forms for targeting.
        name:
          type: string
          description: Name Only letters, numbers, and simple punctuation are allowed.
        series_target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The productseries or distroseries of this milestone.
        summary:
          type: string
          description: Summary A summary of the features and status of this milestone.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: has_milestones
          description: The product, distribution, or project group for this milestone.
    project_group_milestone-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_group_milestone-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: project_group_milestone-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    question-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: question
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        answer_link:
          type: string
          format: uri
          x-lp-resource-type: question_message
          description: Answer The IQuestionMessage that contains the answer confirmed by the owner as providing a solution to their problem.
        answerer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Answered By The person who last provided a response intended to resolve the question.
        assignee_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Assignee The person responsible for helping to resolve the question.
        date_created:
          type: string
          description: Date Created
        date_due:
          type: string
          description: Date Due The date by which we should have resolved this question.
        date_last_query:
          type: string
          description: Date Last Queried The date on which we last heard from the customer (owner).
        date_last_response:
          type: string
          description: Date last Responded The date on which we last communicated with the customer. The combination of datelastquery and datelastresponse tells us in whose court the ball is.
        date_solved:
          type: string
          description: Date Answered The date on which the question owner confirmed that the question is Solved.
        description:
          type: string
          description: "Description Include as much detail as possible: what you’re trying to achieve, what steps you take, what happens, and what you think should happen instead."
        id:
          type: string
          description: Question Number The tracking number for this question.
        language_link:
          type: string
          format: uri
          x-lp-resource-type: language
          description: Language The language in which this question is written.
        messages_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question_message-page-resource
          description: Messages The list of messages that were exchanged as part of this question , sorted from first to last.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
        status:
          type: string
          enum:
            - Open
            - Needs information
            - Answered
            - Solved
            - Expired
            - Invalid
          description: Status
        target_link:
          type: string
          format: uri
          x-lp-resource-type: question_target
          description: This question is about The distribution, source package, or project the question pertains to.
        title:
          type: string
          description: Summary A one-line summary of the issue or problem.
      required:
        - answer_link
        - answerer_link
        - assignee_link
        - date_created
        - date_due
        - date_last_query
        - date_last_response
        - date_solved
        - description
        - id
        - language_link
        - messages_collection_link
        - owner_link
        - status
        - target_link
        - title
    question-diff:
      type: object
      properties:
        assignee_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Assignee The person responsible for helping to resolve the question.
        description:
          type: string
          description: "Description Include as much detail as possible: what you’re trying to achieve, what steps you take, what happens, and what you think should happen instead."
        language_link:
          type: string
          format: uri
          x-lp-resource-type: language
          description: Language The language in which this question is written.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: question_target
          description: This question is about The distribution, source package, or project the question pertains to.
        title:
          type: string
          description: Summary A one-line summary of the issue or problem.
    question-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    question_message-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: question_message
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        action:
          type: string
          enum:
            - Request for more information
            - Give more information
            - Comment
            - Answer
            - Confirm
            - Reject
            - Expire
            - Reopen
            - Change status
          description: Action operated on the question by this message.
        bug_attachments_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_attachment-page-resource
          description: A list of BugAttachments connected to this message.
        content:
          type: string
          description: All the text/plain chunks joined together as a unicode string.
        date_created:
          type: string
          description: Date Created
        date_deleted:
          type: string
          description: When this message was deleted
        date_last_edited:
          type: string
          description: When this message was last edited
        index:
          type: string
          description: Human readable Message index. The message's index in the question's list of messages.
        new_status:
          type: string
          enum:
            - Open
            - Needs information
            - Answered
            - Solved
            - Expired
            - Invalid
          description: Question status after message The status of the question after the transition related the action operated by this message.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person
        parent_link:
          type: string
          format: uri
          x-lp-resource-type: message
          description: Parent
        question_link:
          type: string
          format: uri
          x-lp-resource-type: question
          description: The question related to this message. An IQuestion object.
        revisions_collection_link:
          type: string
          format: uri
          x-lp-resource-type: message_revision-page-resource
          description: Message revision history Revision history of this message, sorted in ascending order.
        subject:
          type: string
          description: Subject
        visible:
          type: string
          description: Message visibility. Whether or not the message is visible.
      required:
        - action
        - bug_attachments_collection_link
        - content
        - date_created
        - date_deleted
        - date_last_edited
        - index
        - new_status
        - owner_link
        - parent_link
        - question_link
        - revisions_collection_link
        - subject
        - visible
    question_message-diff:
      type: object
    question_message-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question_message-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question_message-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    question_subscription-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: question_subscription
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_created:
          type: string
          description: Date subscribed
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Person The person's Launchpad ID or email address.
        question_link:
          type: string
          format: uri
          x-lp-resource-type: question
          description: Question
      required:
        - date_created
        - person_link
        - question_link
    question_subscription-diff:
      type: object
    question_subscription-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question_subscription-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question_subscription-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    question_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: question_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    question_target-diff:
      type: object
    question_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: question_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    revision_status_report-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: revision_status_report
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        ci_build_link:
          type: string
          format: uri
          x-lp-resource-type: ci_build
          description: The CI build that produced this report.
        commit_sha1:
          type: string
          description: The Git commit for which this report is built.
        date_created:
          type: string
          description: When the report was created.
        date_finished:
          type: string
          description: When the report has finished.
        date_started:
          type: string
          description: When the report was started.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture for the CI build job that produced this report.
        git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: The Git repository for which this report is built.
        properties:
          type: string
          description: Metadata for artifacts attached to this report
        result:
          type: string
          enum:
            - Waiting
            - Running
            - Succeeded
            - Failed
            - Skipped
            - Cancelled
          description: Result of the report
        result_summary:
          type: string
          description: A short summary of the result.
        title:
          type: string
          description: A short title for the report.
        url:
          type: string
          description: URL The external url of the report.
      required:
        - ci_build_link
        - commit_sha1
        - date_created
        - date_finished
        - date_started
        - distro_arch_series_link
        - git_repository_link
        - properties
        - result
        - result_summary
        - title
        - url
    revision_status_report-diff:
      type: object
      properties:
        date_finished:
          type: string
          description: When the report has finished.
        date_started:
          type: string
          description: When the report was started.
        result:
          type: string
          enum:
            - Waiting
            - Running
            - Succeeded
            - Failed
            - Skipped
            - Cancelled
          description: Result of the report
        result_summary:
          type: string
          description: A short summary of the result.
        title:
          type: string
          description: A short title for the report.
        url:
          type: string
          description: URL The external url of the report.
    revision_status_report-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: revision_status_report-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: revision_status_report-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    rock_base-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: rock_base
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        build_channels:
          type: string
          description: Source snap channels for builds A dictionary mapping snap names to channels to use when building rock recipes that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
        date_created:
          type: string
          description: Date created
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro series
        processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
          description: Processors The architectures that the rock base supports.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this base.
      required:
        - build_channels
        - date_created
        - distro_series_link
        - processors_collection_link
        - registrant_link
    rock_base-diff:
      type: object
      properties:
        build_channels:
          type: string
          description: Source snap channels for builds A dictionary mapping snap names to channels to use when building rock recipes that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
    rock_base-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_base-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_base-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    rock_recipe_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        arch_tag:
          type: string
          description: Architecture tag
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The Archive context for this build.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        build_metadata_url:
          type: string
          description: URL of the build metadata file URL of the metadata file generated by the fetch service, if it exists.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        channels:
          type: string
          description: Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Currently only 'core', 'core18', 'core20', and 'rockcraft' keys are supported.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture for which to build.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe
          description: The rock recipe to build.
        requester_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who requested this build.
        revision_id:
          type: string
          description: Revision ID The revision ID of the branch used for this build, if available.
        score:
          type: string
          description: Score of the related build farm job (if any).
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - arch_tag
        - archive_link
        - build_log_url
        - build_metadata_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - channels
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_arch_series_link
        - distro_series_link
        - duration
        - pocket
        - recipe_link
        - requester_link
        - revision_id
        - score
        - title
        - upload_log_url
    rock_recipe_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        channels:
          type: string
          description: Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Currently only 'core', 'core18', 'core20', and 'rockcraft' keys are supported.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    rock_recipe_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    rock_recipe_build_request-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build_request
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        architectures:
          type: string
          description: If set, this request is limited to these architecture tags
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build-page-resource
          description: Builds produced by this request
        date_finished:
          type: string
          description: The time when this request finished
        date_requested:
          type: string
          description: The time when this request was made
        error_message:
          type: string
          description: Error message
        recipe_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe
          description: Rock recipe
        status:
          type: string
          enum:
            - Pending
            - Failed
            - Completed
          description: Status
      required:
        - architectures
        - builds_collection_link
        - date_finished
        - date_requested
        - error_message
        - recipe_link
        - status
    rock_recipe_build_request-diff:
      type: object
    rock_recipe_build_request-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build_request-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build_request-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    rock_recipe-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this rock recipe is built automatically when the branch containing its rockcraft.yaml recipe changes.
        auto_build_channels:
          type: string
          description: Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this rock recipe. Currently only 'core', 'core18', 'core20', and 'rockcraft' keys are supported.
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing rockcraft.yaml.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build-page-resource
          description: All builds of this rock recipe. All builds of this rock recipe, sorted in descending order of finishing (or starting if not completed successfully).
        can_upload_to_store:
          type: string
          description: Can upload to the RockStore Whether everything is set up to allow uploading builds of this rockrecipe to the RockStore.
        completed_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build-page-resource
          description: Completed builds of this rock recipe. Completed builds of this rock recipe, sorted in descending order of finishing.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        description:
          type: string
          description: Description A description of the rock recipe.
        failed_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build_request-page-resource
          description: Failed build requests for this rock recipe.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False.
        git_path:
          type: string
          description: Git branch path The path of the Git branch containing a rockcraft.yaml recipe.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a rockcraft.yaml recipe.
        git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Git repository A Git repository with a branch containing a rockcraft.yaml recipe.
        git_repository_url:
          type: string
          description: Git repository URL The URL of a Git repository with a branch containing a rockcraft.yaml at the top level.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this rock recipe.
        is_stale:
          type: string
          description: Rock recipe is stale and is due to be rebuilt.
        name:
          type: string
          description: Rock recipe name The name of the rock recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this rock recipe.
        pending_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build_request-page-resource
          description: Pending build requests for this rock recipe.
        pending_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe_build-page-resource
          description: Pending builds of this rock recipe. Pending builds of this rock recipe, sorted in descending order of creation.
        private:
          type: string
          description: Private Whether this rock recipe is private.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this rock recipe is associated with
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this rock recipe.
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this rock recipe on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this rock to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store name The registered name of this rock in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this rock recipe are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Rock builds will use the fetch-service instead of the builder-proxy to access external resources.
      required:
        - auto_build
        - auto_build_channels
        - build_path
        - builds_collection_link
        - can_upload_to_store
        - completed_builds_collection_link
        - date_created
        - date_last_modified
        - description
        - failed_build_requests_collection_link
        - fetch_service_policy
        - git_path
        - git_ref_link
        - git_repository_link
        - git_repository_url
        - information_type
        - is_stale
        - name
        - owner_link
        - pending_build_requests_collection_link
        - pending_builds_collection_link
        - private
        - project_link
        - registrant_link
        - require_virtualized
        - store_channels
        - store_name
        - store_upload
        - use_fetch_service
    rock_recipe-diff:
      type: object
      properties:
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this rock recipe is built automatically when the branch containing its rockcraft.yaml recipe changes.
        auto_build_channels:
          type: string
          description: Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this rock recipe. Currently only 'core', 'core18', 'core20', and 'rockcraft' keys are supported.
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing rockcraft.yaml.
        description:
          type: string
          description: Description A description of the rock recipe.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. Ignored if use_fetch_service flag is False.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a rockcraft.yaml recipe.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this rock recipe.
        name:
          type: string
          description: Rock recipe name The name of the rock recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this rock recipe.
        private:
          type: string
          description: Private Whether this rock recipe is private.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: The project that this rock recipe is associated with
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this rock recipe on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this rock to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store name The registered name of this rock in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this rock recipe are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Rock builds will use the fetch-service instead of the builder-proxy to access external resources.
    rock_recipe-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: rock_recipe-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    ssh_key-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: ssh_key
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        comment:
          type: string
          description: Comment describing this key
        keytext:
          type: string
          description: Key text
        keytype:
          type: string
          enum:
            - RSA
            - DSA
            - ECDSA
            - ED25519
            - SKED25519
            - SKECDSA
          description: Key type
      required:
        - comment
        - keytext
        - keytype
    ssh_key-diff:
      type: object
    ssh_key-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: ssh_key-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: ssh_key-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    service_factory-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: service_factory
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    service_factory-diff:
      type: object
    service_factory-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: service_factory-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: service_factory-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    sharing_service-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: sharing_service
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        name:
          type: string
          description: Name The name of the service, used to generate the url.
      required:
        - name
    sharing_service-diff:
      type: object
      properties:
        name:
          type: string
          description: Name The name of the service, used to generate the url.
    sharing_service-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: sharing_service-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: sharing_service-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    snap_base-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: snap_base
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        build_channels:
          type: string
          description: Source snap channels for builds A dictionary mapping snap names to channels to use when building snaps that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
        date_created:
          type: string
          description: Date created
        dependencies_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive_dependency-page-resource
          description: Archive dependencies for this snap base.
        display_name:
          type: string
          description: Display name
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro series
        features:
          type: string
          description: Features supported by this base A dictionary designating the features supported by the base. Key is the name of a feature, value is a boolean indicating whether the feature is supported or not.
        is_default:
          type: string
          description: Is default? Whether this base is the default for snaps that do not specify a base.
        name:
          type: string
          description: Name
        processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
          description: Processors The architectures that the snap base supports.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this base.
      required:
        - build_channels
        - date_created
        - dependencies_collection_link
        - display_name
        - distro_series_link
        - features
        - is_default
        - name
        - processors_collection_link
        - registrant_link
    snap_base-diff:
      type: object
      properties:
        build_channels:
          type: string
          description: Source snap channels for builds A dictionary mapping snap names to channels to use when building snaps that specify this base. The special '_byarch' key may have a mapping of architecture names to mappings of snap names to channels, which if present override the channels declared at the top level when building for those architectures.
        display_name:
          type: string
          description: Display name
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro series
        features:
          type: string
          description: Features supported by this base A dictionary designating the features supported by the base. Key is the name of a feature, value is a boolean indicating whether the feature is supported or not.
        name:
          type: string
          description: Name
    snap_base-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_base-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_base-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    snap_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        arch_tag:
          type: string
          description: Architecture tag
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: The archive from which to build the snap package.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        build_metadata_url:
          type: string
          description: URL of the build metadata file URL of the metadata file generated by the fetch service, if it exists.
        build_request_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build_request
          description: The build request that caused this build to be created.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        channels:
          type: string
          description: "Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Supported snap names: 'core', 'core18', 'core20', 'core22', 'core24', 'core26', 'snapcraft', 'snapd'"
        craft_platform:
          type: string
          description: Craft platform name
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_arch_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_arch_series
          description: The series and architecture to build on.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: The pocket for which to build. The package stream within the source archive and distribution series to use when building the snap package. If the source archive is a PPA, then the PPA's archive dependencies will be used to select the pocket in the distribution's primary archive.
        requester_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who requested this build.
        revision_id:
          type: string
          description: Revision ID The revision ID of the branch used for this build, if available.
        score:
          type: string
          description: Score of the related build farm job (if any).
        snap_link:
          type: string
          format: uri
          x-lp-resource-type: snap
          description: The snap package to build.
        snap_base_link:
          type: string
          format: uri
          x-lp-resource-type: snap_base
          description: The snap base to use for this build.
        store_upload_error_message:
          type: string
          description: Store upload error message The error message, if any, from the last attempt to upload this snap build to the store. (Deprecated; use store_upload_error_messages instead.)
        store_upload_error_messages:
          type: string
          description: Store upload error messages A list of dict(message, link) where message is an error description and link, if any, is an external link to extra details, from the last attempt to upload this snap build to the store.
        store_upload_revision:
          type: string
          description: Store revision The revision assigned to this package by the store.
        store_upload_status:
          type: string
          enum:
            - Unscheduled
            - Pending
            - Failed to upload
            - Failed to release to channels
            - Uploaded
          description: Store upload status
        store_upload_url:
          type: string
          description: Store URL The URL to use for managing this package in the store.
        target_architectures:
          type: string
          description: The target architectures to build for.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - arch_tag
        - archive_link
        - build_log_url
        - build_metadata_url
        - build_request_link
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - channels
        - craft_platform
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_arch_series_link
        - distro_series_link
        - duration
        - pocket
        - requester_link
        - revision_id
        - score
        - snap_link
        - snap_base_link
        - store_upload_error_message
        - store_upload_error_messages
        - store_upload_revision
        - store_upload_status
        - store_upload_url
        - target_architectures
        - title
        - upload_log_url
    snap_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        channels:
          type: string
          description: "Source snap channels to use for this build. A dictionary mapping snap names to channels to use for this build. Supported snap names: 'core', 'core18', 'core20', 'core22', 'core24', 'core26', 'snapcraft', 'snapd'"
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        store_upload_status:
          type: string
          enum:
            - Unscheduled
            - Pending
            - Failed to upload
            - Failed to release to channels
            - Uploaded
          description: Store upload status
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    snap_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    snap_build_request-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build_request
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build-page-resource
          description: Builds produced by this request
        date_finished:
          type: string
          description: The time when this request finished
        date_requested:
          type: string
          description: The time when this request was made
        error_message:
          type: string
          description: Error message
        snap_link:
          type: string
          format: uri
          x-lp-resource-type: snap
          description: Snap package
        status:
          type: string
          enum:
            - Pending
            - Failed
            - Completed
          description: Status
      required:
        - builds_collection_link
        - date_finished
        - date_requested
        - error_message
        - snap_link
        - status
    snap_build_request-diff:
      type: object
    snap_build_request-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build_request-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build_request-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    snap-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: snap
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        allow_internet:
          type: string
          description: Allow external network access Allow access to external network resources via a proxy. Resources hosted on Launchpad itself are always allowed.
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this snap package is built automatically when the branch containing its snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe changes.
        auto_build_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Source archive for automatic builds The archive from which automatic builds of this snap package should be built.
        auto_build_channels:
          type: string
          description: "Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this snap package. Supported snap names: 'core', 'core18', 'core20', 'core22', 'core24', 'core26', 'snapcraft', 'snapd'"
        auto_build_pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket for automatic builds The package stream within the source archive and distribution series to use when building the snap package. If the source archive is a PPA, then the PPA's archive dependencies will be used to select the pocket in the distribution's primary archive.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Bazaar branch A Bazaar branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing snapcraft.yaml.
        build_source_tarball:
          type: string
          description: Build source tarball Whether builds of this snap package should also build a tarball containing all source code, including external dependencies.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build-page-resource
          description: All builds of this snap package. All builds of this snap package, sorted in descending order of finishing (or starting if not completed successfully).
        can_upload_to_store:
          type: string
          description: Can upload to store Whether everything is set up to allow uploading builds of this snap package to the store.
        completed_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build-page-resource
          description: Completed builds of this snap package. Completed builds of this snap package, sorted in descending order of finishing.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        description:
          type: string
          description: Description A description of the snap package.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro Series The series for which the snap package should be built. If not set, Launchpad will infer an appropriate series from snapcraft.yaml.
        failed_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build_request-page-resource
          description: Failed build requests for this snap package.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. The “strict” mode only allows certain resources and formats, and errors out in any case the restrictions are violated. The “permissive” mode works similarly, but only logs a warning when encountering any violations.
        git_path:
          type: string
          description: Git branch path The path of the Git branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: Git repository A Git repository with a branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        git_repository_url:
          type: string
          description: Git repository URL The URL of a Git repository with a branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this Snap recipe.
        is_stale:
          type: string
          description: Snap package is stale and is due to be rebuilt.
        name:
          type: string
          description: Snap recipe name The name of the snap build recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this snap package.
        pending_build_requests_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build_request-page-resource
          description: Pending build requests for this snap package.
        pending_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap_build-page-resource
          description: Pending builds of this snap package. Pending builds of this snap package, sorted in descending order of creation.
        private:
          type: string
          description: Private Whether or not this snap is private.
        pro_enable:
          type: string
          description: Enable Ubuntu Pro Allow building this snap recipe using dependencies from Ubuntu Pro, if configured for the corresponding snap base.
        processors_collection_link:
          type: string
          format: uri
          x-lp-resource-type: processor-page-resource
          description: Processors The architectures for which the snap package should be built.
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this snap package.
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this snap package on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this snap package to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store package name The registered name of this snap package in the store.
        store_series_link:
          type: string
          format: uri
          x-lp-resource-type: snappy_series
          description: Store series The series in which this snap package should be published in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this snap package are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Snap builds will use the fetch-service instead of the builder-proxy to access external resources.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - allow_internet
        - auto_build
        - auto_build_archive_link
        - auto_build_channels
        - auto_build_pocket
        - branch_link
        - build_path
        - build_source_tarball
        - builds_collection_link
        - can_upload_to_store
        - completed_builds_collection_link
        - date_created
        - date_last_modified
        - description
        - distro_series_link
        - failed_build_requests_collection_link
        - fetch_service_policy
        - git_path
        - git_ref_link
        - git_repository_link
        - git_repository_url
        - information_type
        - is_stale
        - name
        - owner_link
        - pending_build_requests_collection_link
        - pending_builds_collection_link
        - private
        - pro_enable
        - processors_collection_link
        - registrant_link
        - require_virtualized
        - store_channels
        - store_name
        - store_series_link
        - store_upload
        - use_fetch_service
        - webhooks_collection_link
    snap-diff:
      type: object
      properties:
        allow_internet:
          type: string
          description: Allow external network access Allow access to external network resources via a proxy. Resources hosted on Launchpad itself are always allowed.
        auto_build:
          type: string
          description: Automatically build when branch changes Whether this snap package is built automatically when the branch containing its snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe changes.
        auto_build_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Source archive for automatic builds The archive from which automatic builds of this snap package should be built.
        auto_build_channels:
          type: string
          description: "Source snap channels for automatic builds A dictionary mapping snap names to channels to use when building this snap package. Supported snap names: 'core', 'core18', 'core20', 'core22', 'core24', 'core26', 'snapcraft', 'snapd'"
        auto_build_pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket for automatic builds The package stream within the source archive and distribution series to use when building the snap package. If the source archive is a PPA, then the PPA's archive dependencies will be used to select the pocket in the distribution's primary archive.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Bazaar branch A Bazaar branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        build_path:
          type: string
          description: Build path Subdirectory within the branch containing snapcraft.yaml.
        build_source_tarball:
          type: string
          description: Build source tarball Whether builds of this snap package should also build a tarball containing all source code, including external dependencies.
        description:
          type: string
          description: Description A description of the snap package.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distro Series The series for which the snap package should be built. If not set, Launchpad will infer an appropriate series from snapcraft.yaml.
        fetch_service_policy:
          type: string
          enum:
            - strict
            - permissive
          description: Fetch service policy Which policy to use when using the fetch service. The “strict” mode only allows certain resources and formats, and errors out in any case the restrictions are violated. The “permissive” mode works similarly, but only logs a warning when encountering any violations.
        git_path:
          type: string
          description: Git branch path The path of the Git branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        git_ref_link:
          type: string
          format: uri
          x-lp-resource-type: git_ref
          description: Git branch The Git branch containing a snap/snapcraft.yaml, build-aux/snap/snapcraft.yaml, snapcraft.yaml, or .snapcraft.yaml recipe at the top level.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type The type of information contained in this Snap recipe.
        name:
          type: string
          description: Snap recipe name The name of the snap build recipe.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The owner of this snap package.
        private:
          type: string
          description: Private Whether or not this snap is private.
        pro_enable:
          type: string
          description: Enable Ubuntu Pro Allow building this snap recipe using dependencies from Ubuntu Pro, if configured for the corresponding snap base.
        require_virtualized:
          type: string
          description: Require virtualized builders Only build this snap package on virtual builders.
        store_channels:
          type: string
          description: Store channels Channels to release this snap package to after uploading it to the store. A channel is defined by a combination of an optional track, a risk, and an optional branch, e.g. '2.1/stable/fix-123', '2.1/stable', 'stable/fix-123', or 'stable'.
        store_name:
          type: string
          description: Registered store package name The registered name of this snap package in the store.
        store_series_link:
          type: string
          format: uri
          x-lp-resource-type: snappy_series
          description: Store series The series in which this snap package should be published in the store.
        store_upload:
          type: string
          description: Automatically upload to store Whether builds of this snap package are automatically uploaded to the store.
        use_fetch_service:
          type: string
          description: Use fetch service If set, Snap builds will use the fetch-service instead of the builder-proxy to access external resources.
    snap-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snap-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    snappy_series-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: snappy_series
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        can_infer_distro_series:
          type: string
          description: Can infer distro series? True if inferring a distro series from snapcraft.yaml is supported for this snappy series.
        date_created:
          type: string
          description: Date created
        display_name:
          type: string
          description: Display name
        name:
          type: string
          description: Name
        preferred_distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Preferred distro series
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who registered this snappy series.
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        usable_distro_series:
          type: string
          description: Usable distro series The distro series that can be used for this snappy series.
      required:
        - can_infer_distro_series
        - date_created
        - display_name
        - name
        - preferred_distro_series_link
        - registrant_link
        - status
        - usable_distro_series
    snappy_series-diff:
      type: object
      properties:
        can_infer_distro_series:
          type: string
          description: Can infer distro series? True if inferring a distro series from snapcraft.yaml is supported for this snappy series.
        display_name:
          type: string
          description: Display name
        name:
          type: string
          description: Name
        preferred_distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Preferred distro series
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        usable_distro_series:
          type: string
          description: Usable distro series The distro series that can be used for this snappy series.
    snappy_series-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snappy_series-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: snappy_series-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    social_account-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: social_account
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        identity:
          type: string
          description: "Identity A dictionary with the identity attributes and values for the social account. The format is specific for each platform. Matrix account attributes: username, homeserver"
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
        platform:
          type: string
          enum:
            - Matrix platform
            - GitHub platform
          description: Social Platform Type
      required:
        - identity
        - person_link
        - platform
    social_account-diff:
      type: object
      properties:
        identity:
          type: string
          description: "Identity A dictionary with the identity attributes and values for the social account. The format is specific for each platform. Matrix account attributes: username, homeserver"
        platform:
          type: string
          enum:
            - Matrix platform
            - GitHub platform
          description: Social Platform Type
    social_account-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: social_account-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: social_account-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    source_package-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: source_package
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
        displayname:
          type: string
          description: Display name A displayname, constructed, for this package
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution The distribution for this source package.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution Series The DistroSeries for this SourcePackage
        latest_published_component_name:
          type: string
          description: The name of the component in which the package was last published.
        name:
          type: string
          description: Name The text name of this source package.
        official_bug_tags:
          type: string
          description: Official Bug Tags The list of bug tags defined as official.
        productseries_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: Project series The registered project series that this source package is based on. This series may be the same as the one that earlier versions of this source packages were based on.
      required:
        - bug_reported_acknowledgement
        - bug_reporting_guidelines
        - content_templates
        - displayname
        - distribution_link
        - distroseries_link
        - latest_published_component_name
        - name
        - official_bug_tags
        - productseries_link
    source_package-diff:
      type: object
      properties:
        bug_reported_acknowledgement:
          type: string
          description: After reporting a bug, I can expect the following. This message of acknowledgement will be displayed to anyone after reporting a bug.
        bug_reporting_guidelines:
          type: string
          description: Helpful guidelines for reporting a bug These guidelines will be shown to everyone reporting a bug and should be text or a bulleted list with your particular requirements, if any.
        content_templates:
          type: string
          description: Templates to use for reporting a bug This pre-defined template will be given to the users to guide them when reporting a bug.
    source_package-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    source_package_publishing_history-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_publishing_history
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive ID
        component_name:
          type: string
          description: Component Name
        copied_from_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Original archive ID where this package was copied from.
        creator_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Publication Creator The IPerson who created this publication.
        date_created:
          type: string
          description: The date on which this record was created
        date_made_pending:
          type: string
          description: The date on which this record was set as pending removal
        date_published:
          type: string
          description: The date on which this record was published
        date_removed:
          type: string
          description: The date on which this record was removed from the published set
        date_superseded:
          type: string
          description: The date on which this record was marked superseded
        display_name:
          type: string
          description: Display Name Text representation of the current record.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: The distro series being published into
        package_creator_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Package Creator The IPerson who created the source package.
        package_maintainer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Package Maintainer The IPerson who maintains the source package.
        package_signer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Package Signer The IPerson who signed the source package.
        packageupload_link:
          type: string
          format: uri
          x-lp-resource-type: package_upload
          description: Package upload The Package Upload that caused the creation of this publication.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The pocket into which this entry is published
        removal_comment:
          type: string
          description: Reason why this publication is going to be removed.
        removed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The IPerson responsible for the removal
        scheduled_deletion_date:
          type: string
          description: The date on which this record is scheduled for deletion
        section_name:
          type: string
          description: Section Name
        source_package_name:
          type: string
          description: Source Package Name
        source_package_version:
          type: string
          description: Source Package Version
        sponsor_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Publication sponsor The IPerson who sponsored the creation of this publication.
        status:
          type: string
          enum:
            - Pending
            - Published
            - Superseded
            - Deleted
            - Obsolete
          description: Package Publishing Status The status of this publishing record
      required:
        - archive_link
        - component_name
        - copied_from_archive_link
        - creator_link
        - date_created
        - date_made_pending
        - date_published
        - date_removed
        - date_superseded
        - display_name
        - distro_series_link
        - package_creator_link
        - package_maintainer_link
        - package_signer_link
        - packageupload_link
        - pocket
        - removal_comment
        - removed_by_link
        - scheduled_deletion_date
        - section_name
        - source_package_name
        - source_package_version
        - sponsor_link
        - status
    source_package_publishing_history-diff:
      type: object
      properties:
        date_created:
          type: string
          description: The date on which this record was created
        date_made_pending:
          type: string
          description: The date on which this record was set as pending removal
        date_published:
          type: string
          description: The date on which this record was published
        date_removed:
          type: string
          description: The date on which this record was removed from the published set
        date_superseded:
          type: string
          description: The date on which this record was marked superseded
        display_name:
          type: string
          description: Display Name Text representation of the current record.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: The distro series being published into
        removal_comment:
          type: string
          description: Reason why this publication is going to be removed.
        removed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The IPerson responsible for the removal
        scheduled_deletion_date:
          type: string
          description: The date on which this record is scheduled for deletion
        status:
          type: string
          enum:
            - Pending
            - Published
            - Superseded
            - Deleted
            - Obsolete
          description: Package Publishing Status The status of this publishing record
    source_package_publishing_history-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_publishing_history-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_publishing_history-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    source_package_recipe_build-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe_build
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Archive The Archive context for this build.
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        builder_link:
          type: string
          format: uri
          x-lp-resource-type: builder
          description: Builder The builder assigned to this job.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        can_be_cancelled:
          type: string
          description: Can be cancelled Whether this build record can be cancelled.
        can_be_rescored:
          type: string
          description: Can be rescored Whether this build record can be rescored manually.
        can_be_retried:
          type: string
          description: Can be retried Whether this build record can be retried.
        date_first_dispatched:
          type: string
          description: Date finished The actual build start time. Set when the build is dispatched the first time and not changed in subsequent build attempts.
        date_started:
          type: string
          description: Date started The timestamp when the build farm job was started.
        datebuilt:
          type: string
          description: Date finished The timestamp when the build farm job was finished.
        datecreated:
          type: string
          description: Date created The timestamp when the build farm job was created.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        duration:
          type: string
          description: Duration Duration interval, calculated when the result gets collected.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
      required:
        - archive_link
        - build_log_url
        - builder_link
        - buildstate
        - can_be_cancelled
        - can_be_rescored
        - can_be_retried
        - date_first_dispatched
        - date_started
        - datebuilt
        - datecreated
        - dependencies
        - distribution_link
        - distro_series_link
        - duration
        - pocket
        - title
        - upload_log_url
    source_package_recipe_build-diff:
      type: object
      properties:
        build_log_url:
          type: string
          description: Build Log URL A URL for the build log. None if there is no log available.
        buildstate:
          type: string
          enum:
            - Needs building
            - Successfully built
            - Failed to build
            - Dependency wait
            - Chroot problem
            - Build for superseded Source
            - Currently building
            - Failed to upload
            - Uploading build
            - Cancelling build
            - Cancelled build
            - Gathering build output
          description: Status The current status of the job.
        dependencies:
          type: string
          description: Dependencies Debian-like dependency line that must be satisfied before attempting to build this request.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution Shortcut for its distribution.
        distro_series_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Distribution series Shortcut for its distribution series.
        pocket:
          type: string
          enum:
            - Release
            - Security
            - Updates
            - Proposed
            - Backports
          description: Pocket The build targeted pocket.
        title:
          type: string
          description: Title
        upload_log_url:
          type: string
          description: Upload Log URL A URL for failed upload logs.Will be None if there was no failure.
    source_package_recipe_build-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe_build-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe_build-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    source_package_recipe-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        base_branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: The base branch used by this recipe.
        base_git_repository_link:
          type: string
          format: uri
          x-lp-resource-type: git_repository
          description: The base Git repository used by this recipe.
        build_daily:
          type: string
          description: Built daily Automatically build each day, if the source has changed.
        builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe_build-page-resource
          description: All builds of this recipe. All builds of this recipe, sorted in descending order of finishing (or starting if notcompleted successfully).
        completed_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe_build-page-resource
          description: The completed builds of this recipe. Completed builds of this recipe, sorted in descending order of finishing (or starting if notcompleted successfully).
        daily_build_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: The archive to use for daily builds.
        date_last_modified:
          type: string
        deb_version_template:
          type: string
          description: deb-version template The template that will be used to generate a deb version.
        description:
          type: string
          description: Description A short description of the recipe.
        distroseries:
          type: string
          description: Default distribution series If built daily, these are the distribution versions that the recipe will be built for.
        is_stale:
          type: string
          description: Recipe is stale.
        last_build_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe_build
          description: The the most recent build of this recipe.
        name:
          type: string
          description: Name The name of the recipe is part of the URL and needs to be unique for the given owner.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The person or team who can edit this recipe.
        pending_builds_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe_build-page-resource
          description: The pending builds of this recipe. Pending builds of this recipe, sorted in descending order of creation.
        recipe_text:
          type: string
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who created this recipe.
      required:
        - base_branch_link
        - base_git_repository_link
        - build_daily
        - builds_collection_link
        - completed_builds_collection_link
        - daily_build_archive_link
        - date_last_modified
        - deb_version_template
        - description
        - distroseries
        - is_stale
        - last_build_link
        - name
        - owner_link
        - pending_builds_collection_link
        - recipe_text
        - registrant_link
    source_package_recipe-diff:
      type: object
      properties:
        build_daily:
          type: string
          description: Built daily Automatically build each day, if the source has changed.
        daily_build_archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: The archive to use for daily builds.
        description:
          type: string
          description: Description A short description of the recipe.
        distroseries:
          type: string
          description: Default distribution series If built daily, these are the distribution versions that the recipe will be built for.
        name:
          type: string
          description: Name The name of the recipe is part of the URL and needs to be unique for the given owner.
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner The person or team who can edit this recipe.
        recipe_text:
          type: string
    source_package_recipe-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    specification_branch-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: specification_branch
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who linked the bug to the branch
        specification_link:
          type: string
          format: uri
          x-lp-resource-type: specification
          description: Blueprint
      required:
        - branch_link
        - registrant_link
        - specification_link
    specification_branch-diff:
      type: object
      properties:
        branch_link:
          type: string
          format: uri
          x-lp-resource-type: branch
          description: Branch
    specification_branch-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_branch-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_branch-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    specification-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: specification
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        approver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Approver The person responsible for approving the specification, and for reviewing the code when it's ready to be landed.
        assignee_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Assignee The person responsible for implementing the feature.
        bugs_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug-page-resource
          description: Bugs related to this object.
        completer_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Starter The person who finally set the state of the spec to the values that we consider mark it as complete.
        date_completed:
          type: string
          description: Date Completed The date when this spec was marked complete. Note that complete also includes "obsolete" and superseded. Essentially, it is the state where no more work will be done on the feature.
        date_created:
          type: string
          description: Date Created
        date_started:
          type: string
          description: Date Started The date when this spec was marked started.
        definition_status:
          type: string
          enum:
            - Approved
            - Pending Approval
            - Review
            - Drafting
            - Discussion
            - New
            - Superseded
            - Obsolete
          description: Definition Status The current status of the process to define the feature and get approval for the implementation plan.
        dependencies_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Specs on which this one depends.
        direction_approved:
          type: string
          description: Basic direction approved? Check this to indicate that the drafter and assignee have satisfied the approver that they are headed in the right basic direction with this specification.
        drafter_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Drafter The person responsible for drafting the specification.
        has_accepted_goal:
          type: string
          description: Series goal is accepted Is true if this specification has been proposed as a goal for a specific series, and the drivers of that series have accepted the goal.
        implementation_status:
          type: string
          enum:
            - Unknown
            - Not started
            - Deferred
            - Needs Infrastructure
            - Blocked
            - Started
            - Slow progress
            - Good progress
            - Beta Available
            - Needs Code Review
            - Deployment
            - Implemented
            - Informational
          description: Implementation Status The state of progress being made on the actual implementation or delivery of this feature.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of information contained in this specification.
        is_complete:
          type: string
          description: Is started Is True if this spec is already completely implemented. Note that it is True for informational specs, since they describe general functionality rather than specific code to be written. It is also true of obsolete and superseded specs, since there is no longer any need to schedule work for them.
        is_started:
          type: string
          description: Is started Is True if the spec is in a state which we consider to be "started". This looks at the delivery attribute, and also considers informational specs to be started when they are approved.
        lifecycle_status:
          type: string
          enum:
            - Not started
            - Started
            - Complete
          description: Lifecycle Status
        linked_branches_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_branch-page-resource
          description: Branches associated with this spec, usually branches on which this spec is being implemented.
        milestone_link:
          type: string
          format: uri
          x-lp-resource-type: milestone
          description: Milestone The milestone in which we would like this feature to be delivered.
        name:
          type: string
          description: "Name May contain lower-case letters, numbers, and dashes. It will be used in the specification url. Examples: mozilla-type-ahead-find, postgres-smart-serial."
        owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
        priority:
          type: string
          enum:
            - Not
            - Undefined
            - Low
            - Medium
            - High
            - Essential
          description: Priority
        specification_url:
          type: string
          description: Specification URL The URL of the specification. This is usually a wiki page.
        starter_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Starter The person who first set the state of the spec to the values that we consider mark it as started.
        summary:
          type: string
          description: Summary A single-paragraph description of the feature. This will also be displayed in most feature listings.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: specification_target
          description: For The project for which this proposal is being made.
        title:
          type: string
          description: Title Describe the feature as clearly as possible in up to 70 characters. This title is displayed in every feature list or report.
        whiteboard:
          type: string
          description: Status Whiteboard Any notes on the status of this spec you would like to make. Your changes will override the current text.
        workitems_text:
          type: string
          description: Work Items Work items for this specification input in a text format. Your changes will override the current work items.
      required:
        - approver_link
        - assignee_link
        - bugs_collection_link
        - completer_link
        - date_completed
        - date_created
        - date_started
        - definition_status
        - dependencies_collection_link
        - direction_approved
        - drafter_link
        - has_accepted_goal
        - implementation_status
        - information_type
        - is_complete
        - is_started
        - lifecycle_status
        - linked_branches_collection_link
        - milestone_link
        - name
        - owner_link
        - priority
        - specification_url
        - starter_link
        - summary
        - target_link
        - title
        - whiteboard
        - workitems_text
    specification-diff:
      type: object
      properties:
        approver_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Approver The person responsible for approving the specification, and for reviewing the code when it's ready to be landed.
        assignee_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Assignee The person responsible for implementing the feature.
        definition_status:
          type: string
          enum:
            - Approved
            - Pending Approval
            - Review
            - Drafting
            - Discussion
            - New
            - Superseded
            - Obsolete
          description: Definition Status The current status of the process to define the feature and get approval for the implementation plan.
        direction_approved:
          type: string
          description: Basic direction approved? Check this to indicate that the drafter and assignee have satisfied the approver that they are headed in the right basic direction with this specification.
        drafter_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Drafter The person responsible for drafting the specification.
        implementation_status:
          type: string
          enum:
            - Unknown
            - Not started
            - Deferred
            - Needs Infrastructure
            - Blocked
            - Started
            - Slow progress
            - Good progress
            - Beta Available
            - Needs Code Review
            - Deployment
            - Implemented
            - Informational
          description: Implementation Status The state of progress being made on the actual implementation or delivery of this feature.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information Type The type of information contained in this specification.
        milestone_link:
          type: string
          format: uri
          x-lp-resource-type: milestone
          description: Milestone The milestone in which we would like this feature to be delivered.
        name:
          type: string
          description: "Name May contain lower-case letters, numbers, and dashes. It will be used in the specification url. Examples: mozilla-type-ahead-find, postgres-smart-serial."
        priority:
          type: string
          enum:
            - Not
            - Undefined
            - Low
            - Medium
            - High
            - Essential
          description: Priority
        specification_url:
          type: string
          description: Specification URL The URL of the specification. This is usually a wiki page.
        summary:
          type: string
          description: Summary A single-paragraph description of the feature. This will also be displayed in most feature listings.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: specification_target
          description: For The project for which this proposal is being made.
        title:
          type: string
          description: Title Describe the feature as clearly as possible in up to 70 characters. This title is displayed in every feature list or report.
        whiteboard:
          type: string
          description: Status Whiteboard Any notes on the status of this spec you would like to make. Your changes will override the current text.
        workitems_text:
          type: string
          description: Work Items Work items for this specification input in a text format. Your changes will override the current work items.
    specification-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    specification_subscription-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: specification_subscription
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    specification_subscription-diff:
      type: object
    specification_subscription-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_subscription-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_subscription-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    specification_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: specification_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
      required:
        - all_specifications_collection_link
        - valid_specifications_collection_link
    specification_target-diff:
      type: object
    specification_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    structural_subscription-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        bug_filters_collection_link:
          type: string
          format: uri
          x-lp-resource-type: bug_subscription_filter-page-resource
          description: List of bug filters that narrow this subscription.
        date_created:
          type: string
          description: The date on which this subscription was created.
        date_last_updated:
          type: string
          description: The date on which this subscription was last updated.
        subscribed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Subscribed by The person creating the subscription.
        subscriber_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Subscriber The person subscribed.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription_target
          description: The structure to which this subscription belongs.
      required:
        - bug_filters_collection_link
        - date_created
        - date_last_updated
        - subscribed_by_link
        - subscriber_link
        - target_link
    structural_subscription-diff:
      type: object
    structural_subscription-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    structural_subscription_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
    structural_subscription_target-diff:
      type: object
    structural_subscription_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: structural_subscription_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    team-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: team
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        account_status:
          type: string
          enum:
            - Placeholder
            - Unactivated
            - Active
            - Deactivated
            - Suspended
            - Closed
            - Deceased
          description: The status of this person's account
        account_status_history:
          type: string
          description: Account status history
        admins_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: List of this team's admins.
        all_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: All specifications A list of all specifications, regardless of status or approval or completion, for this object.
        archive_link:
          type: string
          format: uri
          x-lp-resource-type: archive
          description: Default PPA The PPA named 'ppa' owned by this person.
        confirmed_email_addresses_collection_link:
          type: string
          format: uri
          x-lp-resource-type: email_address-page-resource
          description: Confirmed emails of this person. Confirmed emails are the ones in the VALIDATED state
        date_created:
          type: string
          description: Date Created
        deactivated_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Former members of the team.
        default_membership_period:
          type: string
          description: Subscription period Number of days a new subscription lasts before expiring. You can customize the length of an individual subscription when approving it. Leave this empty or set to 0 for subscriptions to never expire.
        default_renewal_period:
          type: string
          description: Self renewal period Number of days members can renew their own membership. The number can be from 1 to 3650 (10 years).
        description:
          type: string
          description: Description Details about interests and goals. Use plain text, paragraphs are preserved and URLs are linked.
        display_name:
          type: string
          description: Display Name Your name as you would like it displayed throughout Launchpad. Most people use their full name here.
        expired_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Expired members of the team.
        gpg_keys_collection_link:
          type: string
          format: uri
          x-lp-resource-type: gpg_key-page-resource
          description: List of valid OpenPGP keys ordered by ID
        hide_email_addresses:
          type: string
          description: Hide my email addresses from other Launchpad users
        homepage_content:
          type: string
          description: Homepage Content Obsolete. Use description.
        id:
          type: string
          description: ID Internal immutable identifier for this person. Only visible by privileged users.
        invited_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: Other teams which have been invited to become members of this team.
        irc_nicknames_collection_link:
          type: string
          format: uri
          x-lp-resource-type: irc_id-page-resource
          description: List of IRC nicknames of this Person.
        is_probationary:
          type: string
          description: Is this a probationary user?
        is_team:
          type: string
          description: Is this object a team?
        is_ubuntu_coc_signer:
          type: string
          description: Signed Ubuntu Code of Conduct
        is_valid:
          type: string
          description: This is an active user or a team.
        jabber_ids_collection_link:
          type: string
          format: uri
          x-lp-resource-type: jabber_id-page-resource
          description: List of Jabber IDs of this Person.
        karma:
          type: string
          description: Karma The cached total karma for this person.
        languages_collection_link:
          type: string
          format: uri
          x-lp-resource-type: language-page-resource
          description: List of languages known by this person
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to you. Traditionally this is a logo, a small picture or a personal mascot. It should be no bigger than 50kb in size.
        mailing_list_auto_subscribe_policy:
          type: string
          enum:
            - Never subscribe to mailing lists
            - Ask me when I join a team
            - Always subscribe me to mailing lists
          description: Mailing List Auto-subscription Policy This attribute determines whether a person is automatically subscribed to a team's mailing list when the person joins said team.
        members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: List of direct members with ADMIN or APPROVED status
        members_details_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
          description: Active TeamMemberships for this object's members. Active TeamMemberships are the ones with the ADMIN or APPROVED status. The results are ordered using Person.sortingColumns.
        membership_policy:
          type: string
          enum:
            - Open Team
            - Delegated Team
            - Moderated Team
            - Restricted Team
          description: Membership policy The policies that describe who can be a member. The choice of policy reflects the need to build a community (inclusive) versus the need to control Launchpad projects, branches, and PPAs (exclusive).
        memberships_details_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
          description: All TeamMemberships for Teams this Team or Person is an active member of.
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Mugshot A large image of exactly 192x192 pixels, that will be displayed on your home page in Launchpad. Traditionally this is a great big picture of your grinning face. Make the most of it! It should be no bigger than 100kb in size.
        name:
          type: string
          description: Name A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
        open_membership_invitations_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
          description: Open membership invitations. All TeamMemberships which represent an invitation (to join a team) sent to this person.
        participants_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: All participants of this team. List of all direct and indirect people and teams who, one way or another, are a part of this team. If you want a method to check if a given person is a member of a team, you should probably look at IPerson.inTeam().
        ppas_collection_link:
          type: string
          format: uri
          x-lp-resource-type: archive-page-resource
          description: PPAs for this person. PPAs owned by the context person ordered by name.
        preferred_email_address_link:
          type: string
          format: uri
          x-lp-resource-type: email_address
          description: Preferred email address The preferred email address for this person. The one we'll use to communicate with them.
        private:
          type: string
          description: This team is private Private teams are visible only to their members.
        proposed_members_collection_link:
          type: string
          format: uri
          x-lp-resource-type: person-page-resource
          description: People who have applied to join the team.
        recipes_collection_link:
          type: string
          format: uri
          x-lp-resource-type: source_package_recipe-page-resource
          description: All recipes associated with the object.
        renewal_policy:
          type: string
          enum:
            - invite them to apply for renewal
            - invite them to renew their own membership
          description: When someone's membership is about to expire, notify them and
        social_accounts_collection_link:
          type: string
          format: uri
          x-lp-resource-type: social_account-page-resource
          description: List of Social Accounts of this Person.
        sshkeys_collection_link:
          type: string
          format: uri
          x-lp-resource-type: ssh_key-page-resource
          description: List of SSH keys
        sub_teams_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team-page-resource
          description: "All subteams of this team. A subteam is any team that is a member (either directly or indirectly) of this team. As an example, let's say we have this hierarchy of teams: Rosetta Translators Rosetta pt Translators Rosetta pt_BR Translators In this case, both 'Rosetta pt Translators' and 'Rosetta pt_BR Translators' are subteams of the 'Rosetta Translators' team, and all members of both subteams are considered members of \"Rosetta Translators\"."
        subscription_policy:
          type: string
          enum:
            - Open Team
            - Delegated Team
            - Moderated Team
            - Restricted Team
          description: "Membership policy Obsolete: use membership_policy"
        super_teams_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team-page-resource
          description: "All superteams of this team. A superteam is any team that this team is a member of. For example, let's say we have this hierarchy of teams, and we are the \"Rosetta pt_BR Translators\": Rosetta Translators Rosetta pt Translators Rosetta pt_BR Translators In this case, we will return both 'Rosetta pt Translators' and 'Rosetta Translators', because we are member of both of them."
        team_description:
          type: string
          description: Team Description Obsolete. Use description.
        team_owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Team Owner
        time_zone:
          type: string
          description: The time zone of this object.
        valid_specifications_collection_link:
          type: string
          format: uri
          x-lp-resource-type: specification-page-resource
          description: Valid specifications All specifications that are not obsolete. When called from an ISpecificationGoal it will also exclude the ones that have not been accepted for that goal
        visibility:
          type: string
          enum:
            - Public
            - Private
          description: Visibility Anyone can see a public team's data. Only team members can see private team data.
        wiki_names_collection_link:
          type: string
          format: uri
          x-lp-resource-type: wiki_name-page-resource
          description: All WikiNames of this Person, sorted alphabetically by URL.
      required:
        - account_status
        - account_status_history
        - admins_collection_link
        - all_specifications_collection_link
        - archive_link
        - confirmed_email_addresses_collection_link
        - date_created
        - deactivated_members_collection_link
        - default_membership_period
        - default_renewal_period
        - description
        - display_name
        - expired_members_collection_link
        - gpg_keys_collection_link
        - hide_email_addresses
        - homepage_content
        - id
        - invited_members_collection_link
        - irc_nicknames_collection_link
        - is_probationary
        - is_team
        - is_ubuntu_coc_signer
        - is_valid
        - jabber_ids_collection_link
        - karma
        - languages_collection_link
        - logo_link
        - mailing_list_auto_subscribe_policy
        - members_collection_link
        - members_details_collection_link
        - membership_policy
        - memberships_details_collection_link
        - mugshot_link
        - name
        - open_membership_invitations_collection_link
        - participants_collection_link
        - ppas_collection_link
        - preferred_email_address_link
        - private
        - proposed_members_collection_link
        - recipes_collection_link
        - renewal_policy
        - social_accounts_collection_link
        - sshkeys_collection_link
        - sub_teams_collection_link
        - subscription_policy
        - super_teams_collection_link
        - team_description
        - team_owner_link
        - time_zone
        - valid_specifications_collection_link
        - visibility
        - wiki_names_collection_link
    team-diff:
      type: object
      properties:
        default_membership_period:
          type: string
          description: Subscription period Number of days a new subscription lasts before expiring. You can customize the length of an individual subscription when approving it. Leave this empty or set to 0 for subscriptions to never expire.
        default_renewal_period:
          type: string
          description: Self renewal period Number of days members can renew their own membership. The number can be from 1 to 3650 (10 years).
        description:
          type: string
          description: Description Details about interests and goals. Use plain text, paragraphs are preserved and URLs are linked.
        display_name:
          type: string
          description: Display Name Your name as you would like it displayed throughout Launchpad. Most people use their full name here.
        hide_email_addresses:
          type: string
          description: Hide my email addresses from other Launchpad users
        homepage_content:
          type: string
          description: Homepage Content Obsolete. Use description.
        logo_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Logo An image of exactly 64x64 pixels that will be displayed in the heading of all pages related to you. Traditionally this is a logo, a small picture or a personal mascot. It should be no bigger than 50kb in size.
        mailing_list_auto_subscribe_policy:
          type: string
          enum:
            - Never subscribe to mailing lists
            - Ask me when I join a team
            - Always subscribe me to mailing lists
          description: Mailing List Auto-subscription Policy This attribute determines whether a person is automatically subscribed to a team's mailing list when the person joins said team.
        membership_policy:
          type: string
          enum:
            - Open Team
            - Delegated Team
            - Moderated Team
            - Restricted Team
          description: Membership policy The policies that describe who can be a member. The choice of policy reflects the need to build a community (inclusive) versus the need to control Launchpad projects, branches, and PPAs (exclusive).
        mugshot_link:
          type: string
          format: uri
          x-lp-resource-type: HostedFile
          description: Mugshot A large image of exactly 192x192 pixels, that will be displayed on your home page in Launchpad. Traditionally this is a great big picture of your grinning face. Make the most of it! It should be no bigger than 100kb in size.
        name:
          type: string
          description: Name A short unique name, beginning with a lower-case letter or number, and containing only letters, numbers, dots, hyphens, or plus signs.
        renewal_policy:
          type: string
          enum:
            - invite them to apply for renewal
            - invite them to renew their own membership
          description: When someone's membership is about to expire, notify them and
        subscription_policy:
          type: string
          enum:
            - Open Team
            - Delegated Team
            - Moderated Team
            - Restricted Team
          description: "Membership policy Obsolete: use membership_policy"
        team_description:
          type: string
          description: Team Description Obsolete. Use description.
        team_owner_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Team Owner
        visibility:
          type: string
          enum:
            - Public
            - Private
          description: Visibility Anyone can see a public team's data. Only team members can see private team data.
    team-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    team_membership-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_expires:
          type: string
          description: Date expires
        date_joined:
          type: string
          description: Date joined The date in which this membership was made active for the first time.
        last_change_comment:
          type: string
          description: Comment on the last change
        last_changed_by_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Last person who change this
        member_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Member
        status:
          type: string
          enum:
            - Proposed
            - Approved
            - Administrator
            - Deactivated
            - Expired
            - Declined
            - Invited
            - Invitation declined
          description: The state of this membership
        team_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Team
      required:
        - date_expires
        - date_joined
        - last_change_comment
        - last_changed_by_link
        - member_link
        - status
        - team_link
    team_membership-diff:
      type: object
    team_membership-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: team_membership-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    temporary_blob-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: temporary_blob
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        token:
          type: string
          description: UUID
      required:
        - token
    temporary_blob-diff:
      type: object
    temporary_blob-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: temporary_blob-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: temporary_blob-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    timeline_project_series-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: timeline_project_series
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        is_development_focus:
          type: string
          description: Is series the development focus of the project
        landmarks:
          type: string
          description: List of milestones and releases
        name:
          type: string
          description: Name The name of the series is a short, unique name that identifies it, being used in URLs. It must be all lowercase, with no special characters. For example, '2.0' or 'trunk'.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: Project
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        uri:
          type: string
          description: Series URI foo
      required:
        - is_development_focus
        - landmarks
        - name
        - project_link
        - status
        - uri
    timeline_project_series-diff:
      type: object
      properties:
        is_development_focus:
          type: string
          description: Is series the development focus of the project
        landmarks:
          type: string
          description: List of milestones and releases
        name:
          type: string
          description: Name The name of the series is a short, unique name that identifies it, being used in URLs. It must be all lowercase, with no special characters. For example, '2.0' or 'trunk'.
        project_link:
          type: string
          format: uri
          x-lp-resource-type: project
          description: Project
        status:
          type: string
          enum:
            - Experimental
            - Active Development
            - Pre-release Freeze
            - Current Stable Release
            - Supported
            - Obsolete
            - Future
          description: Status
        uri:
          type: string
          description: Series URI foo
    timeline_project_series-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: timeline_project_series-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: timeline_project_series-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    translation_group-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        name:
          type: string
          description: "Name Keep this name very short, unique, and descriptive, because it will be used in URLs. Examples: gnome-translation-project, ubuntu-translators."
        title:
          type: string
          description: Title Title of this Translation Group. This title is displayed at the top of the Translation Group page and in lists or reports of translation groups. Do not add "translation group" to this title, or it will be shown double.
      required:
        - name
        - title
    translation_group-diff:
      type: object
      properties:
        name:
          type: string
          description: "Name Keep this name very short, unique, and descriptive, because it will be used in URLs. Examples: gnome-translation-project, ubuntu-translators."
        title:
          type: string
          description: Title Title of this Translation Group. This title is displayed at the top of the Translation Group page and in lists or reports of translation groups. Do not add "translation group" to this title, or it will be shown double.
    translation_group-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_group-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    translation_import_queue_entry-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: translation_import_queue_entry
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_created:
          type: string
          description: The timestamp when this queue entry was created.
        date_status_changed:
          type: string
          description: The timestamp when the status was changed.
        distroseries_link:
          type: string
          format: uri
          x-lp-resource-type: distro_series
          description: Series
        error_output:
          type: string
          description: Error output Output from most recent import attempt.
        format:
          type: string
          enum:
            - PO format
            - MO format
            - Mozilla XPI format
            - KDE PO format
            - XPI PO format
            - Changes from imported translations in partial PO format
          description: The file format of the import.
        id:
          type: string
          description: The entry ID
        path:
          type: string
          description: Path The path to this file inside the source tree. Includes the filename.
        productseries_link:
          type: string
          format: uri
          x-lp-resource-type: project_series
          description: Series
        sourcepackage_link:
          type: string
          format: uri
          x-lp-resource-type: source_package
          description: The sourcepackage associated with this entry.
        status:
          type: string
          enum:
            - Approved
            - Imported
            - Deleted
            - Failed
            - Needs Review
            - Blocked
            - Needs Information
          description: The status of the import.
        uploader_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Uploader The person that uploaded this file to Launchpad.
      required:
        - date_created
        - date_status_changed
        - distroseries_link
        - error_output
        - format
        - id
        - path
        - productseries_link
        - sourcepackage_link
        - status
        - uploader_link
    translation_import_queue_entry-diff:
      type: object
      properties:
        date_status_changed:
          type: string
          description: The timestamp when the status was changed.
        path:
          type: string
          description: Path The path to this file inside the source tree. Includes the filename.
    translation_import_queue_entry-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_import_queue_entry-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: translation_import_queue_entry-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    vulnerability-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: vulnerability
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        creator_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: The person who created the vulnerability.
        cve_link:
          type: string
          format: uri
          x-lp-resource-type: cve
          description: External CVE reference corresponding to this vulnerability, if any.
        cvss:
          type: string
          description: List of Common Vulnerability Scoring System.
        date_coordinated_release:
          type: string
          description: Coordinated Release Date.
        date_created:
          type: string
          description: The date this vulnerability was created.
        date_made_public:
          type: string
          description: The date this vulnerability was made public.
        date_notice_issued:
          type: string
          description: Date when a security notice was issued for this vulnerability.
        description:
          type: string
          description: A short description of the vulnerability.
        distribution_link:
          type: string
          format: uri
          x-lp-resource-type: distribution
          description: Distribution
        id:
          type: string
          description: ID
        importance:
          type: string
          enum:
            - Unknown
            - Undecided
            - Critical
            - High
            - Medium
            - Low
            - Wishlist
          description: Indicates the work priority, not the severity
        importance_explanation:
          type: string
          description: Used to explain why our importance differs from somebody else's CVSS score.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type Indicates the privacy of the vulnerability.
        metadata:
          type: string
          description: Metadata Metadata of Vulnerability
        mitigation:
          type: string
          description: Explains why we're ignoring this vulnerability.
        notes:
          type: string
          description: Free-form notes for this vulnerability.
        private:
          type: string
          description: Private Private vulnerabilities are visible only to their subscribers.
        status:
          type: string
          enum:
            - Needs triage
            - Active
            - Ignored
            - Retired
          description: The status of the vulnerability.
      required:
        - creator_link
        - cve_link
        - cvss
        - date_coordinated_release
        - date_created
        - date_made_public
        - date_notice_issued
        - description
        - distribution_link
        - id
        - importance
        - importance_explanation
        - information_type
        - metadata
        - mitigation
        - notes
        - private
        - status
    vulnerability-diff:
      type: object
      properties:
        cve_link:
          type: string
          format: uri
          x-lp-resource-type: cve
          description: External CVE reference corresponding to this vulnerability, if any.
        cvss:
          type: string
          description: List of Common Vulnerability Scoring System.
        date_coordinated_release:
          type: string
          description: Coordinated Release Date.
        date_made_public:
          type: string
          description: The date this vulnerability was made public.
        date_notice_issued:
          type: string
          description: Date when a security notice was issued for this vulnerability.
        description:
          type: string
          description: A short description of the vulnerability.
        importance:
          type: string
          enum:
            - Unknown
            - Undecided
            - Critical
            - High
            - Medium
            - Low
            - Wishlist
          description: Indicates the work priority, not the severity
        importance_explanation:
          type: string
          description: Used to explain why our importance differs from somebody else's CVSS score.
        information_type:
          type: string
          enum:
            - Public
            - Public Security
            - Private Security
            - Private
            - Proprietary
            - Embargoed
          description: Information type Indicates the privacy of the vulnerability.
        metadata:
          type: string
          description: Metadata Metadata of Vulnerability
        mitigation:
          type: string
          description: Explains why we're ignoring this vulnerability.
        notes:
          type: string
          description: Free-form notes for this vulnerability.
        status:
          type: string
          enum:
            - Needs triage
            - Active
            - Ignored
            - Retired
          description: The status of the vulnerability.
    vulnerability-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: vulnerability-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: vulnerability-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    webhook_delivery-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_delivery
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        date_created:
          type: string
          description: Date created
        date_first_sent:
          type: string
          description: Date first sent Timestamp of the first delivery attempt.
        date_scheduled:
          type: string
          description: Date scheduled Timestamp of the next delivery attempt.
        date_sent:
          type: string
          description: Date sent Timestamp of the last delivery attempt.
        error_message:
          type: string
          description: Error message Details of the error encountered by the most recent delivery attempt.
        event_type:
          type: string
          description: Event type
        payload:
          type: string
          description: Event payload
        pending:
          type: string
          description: Pending Whether a delivery attempt is in progress.
        successful:
          type: string
          description: Successful Whether the most recent delivery attempt succeeded, or null if no attempts have been made yet.
        webhook_link:
          type: string
          format: uri
          x-lp-resource-type: webhook
          description: Webhook The webhook that this delivery is for.
      required:
        - date_created
        - date_first_sent
        - date_scheduled
        - date_sent
        - error_message
        - event_type
        - payload
        - pending
        - successful
        - webhook_link
    webhook_delivery-diff:
      type: object
    webhook_delivery-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_delivery-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_delivery-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    webhook-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: webhook
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        active:
          type: string
          description: Active Deliver details of subscribed events.
        date_created:
          type: string
          description: Date created
        date_last_modified:
          type: string
          description: Date last modified
        deliveries_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_delivery-page-resource
          description: Recent deliveries for this webhook.
        delivery_url:
          type: string
          description: Delivery URL
        event_types:
          type: string
          description: Event types
        git_ref_pattern:
          type: string
          description: Git ref pattern Pattern to match against git-ref/branch name of an event, to filter webhook triggers
        registrant_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Registrant The person who created this webhook.
        target_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_target
          description: Target The object for which this webhook receives events.
      required:
        - active
        - date_created
        - date_last_modified
        - deliveries_collection_link
        - delivery_url
        - event_types
        - git_ref_pattern
        - registrant_link
        - target_link
    webhook-diff:
      type: object
      properties:
        active:
          type: string
          description: Active Deliver details of subscribed events.
        delivery_url:
          type: string
          description: Delivery URL
        event_types:
          type: string
          description: Event types
        git_ref_pattern:
          type: string
          description: Git ref pattern Pattern to match against git-ref/branch name of an event, to filter webhook triggers
    webhook-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    webhook_target-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_target
          description: The canonical link to this resource.
        web_link:
          type: string
          format: uri
          description: The canonical human-addressable web link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        webhooks_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook-page-resource
          description: Webhooks for this target.
      required:
        - webhooks_collection_link
    webhook_target-diff:
      type: object
    webhook_target-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_target-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: webhook_target-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
    wiki_name-full:
      type: object
      properties:
        self_link:
          type: string
          format: uri
          x-lp-resource-type: wiki_name
          description: The canonical link to this resource.
        resource_type_link:
          type: string
          format: uri
          description: The link to the WADL description of this resource.
        http_etag:
          type: string
          description: The value of the HTTP ETag for this resource.
        person_link:
          type: string
          format: uri
          x-lp-resource-type: person
          description: Owner
        url:
          type: string
          description: The URL for this wiki home page.
        wiki:
          type: string
          description: Wiki host
        wikiname:
          type: string
          description: Wikiname
      required:
        - person_link
        - url
        - wiki
        - wikiname
    wiki_name-diff:
      type: object
      properties:
        wiki:
          type: string
          description: Wiki host
        wikiname:
          type: string
          description: Wikiname
    wiki_name-page:
      type: object
      properties:
        resource_type_link:
          type: string
          format: uri
        total_size:
          type: integer
        total_size_link:
          type: string
          format: uri
          x-lp-resource-type: ScalarValue
        start:
          type: integer
        next_collection_link:
          type: string
          format: uri
          x-lp-resource-type: wiki_name-page-resource
        prev_collection_link:
          type: string
          format: uri
          x-lp-resource-type: wiki_name-page-resource
        entries:
          type: array
          items:
            type: object
      required:
        - start
        - entries
